🇮🇩 Bahasa Indonesia · 🇬🇧 [English (source)](AUDIT_STANDAR_PENGEMBANGAN_2026-07-04.md)

<!-- i18n-source-hash: sha256:a7750011041a17788e73dd6397a9dd55b7d48ceaef6dc002064dba6bcba08a7c -->

# Audit Standar Pengembangan Software AWCMS — 2026-07-04

> **⚠️ Dokumen warisan / historis — jangan dibaca sebagai state repo saat ini.**
> Audit ini diadaptasi dari basis `awcms-mini` dan menggambarkan keadaan repo
> acuan, bukan repo `awcms` sekarang. Ia menyebut migrasi (mis. `sql/013_..._enforce_rls`,
> `sql/007_..._sync_storage_outbox`) dan env berprefix `AWCMS_MINI_` (sync HMAC
> secret, app DB password) serta modul (`sync_storage`, `workflow_approval`)
> yang **tidak ada** di repo ini — `sql/007` yang nyata adalah `audit_logging`,
> dan prefix env repo ini adalah `AWCMS_` saja. Untuk keadaan
> kode yang benar, lihat [`../ARCHITECTURE.md`](../ARCHITECTURE.md); untuk status
> paket dokumen, lihat [`README.md`](README.md) §Status.

## Ringkasan verdict

Status repository terbaru: **PASS untuk baseline perencanaan dan foundation skeleton Issue 0.1**. Runtime aplikasi penuh belum selesai karena tenant/auth/RBAC/sync/deployment masih berada di backlog, tetapi scaffold Astro/Bun, health endpoint, module contract, response helper, soft-delete convention, dan folder standar sudah tersedia.

Stack target sudah konsisten di dokumen:

- Runtime: **Bun**.
- Package manager: **Bun** (`packageManager: bun@1.3.14`).
- Backend platform: **Bun-only**. Node.js tidak boleh dipakai kecuali ada izin maintainer dan catatan pengecualian di docs karena Bun belum mendukung kebutuhan teknis terkait.
- Web framework: **Astro 7**.
- Database: **PostgreSQL**.
- Arsitektur: modular monolith, microservice-ready.
- Kontrak: OpenAPI + AsyncAPI.
- Security baseline: RBAC + ABAC + PostgreSQL RLS + audit log.

## Lingkup audit

Audit membaca seluruh isi repo saat ini:

- Root docs dan metadata: `README.md`, `AGENTS.md`, `CHANGELOG.md`, `.gitignore`, `package.json`, `.changeset/`.
- Paket dokumen `docs/awcms/01` sampai `20` + Architecture Decision Records di `docs/adr/`.
- Snapshot GitHub issue aktual di `docs/awcms/github/`.
- Skill dan subagent proyek di `.claude/skills/` dan `.claude/agents/`.
- Struktur aktual repository dan gap terhadap target Issue 0.1.

## Temuan utama

| Area                      | Status                 | Catatan                                                                                                                                                            |
| ------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Status repo               | PASS                   | Repo jujur menyatakan foundation skeleton sudah tersedia, tetapi belum mengklaim app production-ready.                                                             |
| Stack target              | PASS                   | Bun, Astro 7, dan PostgreSQL konsisten di README, AGENTS, doc 01, 10, 11, 13, 15, 16, 18.                                                                          |
| Backend platform Bun-only | PASS                   | Dokumen menetapkan Bun sebagai runtime backend dan melarang Node.js/tooling npm-family tanpa pengecualian tertulis.                                                |
| Governance agent          | PASS                   | `AGENTS.md`, 17 skill, dan 3 subagent tersedia; alur implementasi/review/security jelas.                                                                           |
| Roadmap issue             | PASS                   | Issue 0.1–12.2 terdokumentasi; snapshot GitHub open/closed ada di `docs/awcms/github/`; urutan implementasi tidak melompat ke POS sebelum tenant/auth/access.      |
| Security baseline         | PASS                   | RBAC, ABAC default deny, RLS, audit, masking, idempotency, soft delete, HMAC sync, provider outbox tercakup.                                                       |
| Database standard         | PASS                   | Migration berurutan, PostgreSQL types, RLS, index/FK, transaction wrapper, locking, outbox, pooling terdokumentasi.                                                |
| API/event contract        | PASS                   | OpenAPI/AsyncAPI, response/error shape, idempotency header, soft delete API, tombstone event terdokumentasi.                                                       |
| Frontend standard         | PASS                   | Astro SSR/islands, PWA/offline-first, IndexedDB outbox, API client, a11y, i18n, design token terdokumentasi.                                                       |
| Testing & readiness       | PASS                   | Unit/integration/contract/security/performance target dan production preflight sudah ada.                                                                          |
| Versioning                | PASS setelah perbaikan | Changesets tersedia; `bun.lock` ditambahkan untuk reproducible install.                                                                                            |
| Runtime source            | PASS sebagian          | `src/`, `sql/`, `openapi/`, `asyncapi/`, `tests/`, `astro.config.mjs`, dan `.env.example` tersedia; `docker-compose.yml` dan runner detail masuk issue berikutnya. |
| Security automation       | PASS                   | `SECURITY.md`, Dependabot config, CodeQL workflow untuk GitHub Actions, secret scanning, push protection, dan private vulnerability reporting tersedia.            |
| CI build/test workflow    | PASS sebagian          | CI menjalankan lint, docs-check, typecheck, `bun test`, build Astro, Bun-only hygiene, dan no-`.env`; spec/migration check masuk issue 0.2/0.3.                    |

## Standar yang sudah terpenuhi sebagai baseline

### 1. Software development lifecycle

- Atomic issue/sprint diwajibkan.
- DoD mencakup migration, OpenAPI, AsyncAPI, test, build, docs, changeset, laporan implementasi.
- Branch dan commit convention sudah ditetapkan.
- Versioning memakai SemVer + Changesets.
- Review PR dan security review punya skill/subagent khusus.

### 2. Bun

- `package.json` sudah memakai `type: "module"` dan script Changesets.
- `bun.lock` sudah tersedia untuk mengunci dependency baseline.
- `package.json` mengunci package manager ke `bun@1.3.14`.
- Script Bun aktif: `dev`, `build`, `preview`, `start`, `lint`, `check:docs`, `typecheck`, `test`, `test:coverage`, `check`, dan Changesets. Target script berikutnya tetap `db:migrate`, `api:spec:check`, `api:contract:test`, `security:readiness`, `production:preflight`, `db:pool:health`.
- Node.js exception protocol sudah didefinisikan di `AGENTS.md`, doc 10, doc 12, dan doc 16.

### 2.1 Pengecualian Node.js

Status saat audit: **tidak ada pengecualian Node.js yang disetujui**.

Jika di masa depan Bun belum mendukung kebutuhan teknis tertentu, pengecualian wajib dicatat di tabel ini sebelum merge:

| Tanggal | Scope | Alasan Bun belum cukup | Izin maintainer | Alternatif Bun yang dicoba | Rencana pencabutan |
| ------- | ----- | ---------------------- | --------------- | -------------------------- | ------------------ |
| —       | —     | —                      | —               | —                          | —                  |

Catatan: script aplikasi foundation sudah ada di `package.json`; script database/API/deployment readiness menunggu issue 0.2/0.3/10.3/12.2.

### 3. Astro 7

- Astro 7 dipilih eksplisit sebagai framework.
- Doc 15 menetapkan Astro output server/SSR, islands untuk area interaktif, cookie httpOnly untuk sesi, dan service worker/IndexedDB untuk POS offline.
- Doc 14 menetapkan design token, komponen UI, state pattern, a11y WCAG 2.1 AA, i18n, dan layout admin/POS/customer.
- **Astro berjalan penuh di runtime Bun** (install/dev/build/runtime); bin Astro/Vite dipanggil `bun --bun`. Astro belum punya adapter SSR Bun first-party, sehingga SSR memakai salah satu opsi tersanksi (doc 15 §Astro SSR di atas runtime Bun): (A) seam API `Bun.serve`+Hono dengan Astro sebagai frontend — rekomendasi, atau (B) `@astrojs/node` standalone dijalankan `bun ./dist/server/entry.mjs`. Opsi B = satu-satunya pemakaian paket ber-nama "node" yang diizinkan (binary `node` tidak dipakai) dan wajib dicatat sebagai pengecualian di §2.1.

Catatan: `astro.config.mjs`, halaman index, dan health endpoint sudah tersedia. UI admin/komponen aplikasi tetap menunggu sprint UI.

### 4. PostgreSQL

- PostgreSQL menjadi database utama.
- Doc 04 menetapkan UUID PK, `timestamptz`, `numeric`, FK index, RLS, migration berurutan, data classification, retention, dan soft delete.
- Doc 16 menetapkan akses data konkret: repository, parameterized query, `SET LOCAL app.current_tenant_id`, transaction wrapper, `FOR UPDATE`, outbox, pooling/backpressure, PgBouncer transaction mode, idempotency store.

Catatan: `sql/001_awcms_foundation_schema.sql` sudah tersedia. Migration runner detail tetap target Issue 0.2.

## Gap yang harus ditutup saat implementasi

| Prioritas | Gap                                                                                                          | Target penyelesaian                                  |
| --------: | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------- |
|        P0 | Docker Compose/deployment profile belum ada.                                                                 | Issue 12.2                                           |
|        P0 | Migration runner, OpenAPI/AsyncAPI validator, dan contract test belum ada.                                   | Issue 0.2–0.3                                        |
|        P0 | Spec check OpenAPI/AsyncAPI belum ada.                                                                       | Tambahkan pada Issue 0.3                             |
|        P1 | Markdown/link/spec lint belum otomatis.                                                                      | Tambahkan script docs/spec validation pada Issue 0.3 |
|        P1 | Runtime enforcement belum bisa diuji: TypeScript strict, RLS integration, ABAC, idempotency, audit, masking. | Mulai Issue 2.1–3.4 sesuai urutan                    |
|        P1 | Production preflight masih berupa target script.                                                             | Issue 10.3 dan Sprint 12                             |

## Verifikasi yang dijalankan

```bash
bun --version
bun install --lockfile-only
bun install --frozen-lockfile
```

Hasil:

- Bun tersedia: `1.3.14`.
- `bun.lock` berhasil dibuat.
- `bun install --frozen-lockfile` berhasil setelah lockfile tersedia.

## Keputusan audit

1. Repository **sudah sesuai standar sebagai planning baseline** untuk pengembangan software Bun + Astro + PostgreSQL.
2. Repository **sudah buildable sebagai foundation skeleton Bun/Astro**, tetapi belum boleh diklaim sebagai aplikasi production-ready sampai tenant/auth/RBAC/database runner/API contract/deployment selesai.
3. Tidak ada secret nyata yang terdeteksi; nilai seperti `change-me` dan `awcms_password` hanya placeholder dokumentasi `.env.example`.
4. Perbaikan yang dilakukan dari audit ini:
   - `CHANGELOG.md` disesuaikan dari 14 skill menjadi 17 skill.
   - `bun.lock` ditambahkan untuk reproducible dependency baseline.
   - Dokumen audit ini ditambahkan sebagai rujukan repo-local.
   - Snapshot GitHub issue open/closed, label, milestone, security, dan proses refresh ditambahkan di `docs/awcms/github/`; snapshot live 2026-07-04T11:16:36Z mencatat 0 issue, 76 label, 19 milestone, 0 open Dependabot alert, 0 open code-scanning alert, dan 0 secret-scanning alert.
   - Backlog doc 06 diaktifkan penuh di GitHub pada 2026-07-04T13:58:45Z: 38 issue (`#371`-`#408`), 29 label baru + 9 milestone baru (`M0`-`M8`) sesuai taksonomi doc 06, tanpa menghapus 73 label/19 milestone peninggalan proyek sebelumnya. Detail: `docs/awcms/github/README.md`.
   - **Genericization pada 2026-07-04T14:15:43Z**: 38 issue awal ternyata memuat epic domain POS/retail yang tidak sesuai konteks AWCMS sebagai contoh repo pengembangan umum. 20 issue domain ditutup (`not planned`), 2 issue digeneralisasi wording-nya, 7 label domain dan 4 milestone domain yang jadi kosong dihapus, 2 milestone di-rename (drop CRM/AI). `docs/awcms/06_github_issues_detail.md` dan `docs/awcms/01_canvas_induk.md` ditulis ulang menjadi generik; `AGENTS.md` §Peta modul dan `docs/awcms/09_roadmap_repository_commit.md` §Struktur source diperbaiki agar tidak lagi mencantumkan modul domain (katalog, POS, gudang, pajak, CRM, AI analyst) sebagai bagian base. Detail: `docs/awcms/github/README.md` §Genericization.
   - `SECURITY.md`, `.github/dependabot.yml`, dan `.github/workflows/codeql.yml` ditambahkan sebagai baseline GitHub Security.
   - **Pengerasan standar repo publik pada 2026-07-05**: lisensi `UNLICENSED` → **MIT** (`LICENSE`); berkas komunitas & tata kelola (`CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `GOVERNANCE.md`, `SUPPORT.md`, `.github/CODEOWNERS`, PR/issue templates); konfigurasi kualitas (`.editorconfig`, `.gitattributes`, `.prettierrc.json`); CI kualitas dokumentasi (`.github/workflows/ci.yml` + `scripts/check-docs.mjs`: validasi mermaid/tautan/penamaan, gate Bun-only/no-`.env`); **Architecture Decision Records** `docs/adr/` (0001–0007); **doc 20 — Threat Model & Arsitektur Keamanan**; `SECURITY.md` diperluas (scope, safe harbor, target response time). Berkas cruft `init` (1 byte) dihapus. Seluruh Markdown dijadikan prettier-clean. Issue GitHub `#405`/`#379` diselaraskan untuk merujuk doc 20 + ADR.
   - Tambah `tsconfig.json` baseline + `typescript`/`@types/bun`/`@types/node`, script `typecheck`; rilis `0.0.1`.
   - Tambah **unit testing** (`bun test`): refaktor `scripts/check-docs.mjs` menjadi lib murni (`scripts/lib/docs-checks.mjs`) + CLI tipis, `tests/docs-checks.test.mjs` (23 kasus) + `tests/check-docs-integration.test.mjs`; perbaiki bug fidelity `slugify` (GitHub tidak collapse whitespace beruntun); `bun test` masuk gate CI; rilis `0.0.2`.
   - **Audit menyeluruh GitHub vs doc 06 pada 2026-07-04T22:11:08Z**: membandingkan setiap field (Problem/Scope/Out of Scope/Acceptance Criteria/Security Notes/Testing/Reference Docs) tiap issue open terhadap doc 06, plus label & milestone terhadap tabel rekomendasi. Ditemukan 14/18 issue drift: 2 **konflik konten nyata** (leftover bahasa domain "POS, inventory" di `#371` dan "user/customer/tax/CRM" di `#377`, tersisa dari genericization sebelumnya) + 12 issue dengan Reference Docs basi (dibuat sebelum `docs/adr/`/doc 20 ada). Semua diperbaiki di GitHub tanpa mengubah jumlah/label/milestone (tetap 18 open/20 closed/98 label/24 milestone — 100% cocok tabel doc 06 setelah perbaikan). Detail: `docs/awcms/github/README.md` §Reconciliation #2.
   - **Issue 0.1 foundation skeleton**: Astro 7 ditambahkan, `bun run build` lulus, health endpoint `/api/v1/health` tersedia, `src/modules/_shared/` menyediakan module contract, response helper, soft-delete convention, `src/modules/index.ts` menjadi registry awal, `.env.example` placeholder tersedia, dan folder standar `src/`, `sql/`, `openapi/`, `asyncapi/`, `deploy/`, `fixtures/` dibuat. CI dan `bun run check` menjalankan build. Issue GitHub `#371` ditutup `completed` pada 2026-07-05; snapshot GitHub terbaru menjadi 17 open / 21 closed.
   - **Issue 0.2 SQL migration runner**: `scripts/db-migrate.ts` ditambahkan dengan `Bun.SQL`, advisory lock, checksum SHA-256, skip applied migration, deteksi checksum drift, transaction boundary runner, redaksi `DATABASE_URL`, test helper, script `bun run db:migrate`, dan panduan `docs/awcms/database-migrations.md`. Issue GitHub `#372` ditutup `completed` pada 2026-07-05; snapshot GitHub terbaru menjadi 16 open / 22 closed.
   - **Issue 0.3 OpenAPI/AsyncAPI baseline**: `openapi/awcms-public-api.openapi.yaml`, `asyncapi/awcms-domain-events.asyncapi.yaml`, dan `scripts/api-spec-check.ts` ditambahkan. Kontrak mencakup response/error schema bersama, health endpoint, pola soft delete/restore/purge, header HMAC sync, domain event envelope, dan validasi konsistensi event module registry. Issue GitHub `#373` ditutup `completed` pada 2026-07-05; snapshot GitHub terbaru menjadi 15 open / 23 closed.
   - **Perbaikan arsitektur SSR + kontrak response pada 2026-07-05**: audit pasca-Issue 0.1–0.3 menemukan dua konflik: (1) `astro.config.mjs` memakai `output: "static"` dan `health.ts` memakai `prerender = true`, sehingga `/api/v1/health` menjadi file JSON hasil build, bukan endpoint hidup — bertentangan dengan arsitektur middleware per-request di doc 01/09/16 dan ADR-0002, serta membuat `AGENTS.md` (`bun run start # bun ./dist/server/entry.mjs`) tidak akurat karena `dist/server/entry.mjs` tidak ada; (2) `src/modules/_shared/api-response.ts` dan `openapi/awcms-public-api.openapi.yaml` memakai field envelope `ok: true/false`, padahal doc 05 dan doc 10 menetapkan `success: true/false`. Perbaikan: `astro.config.mjs` → `output: "server"` + adapter `@astrojs/node` (mode `standalone`, pengecualian Bun-only tersanksi sesuai ADR-0002/doc 10/doc 18), `prerender` dihapus dari `health.ts`; seluruh envelope `ok` → `success` (kode, OpenAPI, test, `src/modules/_shared/README.md`); `package.json` `start` diperbaiki jadi `bun ./dist/server/entry.mjs`; `lint`/`format` diperluas mencakup `.ts`/`.mjs`/`.astro` (plus `prettier-plugin-astro`). Diverifikasi dengan menjalankan server hasil build sungguhan dan memanggil `/api/v1/health` dua kali — `generatedAt` berbeda antar-request, membuktikan SSR dinamis (bukan file statis), dan payload memakai `"success":true`.
   - **Rilis `0.1.0` pada 2026-07-05**: 4 changeset yang terkumpul (`foundation-skeleton`, `db-migration-runner`, `api-contract-baseline`, `pin-ci-actions`) dikonsumsi lewat `bun run changeset:version`; `CHANGELOG.md` direkonsiliasi menjadi bagian Added/Changed/Fixed yang bersih. Tabel peta versi doc 09 diperbaiki: sebelumnya `v0.1.0` menumpuk "Foundation, tenant, identity, profile" dalam satu slot, padahal baru Foundation yang selesai — dipecah jadi `v0.1.0` (Foundation) dan `v0.2.0` (tenant/identity/profile), versi setelahnya digeser (`v0.3.0` RBAC/ABAC … `v1.0.0` production-ready); tabel yang sama di `CHANGELOG.md` §Peta versi rencana disinkronkan. `README.md` §Versioning diperbarui merujuk `0.1.0`.
   - **Perbaikan item menengah/rendah pada 2026-07-05**: (1) tipe `SoftDeleteColumns.deletedAt`/`deletedBy`/`deleteReason` di `src/modules/_shared/soft-delete.ts` disamakan ke `string | null` opsional sesuai doc 10 (sebelumnya `deletedAt: Date | null` wajib); (2) `.env.example` dan doc 11 §Minimal `.env.example` — baris `STARSENDER_ENABLED`/`MAILKETING_ENABLED`/`AI_ANALYST_ENABLED` (nama provider spesifik-domain retail/POS) dihapus dari file konfigurasi nyata base, diganti komentar generik yang merujuk aplikasi turunan menambah flag provider-nya sendiri (contoh domain tetap ada di doc 18); (3) doc 13 §Repository artifact checklist ditambah subbagian "Folder standar" yang mengindeks `README.md` di `src/lib/`, `src/modules/_shared/`, `openapi/`, `asyncapi/`, `deploy/`, `fixtures/`.
   - **Koreksi urutan sprint & Issue 2.1 — Tenant/Office Schema pada 2026-07-05**: rekomendasi sebelumnya untuk langsung mengerjakan Issue 12.1 (Setup Wizard) ternyata keliru — 12.1 menginisialisasi tenant/owner/office/role/permission/ABAC, padahal skema database saat itu baru berisi `awcms_modules`/`awcms_schema_migrations` (Issue 0.1–0.3). Skema tenant/identity/RBAC adalah scope Issue 2.1, 2.3, 2.4 (Sprint 2/3), sedangkan doc 06 sebelumnya menempatkan 12.1 di Sprint 1 sejajar 0.1–0.3. Diperbaiki: 12.1 dipindah ke Sprint 3 (setelah 2.4) di `docs/awcms/06_github_issues_detail.md` §Sprint awal rekomendasi + §Koreksi urutan sprint (baru); label GitHub disesuaikan — `#376`/`#377`/`#378` (2.1/2.2/2.3) `status:blocked` → `status:ready` (dependency Sprint 1 sudah selesai), `#407` (12.1) `status:ready` → `status:blocked` (dependency nyata: 2.1/2.3/2.4).
     - **Issue 2.1 diimplementasikan**: migrasi `sql/002_awcms_tenant_office_schema.sql` menambahkan `awcms_tenants` (root tenant, unique `tenant_code`, lifecycle `status` tanpa soft delete), `awcms_offices` (hierarki kantor per tenant, unique parsial `(tenant_id, office_code) WHERE deleted_at IS NULL`, RLS tenant isolation, soft delete), `awcms_physical_locations` (alamat per office, RLS, soft delete), dan `awcms_tenant_settings` (konfigurasi 1:1 per tenant, RLS) — seluruhnya generik (bukan mengambil contoh domain retail dari doc 17). Nama file migration `002` diperbaiki dari rencana awal "`tenant_identity_schema`" (menggabungkan Issue 2.1+2.3) menjadi "`tenant_office_schema`" agar satu migration = satu issue; identity/login (2.3) akan mendapat nomor migration tersendiri. Modul `tenant-admin` (`src/modules/tenant-admin/module.ts`, status `experimental`) didaftarkan di `src/modules/index.ts`. Test baru: registry modul tidak lagi kosong, `discoverMigrationFiles()` menemukan `002` secara berurutan dan checksum valid, serta memverifikasi RLS + kolom soft-delete ada di isi migration (36 test total, semua lulus). Issue GitHub `#376` ditutup `completed`.
   - **Issue 2.2 diimplementasikan pada 2026-07-05**: migrasi `sql/003_awcms_central_profile_management_schema.sql` menambahkan `awcms_profiles` (kanonik person/organization, soft delete, `merged_into_profile_id`), `awcms_profile_identifiers` (email/phone/whatsapp/national_id/tax_id/external_code — digenerikkan dari NPWP/NIK/"customer code" di doc03 yang domain-spesifik Indonesia/retail; dedup lewat `value_hash` unique parsial per tenant+type selama belum soft-deleted), `awcms_profile_channels` (preferensi channel, mengacu identifier tanpa duplikasi nilai sensitif), `awcms_profile_addresses`, `awcms_profile_entity_links` (tautan ke entity modul lain, unique per entity), `awcms_profile_merge_requests` (`CHECK source_profile_id <> target_profile_id`), dan `awcms_profile_audit_logs` (append-only, tanpa soft delete). Domain logic murni ditambahkan di `src/modules/profile-identity/domain/`: `identifier.ts` (`normalizeIdentifier`, `hashIdentifier` bergaya `sha256:<hex>`, `maskIdentifier`) dan `merge.ts` (`assertMergeRequestIsValid`, mencerminkan constraint DB di level aplikasi). Modul `profile_identity` (dependency: `tenant_admin`) didaftarkan. Test baru: 10 kasus domain logic (normalisasi email/telepon, hash stabil, masking email/tail, validasi merge) + assersi RLS/dedup/constraint pada isi migration `003` (46 test total, semua lulus). Diverifikasi live terhadap PostgreSQL 16 sungguhan (bukan hanya build/test): migration apply bersih, RLS mengisolasi tenant, dedup identifier menolak duplikat aktif namun mengizinkan reuse setelah soft delete, dan constraint `source <> target` pada merge request ditolak database saat dilanggar. Issue GitHub `#377` ditutup `completed`.
   - **Issue 2.3 diimplementasikan pada 2026-07-05**: migrasi `sql/004_awcms_identity_login_schema.sql` menambahkan `awcms_identities` (login identity per tenant, `login_identifier` unik per tenant, `password_hash` via `Bun.password` argon2id native — tanpa dependency, `failed_login_count`/`locked_until` untuk lockout), `awcms_tenant_users` (keanggotaan identity pada tenant, status terpisah dari status identity), dan `awcms_sessions` (token sesi opaque — hanya `token_hash` SHA-256 yang disimpan, bukan JWT stateless, agar logout benar-benar bisa mencabut sesi). Menambahkan endpoint live pertama yang menyentuh database: `POST /api/v1/auth/login`, `POST /api/v1/auth/logout`, `GET /api/v1/auth/me` (OpenAPI diperbarui, termasuk kode error baru `AUTH_INVALID_CREDENTIALS`). Karena ini endpoint live pertama, ditambahkan infrastruktur dasar akses data yang dipakai modul lain ke depannya: `src/lib/database/client.ts` (`Bun.SQL` bersama) dan `src/lib/database/tenant-context.ts` (`assertUuid` + `withTenant` — transaction wrapper + `SET LOCAL app.current_tenant_id` sesuai doc 16 §RLS context; connection pooling/backpressure penuh tetap scope Issue 10.2). Domain logic murni di `src/modules/identity-access/domain/login-policy.ts`: `evaluateLoginAttempt` menyatukan pengecekan tenant inactive (dicek pertama, sebelum kredensial), account locked, tenant_user inactive, dan password salah (dengan increment counter + auto-lock setelah `AUTH_LOGIN_MAX_ATTEMPTS` percobaan) — semua alasan penolakan selain tenant-inactive/locked digeneralisasi jadi "invalid credentials" untuk mencegah user enumeration. Modul `identity_access` (dependency: `tenant_admin`, `profile_identity`) didaftarkan. Test baru: 19 kasus domain logic (lockout primitives, evaluateLoginAttempt skenario lengkap, ekstraksi bearer token, hash/verify password, generate/hash session token, `assertUuid`) + assersi RLS pada isi migration `004` (65 test total, semua lulus). Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan yang berjalan (bukan hanya unit test): login sukses/gagal, tenant inactive ditolak sebelum cek kredensial, identifier tak dikenal menghasilkan pesan identik dengan password salah (anti-enumeration), lockout otomatis setelah 5 percobaan gagal (password benar sekalipun tetap ditolak selama terkunci), `me` menampilkan identity dari token valid dan menolak token tidak ada/kedaluwarsa, `logout` benar-benar mencabut sesi (dibuktikan `me` gagal setelahnya). **Temuan operasional**: Astro's built-in CSRF `checkOrigin` protection menolak (403, tanpa body) permintaan `POST` tanpa `Content-Type` sebagai potential cross-site form submission — klien nyata wajib mengirim `Content-Type: application/json` walau body kosong (mis. `logout`); didokumentasikan di README modul. Penomoran migration `004` dst. di doc 09 digeser (identity/login mengambil slot `004`, ABAC/RBAC bergeser ke `005`, dst.). Issue GitHub `#378` ditutup `completed`.
   - **Issue 2.4 diimplementasikan pada 2026-07-05**: migrasi `sql/005_awcms_abac_access_control_schema.sql` menambahkan `awcms_permissions` (katalog global `module_key.activity_code.action`, **tanpa** `tenant_id`/RLS — sama untuk semua tenant; diseed 17 baris hanya untuk modul base yang sudah ada: `tenant_admin.office_management`, `identity_access.user_management`, `identity_access.access_control`, `profile_identity.profile_management`, `profile_identity.profile_merge` — **bukan** 26+ baris matriks domain retail doc 17 yang mencakup katalog/POS/gudang/pajak/CRM/AI), `awcms_roles` (per tenant, tidak diseed — role Owner/Admin/dst. tetap scope Setup Wizard Issue 12.1, generik bukan 10 role retail doc 17), `awcms_role_permissions`, `awcms_access_assignments`, `awcms_abac_policies` (schema saja, belum ada policy row), dan `awcms_abac_decision_logs` (append-only). Domain logic murni `evaluateAccess` di `src/modules/identity-access/domain/access-control.ts` memakai tipe `TenantContext`/`AccessRequest`/`AccessDecision` **persis** sesuai kontrak doc 10 §ABAC guard. **Default deny, deny overrides allow** (ADR-0004): dua aturan ABAC generik dicek lebih dulu sebelum RBAC — tenant isolation (`resourceAttributes.tenantId` beda tenant selalu ditolak) dan self-approval deny (actor tidak boleh approve request yang ia ajukan sendiri); baik allow maupun deny selalu masuk decision log. Endpoint baru: `GET /access/modules`, `POST /access/evaluate`, `POST /access/assignments` (idempotent via `ON CONFLICT ... DO UPDATE`, di-guard `identity_access.access_control.assign`), `GET /access/decision-logs` (di-guard `identity_access.access_control.read`, limit 50). OpenAPI diperbarui (4 path + 8 schema baru). Test baru: 12 kasus domain logic (`permissionKey`, `isHighRiskAction`, `evaluateAccess` — default deny, allow, tenant isolation override, self-approval override, kasus positif) + assersi RLS pada 5 tabel tenant-scoped, RLS **tidak** ada di `awcms_permissions` (katalog global), dan seed hanya berisi 5 modul base tanpa jejak modul domain (75 test total, semua lulus). Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan: 17 permission ter-seed, RLS-free pada tabel permissions dikonfirmasi, operator dengan role granted mendapat `allowed:true`, operator tanpa permission yang cocok mendapat `allowed:false` (`default_deny`), operator **tanpa role sama sekali** ditolak (403) pada endpoint yang di-guard (`assignments`, `decision-logs`) — membuktikan "operator ditolak akses modul yang tidak diizinkan" (acceptance criteria) benar-benar berfungsi, assignment idempotent mengembalikan id yang sama pada percobaan kedua, dan decision log bertambah di tiap pemanggilan evaluator. Issue GitHub `#379` ditutup `completed`. **Sprint 3 dimulai dan Sprint 2 (2.1-2.4 dalam artian broader RBAC epic) sepenuhnya tuntas.**
   - **Issue 12.1 diimplementasikan pada 2026-07-05**: migrasi `sql/006_awcms_setup_wizard_schema.sql` menambahkan `awcms_setup_state`, tabel singleton global (`id boolean PRIMARY KEY DEFAULT true`, tanpa `tenant_id`/RLS — belum ada tenant saat setup berjalan) yang mengunci setup secara permanen. Endpoint baru: `GET /api/v1/setup/status` (public) dan `POST /api/v1/setup/initialize` (public — memang tanpa auth, karena belum ada identity untuk login sebelum tenant pertama dibuat) dalam `src/pages/api/v1/setup/`. `initialize` adalah satu transaksi atomik: klaim lock via `INSERT ... ON CONFLICT DO NOTHING RETURNING id` (mencegah race condition dua request setup bersamaan — request kedua yang gagal klaim langsung ditolak `403` tanpa menyentuh tabel lain), buat tenant, `SET LOCAL app.current_tenant_id` begitu tenant ID diketahui (pola sama seperti `withTenant` tapi manual karena tenant ID belum ada saat transaksi dimulai), lalu buat `tenant_settings`, office `head_office`, profile + identity + tenant_user pemilik, role `owner` (`is_system=true`) berisi **seluruh** permission yang ada di katalog saat itu (17 baris dari Issue 2.4 — generik, bukan role Owner retail doc 17), assignment owner→role, dan mengunci `setup_state`. Domain logic murni `validateSetupInitializeInput` di `src/modules/tenant-admin/domain/setup-validation.ts`: validasi field wajib + kebijakan password minimum 8 karakter ("Password wajib memenuhi policy" doc 03). Modul `tenant_admin` diperbarui (`dependencies: [profile_identity, identity_access]`, `api.basePath: /api/v1/setup`). OpenAPI diperbarui (2 path + 4 schema baru). Test baru: 5 kasus validasi input + assersi skema singleton RLS-free pada migration `006` (81 test total, semua lulus). Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan: `GET /setup/status` awal `locked:false`, `initialize` dengan field kurang ditolak `400`, `initialize` valid berhasil membuat tenant/office/owner/role/assignment, `GET /setup/status` sesudahnya `locked:true`, `initialize` kedua kali ditolak `403` (**seed idempotent; tidak dapat dijalankan ulang setelah locked** — acceptance criteria doc 17), owner baru berhasil login dan `evaluateAccess`-nya `allowed:true` untuk seluruh permission (termasuk `access_control.assign`), `GET /access/modules` mengonfirmasi 17 permission granted ke owner. Issue GitHub `#407` ditutup `completed`. Migration `006` mengambil slot yang semula direncanakan `015` di doc 09 — digeser naik karena diimplementasikan lebih awal (setelah 2.1-2.4, sesuai `06_github_issues_detail.md` §Koreksi urutan sprint); `logging_observability` dst. bergeser turun satu nomor.
   - **Koreksi urutan sprint (2) & Issue 6.1 — Sync Outbox/Inbox pada 2026-07-05**: saat menutup Issue 12.1, ditemukan rekomendasi awal "lanjut ke Epic 4/10.1-10.3" keliru — Issue 10.1/10.2/10.3 (dan 11.1/12.2) bermilestone **M8 (Security/Performance/Production)**, yang menurut `06_github_issues_detail.md` §Ketergantungan milestone butuh **M5 (Sync Storage) dan M7 (UI/UX & Reporting) selesai dulu**, bukan cuma M2. Sprint 4/5 di doc 06 sebelumnya tertukar (M8 di Sprint 4, M5/M7 di Sprint 5). Diperbaiki: Sprint 4 = 6.1/6.2/6.3 (M5) + 8.1/9.1 (M7); Sprint 5 = 10.1/10.2/10.3/11.1/12.2 (M8) — label GitHub `#391`/`#392`/`#393`/`#398`/`#401` diubah `status:blocked` → `status:ready` (M2 tuntas); `#403`-`#406`/`#408` tetap `status:blocked`.
     - **Issue 6.1 diimplementasikan**: migrasi `sql/007_awcms_sync_storage_outbox_inbox_schema.sql` menambahkan `awcms_sync_nodes` (registrasi node per tenant, status active/inactive, checkpoint `last_pull_sequence`), `awcms_sync_outbox` (event lokal tersedia untuk di-pull, `sequence` identity sebagai cursor), `awcms_sync_inbox` (event diterima via push, status `received` — base tidak punya modul domain untuk "menerapkan"-nya), dan `awcms_sync_push_batches` (ledger idempotency `(tenant_id, node_id, batch_id)`). Domain logic murni di `src/modules/sync-storage/domain/`: `sync-hmac.ts` (`computeSyncSignature`/`verifySyncSignature`/`isTimestampWithinSkew`, sesuai skill `awcms-sync-hmac` — `HMAC-SHA256("<timestamp>.<body>")`, timing-safe compare, skew maks 300 detik) dan `sync-validation.ts` (`validateSyncPushRequestBody`). Endpoint baru `POST /sync/push`, `POST /sync/pull`, `GET /sync/status` — **autentikasi HMAC (bukan bearer token)**, secret tunggal dari `AWCMS_MINI_SYNC_HMAC_SECRET` (env, bukan per-node), node auto-registrasi saat kontak pertama (tidak ada endpoint registrasi terpisah di doc 05), dan endpoint menolak (403) jika `AWCMS_MINI_SYNC_ENABLED` bukan `true` — mengaktifkan dua env var yang sudah ada di `.env.example` sejak Issue 0.1 namun belum pernah dibaca kode. Modul `sync_storage` (dependency: `tenant_admin`) didaftarkan. OpenAPI diperbarui (3 path + 8 schema baru). Test baru: 15 kasus domain logic (HMAC compute/verify/tamper/wrong-secret/wrong-length, skew accept/reject/unparseable, validasi push body) + assersi RLS pada 4 tabel + ledger idempotency pada migration `007` (95 test total, semua lulus). Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan: node auto-register saat kontak pertama, signature salah ditolak 401, push valid diterima, push batch sama diulang **idempotent** (`duplicate:true`, event tidak diproses ulang), pull maju checkpoint dengan benar (`hasMore` akurat), timestamp kedaluwarsa (>300 detik) ditolak 401. **Bug ditemukan+diperbaiki saat verifikasi live**: `GET /sync/status` tidak mengecek `node.status !== "active"` (hanya cek node ada/tidak), sehingga node yang di-nonaktifkan tetap bisa query status — tidak konsisten dengan `push`/`pull` yang sudah benar menolak node inactive. Diperbaiki agar ketiga endpoint konsisten menolak (403) node inactive; diverifikasi ulang setelah perbaikan. Migration `007` mengambil slot baru (bukan bagian dari placeholder gabungan `sync_storage_r2` semula) — konflik (6.2) dan R2 queue (6.3) tetap satu placeholder gabungan sampai masing-masing benar-benar diimplementasikan.
   - **Issue 6.2 diimplementasikan pada 2026-07-05**: migrasi `sql/008_awcms_sync_storage_conflict_schema.sql` menambahkan `awcms_sync_aggregate_versions` (versi terakhir per `(aggregate_type, aggregate_id)`, dasar evaluator optimistic-concurrency generik), `awcms_sync_conflicts` (catatan **immutable** — fakta inti tidak pernah diubah, hanya kolom resolusi diisi sekali; dua tipe konflik generik: `missing_base_version` dan `version_mismatch`), `ALTER TABLE` menambah `conflicted_count` pada `sync_push_batches` (koreksi via migration baru, bukan mengedit migration `007` yang sudah ada), dan seed 2 permission baru (`sync_storage.conflict_resolution.read`/`.approve`) ke katalog `awcms_permissions`. `POST /sync/push` (dari Issue 6.1) diperluas: event boleh menyertakan `baseVersion?: number`; event yang konflik dicatat ke `sync_conflicts` (bukan `sync_inbox`) dan tidak memajukan versi aggregate; response menambah field `conflicted`. Endpoint baru `GET /sync/conflicts` (filter `?status=`) dan `POST /sync/conflicts/{id}/resolve` — **berbeda dari endpoint sync lain: bearer token (session), bukan HMAC**, karena "conflict manual" (ADR-0006) adalah keputusan manusia; di-guard permission `sync_storage.conflict_resolution.read`/`.approve`; resolve pada conflict yang sudah `resolved` ditolak `409`. Domain logic murni baru: `evaluatePushEventConflict` (`src/modules/sync-storage/domain/sync-conflict.ts`) dan `validateConflictResolutionRequestBody`. OpenAPI diperbarui (2 path baru + `baseVersion`/`conflicted` pada schema push + 4 schema baru). Test baru: 13 kasus domain logic (evaluator konflik 5 skenario, validasi resolusi 5 skenario, validasi `baseVersion` 3 skenario) + assersi RLS/immutability/seed permission pada migration `008` (108 test total, semua lulus). **Dua bug ditemukan+diperbaiki saat verifikasi live**: (1) push dengan `baseVersion` yang **benar** tetap salah terdeteksi sebagai `version_mismatch` — akar masalah: kolom `bigint` (`current_version`, `sequence`, `last_pull_sequence`) dikembalikan Bun.SQL sebagai **string**, bukan number; `"1" !== 1` (perbandingan strict lintas-tipe) selalu `true`. Diperbaiki dengan `Number(...)` eksplisit di `push.ts`, `pull.ts`, `status.ts` (bug laten sejak Issue 6.1, baru termanifestasi sekarang karena baru Issue 6.2 yang membandingkan nilai bigint dengan angka dari JSON); (2) tanpa perbaikan ini, response `checkpoint`/`sequence` juga melanggar OpenAPI (`type: integer` tapi nilainya string berkutip) — sudah benar setelah perbaikan. Diverifikasi ulang: push pertama diterima (versi 0→1), push kedua tanpa `baseVersion` konflik (`missing_base_version`), push ketiga dengan `baseVersion` basi konflik (`version_mismatch`), push keempat dengan `baseVersion` benar **diterima**, resolve konflik berhasil, resolve ulang ditolak `409`, daftar conflict `open` berkurang setelah resolve.
   - **Issue 6.3 diimplementasikan pada 2026-07-05**: migrasi `sql/009_awcms_object_sync_queue_schema.sql` menambahkan `awcms_object_sync_queue` (antrean objek lokal — mis. lampiran/receipt — menunggu sinkron/upload ke object storage; unique `(tenant_id, node_id, object_key)` — re-enqueue `objectKey` yang sama **upsert**, bukan duplikat, mengembalikan baris ke `status='pending'` dan mereset `retry_count`/`last_error`/`next_retry_at`/`uploaded_at`; index `(tenant_id, status, next_retry_at)` untuk pemindaian retry oleh calon dispatcher) dan seed 2 permission baru (`sync_storage.object_queue.read`/`.retry`). Domain logic murni baru `src/modules/sync-storage/domain/object-queue.ts`: `verifyObjectChecksum` (pure string equality — checksum bukan secret), `evaluateObjectRetry` (backoff eksponensial `2^retryCount` menit, dibatasi `OBJECT_SYNC_MAX_RETRY_DELAY_MINUTES=60`, tidak eligible lagi begitu `retryCount >= OBJECT_SYNC_MAX_RETRIES=5`), dan `validateObjectSyncEnqueueRequestBody` (pola `ValidationError`/discriminated union sama seperti `sync-validation.ts`). Endpoint baru (HMAC node auth, sama seperti push/pull/status — **bukan** bearer, karena ini komunikasi node-ke-server): `POST /sync/objects` (body `{ objects: [{objectKey, localPath, checksumSha256, byteSize}] }`, upsert per objek, response `{queued: <count>}`; `requires_upload` diisi dari env `R2_ENABLED` — **tidak ada pemanggilan R2/Cloudflare SDK nyata di base ini**, sama seperti `awcms_message_outbox` yang juga belum punya dispatcher live, sesuai batas scope issue dan ADR-0006 "provider eksternal opsional, tidak boleh jadi dependency alur kritikal") dan `GET /sync/objects/status` (entri milik node pemanggil yang belum `sent`, limit 100, urut `created_at`). Tidak dibuat endpoint "mark as sent/failed" — didokumentasikan di README modul sebagai backlog: dispatcher masa depan memanggil fungsi aplikasi internal langsung, bukan endpoint publik, karena hanya worker internal terpercaya yang boleh melakukan aksi itu. OpenAPI diperbarui (2 path baru + 6 schema baru: `ObjectSyncQueueItem`, `ObjectSyncEnqueueRequest`/`Response`, `ObjectSyncStatusEntry`/`Response`). Test baru: 13 kasus domain logic (`verifyObjectChecksum` match/mismatch, `evaluateObjectRetry` eligible/delay-bertumbuh/ineligible-at-max/ineligible-over-max, validator enqueue 1 valid + 5 skenario gagal per field) + assersi RLS/index-retry/unique-key/seed permission pada migration `009` (122 test total, semua lulus). Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan (container app terpisah berbagi network namespace container Postgres, sesuai catatan "Docker host port blocked" — publish port ke host stall di sandbox ini, jadi client/server dijalankan di `--network container:<nama-pg>`): migration `009` apply bersih lalu re-run migrate menunjukkan seluruh 9 migration `skip` (idempotent); `POST /sync/objects` enqueue objek baru sebagai `pending` (dikonfirmasi row nyata di Postgres); re-POST `objectKey` yang sama dengan `checksumSha256`/`localPath`/`byteSize` berbeda mengonfirmasi **upsert** — tetap tepat 1 baris untuk `(tenant, node, objectKey)` tersebut (bukan 2), dan field ter-refresh kembali ke `pending` dengan nilai baru; `GET /sync/objects/status` mengembalikan `retryCount`/`byteSize` sebagai **JSON number asli** (dikonfirmasi `typeof === "number"` pada respons mentah, bukan string berkutip); `UPDATE` manual `retry_count=3` + `status='failed'` di Postgres lalu `GET status` mengonfirmasi nilai retry termutakhirkan tampil benar sebagai number; env `R2_ENABLED=true` pada restart server mengonfirmasi objek baru diantre dengan `requiresUpload:true` sedangkan objek lama (`R2_ENABLED=false` saat pertama diantre) tetap `requiresUpload:false`; timestamp kedaluwarsa pada `POST /sync/objects` ditolak `401 AUTH_REQUIRED` (konsisten dengan push/pull/status). **Tidak ada bug baru ditemukan saat verifikasi live** — pelajaran bigint dari Issue 6.2 (`Number(...)` eksplisit untuk `byte_size`/`retry_count`) diterapkan preventif sejak awal implementasi, bukan ditemukan reaktif. Migration `009` melengkapi M5 (Sync Storage, Issue 6.1-6.3) sepenuhnya.

   - **Issue 8.1 diimplementasikan pada 2026-07-05**: Admin Layout Shell — frontend pertama di repo ini (sebelumnya hanya `src/pages/index.astro`). `src/styles/tokens.css` mengimplementasikan token desain doc 14 §Design tokens persis (warna semantik, font, spacing, radius, shadow, z-index, breakpoint referensi) di `:root` + override `:root[data-theme="dark"]`, plus reset minimal. `src/lib/auth/ssr-session.ts` (`resolveSsrContext`) membaca dua cookie SSR (`awcms_session`, `awcms_tenant_id`) dan mendelegasikan ke `resolveTenantContext`/`fetchGrantedPermissionKeys` (pola I/O sama persis dengan `POST /access/evaluate`), mengembalikan `null` tanpa pernah melempar error ke pemanggil. `POST /auth/login`/`POST /auth/logout` (`src/pages/api/v1/auth/`) mendapat perilaku **additive**: login men-set kedua cookie httpOnly + `SameSite=Lax` (`maxAge` = `AUTH_SESSION_TTL_MIN`, `secure` dari `AUTH_COOKIE_SECURE` — env yang sudah ada sejak Issue 0.1, baru dipakai sekarang), logout menerima tenant/token dari cookie sebagai fallback ketika header tidak ada (dibutuhkan karena skrip klien tidak bisa membaca cookie httpOnly untuk menyusun header `Authorization` manual) dan menghapus kedua cookie setelah revoke sukses; body JSON kedua endpoint **tidak berubah** — tes lama tetap lulus tanpa modifikasi. `src/layouts/AdminLayout.astro`: topbar (nama tenant, `TenantSwitcher`/`SyncIndicator`/`ThemeToggle` — ketiganya stub presentational, lihat di bawah — user menu + logout), sidebar (nav difilter permission efektif: "Access & Users" muncul hanya bila ada permission `identity_access.*`, "Sync" hanya bila ada `sync_storage.*`, keduanya dirender sebagai item non-link berlabel "Segera hadir" karena belum ada halaman tujuan — itu scope issue lain), breadcrumb, `<slot />`. Halaman baru: `src/pages/login.astro` (form tenant ID + login identifier + password, fetch client-side, catatan kode bahwa field tenant ID manual adalah simplifikasi demo), `src/pages/admin/index.astro` (placeholder "lihat Issue 9.1"), `src/pages/admin/settings.astro` (placeholder). Tiga backlog didokumentasikan eksplisit (README modul + `src/README.md` baru): tenant switcher tidak bisa nyata selama `awcms_identities.tenant_id` masih 1:1 per tenant (tidak ada cross-tenant identity linking), sync indicator menunggu endpoint sync-health admin-facing (Issue 9.1, `GET /sync/status` yang ada sekarang HMAC node-auth bukan bearer/cookie manusia), dan katalog i18n/PO (tidak ada issue GitHub untuk itu — string UI hardcode `id`). Test baru: 3 kasus `resolveSsrContext` (cookie kosong/parsial mengembalikan `null` tanpa menyentuh database — konsisten dengan gaya test repo yang tidak bergantung pada `DATABASE_URL`) ditambahkan ke `tests/identity-access.test.ts` (125 test total, semua lulus). Tidak ada perubahan skema/OpenAPI/AsyncAPI (cookie tambahan tidak mengubah body JSON yang dikontrak).

     **Bug ditemukan+diperbaiki saat verifikasi live**: desain awal menaruh pengecekan sesi + `return Astro.redirect("/login")` langsung di frontmatter `AdminLayout.astro` (dipakai sebagai komponen anak dari `src/pages/admin/index.astro`/`settings.astro`, bukan sebagai halaman itu sendiri). Live-test terhadap server Astro SSR sungguhan menunjukkan `GET /admin` tanpa cookie mengembalikan `200` berbadan `Internal server error`, dan log server menunjukkan `AstroError: ResponseSentError` ("The response has already been sent to the browser and cannot be altered") — SSR streaming Astro sudah mulai mem-flush output halaman sebelum frontmatter komponen anak selesai resolve, sehingga tidak bisa lagi menyisipkan Response redirect di titik itu. Diperbaiki dengan memindahkan guard ke `src/middleware.ts` baru (`onRequest`, konvensi file middleware Astro) yang mencocokkan prefix `/admin`, memanggil `resolveSsrContext` **sebelum** rendering apa pun dimulai (aman untuk redirect, tidak kena masalah streaming), dan menaruh context di `context.locals.ssrContext` (tipe dideklarasikan di `src/env.d.ts` baru, `App.Locals.ssrContext`) untuk dibaca `AdminLayout.astro` tanpa query ulang. `AdminLayout.astro` sekarang hanya melempar error invariant (bukan mencoba redirect) bila `Astro.locals.ssrContext` kosong — kondisi yang seharusnya tidak pernah terjadi karena middleware sudah menjamin itu untuk seluruh route `/admin/*`.

     Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan hasil build (container app terpisah berbagi network namespace container Postgres via `--network container:<nama-pg>`, pola yang sama seperti Issue 6.3, karena publish port ke host stall di sandbox ini): migration `db:migrate` apply bersih (9 migration, tidak ada perubahan skema issue ini); `GET /admin` tanpa cookie → `302` ke `/login` (setelah perbaikan bug di atas); `POST /setup/initialize` membuat tenant "Acme Inc" + owner; `POST /auth/login` dengan header tenant mengembalikan `Set-Cookie` untuk `awcms_session` dan `awcms_tenant_id` (httpOnly, `SameSite=Lax`, `Max-Age=7200` sesuai `AUTH_SESSION_TTL_MIN=120`) sekaligus body JSON `{token, expiresAt}` yang tidak berubah; `GET /admin` dengan kedua cookie → `200`, HTML mengandung nama tenant ("Acme Inc"), item nav "Access & Users" dan "Sync" (owner Issue 12.1 punya seluruh permission), markup tenant-switcher/sync-indicator/theme-toggle, dan tombol logout; `POST /auth/logout` lewat cookie (tanpa header `Authorization`, membuktikan fallback cookie bekerja) mengembalikan `Set-Cookie` penghapus untuk kedua cookie dan body `{loggedOut:true}`; `GET /admin` sesudahnya dengan cookie yang sama → `302` ke `/login` lagi (sesi benar-benar tercabut, bukan cuma cookie dihapus di klien); alur bearer-token lama (header `Authorization`/`X-AWCMS-Tenant-ID`, tanpa cookie sama sekali) diverifikasi terpisah tetap berfungsi tidak berubah — login lalu logout via header, `GET /auth/me` sesudahnya `401`; `GET /api/v1/health` (di luar prefix `/admin`) tidak tersentuh middleware. **Catatan keterbatasan verifikasi**: seluruh pembuktian di atas berbasis struktur HTML mentah lewat `curl`/log server — sandbox ini tidak punya browser sungguhan, jadi rendering visual, interaktivitas klik (theme toggle/logout), dan CSS di-computed **tidak** diverifikasi visual, hanya keberadaan markup/atribut/header HTTP yang benar. Tidak ada bug lain ditemukan setelah perbaikan middleware; `bun run check` (lint, check:docs, api:spec:check, typecheck, test, build) lulus bersih.

   - **Issue 9.1 diimplementasikan pada 2026-07-05**: Management Reporting Views — menutup dua backlog yang eksplisit dideferred oleh Issue 8.1 (dashboard placeholder "lihat Issue 9.1" dan `SyncIndicator active={false}` stub). Migrasi `sql/010_awcms_management_reporting_permission_schema.sql` **tidak menambah tabel** — hanya seed **satu** permission baru (`reporting.dashboard.read`) ke katalog global `awcms_permissions`, sengaja tidak dipecah jadi empat permission karena keempat view adalah satu fitur dashboard. Modul baru `src/modules/reporting/` (dependency: `tenant_admin`, `identity_access`, `sync_storage`, mengikuti struktur `module.ts`/`domain/`/`application/`/`README.md` yang sama seperti `sync-storage`) berisi murni live read-aggregation atas tabel yang sudah ada dari migration 002-009 — tidak ada tabel/worker/cache baru. Empat endpoint baru (bearer session, pola guard identik `GET /sync/conflicts`: `resolveTenantContext` + `fetchGrantedPermissionKeys` + `evaluateAccess` default-deny + `recordDecisionLog`, digerbang `{moduleKey:"reporting", activityCode:"dashboard", action:"read"}`): `GET /reports/tenant-activity` (nama/status/`created_at` tenant, `activeUserCount` dari `awcms_tenant_users` — tabel ini **tidak punya** kolom `deleted_at` jadi hanya difilter `status='active'`, `activeOfficeCount` dari `awcms_offices`, `MAX(last_login_at)` dari `awcms_identities`), `GET /reports/access-audit` (`awcms_abac_decision_logs` grouped by `decision` window 30 hari + total all-time, plus `awcms_profile_audit_logs` count sebagai proxy audit umum), `GET /reports/sync-health` (agregasi `awcms_sync_nodes`/`awcms_sync_conflicts`/`awcms_object_sync_queue`, plus flag turunan `hasOpenConflicts`/`hasFailedObjects`/`isHealthy` dari fungsi murni `shapeSyncHealth` di `domain/sync-health.ts` — `isHealthy` = minimal satu node aktif DAN tidak ada conflict terbuka DAN tidak ada objek gagal), dan `GET /reports/module-usage` (iterasi `listModules()`, satu row-count "ada data" generik per modul: `tenant_admin`→office, `profile_identity`→profile, `identity_access`→identity, `sync_storage`→sync node, `reporting`→jumlah baris `awcms_permissions` — katalog **global**, bukan tenant-scoped, beda dari empat metrik lain). Setiap `COUNT(*)`/bigint dibungkus `Number(...)` eksplisit mengikuti pelajaran Issue 6.2/6.3. `src/pages/admin/index.astro` (sebelumnya placeholder) sekarang SSR-fetch keempat agregasi **langsung** via `withTenant` (bukan HTTP round-trip ke endpoint sendiri — endpoint tetap ada untuk kontrak API eksternal), merender empat card ringkas; bila `Astro.locals.ssrContext.permissions` tidak memuat `reporting.dashboard.read`, halaman merender panel "Akses ditolak" (bukan card kosong, bukan 500). `src/layouts/AdminLayout.astro` (`SyncIndicator active={false}` stub sejak Issue 8.1) sekarang memakai `fetchSyncIndicatorActive` (`application/sync-indicator.ts`) — **satu query `EXISTS` ringan**, sengaja bukan reuse agregasi penuh `sync-health`, karena layout ini render di setiap request `/admin/*`; formula "aktif" sama dengan `isHealthy`. Sidebar "Access & Users"/"Sync" tetap stub "Segera hadir" (tidak disentuh — di luar scope issue ini). OpenAPI diperbarui (4 path baru tag "Management Reporting" + 6 schema baru: `TenantActivityReport`, `AccessAuditReport`, `SyncHealthReport`, `ModuleUsageEntry`/`ModuleUsageReportResponse`). Test baru `tests/reporting.test.ts` (6 kasus: `permissionKey` guard + 5 skenario `shapeSyncHealth` — sehat, zero-node, open-conflict, failed-object, passthrough counts) + assersi migration `010` (seed tepat 1 baris, `ON CONFLICT DO NOTHING`, tidak ada `CREATE TABLE`/RLS baru) + update test registry modul (5 modul) di `tests/foundation.test.ts` (132 test total, semua lulus). Fungsi I/O (`fetchTenantActivityReport` dkk., `fetchSyncIndicatorActive`) sengaja **tidak** ditest lewat `bun:test` (konsisten dengan gaya test repo yang tidak bergantung `DATABASE_URL`) — divalidasi lewat live verification di bawah.

     Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan hasil build (container app terpisah berbagi network namespace container Postgres via `--network container:<nama-pg>`, pola yang sama seperti Issue 6.3/8.1): migration `db:migrate` apply bersih (10 migration; re-run kedua seluruhnya `skip` — idempotent); `SELECT` langsung mengonfirmasi baris `reporting.dashboard.read` tersimpan. `POST /setup/initialize` membuat tenant "Acme Inc" + owner (owner mendapat seluruh permission termasuk `reporting.dashboard.read` baru, sesuai seeding Issue 12.1); login owner (bearer dan cookie) berhasil. `GET /admin` dengan cookie owner → `200`, HTML mengandung markup `class="dashboard-grid"` nyata (bukan hanya definisi CSS) berisi empat card dengan angka real (`activeUserCount:1`, `activeOfficeCount:1`, `Node aktif: 0/0`, `Status: Perlu perhatian`, tabel modul dengan `recordCount` sesuai state awal). Untuk membuktikan dashboard **mengikuti** perubahan state nyata (bukan angka statis): (a) insert office kedua langsung ke Postgres (belum ada endpoint CRUD office di base ini) → `GET /reports/tenant-activity` dan `GET /admin` keduanya menunjukkan `activeOfficeCount`/"Kantor aktif" naik 1→2; (b) push event sync via HMAC (`POST /sync/push`, auto-register node) → `GET /reports/sync-health` menunjukkan `totalNodeCount`/`activeNodeCount` 0→1, `isHealthy:true`, `mostRecentPushedAt` terisi, dan `<SyncIndicator>` di `/admin` berubah `data-status="inactive"` → `data-status="active"`; (c) insert baris `awcms_object_sync_queue` `status='failed'` langsung → `GET /reports/sync-health` menunjukkan `hasFailedObjects:true`, `isHealthy:false`, dan `SyncIndicator` kembali ke `data-status="inactive"` — membuktikan formula "sehat" bekerja simetris di kedua arah (baik endpoint maupun indicator ringan), persis skenario yang ditest murni di `shapeSyncHealth`. Keempat endpoint `GET /reports/*` dikonfirmasi mengembalikan JSON number **asli** (tidak berkutip) untuk seluruh count/bigint (`activeUserCount`, `totalNodeCount`, `recordCount`, dst.) saat dipanggil langsung dengan bearer token — tidak ada regresi bigint-as-string. **Kasus negatif (default-deny)**: dibuat tenant-user kedua langsung via SQL (`noperm@acme.test`, password di-hash dengan `Bun.password.hash` yang sama seperti kode aplikasi) **tanpa assignment role sama sekali** (mirip cara Issue 2.4 membuktikan default-deny) — `GET /reports/tenant-activity` dengan bearer token user ini → `403 ACCESS_DENIED` (`"No role permission grants this action."`); `GET /admin` dengan cookie user ini → `200` (bukan 500) dan HTML mengandung `class="permission-denied"` berisi teks "Akses ditolak" + nama permission `reporting.dashboard.read`, sementara `class="dashboard-grid"` (elemen nyata, bukan definisi CSS) **tidak ada** di body — mengonfirmasi tidak ada card kosong/diam-diam ditampilkan. Edge-case header juga diverifikasi: tanpa header tenant → `400 TENANT_REQUIRED`; tanpa bearer token → `401 AUTH_REQUIRED`. **Catatan keterbatasan verifikasi**: seluruh pembuktian di atas berbasis structure HTML mentah + header HTTP lewat `curl` — sandbox ini tidak punya browser sungguhan, jadi rendering visual/CSS ter-computed/interaktivitas **tidak** diverifikasi visual, hanya keberadaan markup/atribut/JSON yang benar. **Tidak ada bug ditemukan pada kode aplikasi** selama verifikasi live issue ini (pelajaran bigint Issue 6.2/6.3 diterapkan preventif sejak awal, seperti Issue 6.3). Satu kekeliruan murni pada skrip verifikasi manual milik agen (bukan kode produk): percobaan pertama `POST /sync/push` memakai timestamp Unix-epoch-detik untuk header `X-AWCMS-Timestamp`, padahal `isTimestampWithinSkew` (`domain/sync-hmac.ts`, tidak berubah di issue ini) memakai `new Date(timestamp)` yang mengharapkan string ISO-8601 — percobaan kedua dengan `new Date().toISOString()` berhasil; tidak ada perubahan kode akibat ini, murni kesalahan skrip uji ad hoc. `bun run check` (lint, check:docs, api:spec:check, typecheck, test, build) lulus bersih.

     **Diskrepansi rencana migration doc 09 (dicatat, tidak direkonsiliasi)**: `docs/awcms/09_roadmap_repository_commit.md` §Migration order final rekomendasi merencanakan slot `010` untuk `010_awcms_logging_observability_schema.sql` (Issue 10.1 — Logging) dan slot `016_awcms_management_dashboard_reporting_schema.sql` untuk Management Reporting (tersirat migration yang jauh lebih besar dari sekadar seed permission, sejalan rencana `019_awcms_dashboard_materialized_views.sql` di slot terpisah). Realisasi aktual (issue ini dikerjakan segera setelah 8.1 sesuai §Koreksi urutan sprint — M7 tidak bergantung M8) memakai slot `010` untuk migration reporting (permission-seed-only) yang sebenarnya. Doc 09 sendiri sudah punya catatan bahwa penomoran adalah rencana dan "nomor final ditentukan saat setiap issue benar-benar diimplementasikan berurutan" (preseden sama seperti migration `006` yang bergeser dari rencana slot `015`) — sesuai instruksi, doc 09 **tidak diubah** di sini; maintainer yang memutuskan rekonsiliasi (mis. menggeser rencana `010_awcms_logging_observability_schema.sql` ke slot berikutnya saat Issue 10.1 benar-benar dikerjakan).

   - **Issue 10.1 diimplementasikan pada 2026-07-05**: Structured Logging and Audit Trail. Migrasi `sql/011_awcms_audit_logging_schema.sql` menambahkan `awcms_audit_events` — tabel audit trail **generik lintas modul**, tenant-scoped, RLS (index `(tenant_id, created_at DESC)` + `(tenant_id, resource_type, resource_id)`), berbeda dari dan melengkapi dua tabel audit sempit yang sudah ada (`awcms_profile_audit_logs` migration 003, hanya lifecycle profile; `awcms_abac_decision_logs` migration 005, hanya keputusan allow/deny) — sink umum untuk aksi high-risk apa pun (soft delete/restore/purge, login, access assignment, price change, transaksi posted/cancel/return, stock adjustment, transfer, ekspor Coretax, resolusi konflik sync, tool call AI, keputusan security readiness), sesuai doc 03 & skill `awcms-audit-log`. Migration juga seed dua permission baru: `logging.audit_trail.read` (baru sepenuhnya) dan `profile_identity.profile_management.purge` (`.delete`/`.restore` sudah diseed migration 005 sejak Issue 2.4, mendahului endpoint apa pun — hanya `.purge` yang genuinely baru di issue ini). Modul baru `src/modules/logging/` (dependency: `tenant_admin`) berisi `application/audit-log.ts` (`recordAuditEvent(tx, input: AuditEventInput)`, tipe `AuditEventInput` **persis** kontrak doc 10 §Audit helper, meredaksi `attributes` sebelum INSERT). Fungsi redaksi cross-cutting baru `src/modules/_shared/redaction.ts` (`redactSensitiveAttributes`, pure, rekursif ke object/array bersarang, case-insensitive substring match terhadap 13 redaction key doc 10 §Logger redaction) dipakai **baik** oleh audit trail **maupun** oleh structured JSON logger baru `src/lib/logging/logger.ts` (`log(level, message, context)` — independen dari database, satu baris JSON ke stdout, gated `LOG_LEVEL` dengan severity ordering `debug<info<warning<error`, default `info`) — satu implementasi redaksi, bukan dua, sesuai doc 10 "audit melengkapi, bukan menggantikan, domain event & structured log". Union `AccessAction` (`src/modules/identity-access/domain/access-control.ts`) diperluas dengan `restore`/`purge` (doc 10 §ABAC guard: "Tambahkan action `restore` dan `purge` pada kontrak modul yang membutuhkan pemulihan atau purge retention") dan keduanya ditambahkan ke `HIGH_RISK_ACTIONS` — perluasan murni tipe/set, `evaluateAccess` tidak berubah logikanya. Correlation ID: `src/middleware.ts` (sebelumnya hanya guard `/admin/*` sejak Issue 8.1) diperluas **additive** untuk berjalan pada **setiap** request — membaca `X-Correlation-ID` masuk atau membangkitkan `crypto.randomUUID()`, menaruhnya di `context.locals.correlationId` (tipe baru di `src/env.d.ts`), dan menyetel header yang sama pada response keluar; `withTenant` (`src/lib/database/tenant-context.ts`) tipe parameter callback-nya dilebarkan dari `Bun.SQL` ke `Bun.TransactionSQL` (murni pelebaran tipe, tanpa perubahan perilaku runtime — `sql.begin()` sudah mengembalikan `TransactionSQL` sesungguhnya) agar `tx.savepoint(...)` dapat dipakai oleh endpoint purge. Empat endpoint baru: `GET /api/v1/logs/audit` (bearer session, guard `logging.audit_trail.read`, filter opsional `?resourceType=`/`?action=`/`?severity=`, `LIMIT 100 ORDER BY created_at DESC`; **satu-satunya** endpoint yang mewiring correlation ID end-to-end ke `ApiMeta.correlationId` sebagai demonstrasi representatif — endpoint lain tidak diubah untuk menghindari scope creep besar, header `X-Correlation-ID` sendiri tetap diset middleware untuk seluruh request); dan tiga endpoint lifecycle profile tipis pada `awcms_profiles` (Issue 2.2 belum pernah membangun endpoint CRUD sama sekali — issue ini **tidak** membangun profile CRUD penuh, murni demonstrasi audit trail atas resource nyata): `DELETE /api/v1/profiles/{id}` (guard `.delete`, body `{reason: string}` wajib → `delete_reason`, 404 bila tidak ada/sudah dihapus — bukan 200 no-op, audit `severity:"warning"`), `POST /api/v1/profiles/{id}/restore` (guard `.restore` baru, 404 bila belum soft-deleted, audit `severity:"warning"`), `POST /api/v1/profiles/{id}/purge` (guard `.purge` baru, hard `DELETE` sungguhan, wajib sudah soft-deleted — 400 `PURGE_REQUIRES_SOFT_DELETE` bila belum). OpenAPI diperbarui (2 tag baru "Logging & Audit"/"Profile Identity" + 4 path + 6 schema baru: `AuditEventEntry`/`AuditEventListResponse`, `ProfileDeleteRequest`, `ProfileLifecycleResponse`). Test baru: `tests/audit-log.test.ts` (redaksi — top-level, nested object, nested array, substring case-insensitive, `undefined` tetap `undefined`, tidak memutasi input; shape `AuditEventInput`), `tests/logger.test.ts` (5 kasus — satu baris JSON, gating `LOG_LEVEL` default/`debug`/selalu-muncul untuk `warning`/`error`, redaksi context), 2 kasus baru `isHighRiskAction("restore"/"purge")` di `tests/access-control.test.ts`, assersi migration `011` (RLS, check constraint severity, dua index, dua seed permission) di `tests/foundation.test.ts`, dan update registry modul jadi 6 modul (148 test total, semua lulus).

     **Bug ditemukan+diperbaiki saat verifikasi live**: implementasi awal `recordAuditEvent` melakukan `JSON.stringify(redactedAttributes)` sebelum bind ke kolom `attributes jsonb` — ternyata Bun.SQL (seperti `postgres.js`) men-double-encode nilai tersebut: kolom jsonb menyimpan **string berisi teks JSON** (`"{\"reason\":\"...\"}"`, `pg_typeof` tetap `jsonb` tapi isinya JSON _scalar string_, bukan JSON _object_), bukan object jsonb sungguhan — dikonfirmasi lewat `psql` langsung dan lewat `GET /logs/audit` yang mengembalikan `attributes` sebagai string ter-escape, bukan object bersarang. Diperbaiki dengan membind object JS **langsung** (tanpa `JSON.stringify`) — dikonfirmasi lewat skrip Bun ad hoc bahwa Bun.SQL men-serialize object JS mentah ke jsonb dengan benar. **Catatan untuk maintainer**: `src/pages/api/v1/sync/push.ts` (Issue 6.1, tidak disentuh di issue ini karena di luar scope atomic) memakai pola `JSON.stringify(event.payload)` yang identik untuk kolom `payload_json jsonb` — kemungkinan besar bug laten yang sama ada di sana juga, belum terdeteksi karena `pull.ts` tidak memvalidasi bentuk objek hasil baca; direkomendasikan sebagai item audit/fix terpisah. Bug kedua (murni pada skrip verifikasi manual agen, bukan kode produk): percobaan pertama `POST /profiles/{id}/restore` tanpa header `Content-Type: application/json` ditolak `403` oleh CSRF `checkOrigin` bawaan Astro (pola operasional yang sama seperti dicatat README `identity-access` sejak Issue 2.3) — percobaan kedua dengan header tersebut berhasil, tidak ada perubahan kode.

     Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan hasil build (container app terpisah berbagi network namespace container Postgres via `--network container:<nama-pg>`, pola yang sama seperti Issue 6.3/8.1/9.1): migration `db:migrate` apply bersih (11 migration; re-run kedua seluruhnya `skip` — idempotent). `POST /setup/initialize` membuat tenant "Acme Inc" + owner (`moduleCount:6` pada `GET /health` mengonfirmasi modul `logging` terdaftar live); login owner berhasil. `DELETE /profiles/{id}` pada profile owner sendiri dengan `{reason:"..."}` → `200`, `GET /logs/audit` menunjukkan event `action:"delete"`, `severity:"warning"`, `attributes.reason` sebagai object JSON asli (setelah perbaikan bug di atas) — nilai `reason` yang kebetulan menyebut nomor telepon **tidak** diredaksi (benar: redaksi berbasis nama key, bukan pemindaian isi nilai; key `reason` bukan salah satu dari 13 redaction key). `POST /profiles/{id}/restore` → `200`, event audit `action:"restore"` muncul. Kasus negatif purge: (a) purge profile owner **tanpa** soft delete lebih dulu → `400 PURGE_REQUIRES_SOFT_DELETE`; (b) soft-delete ulang profile owner (yang masih terhubung `awcms_identities`) lalu purge → `409 PURGE_BLOCKED_BY_DEPENDENTS` bersih (FK violation Postgres `23503` ditangkap lewat `tx.savepoint(...)`, **bukan** raw 500 — dikonfirmasi dari log server sebelum perbaikan desain savepoint menampilkan raw `PostgresError` tanpa savepoint; setelah savepoint diterapkan, event audit `severity:"critical"`, `message:"Profile purge blocked by dependent records."` berhasil ter-commit **meski** DELETE di dalamnya di-rollback — dikonfirmasi baris profile tetap ada (`deleted_at` tetap terisi, bukan ter-purge) dan event audit tetap muncul di `GET /logs/audit`, membuktikan desain savepoint bekerja: transaction terluar (termasuk decision log ABAC) tidak ikut batal akibat kegagalan statement di dalam savepoint). Kasus sukses: profile fixture dependency-free dibuat lewat `INSERT` SQL langsung (tidak ada endpoint `POST /profiles`, pola yang sama seperti fixture kedua Issue 9.1) → soft delete → purge → `200`, `SELECT count(*)` mengonfirmasi baris benar-benar hilang dari `awcms_profiles`, dan event audit `severity:"critical"`, `message:"Profile purged."` muncul. **Kasus negatif (default-deny)**: tenant-user kedua dibuat langsung via SQL (`noperm@acme.test`, password `Bun.password.hash` sama seperti kode aplikasi, pola sama seperti Issue 2.4/9.1) **tanpa assignment role** → keempat endpoint baru (`GET /logs/audit`, `DELETE /profiles/{id}`, `POST /profiles/{id}/restore`, `POST /profiles/{id}/purge`) seluruhnya `403 ACCESS_DENIED`, dikonfirmasi pula empat baris `deny`/`"No role permission grants this action."` di `awcms_abac_decision_logs`. Header `X-Correlation-ID` dikonfirmasi diset pada response **setiap** endpoint (baik yang lama maupun baru) berkat middleware; `ApiMeta.correlationId` dikonfirmasi terisi echo `X-Correlation-ID` masuk (atau UUID baru bila tidak dikirim) khusus pada `GET /logs/audit` sesuai cakupan demonstrasi yang dipilih. **Catatan keterbatasan verifikasi**: seluruh pembuktian di atas berbasis JSON/HTTP header mentah lewat `curl` dan `psql` langsung — sandbox ini tidak punya browser sungguhan, tidak ada rendering visual yang diverifikasi (issue ini murni backend/API, tidak ada UI baru). `bun run check` (lint, check:docs, api:spec:check, typecheck, test, build) lulus bersih setelah kedua perbaikan di atas.

     **Diskrepansi rencana migration doc 09 (dicatat, tidak direkonsiliasi)**: doc 09 §Migration order final merencanakan slot `011` untuk `011_awcms_logging_observability_schema.sql` — slot **nomor**-nya (`011`) persis seperti realisasi aktual, hanya nama filenya berbeda (`011_awcms_audit_logging_schema.sql`, mengikuti instruksi eksplisit tugas ini) karena scope aktual issue ini condong ke tabel audit trail generik + permission seed, bukan observability/pooling yang lebih luas tersirat nama "logging_observability" (pooling/health tetap scope Issue 10.2, bukan diklaim di sini). Sesuai instruksi, doc 09 **tidak diubah** di sini; maintainer yang memutuskan apakah nama slot `011` perlu diselaraskan.

   - **Issue 10.2 diimplementasikan pada 2026-07-05**: Database Connection Pooling and Backpressure. **Tidak ada migration baru** — pooling/backpressure murni application-level (config `Bun.SQL` + semaphore in-memory + circuit breaker), tidak ada tabel/kolom/permission baru; `bun run db:migrate` tetap berhenti di 11 migration applied setelah issue ini (dikonfirmasi live, lihat di bawah). `src/lib/database/client.ts` (`getDatabaseClient()`) diperluas dengan `max` (dari `DATABASE_POOL_MAX`), `prepare: DATABASE_PGBOUNCER !== "true"`, dan `connection.statement_timeout` (dari `DATABASE_STATEMENT_TIMEOUT_MS`) — **catatan desain**: brief awal meminta hook `onconnect` untuk menjalankan `SET statement_timeout`, tapi `onconnect` pada `Bun.SQL.Options` (`node_modules/bun-types/sql.d.ts`) bertipe `(err: Error | null) => void`, tidak memberi akses client untuk menjalankan SQL (JSDoc contoh `onconnect: (client) => ...` di file tipe yang sama tidak konsisten dengan signature aktualnya) — dipakai opsi `connection` (Postgres client runtime configuration options) sebagai gantinya, cara yang type-correct untuk menerapkan GUC sesi per koneksi pooled; `onconnect` tetap dipakai hanya untuk log kegagalan koneksi. Modul baru murni `src/lib/database/work-class.ts`: semaphore in-memory per proses untuk 5 work class (`critical_transaction` max 10, `interactive` max 8, `reporting` max 4, `background_sync` max 4, `maintenance` max 1 — angka tetap, tidak env-tunable, mengikuti urutan prioritas doc 16), antrean FIFO dengan timeout yang menolak dengan `WorkClassTimeoutError` bertipe (bukan string-matching). Modul baru murni `src/lib/database/circuit-breaker.ts`: breaker 3-state (`closed`/`open`/`half_open`) murni fungsi dari `now: Date` yang di-inject (tidak ada `Date.now()` tersembunyi), singleton module-level `getDatabaseCircuitBreaker()` (failureThreshold 5, openDurationMs 30 detik). Keduanya diintegrasikan ke `withTenant` (`src/lib/database/tenant-context.ts`) via parameter opsional ke-4 `{ workClass?, queueTimeoutMs? }` (default `workClass: "interactive"`, `queueTimeoutMs: 2000`) — alur: cek `circuitBreaker.canAttempt()` dulu (fail-fast `503 DATABASE_BUSY` bila `open`, skip antrean sepenuhnya), lalu `acquireWorkClassSlot()` (timeout → log `database.pool.saturated` via logger terstruktur Issue 10.1 + `503 DATABASE_BUSY`), lalu transaction seperti biasa, `finally` lepas slot, sukses/exception pada transaction memperbarui breaker (response `fail()` dari `fn` yang tidak melempar dihitung "sukses" pada level breaker — breaker mengukur kegagalan transaksi/koneksi, bukan logika bisnis). 10 endpoint direklasifikasi dari default `"interactive"` sesuai pemetaan contoh doc 16: `background_sync` untuk `sync/push.ts`, `pull.ts`, `status.ts`, `objects/index.ts`, `objects/status.ts`, `conflicts/index.ts`, `conflicts/[id]/resolve.ts`; `reporting` untuk keempat `reports/*.ts` (`access-audit`, `module-usage`, `sync-health`, `tenant-activity`) dan `logs/audit.ts`. Endpoint publik baru `GET /api/v1/database/pool/health` (tanpa auth, mengikuti presedan `/api/v1/health`; hanya agregat — status/`databaseReachable`/`circuitBreakerState`/saturasi per work class — tidak pernah data tenant; `SELECT 1` langsung lewat `getDatabaseClient()`, bukan `withTenant`, dibungkus try/catch). OpenAPI diperbarui (tag baru "Database Connectivity" + 1 path + 2 schema baru: `WorkClassSaturation`, `DatabasePoolHealthResponse`). AsyncAPI diperbarui: channel + operation `database.pool.saturated` didokumentasikan (kategori "DB Connectivity" doc 05) — **belum ada dispatcher pub/sub live** untuk event apa pun di repo ini; produsen konkretnya adalah baris log terstruktur yang sama seperti disebut di atas. Test baru `tests/database-pooling.test.ts` (12 kasus: gate mengisi sampai max, antrean saat penuh, FIFO handoff saat release, timeout `WorkClassTimeoutError` + queue ter-clear, saturasi melaporkan max per class; breaker tetap closed di bawah threshold, terbuka setelah threshold tercapai, sukses me-reset consecutive failure, tetap open sebelum `openDurationMs`, half-open tepat setelah elapsed, half-open sukses menutup, half-open gagal membuka ulang + reset timer) — 160 test total, semua lulus, termasuk seluruh 148 test lama (memastikan parameter ke-4 opsional `withTenant` tidak meregresi call site manapun).

     Dokumentasi baru `docs/awcms/database-pooling.md` (mirror gaya `database-migrations.md`): ringkasan arsitektur, tabel work class + alasan angka, alur integrasi `withTenant`, bentuk response health endpoint, catatan `database.pool.saturated`, dan contoh `pgbouncer.ini` (`pool_mode = transaction`, `max_client_conn`/`default_pool_size` selaras `DATABASE_POOL_MAX`) — dokumentasi murni, **tidak** membuat `docker-compose.yml` (tetap scope Issue 12.2). `src/lib/README.md` diperbarui dengan subseksi `database/` yang menyebut keempat file (`client.ts`, `tenant-context.ts`, `work-class.ts`, `circuit-breaker.ts`).

     **Diskrepansi rencana migration doc 09 (dicatat, tidak direkonsiliasi)**: doc 09 §Migration order final mencantumkan slot `012_awcms_database_connection_pooling_schema.sql` untuk Issue 10.2, dan catatan penjelasnya menyebut "observability/pooling penuh (connection pool, backpressure, health) tetap scope Issue 10.2 di `012`". Realisasi aktual **tidak membuat migration apa pun** — pooling/backpressure/circuit-breaker sepenuhnya application-level (config client, semaphore in-memory, breaker in-memory), tidak ada tabel/kolom/permission yang perlu disimpan. Sesuai instruksi tugas, doc 09 **tidak diubah** di sini; maintainer yang memutuskan apakah slot `012` perlu dihapus/diselaraskan dari rencana migration.

     **Tidak ada bug pada kode aplikasi ditemukan** selama implementasi issue ini. Satu insiden operasional murni pada lingkungan verifikasi (bukan kode produk): kontainer Docker yang dipakai untuk build/migrate (berjalan sebagai root, memakai bind-mount repo) meninggalkan `node_modules/.vite/deps` dan `dist/` dengan pemilik `root` di host setelah kontainer berhenti, membuat `bun run build` berikutnya di host gagal `EACCES`/`Permission denied` — diperbaiki dengan menjalankan kontainer sesaat lagi untuk `chown`/`rm -rf` direktori tersebut kembali ke user host; tidak ada perubahan kode.

     Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan hasil build (container app terpisah berbagi network namespace container Postgres via `--network container:<nama-pg>`, pola yang sama seperti Issue 6.3/8.1/9.1/10.1): migration `db:migrate` apply bersih — **tetap 11 migration**, mengonfirmasi issue ini benar-benar tidak menambah migration. `GET /api/v1/health` dan `GET /api/v1/database/pool/health` keduanya `200` sebelum ada beban: `status:"healthy"`, `databaseReachable:true`, `circuitBreakerState:"closed"`, kelima work class menunjukkan `active:0`. `POST /setup/initialize` membuat tenant "Acme Inc" + owner. **Saturasi work-class dipaksa nyata lewat HTTP** (bukan hanya unit test): skrip beban ad hoc (di luar repo, di scratchpad agent) mengirim hingga 500 request `POST /sync/push` (HMAC valid, masing-masing 100 event) secara konkuren ke tenant yang sama — dari 500 request, **47 mengembalikan `503 DATABASE_BUSY`** (sesuai ekspektasi: `background_sync` max 4, `queueTimeoutMs` default 2000ms terlampaui untuk sebagian antrean), dan **selama beban berlangsung** `GET /database/pool/health` yang dipanggil paralel menunjukkan `background_sync` pada `active:4, max:4, queued` naik hingga 251 lalu turun bertahap ke `0` setelah beban selesai — bukti konkret gate bekerja nyata di jalur HTTP, bukan hanya di unit test. Log server pada saat yang sama menunjukkan baris JSON terstruktur `{"level":"warning","message":"database.pool.saturated","workClass":"background_sync","queueTimeoutMs":2000,"saturation":[...]}` — mengonfirmasi jalur log `database.pool.saturated` benar-benar terpanggil pada saturasi nyata. Setelah beban selesai, health kembali `status:"healthy"`, seluruh work class `active:0, queued:0`. **Circuit breaker**: pada satu percobaan awal beban (sebelum diperbaiki), skrip uji ad hoc memakai `aggregateId` non-UUID yang menyebabkan **kegagalan nyata berulang** di Postgres (`PostgresError: invalid input syntax for type uuid`) pada tiap request — ini murni bug pada skrip verifikasi (bukan kode produk), tapi secara tidak sengaja menjadi bukti live yang representatif: breaker terbuka nyata (`circuitBreakerState:"open"`, `status:"unhealthy"` pada health endpoint) setelah kegagalan transaksi berturut-turut; ~30 detik kemudian (`openDurationMs`), satu request baru dengan payload valid dikirim dan **berhasil (`200`)**, dan pemanggilan health endpoint sesudahnya mengonfirmasi breaker kembali `"closed"` — membuktikan siklus penuh `closed → open → half_open → closed` bekerja nyata dengan kegagalan database sungguhan (bukan hanya `now: Date` sintetis di unit test), meski dipicu secara tidak sengaja lewat bug skrip alih-alih injeksi kegagalan yang sengaja direkayasa. **Catatan keterbatasan verifikasi**: seluruh pembuktian di atas berbasis JSON/HTTP mentah lewat skrip Bun ad hoc dan `curl` — sandbox ini tidak punya browser sungguhan (issue ini murni backend, tidak ada UI baru, jadi tidak relevan). `bun run check` (lint, check:docs, api:spec:check, typecheck, test, build) lulus bersih.

   - **Issue 10.3 diimplementasikan pada 2026-07-05**: Production Security Readiness Checklist. **Tidak ada migration baru** — murni tiga skrip CLI baru + wiring `package.json`, tidak ada perubahan schema/OpenAPI/AsyncAPI (dikonfirmasi `bun run db:migrate` tetap 11 migration, `bun run api:spec:check` tetap OK tanpa perubahan). `scripts/db-pool-health.ts` (`bun run db:pool:health`): wrapper CLI tipis atas `GET /api/v1/database/pool/health` (Issue 10.2), base URL dari `APP_URL` (default `http://localhost:4321`), exit 0 untuk `"healthy"`/`"degraded"` (degraded tetap warning-pass, sesuai semantik 3-tier endpoint itu sendiri), exit non-zero untuk `"unhealthy"` **atau** fetch gagal total (server belum jalan) — keduanya hard failure eksplisit, tidak pernah terlihat lulus diam-diam. `scripts/security-readiness.ts` (`bun run security:readiness`): 10 check bernama, masing-masing `{name, severity, status, evidence}`, exit non-zero bila ada satu saja check `critical` berstatus `fail` — 7 critical (no hardcoded secret via heuristik regex `git ls-files` di `src/`/`scripts/`/config; `.env` tidak tertrack; password hash argon2id; login lockout; RLS aktif; ABAC default-deny; audit table reachable), 2 warning (soft delete/restore/purge permission+audit; sync HMAC secret bukan placeholder — `info` bila sync mati by design), 1 warning/info best-effort (error tidak expose stack trace, butuh server hidup). `scripts/production-preflight.ts` (`bun run production:preflight`): orkestrasi `db:migrate` → `api:spec:check` → `bun test` → `build` → (`db:pool:health` **hanya bila** probe `GET /api/v1/health` menjawab, kalau tidak dicatat `skipped` bukan `failed`) → `security:readiness`, seluruh tahap tetap dijalankan meski tahap sebelumnya gagal (bukan fail-fast, supaya satu kegagalan tidak menyembunyikan yang lain), verdict akhir `GO-LIVE DIIZINKAN`/`GO-LIVE DIBLOKIR`. `bun install` sengaja tidak diorkestrasi (env-setup, bukan readiness check). **Item eksplisit di luar cakupan** (dicetak sebagai bagian laporan sendiri, bukan disembunyikan): tax data masking, CRM opt-out, AI read-only (tidak ada modul domain tersebut di base generik ini) — dan PostgreSQL tidak public, least-privilege DB user, backup/restore tested, PostgreSQL version pinned (concern `docker-compose.yml`/deployment profile, masih Issue 12.2 yang belum ada, tidak bisa diverifikasi dari kode aplikasi saja). Dokumentasi baru `docs/awcms/production-readiness.md` (mirror gaya `database-pooling.md`): pemetaan lengkap checklist doc 07 → otomatis/manual-deferred/out-of-scope, cara menjalankan sebelum go-live. Test baru `tests/security-readiness.test.ts` (13 kasus: heuristik `scanLineForHardcodedSecret` termasuk kasus negatif placeholder/member-expression/`process.env`, `checkAbacDefaultDeny`, `checkLoginLockoutImplemented`, `checkSyncHmacSecretNotDefault` tiga cabang) — check yang butuh Postgres sungguhan (RLS, audit table) sengaja **tidak** di-unit-test dengan DB palsu, dibuktikan lewat verifikasi live saja (173 test total, semua lulus).

     **Deviasi terdokumentasi dari brief awal**: brief meminta check "password hash modern" meng-grep `src/lib/auth/password.ts` untuk literal string `"argon2id"` — tapi file itu memanggil `Bun.password.hash(password)` tanpa argumen algoritma sama sekali, mengandalkan default Bun yang didokumentasikan di `node_modules/bun-types/bun.d.ts` (`@default "argon2id"`), sehingga grep literal akan **selalu gagal** terhadap kode yang sebenarnya aman — bukan sinyal yang benar-benar verifiable. Diganti dengan pemanggilan nyata `hashPassword()` dan memeriksa awalan hash yang dihasilkan (`$argon2id$`) — pembuktian fungsional yang lebih kuat daripada grep, didokumentasikan sebagai komentar di kode. Daftar tabel RLS-free juga diperluas dari 4 yang disebut brief (`awcms_permissions`/`awcms_tenants`/`awcms_setup_state`/`awcms_schema_migrations`) menjadi 5 — ditambah `awcms_modules` (`sql/001`, registry modul global tanpa `tenant_id`) yang sama-sama RLS-free tapi tidak disebut eksplisit di brief; dikonfirmasi lewat inspeksi langsung `sql/001_awcms_foundation_schema.sql`, bukan tebakan.

     Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan hasil build (container app terpisah berbagi network namespace container Postgres via `--network container:<nama-pg>`, pola yang sama seperti Issue 6.3/8.1/9.1/10.1/10.2): migration `db:migrate` apply bersih 11/11 (fresh database, bukan re-run — tetap 11 migration, mengonfirmasi tidak ada migration baru). `bun run security:readiness` terhadap DB live: seluruh 7 check critical + 2 check warning `PASS`, check RLS mengonfirmasi **26 tabel tenant-scoped** benar-benar `relrowsecurity=true` dan mengecualikan 5 tabel RLS-free yang didokumentasikan di atas; exit 0. **Pembuktian gate nyata (deliberately-broken RLS)**: `ALTER TABLE awcms_offices DISABLE ROW LEVEL SECURITY` langsung di Postgres (mutasi DB sementara untuk verifikasi, bukan migration) → `bun run security:readiness` langsung melaporkan `[FAIL] (critical) RLS enabled on tenant-scoped tables` dengan evidence `"Tenant-scoped table(s) without RLS enabled: awcms_offices."`, ringkasan `1 critical failure(s)`, `GO-LIVE DIBLOKIR`, exit non-zero — dikonfirmasi skrip **benar-benar mendeteksi** kondisi nyata, bukan skrip yang selalu mencetak "pass"; `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` mengembalikan ke kondisi semula, re-run `security:readiness` kembali `PASS`/exit 0/`relrowsecurity=true`. `bun run db:pool:health` terhadap server Astro SSR hasil build sungguhan yang berjalan (`bun run start` di background) → `200`, `status:"healthy"`, exit 0. `bun run production:preflight` **dengan server hidup**: seluruh 6 tahap (`db:migrate`/`api:spec:check`/`test`/`build`/`db:pool:health`/`security:readiness`) `PASS`, verdict `GO-LIVE DIIZINKAN`, exit 0 — termasuk check "error tidak expose stack trace" yang beralih dari `info` (server mati) ke `warning`/`PASS` nyata (`POST /sync/push` tanpa header HMAC → `400` bersih tanpa jejak stack trace). `bun run production:preflight` **tanpa server** (server dihentikan) → tahap `db:pool:health` dicatat `[SKIP] db:pool:health (no server reachable at http://localhost:4321)` — bukan `FAIL` — sementara 5 tahap lain tetap `PASS`, verdict tetap `GO-LIVE DIIZINKAN`, membuktikan desain skip-graceful bekerja pada kedua skenario (server hidup dan mati). **Catatan keterbatasan verifikasi**: seluruh pembuktian di atas HTTP/CLI murni lewat `docker exec`/`bun`/`psql` langsung — sandbox ini tidak punya browser sungguhan, tidak relevan untuk issue ini karena murni tooling backend/CLI, tidak ada UI baru.

     **Bug/insiden ditemukan+diperbaiki saat verifikasi** (operasional, bukan kode produk): (1) kontainer Alpine `oven/bun:1-alpine` yang dipakai untuk verifikasi tidak menyertakan `git` secara default — dua check (`No hardcoded secret`, `.env not tracked`) melaporkan `FAIL`/"Executable not found in $PATH: git" saat pertama dijalankan di kontainer itu; ini **membuktikan** desain "jangan crash, laporkan fail dengan pesan jelas" bekerja sesuai spesifikasi, bukan bug kode — diperbaiki di sisi lingkungan verifikasi dengan `apk add git` + `git config --global --add safe.directory /app` (masalah kepemilikan bind-mount root-vs-host yang sama seperti dicatat Issue 10.2), bukan perubahan kode; (2) di dalam kontainer yang berbagi network namespace dengan kontainer Postgres, `wget http://localhost:4321/...` gagal "connection refused" sementara `wget http://127.0.0.1:4321/...` berhasil — `/etc/hosts` meresolusi `localhost` ke `::1` lebih dulu dan server hanya bind IPv4, sedangkan `wget` tidak fallback ke IPv4; dikonfirmasi `fetch()` bawaan Bun (dipakai skrip produk) **tidak** mengalami masalah ini (fallback dual-stack bekerja normal) — murni kuirk `wget` di lingkungan verifikasi ad hoc, tidak ada perubahan kode; (3) kontainer Docker yang dipakai untuk build (berjalan sebagai root, bind-mount repo) kembali meninggalkan `node_modules/.vite/deps` dan `dist/` berpemilik `root` di host setelah kontainer dihentikan (pola identik Issue 10.2) — diperbaiki dengan kontainer Alpine sesaat untuk `rm -rf`, tidak ada perubahan kode. `bun run check` (lint, check:docs, api:spec:check, typecheck, test, build) lulus bersih di host setelah pembersihan tersebut.

   - **Issue 11.1 diimplementasikan pada 2026-07-05**: Workflow Approval Engine. Migration `sql/012_awcms_workflow_approval_schema.sql` (slot `012` dikonfirmasi bebas sebelum menulis — `ls sql/` menunjukkan `011` sebagai migration terakhir, sesuai ekspektasi karena Issue 10.2/10.3 tidak menambah migration) menambahkan persis 4 tabel workflow sesuai doc 04 §Workflow (`awcms_workflow_definitions` dengan `steps` jsonb tervalidasi di domain logic bukan constraint SQL, unique parsial `(tenant_id, workflow_key) WHERE deleted_at IS NULL`; `awcms_workflow_instances`; `awcms_workflow_tasks`; `awcms_workflow_decisions` append-only RLS-only-satu-policy seperti `awcms_abac_decision_logs`/`awcms_audit_events`) plus **satu tabel infrastruktur generik baru** `awcms_idempotency_keys` (doc 16 §Idempotency store: `key`, request hash, status, response/resource) — konsumen pertamanya karena base ini belum punya endpoint mutation high-risk lain (tidak ada POS/warehouse/tax di base generik ini) meski parameter OpenAPI `IdempotencyKey` sudah lama disediakan tanpa pernah dipakai. **Catatan diskrepansi non-blocking**: doc 04 §Table ownership matrix mencantumkan `awcms_idempotency_keys` di grup ilustratif "Sales POS" — dicatat di README modul sebagai bukan kepemilikan eksklusif (doc 16, dokumen backend generik, mendeskripsikannya sebagai infrastruktur lintas modul); doc 04 sendiri **tidak diedit** sesuai batasan tugas. Domain logic murni baru `src/modules/workflow-approval/domain/workflow-transition.ts` (`evaluateDecisionOutcome` — reject di step manapun akhiri instance `rejected` tanpa task baru; approve di tengah maju ke step berikut tetap `pending`; approve di step terakhir jadi `approved`; `validateWorkflowSteps` — non-empty, `stepOrder` positif kontigu dari 1, `name` wajib; `validateWorkflowDecisionRequestBody`). Aplikasi internal-only `application/workflow-instance.ts` (`startWorkflowInstance`) — **tidak ada endpoint publik create-definition/start-instance** karena doc 17 seed hanya mengesahkan `workflow_approval.approval: read, approve` (tidak ada create/configure), pola yang sama seperti Issue 6.3 (tidak ada dispatcher R2 nyata) dan Issue 10.1 (hanya 3 endpoint lifecycle profile tipis). Dua endpoint publik persis "decision API" yang disahkan doc 17: `GET /api/v1/workflows/tasks` (guard `workflow.approval.read`, `LIMIT 100 ORDER BY created_at ASC`) dan `POST /api/v1/workflows/tasks/{id}/decisions` (guard `workflow.approval.approve` — satu action untuk approve **maupun** reject, mencerminkan pola `sync_storage.conflict_resolution.approve` yang sudah ada). Self-approval **dipakai ulang, bukan mekanisme baru** — `evaluateAccess` (`src/modules/identity-access/domain/access-control.ts`, Issue 2.4) sudah punya cek `action === "approve" && requestedBy === context.tenantUserId`; endpoint decision hanya `SELECT` task+instance untuk `requested_by_tenant_user_id` **sebelum** memanggil `evaluateAccess` supaya nilai itu benar. Idempotency (`src/modules/_shared/idempotency.ts`, helper generik dipakai bersama): `Idempotency-Key` wajib (`400 IDEMPOTENCY_REQUIRED` bila hilang), key sama+hash sama → replay response tersimpan, key sama+hash beda → `409 IDEMPOTENCY_CONFLICT`; task yang statusnya sudah bukan `pending` juga `409 IDEMPOTENCY_CONFLICT` (lapis tambahan meniru preseden `POST /sync/conflicts/{id}/resolve`). Modul `workflow` (module key sengaja disingkat dari `workflow_approval` mengikuti preseden `logging`/`reporting` yang juga memendekkan doc 17) didaftarkan (`dependencies: ["tenant_admin", "identity_access"]`). OpenAPI: 2 path baru + 4 schema baru (`WorkflowTaskItem`, `WorkflowTaskListResponse`, `WorkflowTaskDecisionRequest`/`Response`), memakai parameter `IdempotencyKey` yang sudah ada. Test baru: `tests/workflow-approval.test.ts` (23 kasus: `evaluateDecisionOutcome` reject-di-step-manapun/approve-tengah/approve-terakhir/single-step, `validateWorkflowSteps` valid/empty/non-array/non-contiguous/missing-name/non-positive, `validateWorkflowDecisionRequestBody` valid approve/reject+reason/invalid-decision/non-string-reason, `computeRequestHash` stabil-terlepas-urutan-key/berbeda-bila-payload-beda) + assersi migration-shape pada `tests/foundation.test.ts` (5 tabel baru RLS aktif, dedup key, check constraint decision, seed 2 permission) + update assersi module-registry (7 modul) dan daftar migration (`012`) yang sudah ada (191 test total, semua lulus).

     **Bug ditemukan+diperbaiki selama verifikasi live** (kode produk, bukan hanya skrip verifikasi): `saveIdempotencyRecord` (`src/modules/_shared/idempotency.ts`) awalnya menulis `${JSON.stringify(responseBody)}` ke kolom `response_body jsonb` — dikonfirmasi lewat `startWorkflowInstance` fixture yang gagal dengan `steps must be a non-empty array` meski `steps` sudah diisi benar, root cause ditelusuri ke pola serupa: melewatkan parameter yang **sudah** `JSON.stringify`-kan (string) ke kolom jsonb via tagged-template Bun.SQL membuat Postgres menyimpan **string berisi JSON ter-escape ganda**, bukan jsonb asli — dibuktikan lewat eksperimen langsung (`INSERT ... VALUES (${JSON.stringify(obj)})` dibaca kembali sebagai `string`, sementara `INSERT ... VALUES (${obj})` dengan objek/array mentah dibaca kembali sebagai `object`/`array` asli — persis pola yang sudah benar dipakai `recordAuditEvent` di `audit-log.ts`, yang melewatkan `redactedAttributes` mentah tanpa `JSON.stringify`). Diperbaiki: `saveIdempotencyRecord` diubah melewatkan `responseBody` mentah (bukan `JSON.stringify(responseBody)`); dikonfirmasi lewat `psql` langsung `pg_typeof(response_body)` mengembalikan `jsonb` dan isinya objek asli, bukan string. Fixture verifikasi (`startWorkflowInstance` untuk `steps`) mengalami bug yang sama dan diperbaiki dengan pola yang sama sebelum verifikasi dilanjutkan — **bukan** perubahan pada kode modul workflow-approval itu sendiri (`startWorkflowInstance`/`validateWorkflowSteps` sudah benar dari awal; sumber bug murni di helper idempotency generik dan skrip fixture ad hoc).

     Diverifikasi live terhadap PostgreSQL 16 + server Astro SSR sungguhan hasil build (container app terpisah berbagi network namespace container Postgres via `--network container:<nama-pg>`, pola yang sama seperti Issue 6.3/8.1/9.1/10.1/10.2/10.3): migration `db:migrate` apply bersih 12/12 (fresh database); re-run kedua seluruh 12 migration `skip` (idempotent). `POST /setup/initialize` membuat tenant "Acme Inc" + owner (`GET /health` `moduleCount:7` mengonfirmasi modul `workflow` terdaftar live). Workflow definition (2 step: "Manager approval", "Director approval") dibuat lewat `INSERT` SQL langsung (tidak ada endpoint create — sesuai desain), dua instance dimulai lewat pemanggilan nyata `startWorkflowInstance` dari skrip Bun ad hoc (bukan simulasi tangan) yang meng-import fungsi aplikasi asli dari `src/modules/workflow-approval/application/workflow-instance.ts`. `GET /workflows/tasks` sebagai owner → `200`, menampilkan kedua task step-1 `pending` dengan `workflowKey`/`workflowName`/`requestedByTenantUserId` terisi benar. **Bukti self-approval-denied konkret**: `POST /workflows/tasks/{task1.id}/decisions` dengan token **owner sendiri** (yang me-request instance itu) → `403 ACCESS_DENIED` `{"message":"Self-approval is not allowed."}` — bukti mekanisme ter-wiring end-to-end, bukan cuma ada di `access-control.ts`. Role `workflow_approver` dibuat lewat SQL langsung (assignment `workflow.approval.read`+`.approve`) untuk tenant-user kedua ("approver@acme.test"); approve task step-1 sebagai approver → `200`, `instanceStatus:"pending"`, `nextStepOrder:2`, task step-2 baru muncul di `GET /workflows/tasks`; approve task step-2 (step terakhir) sebagai approver yang sama → `200`, `instanceStatus:"approved"`, `nextStepOrder:null`, tidak ada task baru — dikonfirmasi juga lewat `psql` langsung (`current_step_order:2`, `status:'approved'`, tepat 2 baris task keduanya `completed`). **Bukti jalur reject**: instance kedua di-reject pada task step-1-nya sebagai approver (bukan requester) → `200`, `instanceStatus:"rejected"` **seketika**, tidak ada task step-2 dibuat — dikonfirmasi `psql` (`current_step_order` tetap `1`, hanya 1 baris task untuk instance itu, status `completed`). **Idempotency**: POST kedua dengan `Idempotency-Key` sama + body sama → response `200` identik (replay, tidak ada baris keputusan dobel dikonfirmasi `SELECT count(*)` pada `awcms_workflow_decisions`); POST dengan key sama + body beda (`decision` diubah) → `409 IDEMPOTENCY_CONFLICT`; header `Idempotency-Key` hilang → `400 IDEMPOTENCY_REQUIRED`. `GET /logs/audit?resourceType=workflow_instance` sebagai owner → `200`, tiga event (`action` `approve`/`approve`/`reject`) dengan `attributes.reason` tampil apa adanya (bukan salah satu dari 13 redaction key). **Kasus negatif (default-deny)**: tenant-user ketiga dibuat lewat SQL **tanpa** assignment role sama sekali (pola sama seperti Issue 2.4/9.1/10.1) → `GET /workflows/tasks` dan `POST .../decisions` keduanya `403 ACCESS_DENIED` `"No role permission grants this action."`. Edge-case header: tanpa tenant header → `400 TENANT_REQUIRED`; tanpa bearer → `401 AUTH_REQUIRED`; task id tidak ada (UUID acak) dengan permission valid → `404 RESOURCE_NOT_FOUND`. RLS dikonfirmasi lewat `psql` (`relrowsecurity=true` pada seluruh 5 tabel baru: 4 tabel workflow + `awcms_idempotency_keys`). **Catatan keterbatasan verifikasi**: seluruh pembuktian di atas HTTP/CLI murni lewat skrip Bun ad hoc dan `psql` langsung — sandbox ini tidak punya browser sungguhan, tidak relevan untuk issue ini karena murni backend/API, tidak ada UI baru. `bun run check` (lint, check:docs, api:spec:check, typecheck, test, build) lulus bersih setelah perbaikan bug idempotency di atas.

   - **Issue 12.2 diimplementasikan pada 2026-07-05**: Offline/LAN Deployment Profile — **issue terakhir dari 18 issue backlog generik doc 06**; menutup M8 dan seluruh backlog. **Tidak ada migration baru** (murni deployment assets + dua skrip CLI baru; dikonfirmasi `bun run db:migrate` tetap 12 migration sebelum/sesudah, `bun run api:spec:check` tetap OK tanpa perubahan). Dikerjakan dalam dua sesi: sesi pertama menghasilkan `deploy/systemd/awcms.service.example` (`ExecStart` memakai binary `bun` langsung, `EnvironmentFile` di luar repo, user non-root, hardening `NoNewPrivileges`/`ProtectSystem=strict`), `deploy/nginx/awcms.conf.example` (TLS termination opsional — didokumentasikan eksplisit sebagai opsional untuk topologi LAN-first satu server, proxy ke `127.0.0.1:4321`), `deploy/pgbouncer/pgbouncer.ini.example` (salinan kanonik yang dipindahkan dari contoh inline `database-pooling.md` §7, yang sekarang tinggal kutipan singkat + link agar tidak ada dua salinan yang bisa menyimpang), dan pembaruan `docs/awcms/database-pooling.md` §7 untuk merujuk berkas itu. Sesi kedua (sesi ini) melengkapi sisanya: (1) `deploy/backup/backup-postgres.sh` (`pg_dump --format=custom`, checksum `sha256sum` alongside, prune `BACKUP_RETENTION_DAYS` hari via `find -mtime`) dan `deploy/backup/restore-postgres.sh` (default restore ke database sekali-pakai `awcms_restore_test` yang di-drop+create ulang tiap run — **tidak pernah** menyentuh database live kecuali `--target=<dbname>` eksplisit, yang lalu mewajibkan konfirmasi interaktif ketik-ulang nama database atau `--yes` untuk skip; menolak restore bila `--target` sama dengan nama database sumber di `DATABASE_URL`) — keduanya Bash murni (dicatat eksplisit di header komentar: aturan Bun-only AGENTS.md #14 berlaku untuk kode aplikasi/tooling, bukan skrip ops OS-level yang membungkus binary `pg_dump`/`pg_restore` bawaan Postgres), plus `deploy/backup/README.md` (dokumentasi + contoh crontab polos `0 2 * * * .../backup-postgres.sh >> ... 2>&1` — sengaja **tidak** menambah systemd timer kedua yang redundan dengan systemd service yang sudah ada); (2) `docker-compose.yml` di root: service `app` (image `oven/bun:1` — bukan `node`; mount repo + `command: bun install && bun run build && bun ./dist/server/entry.mjs`, dipilih daripada Dockerfile terpisah demi kesederhanaan topologi LAN-first satu server, trade-off didokumentasikan di komentar berkas) dan `db` (`postgres:16`, sesuai versi yang dipakai konsisten di seluruh audit sesi-sesi sebelumnya), plus service `pgbouncer` opsional digerbangi Compose `profiles: ["pgbouncer"]`; `DATABASE_URL` di-override otomatis di `environment:` agar menunjuk `db:5432` (bukan `localhost` seperti default `.env.example` yang ditujukan untuk deployment non-container); (3) `scripts/validate-env.ts` (`bun run config:validate`) — validasi wajib-non-kosong (`APP_ENV`/`APP_URL`/`APP_TIMEZONE`/`DATABASE_URL`/`AUTH_JWT_SECRET`), kondisional sync (memakai ulang `checkSyncHmacSecretNotDefault` dari `security-readiness.ts` Issue 10.3 secara verbatim, bukan logika placeholder terpisah yang bisa menyimpang), kondisional R2 (4 kredensial wajib bila `R2_ENABLED=true`) — tidak pernah mencetak nilai secret, hanya nama variabel; diwire sebagai tahap **pertama** `production-preflight.ts` (sebelum `db:migrate`, sesuai doc 18 §Prinsip konfigurasi #5); (4) `docs/awcms/deployment-profiles.md` baru memetakan 4 profil doc 18 (development/staging/production/offline-LAN) ke berkas `deploy/*`/`docker-compose.yml` yang relevan; (5) `.env.example` ditambah placeholder `R2_*` commented-out (tidak diisi karena hanya wajib "bila R2"). Skill `awcms-production-preflight` dan `OUT_OF_SCOPE_ITEMS` di `scripts/security-readiness.ts` diperbarui — 4 item ("PostgreSQL tidak public", "least-privilege DB user", "backup/restore tested", "PostgreSQL version pinned") yang sebelumnya berkata "deferred to Issue 12.2 (masih open)" kini merujuk berkas nyata yang sudah ada (`docker-compose.yml`, `deploy/backup/*.sh`) sambil tetap dicatat sebagai **manual** (bukan otomatis dari kode aplikasi) — bukan penghapusan diam-diam, murni pembaruan referensi karena issue yang mereka rujuk sudah selesai. Test baru `tests/validate-env.test.ts` (13 kasus: required vars, sync conditional 4 skenario, R2 conditional 3 skenario, end-to-end) — 203 test total, semua lulus; `bun run lint`/`check:docs`/`api:spec:check`/`typecheck`/`test`/`build` seluruhnya lulus bersih.

     **Bug ditemukan+diperbaiki selama pengembangan skrip verifikasi (bukan kode produk)**: draft awal `docker-compose.yml`'s `app` service tidak menyertakan `user:`; menjalankan `docker compose up` tanpa itu membuat container berjalan sebagai root, dan `bun install`/`bun run build` di dalamnya menulis berkas `node_modules/.vite/deps` dan `dist/` sebagai **root-owned** ke repo hasil bind-mount — setelah container dihentikan, perintah `bun run build` di **host** langsung gagal `EACCES: permission denied, rm 'node_modules/.vite/deps'` karena user host tidak lagi bisa menulis ke direktori itu. Diperbaiki dengan dua langkah: (a) menambahkan `user: "${APP_UID:-1000}:${APP_GID:-1000}"` + `HOME: /tmp` pada service `app`, dan (b) mendokumentasikan (di komentar `docker-compose.yml` dan `deployment-profiles.md`) bahwa `APP_UID`/`APP_GID` **wajib** di-export ke uid/gid host asli sebelum `docker compose up` — percobaan dengan fallback default `1000:1000` yang tidak cocok dengan uid host asli (`1001` di sandbox ini, yang direktori repo-nya bermode `700`) gagal lebih parah (`EACCES` pada _setiap_ berkas, termasuk `package.json`, karena hanya uid pemilik persis yang bisa traverse direktori bermode itu) — bukti nyata bahwa fallback hardcoded generik tidak cukup dan `export APP_UID=$(id -u) APP_GID=$(id -g)` benar-benar wajib, bukan sekadar saran. Berkas root-owned yang terlanjur tertulis dibersihkan lewat `docker run --rm -v $PWD:/app alpine chown -R 1001:1001 /app/node_modules /app/dist` (bukan `sudo` — tidak tersedia non-interaktif di sandbox ini); setelah perbaikan, `bun run check` dari host lulus bersih lagi.

     Diverifikasi live end-to-end terhadap Docker Compose sungguhan (Docker 29.6.1 tersedia di sandbox ini), bukan hanya `docker compose config`: **Catatan adaptasi sandbox** — port publish host→container di sandbox ini stall (dikonfirmasi ulang: `psql -h 127.0.0.1 -p 55432` ke `db` yang dipublish langsung timeout; `curl` ke `app` publish port `45321` juga timeout), sesuai catatan `docker-host-port-blocked` yang sudah ada di memori sesi-sesi sebelumnya (Issue 6.3/8.1/9.1/10.1/10.2/10.3/11.1) — bukan hal baru yang ditemukan issue ini, tapi turut dikonfirmasi berlaku untuk `docker-compose.yml` yang baru dibuat ini. Workaround yang dipakai: `docker exec` langsung ke container `app`/`db` yang sudah berjalan via `docker compose up`, dan container tambahan yang di-`docker run --network container:<nama-container>` untuk berbagi network namespace saat perlu proses terpisah (mis. menguji mode "server mati" untuk `production:preflight`). Verifikasi: `docker compose up -d` (project terpisah `-p awcms-verify`, port host dipetakan ulang ke `55432`/`45321` khusus untuk sandbox ini — bukan perubahan pada `docker-compose.yml` itu sendiri, murni port host lain karena `5432`/`4321` bentrok dengan proses lokal sandbox) membuat `db` (`postgres:16`) sehat dan `app` (`oven/bun:1`) sukses `bun install && bun run build`, listening di port 4321; `GET /api/v1/health` dari container terpisah yang berbagi network namespace `app` → `200` `{"status":"ok",...,"moduleCount":7}` — **pembuktian nyata bahwa `docker-compose.yml` benar-benar bisa menjalankan aplikasi**, bukan sekadar valid secara sintaks. `docker exec app bun run db:migrate` → 12/12 apply bersih pada database compose yang benar-benar baru; re-run kedua → 12/12 `skip` (idempotent). Setelah menginstal `git` sementara di dalam container (`oven/bun:1` berbasis Debian tidak menyertakan `git` secara default — dicatat sebagai keterbatasan image dasar, bukan bug kode; instalasi ini murni untuk kebutuhan verifikasi, tidak diusulkan masuk `docker-compose.yml`/image produksi karena skrip preflight/security-readiness lazimnya dijalankan dari checkout git penuh di mesin CI/ops, bukan dari dalam container runtime minimal): `bun run security:readiness` di dalam container `app` yang hidup (server + DB nyata) → 10/10 check `PASS` (0 critical/0 warning failure), termasuk RLS 31 tabel tenant-scoped, audit table reachable, dan "errors don't leak stack traces" nyata via `POST /sync/push` tanpa header HMAC → `400` bersih. `bun run production:preflight` **dengan server hidup** → seluruh 7 tahap `PASS` (`config:validate` baru sebagai tahap pertama, lalu `db:migrate`/`api:spec:check`/`test`/`build`/`db:pool:health`/`security:readiness`), verdict `GO-LIVE DIIZINKAN`. `bun run production:preflight` **tanpa server** (dijalankan dari container terpisah yang hanya berbagi network namespace `db`, bukan `app`) → `db:pool:health` tercatat `[SKIP]` (bukan `FAIL`) sementara 6 tahap lain tetap `PASS` termasuk `config:validate`, verdict tetap `GO-LIVE DIIZINKAN` — membuktikan pola dual-mode Issue 10.3 tetap bekerja dengan tahap baru. **Round-trip backup/restore nyata** (dengan `pg_dump`/`pg_restore`/`psql` v16 dari image `postgres:16` sendiri — percobaan awal dengan `postgresql-client` v17 dari `apt` di image Debian bun sempat memunculkan satu warning non-fatal `unrecognized configuration parameter "transaction_timeout"` pada `pg_restore` karena client v17 lebih baru dari server v16, dicatat sebagai insting verifikasi bukan bug skrip — data tetap ter-restore benar meski exit code jadi non-zero; diulang dengan tool versi cocok untuk bukti bersih): `POST /setup/initialize` membuat 1 tenant nyata ("Acme Inc"); `backup-postgres.sh` menghasilkan `.dump` (140K) + `.dump.sha256`, `sha256sum -c` mengonfirmasi checksum valid; `restore-postgres.sh` (mode default, tanpa `--target`) drop+create `awcms_restore_test`, restore bersih (exit 0), `SELECT count(*) FROM awcms_tenants` pada database sumber **dan** database hasil restore keduanya `1` — **row count sumber dan hasil restore cocok persis**. Kasus keamanan restore dibuktikan konkret: `--target=<nama database sumber di DATABASE_URL>` → ditolak sebelum menyentuh apa pun (`refusing to restore onto ...`, exit 1); `--target=awcms_recovery --yes` (database sudah ada, dibuat manual lebih dulu) → restore langsung tanpa prompt, exit 0; `--target=awcms_recovery2` **tanpa** `--yes` dengan input konfirmasi yang sengaja salah (`wrong-name` bukan nama database) → `confirmation did not match — aborting`, exit 1, database target **tidak** tersentuh pg_restore. **Catatan keterbatasan verifikasi**: seluruh pembuktian di atas HTTP/CLI/Docker murni lewat `docker exec`/`docker run --network container:...`/`bun`/`psql`/`pg_dump`/`pg_restore` langsung — sandbox ini tidak punya browser sungguhan, tidak relevan untuk issue backend/ops murni ini. Seluruh container/network/volume verifikasi (`awcms-verify*`, `awcms-verify2*`) dibersihkan (`docker compose down -v`) sebelum menyelesaikan issue; `.env` sementara yang dibuat untuk pengujian dihapus (tidak pernah masuk git, sudah di-`.gitignore`). `bun run check` (lint, check:docs, api:spec:check, typecheck, test, build) lulus bersih setelah perbaikan `docker-compose.yml` di atas. **Tidak ada item terbuka lain** dari sisa 18 issue backlog generik doc 06 — issue ini adalah yang terakhir; M8 dan seluruh backlog doc 06 tuntas (lihat catatan sanity-check di laporan implementasi issue ini untuk area non-doc-06 yang masih terbuka, bila ada, yang bukan bagian scope backlog ini).

## Rekomendasi berikutnya

Urutan paling aman:

1. Issue 2.1-2.4 (M2), 12.1 (Setup Wizard), 6.1, 6.2, dan 6.3 (Sync Outbox/Inbox + Conflict Tracking + R2 Object Sync Queue — M5 Sync Storage sepenuhnya) selesai.
2. Sejajar/independen, Issue 8.1 (Admin Layout Shell) dan 9.1 (Management Reporting Views) — M7, sudah `status:ready`, tidak bergantung pada M5.
3. Baru setelah M5 (6.1-6.3) **dan** M7 (8.1, 9.1) tuntas, kerjakan Sprint 5 (10.1-10.3, 11.1, 12.2 — M8).
4. Target versi: `v0.2.0`-`v0.5.0` = epic M2; `v0.6.0` = Issue 12.1 (Setup Wizard); `v0.7.0` = Issue 6.1; `v0.8.0` = Issue 6.2; `v0.9.0` = Issue 6.3 — semua fitur di luar peta versi doc 09 yang literal (peta versi doc 09 hanya mengenal slot "Sync storage" gabungan di `v0.4.0` sebagai TARGET jauh sebelumnya; realisasi progresif berbeda, lihat catatan di CHANGELOG).
5. Tidak ada item menengah/rendah terbuka dari audit ini yang memblokir Issue 8.1/9.1.
6. Tidak ada item menengah/rendah terbuka dari audit ini yang memblokir Issue 6.3.

## Perawatan pasca-backlog

Perubahan setelah seluruh 18 issue backlog doc 06 tuntas — bukan bagian backlog, dicatat terpisah agar entri per-issue di atas tetap sebagai log historis apa adanya.

- **Upgrade PostgreSQL 16 → 18.4 pada 2026-07-05**: mengganti pin versi Postgres pada aset deployment forward-looking — `docker-compose.yml` (`image: postgres:16` → `postgres:18.4`), `docs/awcms/deployment-profiles.md` (deskripsi service `db`), dan catatan `OUT_OF_SCOPE_ITEMS` "PostgreSQL version pinned" di `scripts/security-readiness.ts`. **Tidak menyentuh** entri log historis (`CHANGELOG.md` rilis 0.16.0, snapshot `docs/awcms/github/`, dan entri per-issue di atas yang mencatat "diverifikasi terhadap PostgreSQL 16") — itu catatan faktual dari waktunya, bukan pin konfigurasi. Tidak ada perubahan schema/kode aplikasi (`bun run db:migrate` tetap 12 migration, `bun run api:spec:check` tetap OK). Diverifikasi live end-to-end terhadap `postgres:18.4` sungguhan (via `--network container:<pg>` sesuai catatan `docker-host-port-blocked`): `SELECT version()` mengonfirmasi `PostgreSQL 18.4 (Debian 18.4-1.pgdg13+1)`; 12/12 migration apply bersih; server hasil build boot dan `GET /api/v1/health` → `200`; `POST /setup/initialize` + `POST /auth/login` sukses (token terbit); **round-trip backup/restore nyata** dengan `pg_dump`/`pg_restore` v18.4 — `.dump` + `.sha256` (`sha256sum -c` OK), restore ke database sekali-pakai, `SELECT count(*) FROM awcms_tenants` sumber = hasil restore = `1` (cocok persis); `bun run security:readiness` → 10/10 `PASS` (RLS 26 tabel tenant-scoped, dll., pola `pg_class.relrowsecurity` tetap valid di 18.x). **Catatan operasional** (didokumentasikan, bukan bug): volume Docker named `awcms-db-data` yang sudah berisi data cluster Postgres 16 **tidak** otomatis kompatibel dengan image 18.x — operator yang meng-upgrade stack yang sudah jalan wajib `pg_dump` dari 16 lalu `pg_restore` ke volume 18 baru (bukan sekadar mengganti tag image), persis alur `deploy/backup/*.sh` yang sudah ada.

- **Test harness integrasi HTTP + Postgres di CI pada 2026-07-05** (rekomendasi #1 dari analisis repo pasca-backlog): sebelumnya seluruh suite `bun test` (203 test) adalah unit murni + assersi bentuk-migrasi — **tidak satu pun** menembak request nyata atau menyentuh database, sehingga wiring endpoint (auth → ABAC → transaction → RLS → envelope response) hanya pernah diuji verifikasi manual, dan `bun test`/CI tetap hijau meski sebuah endpoint rusak oleh refactor. Ditambahkan `tests/integration/harness.ts` (memanggil route handler Astro nyata via konteks sintetis — tanpa server berjalan/build) + `tests/integration/api.integration.test.ts` (5 test: setup singleton-lock, login argon2 sukses/salah, ABAC allow vs default-deny lewat endpoint nyata, penolakan cross-tenant session, dan jalur soft-delete → audit → read-back dengan payload jsonb aman). Di-gate pada `DATABASE_URL`: skip bersih tanpa DB (203 unit tetap lulus di host), berjalan di CI (service `postgres:18.4` + `bun run db:migrate` + `bun test`). Diverifikasi live: 5/5 lulus terhadap `postgres:18.4` nyata; tanpa DB, suite skip dan 203 unit tetap lulus. **`bun run check` lulus bersih.**
- **RESOLVED — penegakan RLS + role aplikasi least-privilege pada 2026-07-06** (menutup temuan keamanan di bawah): menjadikan RLS multi-tenant benar-benar ditegakkan, bukan sekadar diaktifkan. (1) Migrasi baru `sql/013_awcms_enforce_rls_least_privilege.sql` menerapkan `FORCE ROW LEVEL SECURITY` pada **31** tabel tenant-scoped (agar policy berlaku bahkan untuk pemilik tabel), membuat role `awcms_app` least-privilege (NOLOGIN/tanpa password di migrasi — bukan secret di git; hanya grant DML, tanpa DDL/ownership), dan menetapkan default GUC fail-closed (`app.current_tenant_id` = UUID nol → tak cocok tenant mana pun → 0 baris bila sebuah query mencapai tabel RLS tanpa `withTenant`). (2) Aplikasi kini terhubung sebagai `awcms_app`, migrasi tetap sebagai role privileged: `docker-compose.yml` menambah service `migrate` (satu kali, superuser, `service_completed_successfully`) + `deploy/postgres/10-create-app-role.sh` (hook `docker-entrypoint-initdb.d` yang membuat role dari `AWCMS_MINI_APP_DB_PASSWORD`); `.env.example` + `deployment-profiles.md` §Model dua-peran mendokumentasikan alur bare-metal. (3) `security:readiness` di-upgrade dari cek flag ke cek **penegakan**: check "RLS enabled AND forced" mewajibkan `relforcerowsecurity=true`, dan check baru "App DB connection role does not bypass RLS" **memblokir go-live** bila peran koneksi `DATABASE_URL` superuser/BYPASSRLS. (4) Harness integrasi di-split dua peran (`getAdminSql()` privileged untuk seed lintas-tenant + `provisionAppRole()` yang mengarahkan handler ke role least-privilege), dan test "PostgreSQL RLS enforces tenant row isolation" (sebelumnya di-drop) kini aktif dan lulus — membuktikan tenant A tak pernah melihat baris tenant B meski keduanya ada. Runner `db:migrate` diberi `stripDollarQuotedBlocks()` agar blok `DO $$ … $$` tidak salah dibaca sebagai transaction-control (+ test regresi). **Diverifikasi live** terhadap `postgres:18.4` sungguhan: 13/13 migration apply bersih; `security:readiness` sebagai superuser → **GO-LIVE DIBLOKIR** (menangkap tepat celah ini), sebagai role least-privilege → 11/11 PASS; 6/6 test integrasi lulus dengan handler berjalan sebagai `awcms_app`. **`bun run check` lulus bersih** (unit skip integrasi tanpa DB).
- **TEMUAN KEAMANAN dari harness di atas (belum diperbaiki di perubahan ini — kandidat perbaikan berikutnya)**: RLS multi-tenant (ADR-0003) **tidak benar-benar ditegakkan untuk DB user aplikasi**. Migrasi hanya `ENABLE ROW LEVEL SECURITY` (tidak pernah `FORCE`), dan aplikasi terhubung sebagai `awcms` yang **sekaligus pemilik tabel dan superuser** (`rolbypassrls=t`). PostgreSQL melewati RLS untuk pemilik tabel (tanpa `FORCE`) dan tanpa syarat untuk superuser — dibuktikan: `SET LOCAL app.current_tenant_id` ke UUID bogus tetap mengembalikan baris. Isolasi tenant saat ini **sepenuhnya** bergantung pada filter `WHERE tenant_id` di layer aplikasi; RLS sebagai backstop defense-in-depth inert. Check `security:readiness` "RLS aktif" hanya memverifikasi flag `relrowsecurity=true`, bukan penegakan nyata, sehingga lolos padahal kontrol tidak berfungsi. **Perbaikan yang diperlukan** (perubahan terpisah): (1) migrasi `FORCE ROW LEVEL SECURITY` pada semua tabel tenant-scoped; (2) role aplikasi least-privilege non-superuser (aplikasi terhubung sebagai itu; migrasi tetap sebagai role privileged) — di `docker-compose.yml`/docs; (3) upgrade check `security:readiness` untuk menguji penegakan (query tenant bogus → 0 baris), bukan sekadar flag; (4) test integrasi RLS yang membuktikan isolasi baris nyata (di-un-skip setelah perbaikan).

- **Access & Users management penuh pada 2026-07-06** (permintaan user, PR pertama dari tiga: Access & Users, Sync, Settings): sebelumnya hanya `POST /access/assignments` (assign role) yang live — tidak ada endpoint untuk membuat/mengubah tenant user maupun role generik di luar Setup Wizard, dan sidebar admin menampilkan "Access & Users" sebagai stub "Segera hadir". Ditambahkan lima endpoint baru (`GET/POST /api/v1/users`, `PATCH /api/v1/users/{id}`, `GET/POST /api/v1/roles`, `PATCH/DELETE /api/v1/roles/{id}`, `GET /api/v1/permissions`) + `DELETE /api/v1/access/assignments` (unassign), seluruhnya melalui guard ABAC granular yang sudah diseed (`user_management.{read,create,update}`, `access_control.{read,configure,assign}` — tidak ada permission baru). **Safety rail**: role sistem (`is_system=true`) menolak perubahan permission/delete dengan `409`; delete role juga ditolak `409` bila masih di-assign. Endpoint menerima auth cookie SSR **atau** header bearer (helper `resolveAuthInputs`/`authorizeInTransaction` baru di `application/access-guard.ts`, dipusatkan dari pola yang sudah ada di `POST /auth/logout`) — inilah yang memungkinkan layar admin baru `/admin/access-users` (tabel user + tabel role, form tambah, editor permission per role, chip assign/unassign) memanggil endpoint yang sama persis dengan klien API, tanpa jalur pintas berhak istimewa. Query read-side diekstrak ke `application/user-directory.ts` dan dipakai bersama oleh endpoint JSON dan SSR (pola yang sama seperti `reporting/application/*-report.ts` dengan `admin/index.astro`). Ditemukan bug array-binding Bun SQL saat verifikasi: `${array}::uuid[]` gagal ("malformed array literal"); diperbaiki jadi `tx.array(ids, "uuid")` (API resmi Bun untuk PostgreSQL array parameter). **Diverifikasi live** end-to-end lewat `docker compose` (network host, lihat catatan `docker-host-port-blocked`): create role → create user dengan role tersebut → reload halaman menampilkan keduanya; `bun run check` lulus bersih (222 unit pass/13 skip tanpa DB; 233 pass/0 fail dengan `postgres:18.4` nyata).

- **Sync admin ops dashboard pada 2026-07-06** (PR kedua dari tiga): sebelumnya hanya resolusi konflik punya endpoint bearer-session (`GET/POST /sync/conflicts*`); registrasi node dan antrean objek hanya bisa dilihat/diubah lewat endpoint HMAC node-to-node atau langsung ke DB, dan sidebar admin menampilkan "Sync" sebagai stub "Segera hadir". Ditambahkan `GET /api/v1/sync/nodes` + `PATCH /api/v1/sync/nodes/{id}` (aktifkan/nonaktifkan/ganti nama node — nonaktif langsung memblokir endpoint HMAC yang sudah menolak `status !== 'active'`), migrasi `sql/014_awcms_sync_node_management_permission_schema.sql` (seed `sync_storage.node_management.{read,update}`, tanpa perubahan schema); `GET /api/v1/sync/object-queue` (tenant-wide, filter status, guard `object_queue.read` yang sudah diseed sejak migrasi 009) + `POST /api/v1/sync/object-queue/{id}/retry` (override manual jadwal backoff — reset ke `pending`, hanya entri `failed` yang eligible, ditolak `409` selainnya; guard `object_queue.retry` yang sudah diseed migrasi 009 tapi **tidak pernah ada konsumennya** sampai perubahan ini, sehingga menambah action `"retry"` baru ke union `AccessAction`, pola yang sama seperti Issue 10.1 menambah `restore`/`purge` — sengaja **bukan** high-risk karena hanya nudge terhadap jadwal otomatis). **Menutup gap dokumentasi yang sudah usang**: `POST /sync/conflicts/{id}/resolve` sekarang juga memanggil `recordAuditEvent` (`resourceType: "sync_conflict"`) — README lama mencatat "belum ada tabel audit_events umum" padahal tabel itu sudah ada sejak Issue 10.1, hanya belum pernah dipakai endpoint ini. `GET/POST /sync/conflicts*` di-refactor memakai helper `resolveAuthInputs`/`authorizeInTransaction` (bukan bearer-only) sehingga kini juga menerima cookie SSR — inilah yang memungkinkan layar admin baru `/admin/sync` (ringkasan pakai ulang `fetchSyncHealthReport` Issue 9.1, tabel node/konflik/antrean-gagal, aksi nonaktifkan/resolve/retry) memanggilnya langsung. Query read-side baru di `sync-storage/application/sync-directory.ts`, mengikuti pola `user-directory.ts`. **Diverifikasi live** end-to-end lewat `docker compose`: seed node/conflict/object-queue langsung via SQL (mensimulasikan device yang sudah pernah HMAC push) → list node → nonaktifkan node → list konflik via **cookie** (bukan bearer) → resolve konflik → re-resolve ditolak `409` → audit event `sync_conflict`/`approve` muncul → list antrean gagal → retry → retry kedua ditolak `409` → baris ter-reset (`status=pending, retry_count=0, next_retry_at=null, last_error=null`) → audit event `object_sync_queue`/`retry` muncul (menyimpan `previousError` sebelum di-clear) → halaman `/admin/sync` render ketiga section dengan data live. `bun run check` lulus bersih (228 unit pass/19 skip tanpa DB; 244 pass/0 fail dengan `postgres:18.4` nyata).

- **Settings management pada 2026-07-06** (PR ketiga dari tiga: Access & Users, Sync, Settings — seluruhnya tuntas): sebelumnya `tenant_name`/`legal_name`/`default_locale`/`default_theme` (`awcms_tenants`) dan `timezone`/`feature_flags` (`awcms_tenant_settings`) hanya bisa diisi sekali oleh Setup Wizard, tidak pernah bisa diubah lagi, dan `/admin/settings` hanya berisi placeholder "Pengaturan belum tersedia". Ditambahkan `GET/PATCH /api/v1/settings` (menggabungkan kedua tabel, terima subset field apa pun) + migrasi `sql/015_awcms_tenant_settings_management_permission_schema.sql` (seed `tenant_admin.tenant_settings.{read,update}`, tanpa perubahan schema — semua kolom sudah ada sejak migrasi 002) + layar admin `/admin/settings` (form profil tenant, preferensi tampilan, timezone, editor JSON feature flags). **Pola keamanan baru**: `awcms_tenants` sengaja RLS-free (root tenant itu sendiri, `id`-nya adalah tenant id) — endpoint mengandalkan `WHERE id = <tenantId dari sesi>` eksplisit di setiap `UPDATE`, bukan RLS, dan ini dites langsung (tenant B tidak pernah berubah saat tenant A update). **Menutup gap dokumentasi lama**: `default_locale`/`default_theme` ternyata kolom `awcms_tenants` (doc 04 §ERD), bukan `awcms_tenant_settings` seperti disebut beberapa referensi (`ThemeToggle.astro`, doc 14 §Theming, doc 18 §Presedensi) — diperbaiki bersamaan. **Tema default tenant kini benar-benar dipakai**: skrip resolusi tema no-flash di `AdminLayout.astro` (`is:inline` + `define:vars`) sekarang fallback ke `default_theme` tenant saat browser belum pernah memilih tema personal di localStorage — sebelumnya hardcode `"system"` dan kolom itu tak pernah dibaca kode mana pun sejak Issue 8.1. **Diverifikasi live** end-to-end lewat `docker compose`: GET settings default → PATCH legal name/tema/timezone/feature flags → re-GET mengonfirmasi persist → audit event `tenant_settings`/`update` muncul → halaman `/admin/settings` render nilai live → skrip tema inline mengonfirmasi `tenantDefaultTheme = "dark"` ter-inject dengan benar. `bun run check` lulus bersih (240 unit pass/25 skip tanpa DB; 261 pass/0 fail dengan `postgres:18.4` nyata).

- **Runtime i18n pada 2026-07-06** (Issue #433, milestone M9): sebelumnya seluruh string UI (login, admin shell, dashboard, access-users, sync, settings) hardcode; kolom `awcms_tenants.default_locale` ada sejak migrasi 002 tetapi tak pernah dibaca kode mana pun. Ditambahkan `src/lib/i18n/` — parser `.po` gettext murni tanpa dependency (`po-parser.ts`, dihindari agar tak perlu pengecualian Node.js AGENTS.md rule 14), catalog loader (`catalog.ts`, cache per-locale, path relatif `process.cwd()` seperti `discoverMigrationFiles`), `createTranslator`/`t(key, params)` dengan fallback `locale → en → key mentah` (`translate.ts`), resolusi locale murni `resolveLocale` (`locale.ts`), formatter `Intl` sadar-locale (`format.ts`), dan pemetaan kode error → key `error.*` (`error-messages.ts`). Katalog di `i18n/{messages.pot,en.po,id.po}` (152 key). Komponen baru `LanguageSwitcher.astro` (ikon bendera + nama asli bahasa, set cookie `awcms_locale` lalu reload penuh — bukan swap instan seperti tema, karena locale mengubah teks SSR, bukan cuma CSS). Migrasi `sql/016_awcms_tenant_default_locale_english_schema.sql` mengubah default kolom `'id'` → `'en'` untuk tenant baru (tenant lama tak diubah). Layar Settings (`/admin/settings`) dropdown bahasa dipersempit ke locale yang benar-benar punya katalog (en/id) — kolom DB tetap menerima nilai lama `ms`/`ar` (doc 04 §ERD) untuk kompatibilitas mundur, tapi UI hanya menawarkan yang benar-benar berfungsi. **Konvensi baru**: konten data multi-bahasa (input pengguna, beda dari string UI statis) didokumentasikan di doc 04 §Konten multi-bahasa — disimpan di database per locale aktif, bukan di `.po`; base ini sendiri belum punya field yang memakainya. **Bug ditemukan+diperbaiki saat verifikasi live**: resolusi locale dengan fallback `default_locale` tenant awalnya hanya dilakukan di dalam `AdminLayout.astro` — tapi frontmatter setiap halaman `/admin/*` (dan `t()` miliknya sendiri) berjalan **lebih dulu** daripada frontmatter layout yang membungkusnya, sehingga tenant lama dengan `default_locale='id'` merender shell dalam Indonesia namun konten dashboard tetap Inggris. Diperbaiki dengan memindahkan resolusi (cookie → `default_locale` tenant → `en`) ke `src/middleware.ts`, sebelum halaman mana pun dirender — `SsrContext` (`ssr-session.ts`) diperluas membawa `tenantDefaultLocale` dari query yang sudah ada, tanpa round-trip DB baru. **Diverifikasi live** lewat `docker compose`: tenant baru default `en`; tenant lama (`default_locale='id'`) merender shell **dan** konten Indonesia tanpa cookie; cookie `en` meng-override konsisten di keempat halaman admin; language switcher menampilkan 🇬🇧/🇮🇩; banner error API (`VALIDATION_ERROR`, dll.) ter-lokalisasi sesuai locale aktif. `bun run check` lulus bersih (270 unit pass/25 skip tanpa DB).

- **Audit UX/UI & aksesibilitas WCAG 2.1 AA pada 2026-07-06** (Issue #434, milestone M9 — pakai skill `awcms-ux-review`): menaikkan mutu layar admin yang sudah ada (`/login`, `/admin`, `/admin/access-users`, `/admin/sync`, `/admin/settings`, `AdminLayout.astro`) ke standar doc 14, bukan membangun layar baru. **Kontras di bawah AA, terukur** (formula WCAG relative-luminance, lihat perhitungan di PR): status pill aktif/nonaktif (`--color-success`/`--color-danger` sebagai teks di atas `--color-surface-2`) hanya 2.91:1 (aktif, terang) dan 4.06:1–4.26:1 (nonaktif, kedua tema) — di bawah 4.5:1; tombol/banner primer bertulisan putih di tema gelap (`--color-primary`/`--color-danger` polos) hanya 3.68:1/3.76:1. Ditambahkan token baru `--color-primary-strong`/`--color-success-strong`/`--color-danger-strong` (`tokens.css`, per-tema, semua ≥4.5:1 dengan teks putih — light memakai nilai yang sudah lulus, dark memakai varian yang di-gelapkan 10-31%) dan status pill diubah dari teks-berwarna-di-atas-tint menjadi solid-fill + teks putih (pola Badge/StatusPill doc 14). **Double-submit**: tak satu pun form/tombol mutation (login, tambah/ubah user & role, assign/unassign, toggle status, resolve conflict, retry queue, simpan settings) menonaktifkan dirinya selama request — ditambahkan `lockElement` (`src/lib/ui/admin-form-client.ts`, modul klien baru, juga menyerap empat implementasi `submitJson`/`showBanner` yang sebelumnya duplikat identik di `login.astro`/`admin/access-users.astro`/`admin/sync.astro`/`admin/settings.astro`) yang menonaktifkan + `aria-busy` tombol pemicu selama request dan mengembalikan state semula (termasuk saat gagal, input pengguna tetap utuh). **Cabang Error tak pernah ada** pada empat state pattern doc 14 (`Loading -> Error: gagal`) — kegagalan fetch data SSR (DB error di `AdminLayout.astro`/keempat layar admin) sebelumnya 500 mentah tanpa jalur render; ditambahkan `StateNotice.astro` (`src/components/ui`, komponen pertama di lokasi ini) yang membedakan "akses ditolak" dari "gagal sementara" dan menggantikan empat blok `.permission-denied` yang sebelumnya duplikat identik. **Gap lain yang ditutup**: baris empty-state hilang pada tabel Roles (`/admin/access-users`, tabel Users sudah punya); `ThemeToggle.astro` masih hardcode string Indonesia ("Sistem"/"Terang"/"Gelap") — lolos dari ekstraksi Issue #433 karena PR itu tidak menyentuh komponen ini; skip-link keyboard di `AdminLayout.astro`; container scroll (`overflow-x`) untuk semua tabel lebar (tablet, cegah horizontal scroll pada seluruh halaman); target sentuh ≥44px pada breakpoint tablet/mobile untuk kontrol kecil (theme toggle, tombol aksi tabel, chip hapus role). **Tidak ditangani** (didokumentasikan sebagai follow-up eksplisit, bukan diabaikan diam-diam): dialog konfirmasi hapus role masih `window.prompt` native (aksesibel — fokus terperangkap oleh OS, `Esc` menutup — tapi tidak mengikuti komponen `Dialog` bergaya doc 14 karena komponen itu belum ada di `src/components/ui` untuk layar mana pun; membangunnya adalah unit kerja tersendiri, bukan scope audit ini). Key `.po` baru (`common.error_*`, `common.retry`, `common.please_wait`, `admin.access_users.no_roles`, `admin.dashboard.no_module_usage`, `admin.layout.skip_to_content`, `admin.layout.theme_*`) ditambahkan paralel di ketiga berkas katalog (keyset identik, diverifikasi via diff terurut). **Diverifikasi live** lewat `docker compose`: setup tenant baru → login → keempat halaman admin render 200 dengan token `-strong` terpasang di CSS ter-bundle, `StateNotice` "denied" tampil untuk user tanpa permission (nav item tersembunyi, endpoint tetap terlindungi), `StateNotice` tidak tampil untuk owner (data lengkap), skip-link dan label `ThemeToggle` ikut berganti bahasa saat cookie locale di-set ke `id` (bukti hardcode sudah tertutup), bundle JS ter-minify mengonfirmasi `admin-form-client.js` dipakai bersama lintas empat halaman dan logika `disabled`/`aria-busy` ter-tree-shake dengan benar. `bun run check` lulus bersih (270 unit pass/25 skip tanpa DB; 291 pass/0 fail dengan `postgres:18.4` nyata — integrasi penuh, bukan cuma unit).

- **Audit performa aplikasi & database pada 2026-07-06** (Issue #435, milestone M9 — pakai skill `awcms-performance`): "ukur dulu, baru optimasi" — `EXPLAIN (ANALYZE, BUFFERS)` dijalankan pada query tenant-scoped panas terhadap tenant yang di-seed ~200 ribu baris di empat tabel (`awcms_object_sync_queue`, `awcms_abac_decision_logs`, `awcms_audit_events`, `awcms_sync_conflicts`) SEBELUM mengubah kode apa pun. **Bottleneck nyata yang ditemukan** (index RLS-aware hilang, bukan spekulasi): (1) `GET /api/v1/sync/object-queue` tanpa filter `status` — Parallel Seq Scan penuh + sort, 33.9ms, tidak ada index `(tenant_id, created_at)`; (2) endpoint sama dengan filter `status` — index `awcms_object_sync_queue_retry_idx (tenant_id, status, next_retry_at)` yang ada cocok prefix tapi tak mencakup `created_at`, Bitmap Heap Scan + sort, 42.9ms; (3) `GET /api/v1/sync/objects/status` (endpoint polling node HMAC — QPS tertinggi di deployment offline-first nyata) — Seq Scan penuh, 27.7ms, tidak ada index `(tenant_id, node_id)`; (4) `GET /api/v1/sync/conflicts` tanpa filter `status` — index `awcms_sync_conflicts_tenant_status_idx` hanya membantu bila `status` difilter, Seq Scan 30.1ms tanpanya. Migrasi `sql/017_awcms_sync_queue_conflict_performance_indexes.sql` menambah empat index komposit untuk keempat bentuk query ini — diukur ulang sesudahnya: keempatnya turun ke sub-milidetik (Index Scan/Index Only Scan), dibuktikan dengan `EXPLAIN` sebelum/sesudah di PR, bukan klaim tanpa bukti. **Temuan kedua saat mengukur ulang query object-queue dengan filter `status`**: index barunya TIDAK dipakai planner meski secara teori lebih murah (query planner Postgres salah mengestimasi jumlah baris hasil join ke `awcms_sync_nodes` — off oleh faktor puluhan kali — sehingga menghitung biaya `Index Scan + LIMIT` jauh lebih mahal dari kenyataan). Diperbaiki bukan dengan index tambahan, tapi dengan **menata ulang bentuk query**: `fetchObjectQueueEntries` kini melakukan `LIMIT` di dalam subquery SEBELUM join ke `awcms_sync_nodes`, bukan sort+limit atas hasil join — planner tidak lagi punya pilihan selain memenuhi `ORDER BY ... LIMIT` langsung dari index, execution time turun dari ~40ms ke <1ms di benchmark yang sama. **N+1 dihilangkan** (lihat skill `awcms-performance` §Hindari N+1): empat loop tulis `for (...) { await tx\`INSERT...\` }`(assign permission ke role di`POST/PATCH /api/v1/roles*`, assign role ke user di `POST /api/v1/users`, enqueue object sync di `POST /api/v1/sync/objects`) diganti satu `INSERT ... SELECT ... FROM unnest(...)` (satu round trip, bukan N) — dites end-to-end dengan array multi-item termasuk id duplikat dalam satu request (`ON CONFLICT DO NOTHING`toleran,`DO UPDATE`di-dedupe di aplikasi lebih dulu karena Postgres menolak "affect row a second time" untuk itu).`POST /api/v1/sync/push`(jalur sinkronisasi push, sebelumnya nol test coverage) sebelumnya melakukan satu`SELECT current_version` per event dalam batch — diganti satu prefetch batch (`= ANY(tx.array(ids,"uuid"))`) ke map in-memory bertipe kunci `aggregateType:aggregateId`(bukan id saja — dua tipe aggregate bisa kebetulan berbagi id mentah), map ini diperbarui setelah tiap event diterima sehingga event kedua untuk aggregate yang sama dalam satu batch tetap melihat versi yang baru saja di-bump event pertama (perilaku identik dengan loop baca-per-event lama) — dibuktikan lewat test integrasi baru yang secara spesifik menembak skenario dua event untuk aggregate sama dalam satu batch (baseVersion benar diterima, baseVersion basi tetap konflik) dan dua tipe aggregate berbagi id mentah (tidak saling bocor). **Keyset pagination** ditambahkan (bukan mengganti`LIMIT`yang sudah dibatasi — DoD issue menyebut`decision-logs`/`object-queue`/`audit log`sebagai contoh endpoint "LIMIT tanpa cursor"):`GET /api/v1/access/decision-logs`, `GET /api/v1/logs/audit`, `GET /api/v1/sync/object-queue`kini menerima`?cursor=`opsional (base64 dari`createdAt|id`, divalidasi — cursor rusak → `400 VALIDATION_ERROR`, bukan diam-diam dianggap "tanpa cursor") dan mengembalikan `nextCursor` (`null`bila halaman terakhir), helper baru`src/modules/_shared/keyset-pagination.ts`. **Diaudit tapi TIDAK diubah** (sehat, dilaporkan apa adanya — bukan "perbaikan" kosmetik dipaksakan): tidak ada `OFFSET`di seluruh`src/`; tidak ada `SELECT *`; setiap nilai bigint Postgres (count, byte_size, retry_count, dst.) sudah `Number(...)`eksplisit; index`awcms_abac_decision_logs_tenant_idx`/`awcms_audit_events_tenant_created_idx`/`awcms_workflow_tasks_tenant_status_idx`sudah`(tenant_id, created_at DESC)`-prefixed dan terbukti dipakai (Index Scan, <0.1ms) tanpa perubahan; endpoint reporting/audit/sync sudah diklasifikasi `reporting`/`background_sync`(bukan`interactive`) sejak Issue 10.2. **Diverifikasi live**: `EXPLAIN (ANALYZE, BUFFERS)`sebelum/sesudah untuk keempat query yang diubah (angka lengkap di PR);`bun run db:pool:health`→`status="healthy"`; suite integrasi penuh (termasuk lima test baru untuk `sync/push`, dua test cursor-pagination multi-halaman, dua test batch-insert multi-item) lulus terhadap `postgres:18.4`nyata;`bun run check` lulus bersih.

- **Dispatcher object sync queue nyata + kerasan integrasi eksternal pada 2026-07-06** (Issue #436, milestone M9 — pakai skill `awcms-integration`): menutup gap "dispatcher upload R2 nyata" yang sebelumnya jadi backlog eksplisit di `src/modules/sync-storage/README.md` §Belum tersedia. Ditambahkan `dispatchObjectSyncQueue` (`src/modules/sync-storage/application/object-dispatch.ts`) — fungsi internal, **bukan** endpoint HTTP publik (hanya worker terpercaya boleh menandai `awcms_object_sync_queue` sebagai `sent`/`failed`, bukan node sync mana pun). Pola tiga fase wajib ADR-0006 (provider tidak pernah dipanggil di dalam transaction DB): (1) CLAIM — satu transaction pendek memindahkan baris `pending` jatuh tempo ke status transien baru `sending` (`FOR UPDATE SKIP LOCKED`, migrasi `sql/018_awcms_object_sync_queue_dispatcher_schema.sql` menambah `sending` ke `CHECK` constraint status, reuse kolom `next_retry_at` yang sudah ada sebagai lease claim — tidak ada kolom baru; baris `sending` yang lease-nya kedaluwarsa — mis. proses dispatcher crash di tengah — otomatis bisa diklaim ulang pass berikutnya); (2) UPLOAD — di luar transaction apa pun, memanggil `ObjectUploader` yang di-resolve dari `requires_upload` (`src/modules/sync-storage/infrastructure/object-storage-uploader.ts`): `createNoopObjectUploader` (requires_upload=false, R2 off/`STORAGE_DRIVER=local` — tanpa jaringan/I/O sama sekali, selalu sukses, ADR-0006 "provider opsional... fitur off tidak menghentikan aplikasi") atau `createR2ObjectUploader` (requires_upload=true — upload nyata lewat `Bun.S3Client` **native Bun**, bukan SDK AWS/S3 npm, menegakkan AGENTS.md rule 14 Bun-only; memverifikasi checksum sha256 lokal aktual sebelum upload — pemanggil nyata pertama `verifyObjectChecksum`, sebelumnya hanya diuji langsung oleh unit test tanpa pemanggil produksi); (3) FINALIZE — transaction pendek kedua per baris, sukses → `sent`+`uploaded_at`, gagal → `evaluateObjectRetry` (`domain/object-queue.ts`, di-reuse **verbatim**, tidak diduplikasi sesuai instruksi eksplisit) menentukan `pending`+backoff eksponensial atau `failed`+`last_error` setelah `OBJECT_SYNC_MAX_RETRIES` habis. **Circuit breaker generik diperluas ke provider eksternal**: `src/lib/database/circuit-breaker.ts` (sebelumnya hanya singleton database) menambah `getProviderCircuitBreaker(providerKey)` — registry per-provider-key, dipakai uploader object storage (`"object-storage"`); saat breaker terbuka, baris `requires_upload=true` tidak diklaim sama sekali pada pass tersebut (tetap `pending`, tak tersentuh), baris `requires_upload=false` tetap jalan karena tak pernah menyentuh provider. **Timeout** setiap panggilan upload (`src/lib/integration/timeout.ts`, `withTimeout`, env baru `OBJECT_SYNC_UPLOAD_TIMEOUT_MS` default 10000ms). **Observability**: `correlationId` opsional diteruskan ke setiap baris log terstruktur (`sync_storage.object_dispatch.claimed/sent/retry_scheduled/failed`), diredaksi otomatis oleh `_shared/redaction.ts` yang sudah ada, pesan error provider dipotong ke 500 karakter sebelum disimpan sebagai `last_error`. **Idempotensi**: baris `sent`/`failed` tidak pernah diklaim ulang; kunci upload (`objectKey`) sendiri jadi dedup key alami (PUT S3/R2 ke key yang sama adalah overwrite, bukan duplikat) — tidak perlu ledger batch terpisah. **CLI terjadwal** `bun run sync:objects:dispatch` (`scripts/object-sync-dispatch.ts`) — bukan endpoint HTTP, mengiterasi tenant `active` (`awcms_tenants` tidak ber-RLS, tabel root) dan menguras backlog setiap tenant per batch sampai satu pass tidak mengklaim apa pun; dimaksudkan untuk cron/systemd timer/k8s CronJob. **Tidak ada endpoint/event baru** — dispatcher murni internal, `bun run api:spec:check` tetap OK tanpa perubahan OpenAPI/AsyncAPI. **Scope sengaja dibatasi** sesuai instruksi eksplisit "jangan over-engineer": base ini generik tanpa provider eksternal riil terpasang selain R2 opsional; tidak dibangun integrasi WA/email/pajak spekulatif yang tidak ada kebutuhan konkret di base ini — hanya dispatcher object-queue/R2, generalisasi circuit breaker, dan observability. **Verifikasi webhook masuk (HMAC + anti-replay)** dari DoD **tidak berlaku** untuk PR ini — upload R2 adalah panggilan keluar (push), bukan callback masuk dari provider; tidak ada webhook baru ditambahkan sehingga tidak ada yang perlu diverifikasi. **Diverifikasi live** lewat `docker compose` + script verifikasi mandiri terhadap `postgres:18.4` nyata (bukan cuma test otomatis): provider off (requires_upload=false) → dispatch → `sent` tanpa panggilan jaringan; provider on lewat fake R2 endpoint lokal (`Bun.serve`, PUT round-trip nyata) → `sent`+`uploaded_at` terisi; kegagalan dipaksa nyata (endpoint fake mengembalikan 500) → backoff eksponensial nyata (retry_count naik 1→5, `next_retry_at` mengikuti `2^retryCount` menit) → habis (`OBJECT_SYNC_MAX_RETRIES=5`) → `failed`+`last_error="simulated outage"` → retry manual (transisi sama seperti `POST /sync/object-queue/{id}/retry`) → `pending` → provider pulih → dispatch berikutnya → `sent`; `bun run sync:objects:dispatch` CLI nyata dijalankan terhadap 2 tenant sekaligus, termasuk kasus misconfigurasi (baris `requires_upload=true` tanpa kredensial R2 di env — terdegradasi anggun ke retry/backoff, bukan crash). `bun run check` lulus bersih; suite integrasi penuh (5 test baru khusus dispatcher + 3 test baru circuit-breaker registry + 7 test baru uploader) lulus terhadap `postgres:18.4` nyata.

- **Security hardening berbasis standar OWASP Top 10 / ASVS / ISO 27001 pada 2026-07-06** (Issue #437, milestone M9 — pakai skill `awcms-security-hardening`, **issue terakhir epic M9**, 5/5 anak issue selesai): audit kepatuhan yang memetakan kontrol yang sudah ada (ABAC default-deny, RLS FORCE, audit append-only, redaction, argon2id, HMAC sync) ke standar industri dengan bukti konkret — matrix lengkap ditambahkan sebagai section baru di `docs/awcms/20_threat_model_security_architecture.md` §"Matrix kepatuhan OWASP / ASVS / ISO 27001". Hasil: OWASP Top 10 (2021) **10/10 terpenuhi**; ASVS L1/L2 **8/8 area terpenuhi**; ISO/IEC 27001:2022 Annex A **9/10 terpenuhi** (1 di luar scope kode: A.8.16 monitoring/alerting terpusat/SIEM, tanggung jawab lapisan operasional aplikasi turunan/deployment, dicatat eksplisit bukan diabaikan). **Dua gap prioritas nyata ditutup**: (1) **security response headers** — sebelumnya nol (grep repo tak menemukan `Content-Security-Policy`/`Strict-Transport-Security`/`X-Frame-Options`/`X-Content-Type-Options` di manapun) — ditambahkan `src/lib/security/security-headers.ts` (`X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`, `Strict-Transport-Security` digerbang `APP_ENV=production`), dipasang di `src/middleware.ts` untuk setiap response. **CSP** memakai fitur bawaan Astro `security.csp` (`astro.config.mjs`), bukan nonce/hash manual — dua pendekatan manual dicoba lebih dulu dan **keduanya gagal saat diverifikasi dengan headless-Chrome/CDP nyata** (curl tak bisa mendeteksi pelanggaran CSP karena tak mengeksekusi JS/CSS, jadi bug ini tak akan pernah ketahuan tanpa browser sungguhan): nonce per-request dihapus diam-diam oleh compiler Astro dari atribut `is:inline` pada skrip (header tak pernah cocok); hash SHA-256 manual untuk satu skrip `is:inline` yang diketahui ternyata melewatkan beberapa skrip/style lain yang Astro inline per-komponen tanpa diminta (`ThemeToggle.astro`, `LanguageSwitcher.astro`, tombol logout admin) — **benar-benar memblokir fungsi nyata** (tombol tema tak merespons klik sama sekali) saat diverifikasi lewat sesi CDP sungguhan (navigasi + klik tombol + baca console). Solusi akhir: hash otomatis Astro untuk semua yang di-inline-nya + satu hash manual terdaftar via `security.csp.scriptDirective.hashes` untuk satu-satunya skrip `is:inline` tersisa (pencegah flash tema `AdminLayout.astro`, isi 100% statis — baca nilai tenant dari atribut `data-tenant-default-theme`, bukan `define:vars` — karena hashing Astro sendiri terjadi sekali di build time sehingga isi skrip harus identik di setiap request; sumber tunggal kebenaran di `src/lib/security/theme-init-script.ts`, dijaga sinkron oleh `tests/theme-init-script.test.ts`). Diverifikasi ulang: nol pelanggaran CSP di console, tombol tema/language switcher/logout berfungsi normal. (2) **Rate limiting login** — `evaluateLoginAttempt` (`login-policy.ts`) hanya mengunci per-identitas, tak menahan penyerang yang merotasi `loginIdentifier` dari sumber yang sama (enumerasi lintas-akun); ditambahkan `src/lib/security/rate-limit.ts` (fixed-window counter sederhana, in-memory per-proses — keterbatasan didokumentasikan: tak terbagi antar instance pada deployment multi-instance, cukup untuk topologi default LAN-first single-instance doc 18) dipasang di `POST /api/v1/auth/login` dengan kunci sumber+tenant (env baru `AUTH_LOGIN_RATE_LIMIT_MAX`/`AUTH_LOGIN_RATE_LIMIT_WINDOW_SEC`, default 20/60 detik — jauh di atas `AUTH_LOGIN_MAX_ATTEMPTS=5` agar tak pernah tersandung pengguna sah). Diverifikasi live: percobaan ke-21 dari IP+tenant sama → `429 RATE_LIMITED` + header `Retry-After`; sumber IP berbeda tak terpengaruh. **Bug ditemukan pada gate itu sendiri**: menjalankan `bun run security:readiness` terhadap kode yang SUDAH ADA (bukan perubahan PR ini) mengungkap false-positive — `checkNoHardcodedSecret` menandai `ERROR_CODE_KEYS.TOKEN_EXPIRED: "error.token_expired"` (`src/lib/i18n/error-messages.ts`, sejak Issue #433) sebagai kemungkinan secret karena nama variabel mengandung "TOKEN", padahal nilainya kunci katalog i18n — gate ini akan memblokir go-live untuk alasan yang salah pada kode yang sudah merged. Diperbaiki dengan heuristik tambahan `I18N_KEY_LIKE_VALUE_PATTERN`. `scripts/security-readiness.ts` diperluas dua check baru: `checkSecurityHeadersPresent` (live, hit server nyata, cek 5 header) dan `checkLoginRateLimitImplemented` (murni), keduanya `warning` (defense-in-depth, bukan kontrol akses primer yang sudah `critical`). **Tidak over-engineer**: kontrol yang sudah terpenuhi (ABAC, RLS, audit, redaction, argon2id) dilaporkan apa adanya dengan bukti, tidak "diperbaiki" secara kosmetik; satu gap non-critical (A.8.16 monitoring terpusat) diberi follow-up eksplisit, bukan diabaikan diam-diam. **Diverifikasi live**: `bun run check` lulus bersih (297 unit pass/45 skip tanpa DB; 335 pass/0 fail dengan `postgres:18.4` nyata — termasuk test integrasi rate-limit baru); `bun run security:readiness` lulus (13 check, 0 critical/0 warning failure) terhadap server nyata (`bun run build && bun ./dist/server/entry.mjs`); sesi headless-Chrome/CDP nyata (bukan cuma curl) memverifikasi CSP tak memblokir fungsi apapun dan tema/theme-toggle/language-switcher/logout tetap berfungsi.

- **Aktivasi sistem log & manajemennya pada 2026-07-06** (Issue #447, milestone M9 — **issue baru berdiri sendiri, BUKAN bagian epic #438** yang sudah closed 5/5 pada entri di atas): menutup tiga gap operasional yang sejak awal tercatat eksplisit sebagai backlog di `src/modules/logging/README.md` §Belum tersedia dan doc 20 §Matrix kepatuhan (baris A.8.16), bukan spekulasi baru. (1) **Correlation ID full-propagation**: `ApiMeta.correlationId` sebelumnya hanya diwiring end-to-end ke satu endpoint demo (`GET /logs/audit`, Issue 10.1) — endpoint lain (health, setup, login, reports, sync, dll., total 31 dari 36 file route) tidak pernah mengisinya di body respons meski header `X-Correlation-ID` sudah konsisten di semua respons sejak Issue 10.1. Alih-alih mengedit ~31 file handler (~223 titik pemanggilan `ok()`/`fail()`), diselesaikan dengan **satu titik pemasangan**: `src/lib/logging/correlation-response.ts` (fungsi murni, testable tanpa Astro runtime, pola sama seperti `security-headers.ts` Issue #437 memfaktorkan logika header dari `middleware.ts`) dipanggil dari `src/middleware.ts` — mengisi `meta.correlationId` pada setiap respons JSON `/api/*` HANYA bila handler belum mengisinya sendiri (tidak pernah menimpa nilai eksplisit yang sudah ada, mis. `GET /logs/audit`). Dipilih dibanding pengeditan manual karena setiap handler API sudah lewat helper `ok()`/`fail()` yang selalu mengembalikan objek `meta` (default `{}`) — memvalidasi field itu di satu tempat lebih aman (tak ada risiko lupa satu file dari 31) dan lebih kecil diff-nya daripada duplikasi perubahan yang sama berulang-ulang. (2) **Retensi/purge `awcms_audit_events`**: tabel append-only ini sebelumnya tumbuh tanpa batas, tidak seperti `awcms_idempotency_keys` yang doc 04 sudah tetapkan retensi 7–30 hari — doc 04 sebenarnya sudah punya baris "Security/audit log: 1–5 tahun sesuai kebutuhan" tapi tanpa angka konkret atau mekanisme. Ditambahkan `purgeExpiredAuditEvents` (`src/modules/logging/application/audit-purge.ts`, reuse `recordAuditEvent` yang sudah ada — bukan mekanisme audit baru) dengan retensi default **730 hari** (titik tengah rentang doc 04), dikonfigurasi via `AUDIT_LOG_RETENTION_DAYS` (doc 18) atau flag `--retention-days=N`, dijalankan oleh CLI terjadwal baru `bun run logs:audit:purge` (`scripts/audit-log-purge.ts`, pola dispatcher identik `object-sync-dispatch.ts` Issue #436: iterasi tenant `active`, batch `DELETE ... LIMIT 5000` per pass via `WHERE id IN (SELECT ... LIMIT ...)`, `workClass: "maintenance"` bukan `background_sync` karena ini operasi administratif bulk-delete, bukan replikasi/dispatch) — **bukan** endpoint HTTP publik (konsisten dengan pola dispatcher #436: hanya worker terpercaya). Purge murni berbasis umur (tidak ada FK anak pada tabel ini, migration 011, jadi tidak pernah memutus FK), dan setiap batch yang benar-benar menghapus sesuatu menulis audit event barunya sendiri (`action='purge'`, `resourceType='audit_event'`, severity `warning`) — tidak pernah purge diam-diam (doc 04 §Aturan implementasi). **Tidak ada migration SQL baru** — tidak butuh kolom/tabel/permission baru (permission tidak diperlukan karena ini CLI internal, bukan endpoint HTTP yang butuh guard ABAC — persis pola migrasi `018` untuk dispatcher #436 yang juga tidak menambah permission); `bun run db:migrate` tetap 18 migration sebelum/sesudah. (3) **Extension point observability**: dua hook opsional baru, default `null` (no-op), tanpa dependency baru — `setLogSink()`/`getLogSink()` di `src/lib/logging/logger.ts` (dipanggil dengan `LogEntry` yang sama persis dengan yang ditulis ke stdout, setelah lolos gerbang `LOG_LEVEL`) dan `setAuditExportHook()`/`getAuditExportHook()` di `application/audit-log.ts` (dipanggil dengan event yang baru saja di-INSERT). Keduanya menelan error dari consumer (`console.error`/`log("warning", ...)`, tidak pernah dilempar ulang) sehingga sink/hook yang rusak tak pernah menjatuhkan aplikasi atau menggagalkan transaction pemanggil. **Didokumentasikan eksplisit sebagai bukan implementasi SIEM nyata** — batas scope A.8.16 dari Issue #437 (monitoring/alerting terpusat di luar cakupan base generik) tidak berubah, hanya titik pemasangannya yang kini tersedia untuk aplikasi turunan. `AuditExportHook` didokumentasikan berjalan di dalam transaction yang sama dengan INSERT-nya — pemanggil tidak boleh melakukan I/O eksternal langsung di dalamnya (ADR-0006), harus lewat pola outbox yang sudah ada bila perlu mengirim keluar. Contoh konsumen minimal: `tests/logger.test.ts` §"setLogSink extension point" dan `tests/audit-log.test.ts` §"AuditExportHook extension point". (4) **`LOG_LEVEL` diverifikasi regresi** (bukan fitur baru) — tetap dihormati dengan benar di semua lingkungan; tidak ada perubahan kode, dikonfirmasi ulang lewat test yang sudah ada (`tests/logger.test.ts`, sudah lulus sejak Issue 10.1). **Secara eksplisit tidak dikerjakan** (sesuai scope issue): rotasi file log di dalam aplikasi — `console.log`/stdout tetap sumber kebenaran, capture/rotate tetap tanggung jawab lapisan deployment (doc 18, sudah terdokumentasi, tidak perlu tambahan). **Diverifikasi live** lewat `docker compose` (network host) terhadap `postgres:18.4` nyata: `GET /api/v1/health`, `POST /api/v1/setup/initialize`, `POST /api/v1/auth/login`, `GET /api/v1/reports/tenant-activity` (empat endpoint yang sebelumnya TIDAK PERNAH mengisi `meta.correlationId`) kini konsisten mengisinya dengan nilai yang sama persis dengan header `X-Correlation-ID`, termasuk saat header `X-Correlation-ID` custom dikirim client (diteruskan apa adanya) dan pada jalur error (`fail()`, `400 TENANT_REQUIRED`); `GET /logs/audit` (yang sudah eksplisit sejak Issue 10.1) tidak tertimpa. Purge nyata: 7 audit event berumur 800 hari (di atas cutoff 730 hari) di-seed langsung ke Postgres untuk satu tenant, `bun run logs:audit:purge` dijalankan sungguhan → 7 baris terhapus, 1 baris baru (`< 1` hari) tetap ada, dan satu audit event `purge` baru muncul dengan `attributes.purgedCount=7`+`retentionDays=730`+`cutoffIso` — dikonfirmasi baik langsung via `psql` maupun lewat `GET /api/v1/logs/audit?action=purge` (correlationId run CLI berbeda dari correlationId request GET, keduanya benar sesuai konteks masing-masing). Flag `--retention-days=1` override live dikonfirmasi (purged=0 saat semua event tersisa berumur <1 hari). `bun run check` lulus bersih (327 unit pass/51 skip tanpa DB; 360 pass/0 fail dengan `postgres:18.4` nyata, termasuk 5 test integrasi baru khusus purge dan 10 test unit baru untuk `correlation-response.ts` + extension point logger/audit). `bun run api:spec:check` tetap OK tanpa perubahan (tidak ada endpoint HTTP baru/berubah — purge murni CLI internal).

- **Verifikasi versi Bun + lapisan E2E browser test (Playwright) pada 2026-07-09** (permintaan user — cek versi Bun terkini, tambah skill browser test yang kompatibel Bun): (1) **Bun sudah versi terkini** — `bun --version` = `1.3.14`, dicocokkan ke rilis GitHub `oven-sh/bun` terbaru (`bun-v1.3.14`, dipublikasikan 2026-05-13) — tidak ada upgrade yang dilakukan, `package.json`'s `packageManager`/`engines.bun` dan `.github/workflows/ci.yml`'s `bun-version` sudah konsisten `1.3.14`. (2) **Lapisan E2E baru** (`@playwright/test` ditambahkan ke `devDependencies`, `playwright.config.ts`, `tests/e2e/*.e2e.ts`, skill `awcms-browser-test`) — puncak piramida testing doc 07 yang sebelumnya cuma aspirasi ("sedikit end-to-end di puncak") tanpa tooling nyata. **Keputusan Bun-only krusial, diverifikasi empiris bukan diasumsikan**: `bunx playwright test` (tanpa flag `--bun`) diam-diam menjalankan proses test-runner Playwright di **Node.js sungguhan** (dibuktikan: `process.versions` di dalam proses test menunjukkan `node`, bukan `bun`, saat dijalankan tanpa `--bun`) — pelanggaran diam-diam terhadap AGENTS.md aturan #14 (Backend Bun-only) yang mudah lolos review kalau tidak dicek dengan menginspeksi runtime dari dalam proses. `bun --bun playwright test` (dipakai script `test:e2e`/`test:e2e:install`, pola sama seperti `"dev": "bun --bun astro dev"` yang sudah ada) memaksa proses test-runner-nya sendiri jalan di Bun native — diverifikasi `isBun: true` di dalam proses test, dan `chromium.launch()` + kedua spec nyata di `tests/e2e/login.e2e.ts` lulus konsisten di bawah mode ini (Bun 1.3.14, Linux). Ada laporan lama Bun/Playwright soal `chromium.launch()` hang di bawah Bun native runtime karena subprocess/IPC (`--remote-debugging-pipe` lewat fd3), terutama Windows (`oven-sh/bun#15679`, fix PR `#31932` belum merged per riset saat perubahan ini dibuat) — **tidak tereproduksi** di Linux/Bun 1.3.14 saat diverifikasi langsung (pemanggilan programatik `chromium.launch()` langsung dari skrip `bun script.mjs` DAN lewat `@playwright/test`'s CLI penuh via `bun --bun playwright test`, keduanya berhasil). Dicatat sebagai kelas kegagalan yang sudah diketahui untuk platform/versi lain, bukan diasumsikan aman universal. (3) **Konvensi penamaan `*.e2e.ts`** (bukan `.spec.ts`/`.test.ts`) di `tests/e2e/` — `bun test` secara default merekursif mencocokkan `*.test.*`/`*_test.*`/`*.spec.*`/`*_spec.*`, jadi spec Playwright bernama `.spec.ts` akan ikut dicoba dijalankan `bun test` sebagai file `bun:test` dan gagal (import `test`/`expect` dari `@playwright/test`, konteks runtime beda total) — dicek: `bun test tests/e2e` melaporkan "did not match any test files" untuk `.e2e.ts`. (4) **Root-less sandbox**: `playwright install --with-deps` butuh `apt-get`/root (tidak tersedia di lingkungan sandbox ini — `sudo` gagal karena flag "no new privileges"); ditambahkan escape hatch `PLAYWRIGHT_CHROMIUM_EXECUTABLE` di `playwright.config.ts` yang membaca browser sistem yang sudah terpasang (`/usr/bin/google-chrome` di lingkungan ini) tanpa perlu unduh Chromium Playwright sendiri — diverifikasi berfungsi tanpa `--no-sandbox` tambahan. (5) **Belum diwire ke `bun run check`/CI** — E2E butuh app+Postgres benar-benar hidup (bukan `DATABASE_URL`-gated skip bersih seperti integration test), jadi menaruhnya di `check`/CI tanpa orkestrasi server akan selalu gagal, bukan skip; dijalankan manual (`bun run dev` + `bun run test:e2e`) sampai ada issue terpisah untuk mewire CI. Contoh spec kerja nyata (bukan placeholder): `tests/e2e/login.e2e.ts` — render form login + jalur error identitas tak dikenal tidak membocorkan detail internal (stack/nama tabel Postgres). **Diverifikasi live**: kedua test lulus terhadap `bun run dev` + Postgres 18 sungguhan, dua kali (mode `bunx --bun` dan `bun --bun` langsung); `bun test tests/e2e` mengonfirmasi tidak ada tabrakan discovery; `bun run check:docs` lulus setelah update skill index + mermaid `peta pemakaian` (`.claude/skills/README.md`) dan doc 07.
