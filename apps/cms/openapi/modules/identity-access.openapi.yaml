# Source fragment for the "identity-access" module (Issue #182, epic #177).
#
# Owns every path whose operations carry this module's OpenAPI tag(s), plus
# every components.schemas entry referenced ONLY by this module's operations.
# Schemas shared by 2+ modules (or referenced by none) live in the root
# fragment (openapi/awcms-public-api.src.yaml) instead.
#
# NOT independently valid OpenAPI: $ref targets pointing at shared components
# (securitySchemes/parameters/responses and cross-module schemas) only resolve
# after `bun run openapi:bundle` merges this file with the root fragment and
# every sibling module fragment into openapi/awcms-public-api.openapi.yaml.
# Edit this file (or the root fragment for shared components), never the
# generated bundle.
paths:
  /api/v1/abac/policies:
    get:
      operationId: listAbacPolicies
      tags:
        - Identity & Access
      summary: List the current tenant's ABAC policies (seeded-empty by default; built-in rules apply).
      responses:
        "200":
          description: The tenant's ABAC policies (limit 100).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/AbacPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createAbacPolicy
      tags:
        - Identity & Access
      summary: Author a new ABAC policy for the current tenant (high-risk access-control change; audit-logged).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - policyCode
                - effect
              properties:
                policyCode:
                  type: string
                  description: Unique per tenant. Starts alphanumeric; letters, digits, '.', '_', '-' only (max 100 chars).
                effect:
                  type: string
                  enum:
                    - allow
                    - deny
                description:
                  type: string
                  nullable: true
      responses:
        "201":
          description: Policy created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AbacPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: policyCode is already taken in this tenant (POLICY_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/abac/policies/{id}:
    patch:
      operationId: updateAbacPolicy
      tags:
        - Identity & Access
      summary: Update an ABAC policy's effect/description and/or enable-disable it (high-risk; audit-logged).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        description: Provide at least one of effect, description, isActive. A null description clears it.
        content:
          application/json:
            schema:
              type: object
              properties:
                effect:
                  type: string
                  enum:
                    - allow
                    - deny
                description:
                  type: string
                  nullable: true
                isActive:
                  type: boolean
      responses:
        "200":
          description: Policy updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AbacPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/assignments:
    post:
      operationId: createRoleAssignment
      tags:
        - Identity & Access
      summary: Assign a role to a tenant user (high-risk, audited, idempotent).
      description: Grants a role to a tenant user. Idempotent at the DB unique index — a repeat assign returns 409. Gated on `identity_access.access_control.assign`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantUserId
                - roleId
              properties:
                tenantUserId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: The created assignment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      tenantUserId:
                        type: string
                        format: uuid
                      roleId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The role is already assigned to this tenant user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deleteRoleAssignment
      tags:
        - Identity & Access
      summary: Revoke a role from a tenant user (high-risk, audited).
      description: Removes a role assignment. 404 when no such assignment exists. Gated on `identity_access.access_control.assign`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantUserId
                - roleId
              properties:
                tenantUserId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: The assignment was removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      removed:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/evaluate:
    post:
      operationId: accessEvaluate
      tags:
        - Identity & Access
      summary: "Reflect the ABAC decision for the caller's own access on a hypothetical request (Issue #179)."
      description: Returns what `evaluateAccess` would decide for the CALLER'S OWN access against the tenant's current active ABAC policies. Requires a valid session but no specific permission. The decision is recorded to the ABAC decision log.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccessEvaluateRequest"
      responses:
        "200":
          description: The evaluated decision.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AccessEvaluateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/access/policies:
    get:
      operationId: accessListAbacPolicies
      tags:
        - Identity & Access
      summary: "List the tenant's dynamic ABAC (DSL) policies (Issue #179)."
      description: Reads every stored DSL policy for the tenant (active and inactive). Gated on `identity_access.abac_policies.read`.
      responses:
        "200":
          description: The tenant's DSL ABAC policies.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policies:
                        type: array
                        items:
                          $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: accessCreateAbacPolicy
      tags:
        - Identity & Access
      summary: Author a new dynamic ABAC (DSL) policy (high-risk; audit-logged; only valid DSL is stored).
      description: Validates the condition DSL fail-closed before any write, so an invalid policy can never be stored or enabled. Created disabled by default unless `isActive:true`. Gated on `identity_access.abac_policies.configure`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacDslPolicyWriteRequest"
      responses:
        "200":
          description: The created policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A policy with that policyCode already exists (RESOURCE_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/policies/simulate:
    post:
      operationId: accessSimulateAbacPolicy
      tags:
        - Identity & Access
      summary: "Read-only ABAC decision simulation/preview (Issue #179; audited, never mutates)."
      description: Returns what `evaluateAccess` would decide for a hypothetical subject/request/environment against the tenant's active policies, plus a per-policy structural trace (no attribute VALUES, no PII). Writes no decision log. Gated on `identity_access.abac_policies.analyze`; simulating a DIFFERENT existing tenant user additionally requires `identity_access.access_control.read`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacSimulationRequest"
      responses:
        "200":
          description: The simulated decision and per-policy trace.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AbacSimulationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/access/policies/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: accessGetAbacPolicy
      tags:
        - Identity & Access
      summary: "Read one dynamic ABAC (DSL) policy (Issue #179)."
      description: Gated on `identity_access.abac_policies.read`. 404 when the policy does not exist in this tenant.
      responses:
        "200":
          description: The policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: accessUpdateAbacPolicy
      tags:
        - Identity & Access
      summary: Replace a dynamic ABAC (DSL) policy (high-risk; audit-logged; only valid DSL is stored).
      description: Validates the condition DSL fail-closed before any write. Gated on `identity_access.abac_policies.configure`. 404 when the policy does not exist; 409 on a policyCode collision.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacDslPolicyWriteRequest"
      responses:
        "200":
          description: The updated policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A policy with that policyCode already exists (RESOURCE_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/policies/{id}/enable:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: accessEnableAbacPolicy
      tags:
        - Identity & Access
      summary: Enable a dynamic ABAC (DSL) policy (high-risk; audit-logged).
      description: Marks the policy active so the evaluator applies it. Gated on `identity_access.abac_policies.configure`. 404 when the policy does not exist.
      responses:
        "200":
          description: The enabled policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/policies/{id}/disable:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: accessDisableAbacPolicy
      tags:
        - Identity & Access
      summary: Disable a dynamic ABAC (DSL) policy (high-risk; audit-logged).
      description: Marks the policy inactive so the evaluator stops applying it (deactivate-not-delete). Gated on `identity_access.abac_policies.configure`. 404 when the policy does not exist.
      responses:
        "200":
          description: The disabled policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/machine-credentials:
    get:
      operationId: accessListMachineCredentials
      tags:
        - Identity & Access
      summary: List this tenant's machine credentials (never their secrets).
      description: Gated on `identity_access.machine_credentials.read`. Returns derived `active`/`expired`/`revoked` status plus `lastUsedAt`, so a leak can be traced without any secret material leaving the server.
      responses:
        "200":
          description: The tenant's machine credentials, newest first (limit 200).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/MachineCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: accessIssueMachineCredential
      tags:
        - Identity & Access
      summary: Issue a machine credential (high-risk; audit-logged).
      description: |
        Mints a bearer for a non-human caller, bound to an existing tenant user (a service account), and returns the plaintext token EXACTLY ONCE — no endpoint can return it again.

        The credential AUTHENTICATES only: every request it makes still passes module-enabled, RBAC, ABAC (default-deny) and SoD. `allowedPermissionKeys` NARROWS — effective permissions are the intersection with what the service account holds, so granting that account another role never widens an already-issued credential.

        TWO CLASSES, TWO PERMISSIONS. Omit `allowedWriteActions` and this is the read-only credential of ADR-0049 §3, gated on `identity_access.machine_credentials.create`; requests it makes are refused unless the action is `read`. Name write actions and it is the ADR-0092 write class, gated on `identity_access.machine_credentials_write.create` INSTEAD — a separate key so that opening this class does not hand write-minting authority to every role that already holds `create`.

        The write class is narrower in every other direction: only actions in the code ceiling (`create`, `update` — never a high-risk one), at least one `allowedIpCidrs` entry, at most 30 days, and a request whose caller IP is unknown is refused rather than exempted.

        Not idempotency-keyed, deliberately: replaying the response would mean persisting the plaintext token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IssueMachineCredentialRequest"
      responses:
        "201":
          description: The credential, plus its plaintext token (shown once).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      credential:
                        $ref: "#/components/schemas/MachineCredential"
                      token:
                        type: string
                        description: The plaintext bearer. Shown once; only its hash is stored.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Request body exceeded the endpoint's size ceiling.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The issuance request failed validation (VALIDATION_FAILED); `error.details` lists every offending field.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/machine-credentials/{id}/revoke:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: accessRevokeMachineCredential
      tags:
        - Identity & Access
      summary: Revoke a machine credential (high-risk; audit-logged).
      description: Effective on the credential's very next request, because authentication reads the same row. Gated on `identity_access.machine_credentials.revoke` — separate from `create` so a leak can be stopped by someone who cannot mint one. Re-revoking returns 409 rather than a silent success.
      responses:
        "200":
          description: The revoked credential.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      credential:
                        $ref: "#/components/schemas/MachineCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The credential is already revoked (ALREADY_REVOKED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/login:
    post:
      operationId: postAuthLogin
      tags:
        - Identity & Access
      summary: Authenticate with a login identifier and password; issues a session token.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - loginIdentifier
                - password
              properties:
                loginIdentifier:
                  type: string
                password:
                  type: string
                turnstileToken:
                  type: string
                  description: >-
                    Cloudflare Turnstile response token (Issue #186). Required
                    only on a full-online deployment where Turnstile is enabled;
                    absent and ignored on every LAN/offline deployment. Verified
                    server-side before password verification.
      responses:
        "200":
          description: Session issued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many login attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/session/tenant:
    post:
      operationId: postAuthSessionTenant
      tags:
        - Identity & Access
      summary: Exchange a tenant-selection token for a session in a named tenant.
      description: >-
        ADR-0088. Spends the single-use selection token returned by a
        `POST /api/v1/auth/login` that carried NO tenant header, and issues a
        session in the tenant named in the body. The token lives at most 120
        seconds, is spent whether or not the exchange succeeds, and can never
        authenticate any other endpoint — the authorization chokepoint refuses
        its hash namespace outright. Every gate login applies once a tenant is
        known applies here too, including the target tenant's MFA policy.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - principalToken
                - tenantId
              properties:
                principalToken:
                  type: string
                  description: The selection token from the 409 body.
                tenantId:
                  type: string
                  format: uuid
                  description: >-
                    The tenant to sign in to. Named by the caller: no membership
                    list is disclosed, because reading one would require a
                    cross-tenant scan that FORCE RLS refuses.
      responses:
        "200":
          description: Session issued in the selected tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      tenantId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: >-
            One shape for every refusal an attacker could probe — unknown,
            expired or already-spent token, no membership, inactive identity —
            plus the MFA continuations `MFA_REQUIRED` and
            `MFA_ENROLLMENT_REQUIRED`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: >-
            The tenant is suspended (`TENANT_UNAVAILABLE`) or disables password
            sign-in for this identity (`PASSWORD_LOGIN_DISABLED`). Reachable
            only with a genuine token, so it discloses nothing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/session/switch:
    post:
      operationId: postAuthSessionSwitch
      tags:
        - Identity & Access
      summary: Move the current session to another tenant the same human belongs to.
      description: >-
        ADR-0088. Issues a session in the target tenant and revokes the source
        session — switching leaves, it does not accumulate. Sessions whose
        `origin_auth` is `sso` or `handoff` are refused: only a globally
        verified credential may cross a tenant boundary, otherwise a tenant IdP
        administrator could assert an address and switch into the tenant where
        that person really works. Assurance does not travel: the new session
        starts at `aal1` and the target tenant's MFA policy is applied afresh.
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
          description: The CURRENT tenant, i.e. the session being switched away from.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantId
              properties:
                tenantId:
                  type: string
                  format: uuid
                  description: The tenant to switch to.
      responses:
        "200":
          description: Session issued in the target tenant; the source session is revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      tenantId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: >-
            `SESSION_NOT_SWITCHABLE` (the session was not issued by a global
            credential), `TENANT_UNAVAILABLE`, or `PASSWORD_LOGIN_DISABLED`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: >-
            `MEMBERSHIP_NOT_FOUND` — one shape for "you do not belong there", so
            this endpoint cannot be used to ask whether somebody belongs to a
            tenant the caller names.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/logout:
    post:
      operationId: postAuthLogout
      tags:
        - Identity & Access
      summary: Revoke the current session.
      responses:
        "200":
          description: Logged out.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      loggedOut:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/me:
    get:
      operationId: getAuthMe
      tags:
        - Identity & Access
      summary: Current authenticated identity.
      responses:
        "200":
          description: Current identity.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      identityId:
                        type: string
                        format: uuid
                      loginIdentifier:
                        type: string
                      profileId:
                        type: string
                        format: uuid
                      status:
                        type: string
                      lastLoginAt:
                        type: string
                        format: date-time
                        nullable: true
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/mfa/status:
    get:
      operationId: getAuthMfaStatus
      tags:
        - Identity & Access
      summary: Current identity's MFA enrollment state.
      responses:
        "200":
          description: MFA status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                      factorType:
                        type: string
                        enum:
                          - totp
                      activatedAt:
                        type: string
                        format: date-time
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/mfa/totp/enroll/start:
    post:
      operationId: postAuthMfaEnrollStart
      tags:
        - Identity & Access
      summary: Begin TOTP enrollment; returns a one-time secret and otpauth URI.
      description: Authorized by EITHER a live session OR an enrollment grant (X-AWCMS-MFA-Enrollment-Token) issued by POST /auth/login when a tenant policy requires MFA for an identity without a factor.
      parameters:
        - name: X-AWCMS-MFA-Enrollment-Token
          in: header
          required: false
          description: Enrollment grant token from a login that returned MFA_ENROLLMENT_REQUIRED; used in place of a session.
          schema:
            type: string
      responses:
        "200":
          description: Pending factor created; secret shown once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      secret:
                        type: string
                      otpauthUri:
                        type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: MFA already active for this account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: MFA encryption key is missing/invalid (misconfigured).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/totp/enroll/verify:
    post:
      operationId: postAuthMfaEnrollVerify
      tags:
        - Identity & Access
      summary: Confirm a pending TOTP enrollment; activates the factor and returns recovery codes.
      description: Authorized by EITHER a live session OR an enrollment grant. When authorized via an enrollment grant (a login that returned MFA_ENROLLMENT_REQUIRED), the grant is consumed and a fresh aal2 session token is returned.
      parameters:
        - name: X-AWCMS-MFA-Enrollment-Token
          in: header
          required: false
          description: Enrollment grant token; used in place of a session to complete the "must enroll" login.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        "200":
          description: Factor activated; recovery codes shown once. token/expiresAt/assuranceLevel are present only when enrolled via an enrollment grant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      activated:
                        type: boolean
                      recoveryCodes:
                        type: array
                        items:
                          type: string
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      assuranceLevel:
                        type: string
                        enum:
                          - aal2
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          description: MFA misconfigured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/totp/verify:
    post:
      operationId: postAuthMfaVerify
      tags:
        - Identity & Access
      summary: Complete a login paused with MFA_REQUIRED by submitting a TOTP or recovery code.
      description: Authenticated by possession of the mfaChallengeToken from POST /auth/login, not by a session. On success issues an aal2 session.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mfaChallengeToken
              properties:
                mfaChallengeToken:
                  type: string
                code:
                  type: string
                recoveryCode:
                  type: string
      responses:
        "200":
          description: MFA verified; aal2 session issued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      assuranceLevel:
                        type: string
                        enum:
                          - aal2
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many verification attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: MFA misconfigured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/totp/disable:
    post:
      operationId: postAuthMfaDisable
      tags:
        - Identity & Access
      summary: Self-service disable of the current identity's MFA factor.
      responses:
        "200":
          description: MFA disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      disabled:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: No active MFA to disable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/recovery-codes/regenerate:
    post:
      operationId: postAuthMfaRecoveryRegenerate
      tags:
        - Identity & Access
      summary: Invalidate existing recovery codes and issue a fresh single-use set.
      responses:
        "200":
          description: New recovery codes shown once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      recoveryCodes:
                        type: array
                        items:
                          type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: No active MFA.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/step-up:
    post:
      operationId: postAuthMfaStepUp
      tags:
        - Identity & Access
      summary: Raise the current session to aal2 for high-risk actions.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                recoveryCode:
                  type: string
      responses:
        "200":
          description: Session stepped up to aal2 (rotated when rising from aal1).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assuranceLevel:
                        type: string
                        enum:
                          - aal2
                      rotated:
                        type: boolean
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many verification attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: MFA misconfigured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/admin/reset:
    post:
      operationId: postAuthMfaAdminReset
      tags:
        - Identity & Access
      summary: Administratively reset another user's MFA factor (requires reason).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - identityId
                - reason
              properties:
                identityId:
                  type: string
                  format: uuid
                reason:
                  type: string
      responses:
        "200":
          description: Target MFA reset.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      reset:
                        type: boolean
                      hadFactor:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/auth/mfa/policy:
    get:
      operationId: getAuthMfaPolicy
      tags:
        - Identity & Access
      summary: Read the tenant MFA enforcement policy.
      responses:
        "200":
          description: Tenant MFA policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantMfaPolicy"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      operationId: putAuthMfaPolicy
      tags:
        - Identity & Access
      summary: Set the tenant MFA enforcement level.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - enforcementLevel
              properties:
                enforcementLevel:
                  type: string
                  enum:
                    - optional
                    - required_for_privileged
                    - required_for_all
      responses:
        "200":
          description: Policy updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantMfaPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/auth/password/forgot:
    post:
      operationId: postAuthPasswordForgot
      tags:
        - Identity & Access
      summary: Request a password reset link (account-enumeration-safe).
      description: >-
        Always returns the same 200 body regardless of whether the identifier
        matched an eligible account. An unknown identifier, an inactive
        identity or tenant-user, an identity the tenant has taken off password
        login, and a successfully queued email are indistinguishable to the
        caller by response body, status code, or error code. Rate limited per
        client IP and tenant.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - loginIdentifier
              properties:
                loginIdentifier:
                  type: string
                turnstileToken:
                  type: string
                  description: >-
                    Cloudflare Turnstile response token (Issue #186), solved for
                    the `password_reset` action. Required only on a full-online
                    deployment where Turnstile is enabled; absent and ignored on
                    every LAN/offline deployment.
      responses:
        "200":
          description: >-
            Request accepted. Says nothing about whether an account exists.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      requested:
                        type: boolean
                      message:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many password reset requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/password/reset:
    post:
      operationId: postAuthPasswordReset
      tags:
        - Identity & Access
      summary: Complete a password reset with a single-use token.
      description: >-
        On success the password is replaced, the lockout counters are cleared,
        the token is burned, and every session belonging to that identity is
        revoked. Every rejection — unknown token, expired, already used, the
        identity deactivated since issue, or password login disabled for it —
        returns the same `PASSWORD_RESET_INVALID` error, so the endpoint cannot
        be used to fingerprint the state of a token.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - token
                - newPassword
              properties:
                token:
                  type: string
                  description: The raw single-use token from the emailed link.
                newPassword:
                  type: string
                  minLength: 8
                turnstileToken:
                  type: string
                  description: >-
                    Cloudflare Turnstile response token (Issue #186), solved for
                    the `password_reset` action. Required only on a full-online
                    deployment where Turnstile is enabled.
      responses:
        "200":
          description: Password changed and all sessions revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      reset:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many password reset attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/password/change:
    post:
      operationId: changeOwnAuthPassword
      tags:
        - Identity & Access
      summary: Change your own password while signed in (self-service, no permission).
      description: >-
        The counterpart to /api/v1/auth/password/reset: that endpoint serves
        someone who CANNOT sign in and proves control of a mailbox, this one
        serves someone who is signed in and proves control of the credential.
        Self-service by construction — the subject is the session bearer and the
        route accepts no parameter naming anybody else.

        A fresh second factor is required ONLY from callers who have one
        enrolled. Requiring aal2 unconditionally would permanently prevent every
        user without MFA from changing their password, and the users most likely
        to need to are the ones who just learned it leaked. The current password
        is the re-authentication for everyone; the second factor is additional
        for those who can supply it.

        On success the password is replaced, the lockout counters are cleared
        (whoever supplied the current password proved control of the credential),
        and every OTHER session of that identity is revoked — the calling one
        survives, because a password change that signs you out of the tab you
        changed it in reads as a failure and the security property is unaffected.

        Rate limited on the SOURCE despite being authenticated: `currentPassword`
        is a guessable secret, so this is a credential-guessing surface whenever a
        session is used by someone who does not know the password behind it. An
        identifier-keyed bucket would instead let anyone reaching the endpoint
        hold one person's own password change hostage.

        Audited on both the success and the failure, with the device shape and IP
        pseudonym, and with no password-shaped attribute — not even a length.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                newPassword:
                  type: string
                  minLength: 8
                  description: Must differ from currentPassword — an unchanged password is refused as a validation error, never answered as a no-op success.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Password changed; the other sessions were revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      changed:
                        type: boolean
                        enum: [true]
                      revokedSessionCount:
                        type: integer
                        minimum: 0
        "400":
          description: Validation failed, or currentPassword did not match (INVALID_CREDENTIALS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: MFA is enrolled and this session is not currently stepped up (STEP_UP_REQUIRED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The tenant policy signs this identity in through SSO; there is no password to change (PASSWORD_LOGIN_DISABLED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many password change attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/register:
    post:
      operationId: postAuthRegister
      tags:
        - Identity & Access
      summary: Submit a self-registration request for admin review.
      description: >-
        Records a request; it NEVER creates an account. Accepts no password and
        no privilege field — approval creates the account with an unusable
        credential and emails a password-reset link. Returns `404` when
        `AUTH_SELF_REGISTRATION_ENABLED` is not `true`, indistinguishable from a
        route that does not exist. When enabled, an address that already has an
        account, an address with a request already pending, and a freshly
        recorded request all return the identical 200.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - displayName
                - loginIdentifier
              properties:
                displayName:
                  type: string
                  maxLength: 120
                loginIdentifier:
                  type: string
                  maxLength: 320
                  description: Must be an email address — the approval link is sent to it.
                turnstileToken:
                  type: string
                  description: >-
                    Cloudflare Turnstile response token (Issue #186), solved for
                    the `register` action. Required only on a full-online
                    deployment where Turnstile is enabled.
      responses:
        "200":
          description: >-
            Request accepted. Says nothing about whether the address is already
            registered or already pending.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      requested:
                        type: boolean
                      message:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          description: Self-registration is not enabled for this deployment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many registration attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/registration-requests:
    get:
      operationId: listRegistrationRequests
      tags:
        - Identity & Access
      summary: Pending self-registration requests for this tenant.
      description: >-
        Oldest first, bounded. Login identifiers are MASKED — a reviewer decides
        on a name and a domain, and this is the one response that would
        otherwise expose every applicant address at once.
      responses:
        "200":
          description: The pending queue.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/RegistrationRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/registration-requests/{id}/approve:
    post:
      operationId: approveRegistrationRequest
      tags:
        - Identity & Access
      summary: Approve a request, creating a real account.
      description: >-
        The only path that materializes profile + identity + tenant_user, hence
        its own permission (`registration_requests.approve`), separate from
        `reject`. The account is created with an UNUSABLE password and the
        applicant is emailed a password-reset link; `delivery` reports whether
        that link could actually be queued. `roleIds` is optional and defaults
        to none — an approval never grants a role by default, and a SYSTEM role
        (`owner`) is refused with 409 `ROLE_SYSTEM_PROTECTED`: admitting a
        person to a tenant is not the authority to hand out the tenant's whole
        permission catalogue.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                roleIds:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: Account created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      approved:
                        type: boolean
                      identityId:
                        type: string
                        format: uuid
                      tenantUserId:
                        type: string
                        format: uuid
                      delivery:
                        type: string
                        enum:
                          - queued
                          - unavailable
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: >-
            `IDENTIFIER_TAKEN` — an account with that login identifier already
            exists; or `ROLE_SYSTEM_PROTECTED` — one of `roleIds` names a system
            role, which no approval may grant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/registration-requests/{id}/reject:
    post:
      operationId: rejectRegistrationRequest
      tags:
        - Identity & Access
      summary: Reject a request. Creates nothing and notifies nobody.
      description: >-
        A rejection email would confirm to an anonymous submitter that this
        tenant exists and reviewed them — the same disclosure the public submit
        endpoint refuses to make.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Request rejected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      rejected:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/identity/business-scope/assignments:
    get:
      operationId: listBusinessScopeAssignments
      tags:
        - Identity & Access
      summary: "List this tenant's business-scope assignments (Issue #180)."
      description: >-
        Lists business-scope assignments for the caller's tenant, optionally
        filtered by status/tenantUserId/scopeType. Gated on
        `identity_access.business_scope_assignments.read`.
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - active
              - expired
              - revoked
        - name: tenantUserId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: scopeType
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: The tenant's business-scope assignments (newest first, bounded).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assignments:
                        type: array
                        items:
                          $ref: "#/components/schemas/BusinessScopeAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createBusinessScopeAssignment
      tags:
        - Identity & Access
      summary: Create a business-scope assignment (high-risk, audited, idempotent).
      description: >-
        Grants a subject a role/permission context restricted to one business
        scope. The `(scopeType, scopeId)` reference is validated server-side
        through the `BusinessScopeHierarchyPort` capability (never trusted from
        the request alone); an unresolved scope is denied `SCOPE_UNRESOLVED`.
        Self-grant is denied. Gated on
        `identity_access.business_scope_assignments.create`. Requires
        `Idempotency-Key`.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantUserId
                - scopeType
                - scopeId
              properties:
                tenantUserId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
                  nullable: true
                scopeType:
                  type: string
                  description: Lowercase snake_case scope type (e.g. "office").
                scopeId:
                  type: string
                  format: uuid
                effectiveFrom:
                  type: string
                  format: date-time
                effectiveTo:
                  type: string
                  format: date-time
                  nullable: true
                isTemporary:
                  type: boolean
                reason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The created business-scope assignment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assignment:
                        $ref: "#/components/schemas/BusinessScopeAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/assignments/{id}/revoke:
    post:
      operationId: revokeBusinessScopeAssignment
      tags:
        - Identity & Access
      summary: Revoke a business-scope assignment (high-risk, audited, idempotent).
      description: >-
        Revokes an active business-scope assignment (transitions it to
        `revoked`; append-only lifecycle history is recorded). Revocation takes
        effect on the next authorization decision immediately. Gated on
        `identity_access.business_scope_assignments.revoke`. Requires
        `Idempotency-Key`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - revokeReason
              properties:
                revokeReason:
                  type: string
      responses:
        "200":
          description: The revoked business-scope assignment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assignment:
                        $ref: "#/components/schemas/BusinessScopeAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The assignment is not active, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/conflicts:
    get:
      operationId: listSoDConflictEvaluations
      tags:
        - Identity & Access
      summary: "List the SoD conflict evaluation log (Issue #181)."
      description: >-
        Keyset-paginated, permission-gated segregation-of-duties conflict
        evaluation history (the conflict preview / audit view). Recorded for
        every assignment-create and high-risk-action conflict check, regardless
        of outcome. Safe projection: rule key, subject id, trigger context,
        outcome, reason, and timestamp only. Gated on
        `identity_access.business_scope_conflicts.read`.
      parameters:
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Opaque keyset cursor from a previous page's `nextCursor`.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: ruleKey
          in: query
          required: false
          schema:
            type: string
        - name: conflictDetected
          in: query
          required: false
          schema:
            type: string
            enum:
              - "true"
              - "false"
      responses:
        "200":
          description: A page of SoD conflict evaluations (newest first) plus the next cursor.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      conflicts:
                        type: array
                        items:
                          $ref: "#/components/schemas/SoDConflictEvaluation"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/identity/business-scope/exceptions:
    get:
      operationId: listSoDConflictExceptions
      tags:
        - Identity & Access
      summary: "List this tenant's SoD conflict exceptions (Issue #181)."
      description: >-
        Lists the tenant's segregation-of-duties conflict exceptions, optionally
        filtered by `status`/`ruleKey`. Gated on
        `identity_access.business_scope_exceptions.read`.
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - pending
              - approved
              - rejected
              - expired
              - revoked
        - name: ruleKey
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: The tenant's SoD conflict exceptions (newest first, bounded).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exceptions:
                        type: array
                        items:
                          $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createSoDConflictException
      tags:
        - Identity & Access
      summary: Request a SoD conflict exception (high-risk, audited, idempotent).
      description: >-
        Requests a bounded-lifetime, scope-bound exception to a registered SoD
        rule (`status: "pending"`, requires separate approval by a different
        user holding the rule's approval permission). The rule must exist in the
        code registry and permit exceptions; the exception must have an end date
        (no indefinite override). Gated on
        `identity_access.business_scope_exceptions.create`. Requires
        `Idempotency-Key`.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ruleKey
                - subjectTenantUserId
                - justification
                - effectiveTo
              properties:
                ruleKey:
                  type: string
                subjectTenantUserId:
                  type: string
                  format: uuid
                scopeType:
                  type: string
                  nullable: true
                  description: Set together with scopeId for a scope-specific exception; both null for a blanket exception.
                scopeId:
                  type: string
                  format: uuid
                  nullable: true
                justification:
                  type: string
                  minLength: 10
                effectiveFrom:
                  type: string
                  format: date-time
                effectiveTo:
                  type: string
                  format: date-time
      responses:
        "200":
          description: The created (pending) SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/partners/{partnerTenantId}/status:
    patch:
      operationId: setPartnerRegistryStatus
      tags:
        - Identity & Access
      summary: Suspend or reinstate a registered partner (PLATFORM-scoped, audited).
      description: >-
        Writes one column. Suspending stops every delegated actor the partner
        placed from being served, in every customer tenant, at their next
        request — enforced at the authorization chokepoint, not by a job, so
        there is no window.


        It revokes NOTHING. No grant row is touched and no engagement is
        severed: a grant is the record of who could see a customer's data and
        until when, and that has to stay answerable after a partner is
        suspended. Effectiveness is computed per request, so reinstating
        restores every surviving grant's reach without rewriting a row.


        Two permissions, both PLATFORM-scoped, because they are two
        authorities: `identity_access.partner_registry.disable` to suspend and
        `identity_access.partner_registry.restore` to reinstate. Setting the
        status it already has succeeds with `changed: false` and writes no
        audit row.
      parameters:
        - name: partnerTenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - active
                    - suspended
      responses:
        "200":
          description: The partner, and whether this request changed anything.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      partner:
                        $ref: "#/components/schemas/RegisteredPartner"
                      changed:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: >-
            `status` was absent or not one of the two values the CHECK
            constraint allows (VALIDATION_FAILED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/exceptions/{id}/approve:
    post:
      operationId: approveSoDConflictException
      tags:
        - Identity & Access
      summary: Approve a pending SoD conflict exception (high-risk, audited, idempotent).
      description: >-
        Approves a pending exception (the sanctioned administrative override).
        Self-approval is denied on BOTH independence axes, re-checked from the
        DB row: the approver may be neither the requester nor the subject.


        TWO permissions are checked. Every approval needs the dedicated
        `identity_access.business_scope_exceptions.approve`. A rule may also
        name its own checker in
        `SoDRuleDescriptor.exceptionPolicy.requiresApprovalPermission`, and
        when that differs from the dedicated key the caller must hold it too
        (403 `ACCESS_DENIED`). An exception whose rule no installed module
        declares any more cannot be approved at all (403 `SOD_RULE_UNKNOWN`);
        rejecting and revoking it stay available.


        Requires `Idempotency-Key`; audited at `critical` severity.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                decisionReason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The approved SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The exception is not pending, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/exceptions/{id}/reject:
    post:
      operationId: rejectSoDConflictException
      tags:
        - Identity & Access
      summary: Reject a pending SoD conflict exception (audited, idempotent).
      description: >-
        Rejects a pending exception — the safe outcome (the conflict stays
        denied). Gated on `identity_access.business_scope_exceptions.reject`.
        Requires `Idempotency-Key`; audited at `warning` severity.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                decisionReason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The rejected SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The exception is not pending, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/exceptions/{id}/revoke:
    post:
      operationId: revokeSoDConflictException
      tags:
        - Identity & Access
      summary: Revoke an approved SoD conflict exception (high-risk, audited, idempotent).
      description: >-
        Revokes a previously approved exception, ending the override early
        (immediately ineffective at the next decision). Reason required. Gated
        on `identity_access.business_scope_exceptions.revoke`. Requires
        `Idempotency-Key`; audited at `critical` severity.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - revokeReason
              properties:
                revokeReason:
                  type: string
      responses:
        "200":
          description: The revoked SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The exception is not approved, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/roles:
    get:
      operationId: listRoles
      tags:
        - Identity & Access
      summary: List the current tenant's (non-deleted) roles with a permission count.
      responses:
        "200":
          description: The tenant's roles (limit 100), each with a permission count.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createRole
      tags:
        - Identity & Access
      summary: Create a custom role for the current tenant (audited; requires access_control.configure).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - roleCode
                - roleName
              properties:
                roleCode:
                  type: string
                  description: "Slug: 2–64 chars, lowercase letters, digits, '_' or '-'. Unique per tenant among live roles."
                roleName:
                  type: string
      responses:
        "201":
          description: Role created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: roleCode is already taken by a live role in this tenant (ROLE_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/roles/{id}:
    patch:
      operationId: updateRole
      tags:
        - Identity & Access
      summary: Rename a role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - roleName
              properties:
                roleName:
                  type: string
      responses:
        "200":
          description: Role updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteRole
      tags:
        - Identity & Access
      summary: Soft-delete a role (audited; requires access_control.configure). System roles are rejected with 409.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  description: Optional free-text reason echoed into the soft-delete audit event.
      responses:
        "200":
          description: Role soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The role is a system role and cannot be deleted (ROLE_SYSTEM_PROTECTED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/roles/{id}/permissions:
    post:
      operationId: grantRolePermission
      tags:
        - Identity & Access
      summary: Grant a catalogued permission to a role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - permissionId
              properties:
                permissionId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Permission granted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      roleId:
                        type: string
                        format: uuid
                      permissionId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The permission is already granted to this role (ROLE_PERMISSION_ALREADY_GRANTED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: revokeRolePermission
      tags:
        - Identity & Access
      summary: Revoke a permission from a role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - permissionId
              properties:
                permissionId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Permission revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      roleId:
                        type: string
                        format: uuid
                      permissionId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/roles/{id}/restore:
    post:
      operationId: restoreRole
      tags:
        - Identity & Access
      summary: Restore a soft-deleted role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Role restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The role's code was re-used by a live role while it was deleted (ROLE_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/users:
    get:
      operationId: listTenantUsers
      tags:
        - Identity & Access
      summary: List the current tenant's users with their assigned role codes (login identifiers masked).
      responses:
        "200":
          description: The tenant's users (limit 100), with masked login identifiers and assigned role codes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/TenantUserMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/users/{id}:
    patch:
      operationId: setTenantUserStatus
      tags:
        - Identity & Access
      summary: Activate or deactivate a tenant user (high-risk, audited).
      description: Sets the tenant user's status. `awcms_tenant_users` has no `deleted_at`, so deactivate = status `inactive`, reactivate = status `active`. Gated on `identity_access.access_control.configure`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - active
                    - inactive
      responses:
        "200":
          description: The tenant user's new status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - active
                          - inactive
                      updatedAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/users/{id}/sessions:
    get:
      operationId: listTenantUserSessions
      tags:
        - Identity & Access
      summary: List one tenant user's live sessions (identity_access.user_sessions.read).
      description: >-
        Where is this person signed in? Gated on
        `identity_access.user_sessions.read` — the SENSITIVE half of the pair,
        because it is a standing window into a colleague's movements. The revoke
        endpoint beside it destroys access instead of disclosing anything, which
        is why the two are separate permissions and why an incident responder can
        hold the second without the first.

        Returns no token, no token hash, no raw IP and no raw User-Agent.
        `isCallerSession` is true only when an administrator points this at their
        own tenant user. A malformed id, an id from another tenant and an unknown
        id all answer 404 — distinguishing them would make this a probe for which
        tenant user ids exist.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant user's live sessions, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      sessions:
                        type: array
                        items:
                          $ref: "#/components/schemas/TenantUserSessionSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/users/{id}/sessions/revoke-all:
    post:
      operationId: revokeAllTenantUserSessions
      tags:
        - Identity & Access
      summary: End every live session of one tenant user (identity_access.user_sessions.revoke, audited).
      description: >-
        The incident control. Effective on the next request of every revoked
        session, because authentication reads the same row. Grantable WITHOUT
        `user_sessions.read`: stopping a suspected-compromised account should not
        cost a standing window into where everyone is signed in.

        The CALLER's own session is never revoked — the exclusion matches nothing
        for any target other than the caller's own tenant user, and stops an
        administrator from signing themselves out of the console mid-incident by
        clicking their own row. `keptCallerSession` reports when that happened;
        signing yourself out everywhere is the unpermissioned self-service
        surface under `/api/v1/auth/sessions`.

        No `Idempotency-Key`: the second call finds nothing live and reports
        `revokedCount: 0`, so there is no duplicate to suppress. Audited even
        when it revokes nothing.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: How many sessions were ended.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      revokedCount:
                        type: integer
                        minimum: 0
                      keptCallerSession:
                        type: boolean
                        description: True when the target was the caller's own tenant user, so their current session was left alive.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/auth/session-handoff/issue:
    post:
      operationId: issueSessionHandoffCode
      tags:
        - Identity & Access
      summary: Mint a one-time code letting a registered BFF obtain a session as the caller.
      description: |
        ADR-0050. The authenticated human asks for a short-lived (≤60 second), single-use code; a registered BFF then spends it server-to-server at `/api/v1/auth/session-handoff/redeem` and receives a session token for that person.

        **Self-service, not permission-gated.** The identity and assurance level come from the presented session, never from the body, so the caller can only ever mint a code for themselves. There is no permission that answers "may I hand my own session to a client I am already logged into", and inventing one would deny everyone including the tenant owner. Same reasoning as `GET /api/v1/auth/session`.

        What constrains it is the CLIENT registry: a code is only issued for a registered, enabled client, bound to a `redirectUri` on that client's **exact-match** allow-list. Prefix and origin matches are refused — `https://app.example.com` prefix-matches `https://app.example.com.evil.test`, and an attacker who can choose the path on a permitted origin can forward the code onward.

        Every rejection — unknown client, non-allow-listed URI, a URI carrying a query or fragment, a non-https URI — answers the same `400 HANDOFF_NOT_ALLOWED`. Distinguishing them turns this into a probe for which clients are registered and which URIs they accept.

        **No `Idempotency-Key`**, deliberately: each call mints a fresh single-use credential, and replaying one would mean two live codes for one request.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IssueSessionHandoffRequest"
      responses:
        "200":
          description: A one-time code, and the redirect_uri it is bound to.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/SessionHandoffCodeIssued"
        "400":
          description: The client and redirect_uri combination cannot be used (`HANDOFF_NOT_ALLOWED`), or the body is missing a field.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/session-handoff/redeem:
    post:
      operationId: redeemSessionHandoffCode
      tags:
        - Identity & Access
      summary: Spend a one-time handoff code and receive a session token (server-to-server).
      description: |
        ADR-0050. The only endpoint here authenticated by a **client secret** rather than a session: this is the request that obtains a session, so there is none to present yet. Not a machine credential either. ADR-0092 lets one write inside a narrow ceiling (`create`/`update`, never a high-risk action), and minting a human session is precisely the authority that ceiling exists to keep away from a non-human bearer.

        Call it from the BFF's SERVER. The token must never reach a browser: the BFF stores it server-side and gives the browser its own portal cookie.

        The code is spent exactly once, claimed with a guarded `UPDATE … WHERE redeemed_at IS NULL` so two concurrent redemptions cannot both succeed. The spent row is kept rather than deleted, so a replay is answered from evidence instead of from the absence of it.

        **Every failure is one answer.** Unknown code, expired code, already-spent code, wrong client, wrong `redirectUri`, bad secret — all `401 HANDOFF_REJECTED`, including a malformed body. The distinctions are recorded in the audit trail; handing them to the caller tells whoever holds a stolen code whether it was ever valid.

        The minted session inherits the assurance level the original login REACHED and never more: an `aal1` login cannot be laundered into an `aal2` session.

        **No `Idempotency-Key`**: the code IS the idempotency key, with the opposite contract — the second attempt must fail rather than replay the first response.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedeemSessionHandoffRequest"
      responses:
        "200":
          description: A session token for the human who authenticated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/SessionHandoffRedeemed"
        "401":
          description: The handoff could not be completed (`HANDOFF_REJECTED`) — one answer for every cause, deliberately.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sessions:
    get:
      operationId: listOwnAuthSessions
      tags:
        - Identity & Access
      summary: List the caller's own live sessions (self-service, no permission).
      description: >-
        Where am I signed in? Self-service by construction — the subject is the
        session bearer and the route accepts no tenantUserId, so there is nobody
        else it could be pointed at. Deliberately UNPERMISSIONED, like
        GET /api/v1/auth/session: inventing a permission for "see your own
        sessions" would wall off the feature and install a latent-authz trap
        (an action nothing seeds denies everyone, including the tenant owner).
        Returns no token, no raw IP and no raw User-Agent. clientIpHash is a
        keyed pseudonym and is null on a deployment without AUTH_IP_HASH_SECRET,
        because the fallback key is per-process and a stored hash would stop
        being comparable after a restart.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The caller's live sessions, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      sessions:
                        type: array
                        items:
                          $ref: "#/components/schemas/OwnSessionSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many session requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/profile:
    get:
      operationId: getOwnProfile
      tags:
        - Identity & Access
      summary: The caller's own display name and sign-in address (self-service).
      description: >-
        ADR-0096. Self-service by construction — it accepts no id, so the profile
        it reads is the one behind the calling session and there is nobody else it
        could be pointed at. Unpermissioned for the reason its siblings state:
        inventing a permission for "read your own name" would install the
        latent-authz trap (an action nothing seeds denies everyone, tenant owner
        included).

        `loginIdentifier` is returned unmasked. It is the caller's OWN address —
        the one they type to sign in — so masking it would disclose nothing and
        would make "am I signed in as the right account" unanswerable.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The caller's own profile.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      profileId:
                        type: string
                        format: uuid
                      displayName:
                        type: string
                      loginIdentifier:
                        type: string
                      publicBylineName:
                        description: >-
                          ADR-0109 — the name this person's articles are
                          published under, or `null` for "no byline" (the
                          article keeps the organisation-level attribution).
                        type: string
                        nullable: true
                        maxLength: 120
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: updateOwnProfile
      tags:
        - Identity & Access
      summary: Change the caller's own display name (self-service, no permission).
      description: >-
        ADR-0096. Writes ONLY `display_name`, and only on the profile behind the
        calling session — the profile id is never accepted from the caller, so
        there is no id to tamper with and no ownership check that could be
        forgotten.

        Deliberately cannot change `legal_name` (a verification-relevant field:
        `verification_status` exists because a legal name is asserted and then
        checked, so letting the subject rewrite it would make the verification
        meaningless), `status`, `verification_status`, `risk_level`, or the
        identifiers. Those are administrative fields that happen to live on the
        same row, and a self-service endpoint that wrote them would be a privilege
        escalation wearing a profile editor's clothes.

        Changing the sign-in address is NOT here: that is account recovery and
        needs proof the new address is the caller's.

        This is a SEPARATE route from the permissioned `PATCH /api/v1/profiles/{id}`
        rather than a relaxed guard on it — an endpoint with two authorization
        modes forces every reader to prove which branch applies before they can
        say anything about its security.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [displayName]
              properties:
                displayName:
                  type: string
                  minLength: 1
                  maxLength: 200
                publicBylineName:
                  description: >-
                    ADR-0109 — OPTIONAL, and the three states are distinct:
                    ABSENT leaves the byline unchanged, `null` (or an empty
                    string) clears it, a string sets it. Absence cannot mean
                    "clear" — a PATCH that saved only a display name would
                    delete the byline every time.

                    There is no administrative sibling for this field on
                    purpose: an editor who could set somebody else's byline
                    could publish an article under a colleague's name.
                  type: string
                  nullable: true
                  maxLength: 120
      responses:
        "200":
          description: The stored display name.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      profileId:
                        type: string
                        format: uuid
                      displayName:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many profile updates from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/preferences:
    get:
      operationId: getOwnDisplayPreferences
      tags:
        - Identity & Access
      summary: The caller's own UI locale, colour theme and time zone (self-service, no permission).
      description: >-
        ADR-0095. Self-service by construction — the subject is the session
        bearer and the route accepts no tenantUserId, so there is nobody else it
        could be pointed at. Deliberately UNPERMISSIONED, like
        GET /api/v1/auth/sessions: inventing a permission for "read your own
        language" would wall off the feature and install the latent-authz trap
        (an action nothing seeds denies everyone, tenant owner included).

        All three fields are nullable, and null means "not chosen" rather than a
        default: a null locale falls through to awcms_tenants.default_locale and
        then to Accept-Language, so a reader who never opened the switcher still
        gets the language their browser asked for, and a null timeZone renders
        UTC. `storable` is false when the identity has no linked principal
        (sql/112 leaves that legal), which is the one case where a choice cannot
        be made durable.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The caller's stored display preferences.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      locale:
                        type: string
                        nullable: true
                        enum: ["en", "id", null]
                        description: Stored UI locale, or null when not chosen.
                      theme:
                        type: string
                        nullable: true
                        enum: ["system", "light", "dark", null]
                        description: Stored colour theme, or null when not chosen.
                      timeZone:
                        type: string
                        nullable: true
                        description: >-
                          Stored IANA time zone, or null when not chosen (which
                          renders UTC). Not enumerated: the zone list is
                          tzdata's, ships with the runtime and changes several
                          times a year, so an enum here would start refusing
                          valid zones within months. sql/130 constrains only the
                          shape for the same reason; Intl is the authority on
                          whether a zone can be rendered.
                      storable:
                        type: boolean
                        description: >-
                          False when this identity has no linked principal, so a
                          choice can only be cookied for this browser.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: updateOwnDisplayPreferences
      tags:
        - Identity & Access
      summary: Store the caller's UI locale / colour theme (self-service, no permission).
      description: >-
        ADR-0095. Writes the durable copy against the caller's PRINCIPAL — the
        global person behind every tenant membership — and additionally sets the
        awcms_locale cookie, which outranks the stored value so the change is
        visible on the next render rather than the next login.

        A field that is ABSENT is left alone; a field explicitly null is RESET to
        "not chosen". The two cannot collapse: sql/128 withholds DELETE so a reset
        must be expressible as null, while a request mentioning only `locale` must
        not thereby wipe `theme`. Read and write happen in one transaction, so the
        unmentioned axis is carried forward rather than lost to an interleaving.

        Accepts JSON or application/x-www-form-urlencoded; the form variant answers
        303 back to `return_to`, which is validated as a path-absolute same-origin
        reference and otherwise replaced with /admin.

        Unpermissioned for the same reason as GET above. Changing somebody ELSE's
        language is not a feature, so there is no permissioned sibling.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                locale:
                  type: string
                  nullable: true
                  enum: ["en", "id", null]
                theme:
                  type: string
                  nullable: true
                  enum: ["system", "light", "dark", null]
                timeZone:
                  type: string
                  nullable: true
                  description: >-
                    IANA zone this deployment can render, or null to reset.
                    Rejected with UNSUPPORTED_TIME_ZONE when Intl cannot
                    resolve it.
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                locale:
                  type: string
                  enum: ["en", "id"]
                return_to:
                  type: string
                  description: Path-absolute same-origin path to return to.
      responses:
        "200":
          description: The preferences now in effect.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      locale:
                        type: string
                        nullable: true
                        enum: ["en", "id", null]
                      theme:
                        type: string
                        nullable: true
                        enum: ["system", "light", "dark", null]
                      persisted:
                        type: boolean
                        description: >-
                          False when the identity has no linked principal — the
                          cookie was still set.
        "303":
          description: >-
            Form submission accepted; redirect back to the validated return path.
          headers:
            Location:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many preference updates from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/preferences/locale:
    post:
      operationId: setDisplayLocaleCookie
      tags:
        - Identity & Access
      summary: Switch UI language for this browser only — works UNAUTHENTICATED.
      description: >-
        ADR-0095. Sets the awcms_locale cookie and touches no database at all.

        It exists separately from POST /api/v1/auth/preferences because the
        language switch has to work on /login and on the tenant-selection screen
        (ADR-0088) — the two places a reader most often discovers they are being
        shown the wrong language, and the two places where no session exists to
        hang a preference on. The self-service route requires a bearer and answers
        401 without one, so it cannot serve that caller; this one can, and pays for
        it by never persisting (`persisted` is always false).

        Accepts JSON or application/x-www-form-urlencoded; the form variant answers
        303 back to `return_to`, validated as a path-absolute same-origin reference
        and otherwise replaced with /admin.

        No CSRF token, deliberately: the whole effect is which of two message
        catalogs a reader is shown — cosmetic, immediately visible, reversible in
        one click — and the cookie is not HttpOnly, so same-origin script could set
        it anyway. There is no server state to corrupt.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [locale]
              properties:
                locale:
                  type: string
                  enum: ["en", "id"]
          application/x-www-form-urlencoded:
            schema:
              type: object
              required: [locale]
              properties:
                locale:
                  type: string
                  enum: ["en", "id"]
                return_to:
                  type: string
      responses:
        "200":
          description: The cookie was set for this browser.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      locale:
                        type: string
                        enum: ["en", "id"]
                      persisted:
                        type: boolean
                        enum: [false]
                        description: >-
                          Always false — this route has no database access. Stated
                          rather than omitted so a client comparing the two routes
                          sees the difference in the payload.
        "303":
          description: >-
            Form submission accepted; redirect back to the validated return path.
          headers:
            Location:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/auth/sessions/revoke-all:
    post:
      operationId: revokeOtherOwnAuthSessions
      tags:
        - Identity & Access
      summary: Sign the caller out of every OTHER session (self-service, no permission).
      description: >-
        Ends every live session of the caller's identity except the one making
        the request. Self-service by construction — the subject is the session
        bearer and the route accepts no parameter with which to name anybody
        else — and unpermissioned for the same reason as the two endpoints beside
        it: this is what a person reaches for after "I think my password leaked",
        which is exactly when a permission wall is most expensive.

        There is deliberately no `exceptCurrent` flag. The only other value would
        also end the requesting session, which is `POST /api/v1/auth/logout` —
        that endpoint additionally clears the cookies this one cannot see, so the
        flag would ship a second, worse logout whose distinguishing feature is
        leaving the caller holding a dead cookie.

        It changes no credential and clears no lockout counter: ending stray
        sessions proves nothing new about the password, and folding the two
        together would make session hygiene a lockout-reset oracle. Not audited —
        `awcms_audit_events` records what administrators do to OTHER people; the
        paired admin endpoint under /api/v1/users/{id}/sessions/revoke-all writes
        that entry.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: How many other sessions were ended. Zero means there were none.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      revokedCount:
                        type: integer
                        minimum: 0
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many revocation requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sessions/{id}:
    delete:
      operationId: revokeOwnAuthSession
      tags:
        - Identity & Access
      summary: End one of the caller's own sessions (self-service, no permission).
      description: >-
        Ownership is enforced in the UPDATE's WHERE clause, never by a preceding
        read. Unknown id, another person's session, another tenant's session and
        one already revoked or expired all answer 404 — distinguishing them
        would make this an existence oracle for session ids, and the caller
        could do nothing differently with the distinction. Revoking the CURRENT
        session answers 409: that is POST /api/v1/auth/logout's job, which also
        clears the cookies.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The session was revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      revoked:
                        type: boolean
                        enum: [true]
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The id names the session making this request (SESSION_IS_CURRENT) — use POST /api/v1/auth/logout.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many revocation requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/session:
    get:
      operationId: introspectAuthSession
      tags:
        - Identity & Access
      summary: Introspect the calling session — safe claims only (for a cross-origin BFF).
      description: |
        Cross-origin session introspection (ADR-0045 §3, ADR-0049 §7). Intended for `awcms-astro`'s BFF, which holds the session token server-side; a browser never calls it directly.

        Self-service: authorized by holding the session, not by a permission. Returns ONLY claims a portal header needs — never a token, token hash, password state, MFA secret/recovery code, or a raw email/phone identifier.

        Anti-oracle: a missing bearer, an unknown/expired/revoked session, a deactivated identity, and a machine credential presented here all produce the SAME 401. Rate-limited per source; `Cache-Control: private, no-store` on every path.
      responses:
        "200":
          description: Safe claims for the live session.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SessionIntrospection"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many introspection requests from this source (RATE_LIMITED); `Retry-After` carries the window.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso/{providerKey}/start:
    get:
      operationId: getAuthSsoStart
      tags:
        - Identity & Access
      summary: Begin an OIDC SSO login — 302-redirects to the tenant provider's authorization endpoint (Auth Code + PKCE + state + nonce).
      security: []
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
        - name: tenantId
          in: query
          required: false
          description: Tenant id fallback when no tenant header/cookie is present (a fresh browser navigation).
          schema:
            type: string
            format: uuid
        - name: returnTo
          in: query
          required: false
          description: Same-origin relative path to return to after login (sanitized server-side; open-redirect targets are ignored).
          schema:
            type: string
      responses:
        "302":
          description: Redirect to the provider's authorization endpoint.
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          description: Too many requests from this source (RATE_LIMITED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: The provider could not be reached (SSO_PROVIDER_UNAVAILABLE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso/{providerKey}/callback:
    get:
      operationId: getAuthSsoCallback
      tags:
        - Identity & Access
      summary: OIDC provider redirect target — validates state/nonce/PKCE/ID-token, then mints an opaque awcms session (or a 401 MFA_REQUIRED).
      security: []
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
        - name: state
          in: query
          required: false
          schema:
            type: string
        - name: code
          in: query
          required: false
          schema:
            type: string
        - name: error
          in: query
          required: false
          schema:
            type: string
      responses:
        "302":
          description: Login (or link) succeeded — redirect to the return path.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The provider account is already linked (SSO_ALREADY_LINKED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: The provider could not be reached (SSO_PROVIDER_UNAVAILABLE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso/{providerKey}/link:
    post:
      operationId: postAuthSsoLink
      tags:
        - Identity & Access
      summary: Explicitly link an OIDC provider account to the caller's identity (authenticated + step-up required; never auto-links by email).
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The provider authorization URL to complete the link.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      authorizationUrl:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          description: The provider could not be reached (SSO_PROVIDER_UNAVAILABLE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso/{providerKey}/unlink:
    post:
      operationId: postAuthSsoUnlink
      tags:
        - Identity & Access
      summary: Unlink the caller's OIDC provider account (authenticated + step-up required; audit-logged).
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The provider account was unlinked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      unlinked:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: No provider account is currently linked (SSO_NOT_LINKED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso-providers:
    get:
      operationId: listSsoProviders
      tags:
        - Identity & Access
      summary: List the tenant's configured OIDC SSO providers (client secrets are never returned).
      responses:
        "200":
          description: The tenant's SSO providers.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      providers:
                        type: array
                        items:
                          $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createSsoProvider
      tags:
        - Identity & Access
      summary: Add a tenant OIDC SSO provider (high-risk; audit-logged). Exactly one of clientSecret / clientSecretEnvVar.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - providerKey
                - displayName
                - issuerUrl
                - clientId
              properties:
                providerKey:
                  type: string
                  description: Stable slug (^[a-z0-9][a-z0-9_-]*$), used in the /sso/{providerKey}/... path.
                displayName:
                  type: string
                issuerUrl:
                  type: string
                  description: HTTPS issuer/discovery base URL.
                clientId:
                  type: string
                clientSecret:
                  type: string
                  nullable: true
                  description: Plaintext client secret (encrypted at rest; never returned). Provide this OR clientSecretEnvVar.
                clientSecretEnvVar:
                  type: string
                  nullable: true
                  description: Name of an env var holding the secret (resolved at token-exchange time; never persisted).
                scopes:
                  type: string
                allowedEmailDomains:
                  type: array
                  items:
                    type: string
                enabled:
                  type: boolean
      responses:
        "200":
          description: The created provider.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: providerKey conflict or per-tenant provider limit reached (SSO_PROVIDER_KEY_CONFLICT / SSO_PROVIDER_LIMIT_EXCEEDED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: The credential encryption key is not configured (SSO_MISCONFIGURED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso-providers/{id}:
    get:
      operationId: getSsoProvider
      tags:
        - Identity & Access
      summary: Read one tenant OIDC SSO provider (client secret never returned).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The provider.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateSsoProvider
      tags:
        - Identity & Access
      summary: Update a tenant OIDC SSO provider (partial; high-risk; audit-logged).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                displayName:
                  type: string
                issuerUrl:
                  type: string
                clientId:
                  type: string
                clientSecret:
                  type: string
                  nullable: true
                clientSecretEnvVar:
                  type: string
                  nullable: true
                scopes:
                  type: string
                allowedEmailDomains:
                  type: array
                  items:
                    type: string
                enabled:
                  type: boolean
      responses:
        "200":
          description: The updated provider.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          description: The credential encryption key is not configured (SSO_MISCONFIGURED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deleteSsoProvider
      tags:
        - Identity & Access
      summary: Soft delete a tenant OIDC SSO provider (reason required; high-risk; audit-logged).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: The provider was soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/auth/sso-policy:
    get:
      operationId: getSsoPolicy
      tags:
        - Identity & Access
      summary: Read the tenant authentication policy (password/SSO/JIT/break-glass).
      responses:
        "200":
          description: The tenant authentication policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantAuthPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      operationId: updateSsoPolicy
      tags:
        - Identity & Access
      summary: Update the tenant authentication policy (partial upsert; high-risk; audit-logged; break-glass enforced server-side).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                passwordLoginEnabled:
                  type: boolean
                ssoEnabled:
                  type: boolean
                ssoRequired:
                  type: boolean
                autoLinkVerifiedEmail:
                  type: boolean
                jitProvisioningEnabled:
                  type: boolean
                allowedEmailDomains:
                  type: array
                  items:
                    type: string
                breakGlassIdentityIds:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: The updated policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantAuthPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: sso_required/password_login_disabled without an eligible break-glass owner (BREAK_GLASS_REQUIRED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/user-groups:
    get:
      operationId: listUserGroups
      tags:
        - Identity & Access
      summary: List the tenant's user groups (identity_access.user_groups.read).
      description: >-
        A group is a SUBJECT that can hold role grants, so this list carries a
        `roleCount` beside `memberCount`: a group holding grants is an authority
        no amount of looking at PEOPLE would reveal.

        `source` is `local` or `scim`. A `scim` group is managed by an external
        directory and refuses local mutation with 409
        GROUP_EXTERNALLY_MANAGED — SCIM itself is not implemented, only the
        refusal, because a local edit a later sync silently reverts is worse
        than one that was never accepted.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant's live user groups, ordered by code.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      groups:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserGroupSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createUserGroup
      tags:
        - Identity & Access
      summary: Create a local user group (identity_access.user_groups.create, audited).
      description: >-
        Creates a group with no members and no grants. `source` is never accepted
        from the request: a caller who could declare a group `scim` would be
        declaring it un-editable through the only surface that exists, with no
        directory behind it to edit it instead.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - groupCode
                - groupName
              properties:
                groupCode:
                  type: string
                  pattern: "^[a-z][a-z0-9_-]{1,63}$"
                groupName:
                  type: string
                  maxLength: 200
                description:
                  type: string
                  nullable: true
                  maxLength: 1000
      responses:
        "201":
          description: The created group.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/UserGroupRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A live group already holds this code (GROUP_CODE_TAKEN).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/user-groups/{id}:
    get:
      operationId: listUserGroupMembers
      tags:
        - Identity & Access
      summary: List one group's members (identity_access.user_groups.read).
      description: >-
        Tenant-user ids only. The login identifier is PII and the caller who may
        read this already has the user list to resolve them against.

        An unknown group and an empty group answer identically — the alternative
        needs a pre-read that says whether an id exists in this tenant, which is
        the existence oracle every other read here refuses to be.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The group's members.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      members:
                        type: array
                        items:
                          type: object
                          properties:
                            tenantUserId:
                              type: string
                              format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      operationId: updateUserGroup
      tags:
        - Identity & Access
      summary: Rename a local user group (identity_access.user_groups.update, audited).
      description: >-
        Changes the DISPLAY name and description. `groupCode` is not editable: it
        is the tenant-facing identity of the group and the thing a tenant's own
        runbooks name, so renaming it silently would make those wrong.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - groupName
              properties:
                groupName:
                  type: string
                  maxLength: 200
                description:
                  type: string
                  nullable: true
                  maxLength: 1000
      responses:
        "200":
          description: The updated group.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/UserGroupRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The group is directory-managed (GROUP_EXTERNALLY_MANAGED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/user-groups/members:
    post:
      operationId: addUserGroupMember
      tags:
        - Identity & Access
      summary: Add a tenant user to a group (identity_access.user_groups.assign, audited).
      description: >-
        A grant in everything but name: membership confers every role the group
        holds, at once and with no further step. `assign` is a HIGH-RISK action,
        so this additionally passes the segregation-of-duties chokepoint.

        Idempotent by intent: re-adding somebody already in the group answers 200
        with `added: false`. Adding a person to a group they are already in
        changes nothing about their access, and a 409 there would be an error
        message about the state the caller asked for.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserGroupMembershipInput"
      responses:
        "200":
          description: Membership after the call.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      added:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The group is directory-managed (GROUP_EXTERNALLY_MANAGED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: removeUserGroupMember
      tags:
        - Identity & Access
      summary: Remove a tenant user from a group (identity_access.user_groups.assign, audited).
      description: >-
        Takes away every role the group confers on that person, effective on
        their next authorization decision — there is no cache between membership
        and the grant read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserGroupMembershipInput"
      responses:
        "200":
          description: The membership was removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      removed:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The group is directory-managed (GROUP_EXTERNALLY_MANAGED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/invitations:
    get:
      operationId: listInvitations
      tags:
        - Identity & Access
      summary: List this tenant's invitations (identity_access.invitations.read).
      description: >-
        Keyset-paginated, newest first. The invitee's address is MASKED: these
        rows are addresses of people who are not users here — often people who
        never will be — so the unmasked value has no reason to leave the
        database.

        `roleCodes` is what the invitation carries, and it becomes a real grant
        only on acceptance. Nothing in this list confers access.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [pending, accepted, revoked, expired]
        - name: cursor
          in: query
          required: false
          description: An opaque cursor from a previous response's `nextCursor`. A cursor that does not decode is a 400, never a silent restart.
          schema:
            type: string
      responses:
        "200":
          description: A page of invitations, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      invitations:
                        type: array
                        items:
                          $ref: "#/components/schemas/InvitationSummary"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createInvitation
      tags:
        - Identity & Access
      summary: Invite a person to this tenant (identity_access.invitations.create, audited).
      description: >-
        Issues an offer of membership and mails its link. No account exists until
        the link is used.

        Inviting and GRANTING A ROLE are two authorities: a body naming
        `roleIds` additionally requires `identity_access.access_control.assign`,
        so an administrator holding only `invitations.create` can admit a person
        and nothing more. A role marked `is_system` is refused
        (409 ROLE_SYSTEM_PROTECTED), and unknown role ids refuse the whole
        invitation rather than granting a subset.

        `skipEmailConfirmation` requires the PLATFORM-scoped
        `identity_access.invitations.configure`, unless the address already holds
        an active identity in this tenant — which is the same proof, already
        given. It removes the only evidence that the recipient controls that
        mailbox.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInvitationInput"
      responses:
        "201":
          description: The invitation was issued. `delivery` is `unavailable` when the tenant has no active `auth.invitation` template or the address is suppressed — the invitation still exists and can be resent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/InvitationCreated"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: >-
            The address already has an account (IDENTIFIER_TAKEN) or a pending
            invitation (INVITATION_ALREADY_PENDING); a named role is a system
            role (ROLE_SYSTEM_PROTECTED); or the Idempotency-Key was already used
            with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/invitations/{id}/revoke:
    post:
      operationId: revokeInvitation
      tags:
        - Identity & Access
      summary: Revoke a pending invitation (identity_access.invitations.revoke, audited).
      description: >-
        Kills the link; keeps the row. The surviving row is what answers "who
        offered what, to whom, and what happened" — which is why this activity
        has no `delete`.

        An invitation that is not pending and one that does not exist answer
        identically, so the response cannot be used to enumerate ids or to learn
        that a given invitation was already accepted.

        No Idempotency-Key: the UPDATE carries `AND status = 'pending'`, so a
        double submit revokes once and the second call answers 404.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  nullable: true
                  maxLength: 500
      responses:
        "200":
          description: The invitation is revoked and its link is dead.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum: [revoked]
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/invitations/{id}/resend:
    post:
      operationId: resendInvitation
      tags:
        - Identity & Access
      summary: Resend an invitation, rotating its token (identity_access.invitations.create, audited).
      description: >-
        Guarded by `create`, not by an action of its own: resend MINTS A NEW
        TOKEN, which is exactly the authority `create` already names.

        The previous link stops working immediately. Without rotation, "resend"
        would grow N live links from one invitation, and revoking it would mean
        revoking N secrets nobody counted.

        Capped at five resends per invitation by a database CHECK; past that,
        revoke and issue a new one. No Idempotency-Key — replaying would have to
        return a token that has already been rotated away, or persist a plaintext
        token in the idempotency store.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: A fresh link was issued and the previous one is dead.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      resendCount:
                        type: integer
                      delivery:
                        type: string
                        enum: [queued, unavailable]
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The resend ceiling has been reached (INVITATION_RESEND_LIMIT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/invitations/{token}:
    get:
      operationId: getAuthInvitationPreview
      tags:
        - Identity & Access
      summary: Preview an invitation (public, token-bearing).
      security: []
      description: >-
        Returns the tenant's name, the inviter's name, and the expiry — and
        **never the invited address**. Whoever legitimately holds this link read
        that address in their own mailbox; whoever holds a stolen one did not.

        Unknown, revoked, already-accepted, expired and belonging-to-another-tenant
        all answer one identical `404`. **404, not 410**: `410 Gone` would tell a
        token holder that the token was once valid, which is an oracle worth
        having if you are working through addresses you scraped. The real reason
        is written to the tenant's audit trail, never to the response.

        Rate-limited through the shared limiter's source ceiling before any
        database work.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The invitation is live and may be accepted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/InvitationPreview"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          description: Rate limited (RATE_LIMITED). Carries `retry-after`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/invitations/{token}/accept:
    post:
      operationId: postAuthInvitationAccept
      tags:
        - Identity & Access
      summary: Accept an invitation, creating the membership (public, token-bearing).
      security: []
      description: >-
        Creates the profile, identity and tenant user, and materializes the roles
        the invitation carried. Those roles are read from
        `awcms_invitation_policies` — **never** from this request body, which
        declares no privilege field at all. A role that has since become
        `is_system`, been soft-deleted, or left the catalogue refuses the whole
        acceptance rather than granting a subset.

        **No session is issued.** The invitee signs in at `/login` afterwards, so
        the tenant's MFA policy, its SSO-only policy, and the login rate limit
        all still stand between the new account and a signed-in browser.

        Every refusal — unknown, revoked, already accepted, expired, wrong
        tenant, or an address that acquired an account in the meantime — answers
        the same `404`.

        Turnstile-gated with its own action, so a token solved on any other form
        cannot be spent here.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - password
              properties:
                password:
                  type: string
                  minLength: 8
                displayName:
                  type: string
                  maxLength: 120
                  description: Lets the invitee correct the name the inviter typed for them. Omitted, the inviter's version stands.
                turnstileToken:
                  type: string
      responses:
        "200":
          description: The membership exists. Sign in at /login.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      accepted:
                        type: boolean
                        enum: [true]
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: The request body exceeded the size limit (PAYLOAD_TOO_LARGE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Rate limited (RATE_LIMITED). Carries `retry-after`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/partner-engagements:
    get:
      operationId: listPartnerEngagements
      tags:
        - Identity & Access
      summary: List the partners that reach this tenant.
      description: >-
        ADR-0089. The customer's authoritative view of every partnership into
        their own tenant. The partner's mirror of this
        (`GET /api/v1/partner/tenants`) is a convenience served by a narrow
        SECURITY DEFINER function; this one is the record.
      responses:
        "200":
          description: The partnerships reaching this tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      engagements:
                        type: array
                        items:
                          $ref: "#/components/schemas/PartnerEngagement"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: engagePartner
      tags:
        - Identity & Access
      summary: Engage a partner for this tenant.
      description: >-
        ADR-0089 — the customer initiates, always. There is no partner-facing
        counterpart: a partner that could insert its own engagement would be
        handing itself reach. "Not a registered partner" and "no such tenant"
        answer identically, so the endpoint cannot be swept as a directory of
        the platform's partners.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - partnerTenantId
              properties:
                partnerTenantId:
                  type: string
                  format: uuid
      responses:
        "201":
          description: The partner now reaches this tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      engagement:
                        $ref: "#/components/schemas/PartnerEngagement"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: >-
            No such partner. Deliberately indistinguishable from "no such
            tenant" and from "that tenant is not a registered partner".
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: That partner is already engaged for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/partner-engagements/{id}:
    delete:
      operationId: severPartnerEngagement
      tags:
        - Identity & Access
      summary: Sever a partnership, killing every grant under it.
      description: >-
        ADR-0089/ADR-0090. Every live delegated-access grant under the
        partnership is revoked in the SAME transaction — memberships
        deactivated, sessions killed. The ordering is enforced by a foreign key
        rather than by remembering: deleting the engagement while a grant still
        names it fails.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Severed, with the number of grants revoked with it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      severed:
                        type: boolean
                      partnerTenantId:
                        type: string
                        format: uuid
                      revokedGrants:
                        type: integer
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/delegated-grants:
    get:
      operationId: listDelegatedGrants
      tags:
        - Identity & Access
      summary: List delegated-access grants, live and historical.
      description: >-
        ADR-0090. Revoked and expired grants are listed too: "who could see our
        data last March, and until when" is the question an audit asks, and a
        list of only live grants answers a different one. The redemption code is
        never returned — its column is not even selected.
      responses:
        "200":
          description: Every grant this tenant has approved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      grants:
                        type: array
                        items:
                          $ref: "#/components/schemas/DelegatedAccessGrant"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: approveDelegatedAccess
      tags:
        - Identity & Access
      summary: Approve delegated access for a partner, at a role you choose.
      description: >-
        ADR-0090. Mints a single-use redemption code, returned EXACTLY ONCE in
        this response and never readable again. The role must already exist in
        this tenant and may not be a system role, so `owner` cannot be
        delegated; the grant may last at most 30 days. Guarded by
        `partner_access.assign`, because what this does is hand a role to
        somebody outside the organisation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - partnerTenantId
                - roleId
                - purpose
                - expiresAt
              properties:
                partnerTenantId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
                  description: An existing, non-system role in THIS tenant.
                purpose:
                  type: string
                  maxLength: 500
                  description: >-
                    Required and never defaulted. "Why does this vendor have
                    access" is the first question an audit asks, and a blank
                    answer supplied by the system reads as an answer.
                expiresAt:
                  type: string
                  format: date-time
      responses:
        "201":
          description: >-
            Approved. `accessCode` appears here and nowhere else, ever.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      grantId:
                        type: string
                        format: uuid
                      accessCode:
                        type: string
                        description: Returned once. Hand it to the partner out of band.
                      codeExpiresAt:
                        type: string
                        format: date-time
                      expiresAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No such partner engagement, or no such role in this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/delegated-grants/{id}:
    delete:
      operationId: revokeDelegatedAccess
      tags:
        - Identity & Access
      summary: Revoke delegated access immediately.
      description: >-
        ADR-0090. The grant dies, the membership it printed goes inactive, and
        every session on it is revoked — one transaction, no ordering that can
        leave one of the three behind. A grant that was already revoked answers
        404, which is all the caller is owed.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: reason
          in: query
          required: false
          schema:
            type: string
            maxLength: 500
      responses:
        "200":
          description: Revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      revoked:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/partners:
    get:
      operationId: listRegisteredPartners
      tags:
        - Identity & Access
      summary: "PLATFORM: the partner registry."
      description: >-
        ADR-0089. Lists every partner registered on the deployment, and is
        PLATFORM-scoped for exactly that reason: a tenant-scoped version of this
        read is the cross-tenant directory the same ADR refused as a table.
        Reachable only when the acting tenant IS the platform tenant, and every
        row carries the platform tenant's `tenant_id` so FORCE RLS hides them
        from any other session even if a grant row existed. Both mechanisms are
        load-bearing; neither is a backstop for the other.
      responses:
        "200":
          description: The registry, newest first (limit 200).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/RegisteredPartner"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: registerPartner
      tags:
        - Identity & Access
      summary: "PLATFORM: register an existing tenant as a partner (audit-logged)."
      description: >-
        ADR-0089. Declares who may BE a partner — the platform's half of a split
        this ADR keeps apart from the customer's `partner_access.configure`
        ("which partners reach MY tenant"). No single actor holds both.

        It creates nothing but a row. A partner is an ordinary tenant: the one
        named here must already exist, this does not provision it, and the row
        grants nothing. It is the PRECONDITION a customer's engagement checks
        through a foreign key, and `activeRoleGrants` never reads it.

        `status` is not accepted — `sql/116` pins it to `active` until something
        reads suspension. There is no DELETE: the row is the target of foreign
        keys from engagements and from delegated grants that deliberately
        outlive them, so retirement will be a status change, not a removal.

        Not idempotency-keyed: both natural keys are globally unique, so a
        duplicate submit is a 409 naming which one was taken rather than a
        second row.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterPartnerRequest"
      responses:
        "201":
          description: The registered partner.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      partner:
                        $ref: "#/components/schemas/RegisteredPartner"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No such tenant on this deployment (TENANT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: >-
            The tenant is already a partner, the partnerCode is taken, or the
            platform tenant named itself (CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The registration failed validation (VALIDATION_FAILED); `error.details` lists every offending field.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/partner/tenants:
    get:
      operationId: listManagedTenants
      tags:
        - Identity & Access
      summary: The partner's own view of which tenants have engaged it.
      description: >-
        ADR-0089. Served by a narrow SECURITY DEFINER function (`sql/119`),
        because the engagement rows belong to the TARGET tenant and are
        unreadable from here under FORCE RLS. The partner tenant is taken from
        the CALLER'S CONTEXT, never from the request, so nobody can ask for
        somebody else's book. Returns no `engagedBy` — that is a third party's
        identifier the partner does not need.
      responses:
        "200":
          description: The tenants this partner manages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      managedTenants:
                        type: array
                        items:
                          $ref: "#/components/schemas/ManagedTenant"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/auth/delegated-access/redeem:
    post:
      operationId: redeemDelegatedAccess
      tags:
        - Identity & Access
      summary: Exchange a delegated-access code for a membership in the customer's tenant.
      description: >-
        ADR-0090. Requires a LIVE SESSION in the caller's own tenant: the code
        authenticates nothing, and what proves who is redeeming is the global
        principal behind that session. It returns a MEMBERSHIP, not a session —
        ordinary sign-in or `POST /api/v1/auth/session/switch` works afterwards,
        because afterwards they are a member. Minting a session here would mean
        a second copy of the target tenant's entry policy, and a second copy is
        where the MFA gate goes quietly missing. Every refusal answers 404.
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
          description: The caller's OWN tenant — the one their session belongs to.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - targetTenantId
                - code
              properties:
                targetTenantId:
                  type: string
                  format: uuid
                  description: The customer tenant the grant was approved in.
                code:
                  type: string
                  description: The single-use code the customer handed over.
      responses:
        "200":
          description: The caller is now a delegated member of the target tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      tenantUserId:
                        type: string
                        format: uuid
                      nextStep:
                        type: string
                        enum:
                          - sign_in_to_tenant
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: >-
            One shape for every refusal: unknown code, expired grant, revoked
            grant, wrong tenant, membership refused. A holder must not learn
            whether the code is real.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many attempts from this source. Carries `retry-after`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
components:
  schemas:
    InvitationPreview:
      type: object
      description: Deliberately carries NO email address — see the operation description.
      required:
        - tenantName
        - inviterName
        - expiresAt
      properties:
        tenantName:
          type: string
        inviterName:
          type: string
          description: Empty when the inviter's account has since been deactivated, so the shape stays stable.
        expiresAt:
          type: string
          format: date-time
    InvitationSummary:
      type: object
      required:
        - id
        - loginIdentifierMasked
        - displayName
        - status
        - resendCount
        - expiresAt
        - createdAt
        - roleCodes
      properties:
        id:
          type: string
          format: uuid
        loginIdentifierMasked:
          type: string
          description: The invitee's address, masked. The unmasked value is never returned by any surface.
        displayName:
          type: string
        status:
          type: string
          enum: [pending, accepted, revoked, expired]
        skipEmailConfirmation:
          type: boolean
        resendCount:
          type: integer
        expiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        roleCodes:
          type: array
          items:
            type: string
    InvitationCreated:
      type: object
      required:
        - id
        - delivery
        - roleCodes
      properties:
        id:
          type: string
          format: uuid
        delivery:
          type: string
          enum: [queued, unavailable]
        roleCodes:
          type: array
          items:
            type: string
    CreateInvitationInput:
      type: object
      required:
        - loginIdentifier
        - displayName
      properties:
        loginIdentifier:
          type: string
          maxLength: 320
          description: An email address. Stored trimmed and NEVER lowercased, matching every identifier lookup on the auth path.
        displayName:
          type: string
          maxLength: 120
        roleIds:
          type: array
          maxItems: 20
          items:
            type: string
            format: uuid
          description: Absent or empty admits the person without granting anything. A non-empty list additionally requires identity_access.access_control.assign.
        skipEmailConfirmation:
          type: boolean
          default: false
          description: PLATFORM-scoped unless the address already holds an active identity in this tenant. Only the literal `true` opts in.
    UserGroupSummary:
      type: object
      description: One live user group, with the two counts that make the list decidable.
      required:
        - id
        - groupCode
        - groupName
        - source
        - memberCount
        - roleCount
      properties:
        id:
          type: string
          format: uuid
        groupCode:
          type: string
        groupName:
          type: string
        description:
          type: string
          nullable: true
        source:
          type: string
          enum: [local, scim]
        externalId:
          type: string
          nullable: true
          description: The directory's own identifier. It is the sync key rather than groupCode, because humans rename group codes and a rename must not orphan the group.
        memberCount:
          type: integer
          minimum: 0
        roleCount:
          type: integer
          minimum: 0
          description: Active role grants held BY the group. Everyone in the group holds them too.
    UserGroupRecord:
      type: object
      required:
        - id
        - groupCode
        - groupName
        - source
      properties:
        id:
          type: string
          format: uuid
        groupCode:
          type: string
        groupName:
          type: string
        description:
          type: string
          nullable: true
        source:
          type: string
          enum: [local, scim]
    UserGroupMembershipInput:
      type: object
      required:
        - userGroupId
        - tenantUserId
      properties:
        userGroupId:
          type: string
          format: uuid
        tenantUserId:
          type: string
          format: uuid
    OwnSessionSummary:
      type: object
      description: One live session belonging to the caller. Carries no token, no raw IP and no raw User-Agent.
      required:
        - id
        - issuedAt
        - expiresAt
        - assuranceLevel
        - originAuth
        - current
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        assuranceLevel:
          type: string
          enum: [aal1, aal2]
        originAuth:
          type: string
          enum: [password, sso, handoff]
          description: How the session was issued. A step-up rotation carries the original value forward.
        clientIpHash:
          type: [string, "null"]
          description: Keyed, non-reversible pseudonym of the client IP. null when the deployment has no stable AUTH_IP_HASH_SECRET.
        userAgentSummary:
          type: [string, "null"]
        current:
          type: boolean
          description: True for the session this request is authenticated by.
    TenantUserSessionSummary:
      type: object
      description: One live session of a named tenant user, as seen by an administrator. Same exclusions as OwnSessionSummary — no token, no raw IP, no raw User-Agent.
      required:
        - id
        - issuedAt
        - expiresAt
        - assuranceLevel
        - originAuth
        - isCallerSession
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        assuranceLevel:
          type: string
          enum: [aal1, aal2]
        originAuth:
          type: string
          enum: [password, sso, handoff]
        clientIpHash:
          type: [string, "null"]
          description: Keyed, non-reversible pseudonym of the client IP. null when the deployment has no stable AUTH_IP_HASH_SECRET.
        userAgentSummary:
          type: [string, "null"]
        isCallerSession:
          type: boolean
          description: True only when the endpoint is pointed at the caller's own tenant user AND this row is the session making the request.
    IssueSessionHandoffRequest:
      type: object
      required:
        - clientKey
        - redirectUri
      properties:
        clientKey:
          type: string
          description: The registered BFF client's public key. Not a secret, and never treated as one — the secret authenticates at redeem time.
        redirectUri:
          type: string
          format: uri
          description: Must appear EXACTLY in the client's registered allow-list. https only, no query, no fragment.
    SessionHandoffCodeIssued:
      type: object
      properties:
        code:
          type: string
          description: Single-use, ≤60 second lifetime. Returned once and never stored in clear text.
        redirectUri:
          type: string
          format: uri
          description: The URI the code is bound to. Redemption must present the same value.
        expiresAt:
          type: string
          format: date-time
    RedeemSessionHandoffRequest:
      type: object
      required:
        - clientKey
        - clientSecret
        - code
        - redirectUri
      properties:
        clientKey:
          type: string
        clientSecret:
          type: string
          description: Server-to-server only. A browser must never hold this.
        code:
          type: string
        redirectUri:
          type: string
          format: uri
          description: Must equal the URI the code was issued against.
    SessionHandoffRedeemed:
      type: object
      properties:
        token:
          type: string
          description: A session token for the human who authenticated. Store it SERVER-SIDE; it must never reach a browser.
        expiresAt:
          type: string
          format: date-time
        assuranceLevel:
          type: string
          enum: [aal1, aal2]
          description: Inherited from the original login, never above it.
    MachineCredential:
      type: object
      description: A machine credential (ADR-0049; write class ADR-0092). Never carries the token or its hash — an operator tracing a leak needs to know which credential is alive and whether anything still uses it, and none of that requires secret material.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        tenantUserId:
          type: string
          format: uuid
          description: The service account this credential authenticates AS. Its roles, narrowed by allowedPermissionKeys, are the whole of what the credential may do.
        allowedPermissionKeys:
          type: array
          description: NARROWING filter — effective permissions are the intersection with what the service account holds. It can never widen them.
          items:
            type: string
        allowedWriteActions:
          type: array
          description: ADR-0092. Empty on every read-only credential, which is every credential issued before the write class existed. Effective write actions are the intersection with the CODE ceiling, so this list can only narrow it.
          items:
            type: string
        allowedIpCidrs:
          type: array
          description: ADR-0092. Non-empty if and only if allowedWriteActions is. Consulted ONLY for write actions — a read request is never restricted by it.
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
          description: Refreshed at most hourly, so an idle or leaked credential is visible without a write on every request.
        revokedAt:
          type: string
          format: date-time
          nullable: true
        createdByTenantUserId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        status:
          type: string
          description: Derived from the clock and revokedAt, not stored.
          enum:
            - active
            - expired
            - revoked
    IssueMachineCredentialRequest:
      type: object
      required:
        - name
        - tenantUserId
        - allowedPermissionKeys
        - expiresAt
      properties:
        name:
          type: string
          maxLength: 120
          description: Operator-facing label, e.g. "awcms-astro build feed".
        tenantUserId:
          type: string
          format: uuid
          description: An existing tenant user in THIS tenant (the service account).
        allowedPermissionKeys:
          type: array
          minItems: 1
          maxItems: 50
          description: Permission keys (`module.activity.action`) this credential may use. Required and non-empty — an empty list means "can do nothing", never "unrestricted".
          items:
            type: string
        allowedWriteActions:
          type: array
          maxItems: 2
          description: |
            ADR-0092. OPTIONAL — omitting it (or sending an empty array) issues the read-only credential of ADR-0049, which is what every caller written before this class got and still gets.

            Naming any action switches the guard to `identity_access.machine_credentials_write.create` and makes `allowedIpCidrs` mandatory. `read` is REJECTED here rather than accepted as a no-op: every credential may already read whatever its `allowedPermissionKeys` name, and accepting it would suggest otherwise.
          items:
            type: string
            enum:
              - create
              - update
        allowedIpCidrs:
          type: array
          maxItems: 20
          description: |
            ADR-0092. IPv4/IPv6 literals or CIDR blocks. Required and non-empty when `allowedWriteActions` is non-empty, and REJECTED when it is empty — a read-only credential is not restricted by this list, so accepting one would describe a binding that is not enforced.

            Unparseable entries are refused at issuance. At request time an unreadable entry matches nothing, so the failure would otherwise be a credential that reads as bound and can never authenticate.
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
          description: Required, in the future, at most 365 days away — or at most 30 days when `allowedWriteActions` is non-empty. There is no perpetual credential.
    SessionIntrospection:
      type: object
      description: Safe claims for a live session (ADR-0049 §7). Deliberately excludes the raw login identifier that GET /api/v1/auth/me returns.
      properties:
        identityId:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        displayName:
          type: string
        roles:
          type: array
          items:
            type: string
        assuranceLevel:
          type: string
          enum:
            - aal1
            - aal2
        expiresAt:
          type: string
          format: date-time
        scopes:
          type: array
          description: Active, effective-dated business-scope references — the same evaluation the authorization layer uses.
          items:
            type: object
            properties:
              scopeType:
                type: string
              scopeId:
                type: string
                format: uuid
    RegistrationRequest:
      type: object
      description: A pending self-registration request. The login identifier is masked — the raw address is never returned in a list.
      properties:
        id:
          type: string
          format: uuid
        loginIdentifierMasked:
          type: string
        displayName:
          type: string
        requestedAt:
          type: string
          format: date-time
    SsoProvider:
      type: object
      description: A tenant OIDC SSO provider. The client secret is never included — only whether one is configured and, for the env-referenced case, the variable name.
      properties:
        id:
          type: string
          format: uuid
        providerKey:
          type: string
        providerType:
          type: string
        displayName:
          type: string
        issuerUrl:
          type: string
        clientId:
          type: string
        secretSource:
          type: string
          enum:
            - encrypted
            - env
        clientSecretEnvVar:
          type: string
          nullable: true
        scopes:
          type: string
        allowedEmailDomains:
          type: array
          items:
            type: string
        enabled:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TenantAuthPolicy:
      type: object
      properties:
        tenantId:
          type: string
          format: uuid
        passwordLoginEnabled:
          type: boolean
        ssoEnabled:
          type: boolean
        ssoRequired:
          type: boolean
        autoLinkVerifiedEmail:
          type: boolean
        jitProvisioningEnabled:
          type: boolean
        allowedEmailDomains:
          type: array
          items:
            type: string
        breakGlassIdentityIds:
          type: array
          items:
            type: string
            format: uuid
        updatedAt:
          type: string
          format: date-time
          nullable: true
    TenantMfaPolicy:
      type: object
      properties:
        tenantId:
          type: string
          format: uuid
        enforcementLevel:
          type: string
          enum:
            - optional
            - required_for_privileged
            - required_for_all
        updatedAt:
          type: string
          format: date-time
          nullable: true
    AbacPolicy:
      type: object
      properties:
        id:
          type: string
          format: uuid
        policyCode:
          type: string
        effect:
          type: string
          enum:
            - allow
            - deny
        description:
          type: string
          nullable: true
        isActive:
          type: boolean
    AbacDslPolicyConditions:
      type: object
      description: >-
        A bounded, deterministic condition AST (Issue #179). A node is either a
        composition — exactly one of `allOf` (array; empty = true), `anyOf`
        (array; empty = false), or `not` (a single node) — or a leaf
        `{ attr, op, value | valueAttr }` over the server-side attribute
        allow-list (`subject.*`, `resource.*`, `action`, `env.*`) with the
        bounded operator set (eq, ne, in, nin, lt, lte, gt, gte, exists). No
        regex, functions, or arbitrary expressions. Depth ≤ 32, ≤ 512 nodes.
      additionalProperties: true
    AbacDslPolicy:
      type: object
      description: "One stored dynamic ABAC (DSL) policy (Issue #179). Applicability fields are wildcards when null."
      properties:
        id:
          type: string
          format: uuid
        policyCode:
          type: string
        effect:
          type: string
          enum:
            - allow
            - deny
        description:
          type: string
          nullable: true
        moduleKey:
          type: string
          nullable: true
        activityCode:
          type: string
          nullable: true
        action:
          type: string
          nullable: true
        resourceType:
          type: string
          nullable: true
        dslVersion:
          type: integer
        priority:
          type: integer
        conditions:
          $ref: "#/components/schemas/AbacDslPolicyConditions"
        isActive:
          type: boolean
        isDslManaged:
          type: boolean
          description: >-
            Whether the evaluator CONSUMES this policy (ADR-0033). The cache
            loads `is_active AND is_dsl_managed`, so `false` — every row
            authored through the flat /api/v1/abac/policies CRUD — is stored but
            never compiled and never affects a decision. This list returns every
            policy row for the tenant, flat and DSL alike, so without this field
            a client cannot tell an inert policy from one in force.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AbacDslPolicyWriteRequest:
      type: object
      required:
        - policyCode
        - effect
        - conditions
      properties:
        policyCode:
          type: string
          description: 3-100 chars, alphanumerics plus . _ - (not at the edges).
        effect:
          type: string
          enum:
            - allow
            - deny
        description:
          type: string
          nullable: true
        moduleKey:
          type: string
          nullable: true
          description: Applicability filter — null is a wildcard.
        activityCode:
          type: string
          nullable: true
        action:
          type: string
          nullable: true
        resourceType:
          type: string
          nullable: true
        dslVersion:
          type: integer
          description: Defaults to the current DSL version (1). A value newer than supported is rejected.
        priority:
          type: integer
          description: Lower evaluates first (deterministic). Defaults to 100.
        conditions:
          $ref: "#/components/schemas/AbacDslPolicyConditions"
        isActive:
          type: boolean
          description: On create only — author enabled immediately. Defaults to false (author, then enable).
    AbacSimulationRequest:
      type: object
      required:
        - request
      properties:
        subject:
          type: object
          properties:
            tenantUserId:
              type: string
              format: uuid
              nullable: true
              description: Simulate an EXISTING tenant user (resolves their real roles). Different from the caller requires identity_access.access_control.read.
            roles:
              type: array
              items:
                type: string
              description: Simulate a hypothetical role-code set instead of an existing user.
        request:
          type: object
          required:
            - moduleKey
            - activityCode
            - action
          properties:
            moduleKey:
              type: string
            activityCode:
              type: string
            action:
              type: string
            resourceType:
              type: string
              nullable: true
            resourceAttributes:
              type: object
              additionalProperties: true
              description: Hypothetical resource attributes (treated as server-verified).
        environment:
          type: object
          properties:
            ipTrusted:
              type: boolean
            now:
              type: string
              format: date-time
              nullable: true
    AbacSimulationResponse:
      type: object
      properties:
        decision:
          type: object
          properties:
            allowed:
              type: boolean
            reason:
              type: string
            matchedPolicy:
              type: string
              nullable: true
            matchedPolicyVersion:
              type: integer
              nullable: true
        evaluatedPolicies:
          type: array
          description: Per-policy structural trace — booleans only, never attribute VALUES (no PII).
          items:
            type: object
            properties:
              policyCode:
                type: string
              effect:
                type: string
                enum:
                  - allow
                  - deny
              dslVersion:
                type: integer
              priority:
                type: integer
              applicable:
                type: boolean
              conditionSatisfied:
                type: boolean
                nullable: true
              invalid:
                type: boolean
        subject:
          type: object
          properties:
            tenantUserId:
              type: string
            roles:
              type: array
              items:
                type: string
    AccessEvaluateRequest:
      type: object
      required:
        - moduleKey
        - activityCode
        - action
      properties:
        moduleKey:
          type: string
        activityCode:
          type: string
        action:
          type: string
        resourceType:
          type: string
        resourceId:
          type: string
          format: uuid
        resourceAttributes:
          type: object
          additionalProperties: true
    AccessEvaluateResponse:
      type: object
      properties:
        allowed:
          type: boolean
        reason:
          type: string
        matchedPolicy:
          type: string
          nullable: true
        matchedPolicyVersion:
          type: integer
          nullable: true
    Role:
      type: object
      properties:
        id:
          type: string
          format: uuid
        roleCode:
          type: string
        roleName:
          type: string
        isSystem:
          type: boolean
        permissionCount:
          type: integer
    TenantUserMasked:
      type: object
      properties:
        id:
          type: string
          format: uuid
        loginIdentifierMasked:
          type: string
          description: Masked login identifier — PII is never returned raw in a list.
        status:
          type: string
        roles:
          type: array
          items:
            type: string
          description: Assigned role codes (empty when the user holds no role).
    BusinessScopeAssignment:
      type: object
      description: "One business-scope assignment (Issue #180) — a subject granted a role/permission context restricted to one generic business scope, with effective dating and a lifecycle status."
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        tenantUserId:
          type: string
          format: uuid
        roleId:
          type: string
          format: uuid
          nullable: true
        scopeType:
          type: string
        scopeId:
          type: string
          format: uuid
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: string
          format: date-time
          nullable: true
        isTemporary:
          type: boolean
        reason:
          type: string
          nullable: true
        grantedByTenantUserId:
          type: string
          format: uuid
        approvedByTenantUserId:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
          enum:
            - active
            - expired
            - revoked
        revokedAt:
          type: string
          format: date-time
          nullable: true
        revokedByTenantUserId:
          type: string
          format: uuid
          nullable: true
        revokeReason:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SoDConflictEvaluation:
      type: object
      description: "One append-only segregation-of-duties conflict-check decision log entry (Issue #181). Safe projection — no request/resource payload."
      properties:
        id:
          type: string
          format: uuid
        ruleKey:
          type: string
        subjectTenantUserId:
          type: string
          format: uuid
          nullable: true
        triggerContext:
          type: string
          enum:
            - assignment_create
            - high_risk_decision
        conflictDetected:
          type: boolean
        resolvedVia:
          type: string
          enum:
            - none
            - exception
            - denied
        decisionReason:
          type: string
        occurredAt:
          type: string
          format: date-time
    SoDConflictException:
      type: object
      description: "One segregation-of-duties conflict exception (Issue #181) — a scope-bound, time-bound, revocable, audited override to a detected conflict."
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        ruleKey:
          type: string
        subjectTenantUserId:
          type: string
          format: uuid
        scopeType:
          type: string
          nullable: true
        scopeId:
          type: string
          format: uuid
          nullable: true
        justification:
          type: string
        requestedByTenantUserId:
          type: string
          format: uuid
        approvedByTenantUserId:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
          enum:
            - pending
            - approved
            - rejected
            - expired
            - revoked
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    PartnerEngagement:
      type: object
      description: >-
        A partnership reaching THIS tenant (ADR-0089). The partner's name comes
        from the RLS-free `awcms_tenants` row rather than being denormalised
        into the mapping — a copy could go stale with nobody noticing.
      properties:
        id:
          type: string
          format: uuid
        partnerTenantId:
          type: string
          format: uuid
        partnerTenantCode:
          type: string
        partnerTenantName:
          type: string
        engagedAt:
          type: string
          format: date-time
    DelegatedAccessGrant:
      type: object
      description: >-
        A delegated-access grant as its OWNER tenant sees it (ADR-0090). The
        redemption code is absent by construction: the column is never selected.
      properties:
        id:
          type: string
          format: uuid
        partnerTenantId:
          type: string
          format: uuid
        roleId:
          type: string
          format: uuid
        purpose:
          type: string
        expiresAt:
          type: string
          format: date-time
        redeemedAt:
          type: string
          format: date-time
          nullable: true
        revokedAt:
          type: string
          format: date-time
          nullable: true
        grantedTenantUserId:
          type: string
          format: uuid
          nullable: true
    RegisteredPartner:
      type: object
      description: >-
        A row of the platform's partner registry (ADR-0089). `tenantCode` and
        `tenantName` come from `awcms_tenants` through a join rather than being
        denormalised here, so the registry can never hold a stale copy of a
        tenant's name.
      properties:
        id:
          type: string
          format: uuid
        partnerTenantId:
          type: string
          format: uuid
          description: The tenant that IS the partner. It already existed; this row did not create it.
        partnerCode:
          type: string
        displayName:
          type: string
        status:
          type: string
          description: >-
            ADR-0093. `suspended` stops every delegated actor this partner
            placed from being served, in every customer tenant, at their next
            request — and touches no grant row.
          enum:
            - active
            - suspended
        registeredAt:
          type: string
          format: date-time
        tenantCode:
          type: string
        tenantName:
          type: string
    RegisterPartnerRequest:
      type: object
      required:
        - partnerTenantId
        - partnerCode
        - displayName
      properties:
        partnerTenantId:
          type: string
          format: uuid
          description: >-
            An EXISTING tenant on this deployment, and not the platform tenant
            itself. The platform learns this id out of band — there is no picker
            and there is not meant to be.
        partnerCode:
          type: string
          maxLength: 64
          description: >-
            Lower-case letters, digits and hyphens; starts and ends with a
            letter or digit. Globally unique. The format is enforced in the
            application rather than by a CHECK because the uniqueness index is
            global: a stray capital produces a second row that reads like the
            first one to a human.
        displayName:
          type: string
          maxLength: 160
    ManagedTenant:
      type: object
      description: >-
        A tenant that has engaged this partner, as the PARTNER sees it
        (ADR-0089). Deliberately narrower than `PartnerEngagement`: no
        `engagedBy`, because that is a third party's identifier.
      properties:
        tenantId:
          type: string
          format: uuid
        tenantCode:
          type: string
        tenantName:
          type: string
        tenantStatus:
          type: string
        engagedAt:
          type: string
          format: date-time
