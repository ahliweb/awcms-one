# Source fragment for the "Blog Content" module/tag — ported from awcms-mini
# (epic #536, issues #537-#543 plus the later hardening lineage; see
# src/modules/blog-content/module.ts's own description field and README.md
# for the full port-adaptation notes, including what was deliberately
# dropped: the host-resolved /news/** route family and its two ports'
# concrete adapters, replaced here by this module's own no-op adapters).
#
# Owns every `/api/v1/blog/*` admin path and the schemas referenced ONLY by
# this tag's operations. The public, anonymous `/blog/{tenantCode}/...`
# route family (index/detail/category/tag/search/feed/sitemap) is NOT here —
# same convention theming's own public preview routes use (Astro
# text/html/xml routes under src/pages/**, not src/pages/api/v1/**, so they
# are outside the api-spec-check route tree).
#
# ADR-0044 (news_portal merged into blog_content): this fragment ALSO owns the
# two absorbed surfaces — `/api/v1/news-portal/homepage-sections*` (tag
# `News Portal Homepage Sections`) and `/api/v1/news-portal/ad-placements*`
# (tag `News Portal Ad Placements`) — and their schemas, moved here verbatim
# from the deleted openapi/modules/news-portal.openapi.yaml. Paths and tag
# NAMES are deliberately unchanged (ADR-0044 §3/§6: the merge moved ownership,
# not the public surface); what changes here is which module's fragment file
# declares them, so `openapi/modules/*` stays one-file-per-REGISTERED-module
# (ADR-0026) instead of carrying a file for a module that no longer exists.
#
# NOT independently valid OpenAPI -- $ref targets pointing at shared
# components only resolve after `bun run openapi:bundle` merges this file
# with the root and every sibling module fragment. Edit this file (or the
# root for shared components), never the generated bundle.
paths:
  /api/v1/blog/posts:
    get:
      tags:
        - Blog Content
      operationId: blogListPosts
      summary: List this tenant's non-deleted blog posts
      description: |
        Gated by blog_content.posts.read. Optional ?status= and ?locale= filters, ?limit= bounded (default 20, max 100 — max 50 with `view=full`, whose rows carry `contentJson`).

        Ordering defaults to `updated_at DESC` — right for an admin table, and unsound as a keyset key because editing a post moves it, so a row can cross a page boundary between requests and be skipped or repeated. A caller that needs EVERY post (a build feed) passes `?order=created_at`, which is immutable, and follows `nextCursor` until it is null. `?cursor=` without `?order=created_at` is refused with 400 rather than quietly honoured.

        **The default response is `BlogPostSummary`, not `BlogPost`.** It carries no `contentJson`, `excerpt`, `metaDescription`, `canonicalUrl`, or `translationGroupId`. This document used to say otherwise, and a client that believed it built an entire static site with every article body empty — nothing failed, because a missing field reads as `undefined`. A caller that needs the body asks for `?view=full` (which requires `order=created_at`) and receives `BlogPost`.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, review, scheduled, published, archived]
        - name: locale
          in: query
          description: Exact match on the post's stored locale. Absent means every locale, which is the right default for an admin table but wasteful for a single-language build feed — that is what this parameter exists for. Not shape-validated beyond non-empty and a 35-character bound, because the write path accepts any non-empty string and a stricter read filter would make a stored locale unreachable. An empty value is a 400, never treated as absent.
          schema:
            type: string
            minLength: 1
            maxLength: 35
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: order
          in: query
          description: Sort key. `created_at` selects the STABLE, cursor-capable traversal; `updated_at` (default) is the admin ordering and rejects `cursor`.
          schema:
            type: string
            enum: [created_at, updated_at]
        - name: cursor
          in: query
          description: Opaque keyset cursor from a previous response's `nextCursor`. Requires `order=created_at`. A malformed value is a 400, never treated as "no cursor".
          schema:
            type: string
        - name: view
          in: query
          description: |
            Response projection. `summary` (default) returns `BlogPostSummary`; `full` returns `BlogPost` — every column the detail endpoint returns, `termIds` and `institutionIds` included.

            Those two were originally left out because fetching them per post would have been one extra query each. A page fetches them for the whole page in one query instead, so the cost is three round trips per page rather than one per post — and without them no consumer could build a category or tag archive at all, which is the state Issue #597 item 1 describes.

            `full` requires `order=created_at`: a full traversal is only sound over the immutable ordering, and the ordering is demanded rather than silently substituted. An unrecognised value is a 400, never a silent fallback to `summary`.
          schema:
            type: string
            enum: [summary, full]
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching posts.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      posts:
                        type: array
                        description: "`BlogPostSummary` by default; `BlogPost` when `view=full`."
                        items:
                          oneOf:
                            - $ref: "#/components/schemas/BlogPostSummary"
                            - $ref: "#/components/schemas/BlogPost"
                      nextCursor:
                        type: string
                        nullable: true
                        description: Present only with `order=created_at`. Null means the traversal is complete.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreatePost
      summary: Create a draft blog post
      description: Gated by blog_content.posts.create. Not idempotency-keyed (a retry duplicating a create is caught by the (tenant_id, locale, slug) partial unique index).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPostWriteInput"
      responses:
        "200":
          description: Post created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A post already exists for this slug/locale, or the referenced media is not R2-verified in full-online R2-only mode (422 for the latter).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: One or more image/video-thumbnail references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPost
      summary: Read one blog post
      description: Gated by blog_content.posts.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The post.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdatePost
      summary: Update a blog post
      description: "Gated by blog_content.posts.update (with an ownership carve-out — a draft's own author may update it without the broader permission). Only fields present in the body are changed. A significant title/contentJson/contentText change snapshots an append-only revision first. Issue #784 — when `slug` is present and differs from the stored slug, the SAME transaction also captures an ADR-0039 redirect (proposed or active, per the tenant's url_change_auto_policy); see the response schema's `redirectCapture` field."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPostWriteInput"
      responses:
        "200":
          description: Post updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: One or more image/video-thumbnail references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeletePost
      summary: Soft delete a blog post
      description: Gated by blog_content.posts.delete. Soft delete only (deleted_at/deleted_by/delete_reason) — see POST .../purge for the permanent, high-risk purge.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/submit-review:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogSubmitPostForReview
      summary: Submit a draft post for review
      description: Transitions draft -> review. Gated by blog_content.posts.update (same ownership carve-out as PATCH .../{id}). Not idempotency-keyed — the status transition is naturally idempotent.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post submitted for review.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The post's current status cannot transition to review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/publish:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPublishPost
      summary: Publish a blog post
      description: Gated by blog_content.posts.publish (no ownership carve-out). High-risk, requires Idempotency-Key. Blocked (422) if the content quality checklist fails when full-online R2-only mode is active for the tenant. On success, also invokes the social-publishing outbox hook (a documented no-op in this base — social_publishing is not ported).
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post published (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Publish is blocked by the content quality checklist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/schedule:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogSchedulePost
      summary: Schedule a blog post for future publishing
      description: Gated by blog_content.posts.schedule. High-risk, requires Idempotency-Key. Same content quality checklist gate as publish. The blog:publish:scheduled job later performs the actual publish once scheduledAt <= now().
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [scheduledAt]
              properties:
                scheduledAt:
                  type: string
                  format: date-time
                unpublishAt:
                  type: string
                  format: date-time
                  nullable: true
                  description: >-
                    Issue #591 — closes the publication window. When the moment
                    arrives, blog:publish:scheduled transitions the post to
                    archived. Must be strictly after scheduledAt; omitted or null
                    means the post stays published until an editor archives it by
                    hand. Carried in the idempotency request hash, so replaying a
                    key with a different withdrawal date is a 409, not a no-op.
      responses:
        "200":
          description: Post scheduled (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Scheduling is blocked by the content quality checklist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/archive:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogArchivePost
      summary: Archive a blog post
      description: Gated by blog_content.posts.archive. High-risk, requires Idempotency-Key.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post archived (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestorePost
      summary: Restore a soft-deleted blog post
      description: Gated by blog_content.posts.restore. High-risk, requires Idempotency-Key.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post restored (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/purge:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPurgePost
      summary: Permanently purge a soft-deleted blog post
      description: Gated by blog_content.posts.purge. High-risk, irreversible, requires Idempotency-Key. Only a previously soft-deleted post may be purged.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post purged (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The post is not soft-deleted, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/quality-checklist:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPostQualityChecklist
      summary: Preview the content quality checklist for a post
      description: Read-only preview for the admin editor — runs the exact same evaluator POST .../publish and .../schedule enforce. Gated by blog_content.posts.read (no separate permission).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The checklist result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      postId:
                        type: string
                        format: uuid
                      qualityChecklist:
                        $ref: "#/components/schemas/ContentQualityChecklistResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/internal-links/preview:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogPreviewPostInternalLinks
      summary: Preview automatic internal tag linking for a post
      description: Read-only preview of which tags would be automatically linked in this post's rendered content before publishing. Gated by blog_content.internal_links.preview.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The preview result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      postId:
                        type: string
                        format: uuid
                      enabled:
                        type: boolean
                      disabledReason:
                        type: string
                        nullable: true
                        enum:
                          [
                            deployment_disabled,
                            tenant_disabled,
                            post_disabled,
                            null
                          ]
                      matches:
                        type: array
                        items:
                          type: object
                      totalLinked:
                        type: integer
                      maxPerPost:
                        type: integer
                      maxPerTag:
                        type: integer
                      vocabulary:
                        type: object
                        description: |
                          How much of the tenant's tag vocabulary the matching engine could see (Issue #648).

                          Only a bounded number of tags enter the engine at once, because it compiles one alternation regex from all of them. Which ones are chosen is by USAGE — the tags carried by the most posts — since those are the ones most likely to occur in prose. Until Issue #648 the selection was the alphabetically-first hundred, inherited from the admin list, so whether a tag was ever linked was decided by its first letter and nothing said so.

                          `truncated: true` is the one reason a tag went unlinked that the editor cannot fix by editing.
                        required: [total, limit, truncated]
                        properties:
                          total:
                            type: integer
                            description: Non-deleted tags this tenant has.
                          limit:
                            type: integer
                            description: The cap applied to the candidate list.
                          truncated:
                            type: boolean
                            description: True when the vocabulary is larger than the cap, so some tags cannot be linked at all.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/revisions:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogListPostRevisions
      summary: List a post's revision history
      description: Gated by blog_content.revisions.read. Newest first.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The revision list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      revisions:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogRevision"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/revisions/{revisionId}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revisionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPostRevision
      summary: Read one revision of a post
      description: Gated by blog_content.revisions.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The revision.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogRevision"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/revisions/{revisionId}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revisionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestorePostRevision
      summary: Restore a post to an earlier revision
      description: Gated by blog_content.revisions.restore. High-risk, requires Idempotency-Key. Restoring APPENDS a new revision snapshot of the restored content — it never overwrites or removes the revision being restored from.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post restored to the given revision (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: One or more image/video-thumbnail references in the restored content are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/public:
    get:
      tags:
        - Blog Content
      summary: The static pages a reader can actually reach
      description: >-
        Gated by blog_content.pages.read. NOT the same answer as
        `/api/v1/blog/pages?status=published`: the admin list is an editor's view
        and returns private and unlisted pages too, so a consumer reaching for it
        with a status filter would publish every private page the newsroom has and
        nothing would report an error. The predicate here is the one the public
        route enforces, shared with sitemap-blog.xml so the two cannot disagree.
        Metadata only — a body is fetched per page from /{slug}, because a tenant's
        legal pages are few but long.
      operationId: blogPagesPublicList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Reachable static pages, in menu order.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      pages:
                        type: array
                        items:
                          $ref: "#/components/schemas/PublicBlogPageSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/pages/public/{slug}:
    get:
      tags:
        - Blog Content
      summary: One reachable static page, body included
      description: >-
        Gated by blog_content.pages.read. The predicate is shared byte-for-byte
        with the public route /blog/{tenantCode}/pages/{slug}, so a draft cannot be
        read here for the same reason it cannot be read there. An unreachable slug
        answers 404 without distinguishing "no such page" from "that page is a
        draft" — not an information-disclosure boundary (the caller is
        authenticated) but consistency, so a consumer cannot depend on a
        distinction the browser-facing surface does not make. The body ships as
        BOTH `contentJson` (the projection this repo renders today) and
        `bodyPortableText` (the canonical column, ADR-0100), which is what lets a
        consumer move to the canonical shape on its own schedule.
      operationId: blogPagesPublicDetail
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The page, with every referenced media object resolved in one call.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/PublicBlogPageDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/pages:
    get:
      tags:
        - Blog Content
      operationId: blogListPages
      summary: List this tenant's non-deleted blog pages
      description: Gated by blog_content.pages.read. Optional ?status= filter, ?limit= bounded.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, review, scheduled, published, archived]
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching pages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      pages:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreatePage
      summary: Create a draft blog page
      description: Gated by blog_content.pages.create. Not idempotency-keyed (caught by the (tenant_id, locale, slug) partial unique index).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPageWriteInput"
      responses:
        "200":
          description: Page created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A page already exists for this slug/locale.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: One or more image references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPage
      summary: Read one blog page
      description: Gated by blog_content.pages.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The page.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdatePage
      summary: Update a blog page
      description: "Gated by blog_content.pages.update. Only fields present in the body are changed. A significant change snapshots an append-only revision first. Issue #787 (sibling of #784) — when `slug` is present and differs from the stored slug, the SAME transaction also captures an ADR-0039 redirect (proposed or active, per the tenant's url_change_auto_policy); see the response schema's `redirectCapture` field."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPageWriteInput"
      responses:
        "200":
          description: Page updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: One or more image references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeletePage
      summary: Soft delete a blog page
      description: Gated by blog_content.pages.delete.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/pages/{id}/quality-checklist:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPageQualityChecklist
      summary: Preview the content quality checklist for a page
      description: Read-only preview for the admin editor. Gated by blog_content.pages.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The checklist result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      pageId:
                        type: string
                        format: uuid
                      qualityChecklist:
                        $ref: "#/components/schemas/ContentQualityChecklistResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/pages/{id}/publish:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPublishPage
      summary: Publish a blog page
      description: Gated by blog_content.pages.publish (ADR-0057). High-risk, requires Idempotency-Key. Blocked (422) if the content quality checklist fails when full-online R2-only mode is active for the tenant. Pages have a narrower lifecycle than posts - no review, no scheduled - so the only sources for this transition are draft and archived. No social-publishing hook is invoked - that port's trigger is post_published and a page is not an article.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page published (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Publish is blocked by the content quality checklist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}/archive:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogArchivePage
      summary: Archive a blog page
      description: Gated by blog_content.pages.archive (ADR-0057). High-risk, requires Idempotency-Key. Removes the page from the public site without destroying it; published_at is retained so a later re-publish keeps the original go-live date.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page archived (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestorePage
      summary: Restore a soft-deleted blog page
      description: Gated by blog_content.pages.restore (ADR-0057). High-risk, requires Idempotency-Key. Undoes a soft delete, not an archive - lifecycle status is left untouched, so a page soft-deleted while published comes back published. A page that is not soft-deleted answers 404, the same shape as an unknown id, so restore cannot be used to probe which ids exist.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page restored (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}/purge:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPurgePage
      summary: Permanently purge an archived or soft-deleted blog page
      description: Gated by blog_content.pages.purge (ADR-0057). High-risk, irreversible, requires Idempotency-Key. Only an archived or previously soft-deleted page may be purged. Advertisement placements targeting this page neither block the purge nor are deleted with it - they simply go inert, exactly as they already do for a soft-deleted page, because the render query matches target_id against the page being rendered. Their count is returned as adPlacementsNowInert so the change is visible rather than silent.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page purged (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum: [purged]
                      adPlacementsNowInert:
                        type: integer
                        minimum: 0
                        description: Advertisement placements that targeted this page and are now inert.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The page is neither archived nor soft-deleted, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/terms:
    get:
      tags:
        - Blog Content
      operationId: blogListTerms
      summary: List this tenant's categories/tags
      description: |
        Gated by blog_content.taxonomies.read. Optional ?taxonomyType= filter, ?limit= bounded (default 100, max 200).

        Ordering defaults to `name ASC` — right for the admin taxonomy screen, and unsound as a keyset key because renaming a term moves it, so a row can cross a page boundary between requests and be skipped or repeated. A caller that needs EVERY term passes `?order=created_at`, which is immutable, and follows `nextCursor` until it is null. `?cursor=` without `?order=created_at` is refused with 400 rather than quietly honoured.

        **The default list is bounded and says nothing about what it left out.** For `category` or `channel` that is harmless — a newsroom has a dozen of each. For `tag` it is not: a vocabulary grown over tens of thousands of articles runs to thousands of entries, and a caller that generated one archive page per tag from the default list would build a hundred pages, `200 OK`, with every article filed under a later tag pointing at a page nobody generated. That is what `order=created_at` exists for.
      parameters:
        - name: taxonomyType
          in: query
          schema:
            type: string
            enum: [category, tag, channel, topic]
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: order
          in: query
          description: Sort key. `created_at` selects the STABLE, cursor-capable traversal; `name` (default) is the admin ordering and rejects `cursor`.
          schema:
            type: string
            enum: [created_at, name]
        - name: cursor
          in: query
          description: Opaque keyset cursor from a previous response's `nextCursor`. Requires `order=created_at`. A malformed value is a 400, never treated as "no cursor".
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching terms.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      terms:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogTerm"
                      nextCursor:
                        type: string
                        nullable: true
                        description: Present only with `order=created_at`. Null once the traversal is complete.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateTerm
      summary: Create a category or tag
      description: Gated by blog_content.taxonomies.configure. A tag must never carry a parentId.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogTermWriteInput"
      responses:
        "200":
          description: Term created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogTerm"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A term already exists for this taxonomyType/slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/terms/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateTerm
      summary: Update a category or tag
      description: Gated by blog_content.taxonomies.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogTermWriteInput"
      responses:
        "200":
          description: Term updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogTerm"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteTerm
      summary: Soft delete a category or tag
      description: Gated by blog_content.taxonomies.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Term soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogTerm"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/institutions:
    get:
      tags:
        - Blog Content
      operationId: blogListInstitutions
      summary: List this tenant's institutions
      description: >-
        Gated by blog_content.institutions.read. Optional ?branch= filter, which is
        what builds each mega menu in one query. Bounded at 200 rows ordered by name.
      parameters:
        - name: branch
          in: query
          schema:
            type: string
            enum: [legislative, executive]
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching institutions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      institutions:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateInstitution
      summary: Register an institution
      description: >-
        Gated by blog_content.institutions.create. Not idempotent: a retry that
        duplicates a create is refused 409 by the slug unique index, so no
        Idempotency-Key is read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogInstitutionCreateInput"
      responses:
        "200":
          description: Institution created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: An institution already exists for this slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/institutions/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetInstitution
      summary: Read one institution
      description: Gated by blog_content.institutions.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The institution.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateInstitution
      summary: Update an institution
      description: >-
        Gated by blog_content.institutions.update. Only supplied fields change; an
        explicit null clears an optional field. An empty body is rejected 400 rather
        than recorded as a no-op edit.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogInstitutionUpdateInput"
      responses:
        "200":
          description: Institution updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: An institution already exists for this slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteInstitution
      summary: Soft delete an institution
      description: >-
        Gated by blog_content.institutions.delete. The articles filed against it keep
        their link, so restoring the institution restores its archive intact.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [reason]
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Institution soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
                        enum: [true]
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/institutions/{id}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestoreInstitution
      summary: Restore a soft-deleted institution
      description: >-
        Gated by blog_content.institutions.restore. No Idempotency-Key is read: the
        statement only matches a currently soft-deleted row, so a replay writes no
        audit event and answers 404. Deleting released the slug, so 409 is returned
        when a live institution has taken it in the meantime.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Institution restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A live institution now holds this slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/institutions/{id}/purge:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPurgeInstitution
      summary: Permanently remove a soft-deleted institution
      description: >-
        Gated by blog_content.institutions.purge. Requires a prior soft delete: the
        statement matches only a row whose deleted_at is set, so a live institution
        answers 404 rather than being destroyed. Deletes every
        awcms_blog_post_institutions link first, so the articles that named it lose
        this classification permanently. Idempotency-Key is REQUIRED — unlike
        restore, the effect cannot be safely replayed once the row is gone.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Institution purged.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum: [purged]
                      articleLinksRemoved:
                        type: integer
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/search:
    get:
      tags:
        - Blog Content
      operationId: blogSearchContent
      summary: Full-text search across posts and pages
      description: Gated by blog_content.search.read. May return content of any status per the caller's granted permission. Keyset-paginated (?cursor=).
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: resourceType
          in: query
          schema:
            type: string
            enum: [post, page]
        - name: status
          in: query
          schema:
            type: string
            enum: [draft, review, scheduled, published, archived]
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching results.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          type: object
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/settings:
    get:
      tags:
        - Blog Content
      operationId: blogGetSettings
      summary: Read this tenant's blog settings
      description: Gated by blog_content.settings.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant's blog settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateSettings
      summary: Update this tenant's blog settings
      description: Gated by blog_content.settings.configure. Only fields present in the body are changed.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Settings updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/templates:
    get:
      tags:
        - Blog Content
      operationId: blogListTemplates
      summary: List this tenant's presentation templates
      description: Gated by blog_content.templates.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching templates.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      templates:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateTemplate
      summary: Create a presentation template
      description: "Gated by blog_content.templates.configure. layoutJson is a whitelisted shape: { columns: 1|2|3, sidebarPosition: left|right|none }."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [key, name]
              properties:
                key:
                  type: string
                name:
                  type: string
                layoutJson:
                  type: object
                isActive:
                  type: boolean
      responses:
        "200":
          description: Template created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A template already exists for this key.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/templates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateTemplate
      summary: Update a presentation template
      description: Gated by blog_content.templates.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                layoutJson:
                  type: object
                isActive:
                  type: boolean
      responses:
        "200":
          description: Template updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteTemplate
      summary: Soft delete a presentation template
      description: Gated by blog_content.templates.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Template soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/menus:
    get:
      tags:
        - Blog Content
      operationId: blogListMenus
      summary: List this tenant's navigation menus (with items)
      description: |
        Gated by blog_content.menus.read. Bounded at 100 menus, name ascending — a site has a handful, and unlike the tag vocabulary this one does not grow with the archive.

        Each menu carries its own `items`, already sorted by `sortOrder`. That costs one query per menu rather than one for all of them, and the route says why in a comment: the queries run on one transaction, so they cannot be issued concurrently.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching menus.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      menus:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogMenu"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateMenu
      summary: Create a navigation menu
      description: "Gated by blog_content.menus.configure. Optionally seeds its initial items tree (one level of nesting; link_type post|page|url gates target_id/url)."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [key, name]
              properties:
                key:
                  type: string
                name:
                  type: string
                items:
                  type: array
                  maxItems: 200
                  description: At most 200 entries; a longer array is refused with `VALIDATION_ERROR` before any entry is validated.
                  items:
                    $ref: "#/components/schemas/BlogMenuItem"
      responses:
        "200":
          description: Menu created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A menu already exists for this key.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/menus/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateMenu
      summary: Update a navigation menu (name and/or its items tree)
      description: |
        Gated by blog_content.menus.configure. Providing items REPLACES the
        whole tree — a partial list deletes the rest. At most 200 items.

        Read `itemsTruncated` on whatever you are editing before sending
        `items`: a menu stored before the cap existed reads back bounded, and
        saving that list back would delete everything past the bound.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                items:
                  type: array
                  maxItems: 200
                  description: At most 200 entries; a longer array is refused with `VALIDATION_ERROR` before any entry is validated. Omit the field entirely to leave the existing tree untouched — an empty array CLEARS it.
                  items:
                    $ref: "#/components/schemas/BlogMenuItem"
      responses:
        "200":
          description: Menu updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteMenu
      summary: Soft delete a navigation menu
      description: Gated by blog_content.menus.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Menu soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/widgets:
    get:
      tags:
        - Blog Content
      operationId: blogListWidgets
      summary: List this tenant's widgets
      description: |
        Gated by blog_content.widgets.read. Optional `?position=` filter. Bounded at 100, ordered by `position` then `sortOrder`.

        **Inactive widgets are returned too.** There is no `?activeOnly=` parameter: `isActive` is on every row, and a consumer that wants only the live ones filters on it. An endpoint that hid them would make "this widget is switched off" and "this widget was deleted" the same answer.
      parameters:
        - name: position
          in: query
          schema:
            type: string
            enum: [header, sidebar, footer, content_before, content_after]
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching widgets.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      widgets:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogWidget"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateWidget
      summary: Create a widget
      description: Gated by blog_content.widgets.configure. bodyText is plain text, escaped at render time (no raw-HTML field).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [position, title]
              properties:
                position:
                  type: string
                  enum: [header, sidebar, footer, content_before, content_after]
                title:
                  type: string
                bodyText:
                  type: string
                isActive:
                  type: boolean
                sortOrder:
                  type: integer
      responses:
        "200":
          description: Widget created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/widgets/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateWidget
      summary: Update a widget
      description: Gated by blog_content.widgets.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                position:
                  type: string
                  enum: [header, sidebar, footer, content_before, content_after]
                title:
                  type: string
                bodyText:
                  type: string
                isActive:
                  type: boolean
                sortOrder:
                  type: integer
      responses:
        "200":
          description: Widget updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteWidget
      summary: Soft delete a widget
      description: Gated by blog_content.widgets.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Widget soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/ads:
    get:
      tags:
        - Blog Content
      operationId: blogListAds
      summary: List this tenant's advertisements
      description: Gated by blog_content.ads.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching ads.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      ads:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateAd
      summary: Create an advertisement (RETIRED — always 410)
      deprecated: true
      description: "RETIRED by ADR-0044 §4. Always responds 410 ENDPOINT_RETIRED, without auth or any database access. This endpoint stored a free-text imageUrl — any URL an admin typed, rendered straight into a public img src — which is the managed-media bypass ADR-0036 closed. Upload the image through the media library, then use POST /api/v1/news-portal/ad-placements, which requires a verified media object. GET and DELETE on this resource still work, so the ingest job's residue report stays resolvable."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "410":
          description: Endpoint retired. The successor is named in the error message.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/ads/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateAd
      summary: Update an advertisement (RETIRED — always 410)
      deprecated: true
      description: "RETIRED by ADR-0044 §4. Always responds 410 ENDPOINT_RETIRED, without auth or any database access. Closing POST alone would not have sufficed: this endpoint could rewrite imageUrl on an existing ad, the same free-URL bypass by a quieter route that creates no new row. Use PATCH /api/v1/news-portal/ad-placements/{id}, which requires a verified media object."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "410":
          description: Endpoint retired. The successor is named in the error message.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteAd
      summary: Soft delete an advertisement
      description: Gated by blog_content.ads.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Ad soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/theme:
    get:
      tags:
        - Blog Content
      operationId: blogGetTheme
      summary: Read this tenant's blog theme mode setting
      description: Gated by blog_content.theme.read. Absence of a row means "inherit the tenant's default_theme".
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The theme setting.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum: [light, dark, system]
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateTheme
      summary: Update this tenant's blog theme mode setting
      description: Gated by blog_content.theme.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [mode]
              properties:
                mode:
                  type: string
                  enum: [light, dark, system]
      responses:
        "200":
          description: Theme setting updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum: [light, dark, system]
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/internal-tag-links/settings:
    get:
      tags:
        - Blog Content
      operationId: blogGetInternalTagLinkSettings
      summary: Read this tenant's automatic internal tag linking policy
      description: Gated by blog_content.internal_links.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogInternalTagLinkSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateInternalTagLinkSettings
      summary: Update this tenant's automatic internal tag linking policy
      description: Gated by blog_content.internal_links.configure. disabledTagIds are validated against this tenant's own tag catalog.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                caseInsensitive:
                  type: boolean
                disabledTagIds:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: Policy updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/BlogInternalTagLinkSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/news-portal/homepage-sections/composed:
    get:
      tags:
        - News Portal Homepage Sections
      summary: The RESOLVED homepage, for a build client that renders its own templates
      description: >-
        Gated by blog_content.homepage_sections.read. Returns what the front page
        actually shows, not its configuration: every reference resolved against
        live public content, the deterministic fallback applied to a curated slot
        whose articles have all gone, and the render caps already enforced. A
        consumer must not resolve the configuration itself — that would
        re-implement the publication predicate in a second repository on a second
        deploy cadence, and the first disagreement is a draft article on somebody's
        front page. Media arrive as resolved public URLs here (unlike
        /api/v1/site-profile/composed, which returns ids) because a homepage is
        many short-lived images rather than one long-lived logo.
      operationId: newsPortalHomepageComposed
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The composed homepage.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      isEmpty:
                        type: boolean
                        description: True when the tenant has composed nothing; the caller renders its own chronological listing.
                      sections:
                        type: array
                        items:
                          $ref: "#/components/schemas/ComposedHomepageSection"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/news-portal/ad-placements/active:
    get:
      tags:
        - News Portal Ad Placements
      summary: Currently-runnable creatives per slot, already rotated and capped
      description: >-
        Gated by blog_content.ad_placements.read. Returns all twelve slots,
        including the three this repo's own templates do not render — the sidebar
        exists in the consuming front end, and an endpoint shaped around what this
        repo draws would withhold inventory the consumer exists to show. Rotation
        and the per-slot item cap are applied HERE so four rotation modes are not
        re-implemented elsewhere; random_safe and weighted make the response
        deliberately not byte-stable, so it is not cacheable and does not try to
        be. An invalid targetType is refused rather than falling back to global,
        because silently widening an ad query is how a placement booked against one
        article appears on all of them.
      operationId: newsPortalAdPlacementsActive
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: targetType
          in: query
          required: false
          schema:
            type: string
            enum: [global, widget, post, page]
          description: The kind of page being rendered. Omit for the global inventory.
        - name: targetId
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Required when targetType is not global, and refused when it is.
      responses:
        "200":
          description: Selected creatives per placement key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      slots:
                        type: object
                        additionalProperties:
                          type: array
                          items:
                            $ref: "#/components/schemas/PublicAdPlacement"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/news-portal/homepage-sections:
    get:
      tags:
        - News Portal Homepage Sections
      summary: List this tenant's homepage sections (admin view)
      description: Gated by news_portal.homepage_sections.read.
      operationId: newsPortalHomepageSectionsList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Homepage sections.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      sections:
                        type: array
                        items:
                          $ref: "#/components/schemas/HomepageSectionItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - News Portal Homepage Sections
      summary: Create a homepage section
      description: Gated by news_portal.homepage_sections.configure. `config` is validated per `sectionType`; every referenced post/category/media object must already exist for this tenant (and gallery_block media must be a verified R2 media object).
      operationId: newsPortalHomepageSectionsCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HomepageSectionCreateRequest"
      responses:
        "200":
          description: Homepage section created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/HomepageSectionItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: sectionKey is already in use for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: config references content that does not exist, does not belong to this tenant, or (for gallery_block) is not a verified R2 media object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/news-portal/homepage-sections/{id}:
    patch:
      tags:
        - News Portal Homepage Sections
      summary: Update a homepage section (title/config/sortOrder/isEnabled/schedule) — sectionType is immutable
      description: Gated by news_portal.homepage_sections.configure.
      operationId: newsPortalHomepageSectionsUpdate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HomepageSectionUpdateRequest"
      responses:
        "200":
          description: Homepage section updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/HomepageSectionItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: config references content that does not exist, does not belong to this tenant, or (for gallery_block) is not a verified R2 media object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - News Portal Homepage Sections
      summary: Soft-delete a homepage section
      description: Gated by news_portal.homepage_sections.configure.
      operationId: newsPortalHomepageSectionsDelete
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Homepage section deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/news-portal/ad-placements:
    get:
      tags:
        - News Portal Ad Placements
      summary: List this tenant's ad placements (admin view)
      description: Gated by news_portal.ad_placements.read.
      operationId: newsPortalAdPlacementsList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Ad placements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      placements:
                        type: array
                        items:
                          $ref: "#/components/schemas/AdPlacementItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - News Portal Ad Placements
      summary: Create an ad placement (R2-only image, verified media object required)
      description: Gated by news_portal.ad_placements.configure. `mediaObjectId` must reference a verified (`verified`/`attached`) R2 media object for this tenant.
      operationId: newsPortalAdPlacementsCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdPlacementCreateRequest"
      responses:
        "200":
          description: Ad placement created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/AdPlacementItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: mediaObjectId does not exist, does not belong to this tenant, is not a verified R2 media object, or is not an allowed mime type for the target placement.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/news-portal/ad-placements/{id}:
    patch:
      tags:
        - News Portal Ad Placements
      summary: Update an ad placement (media reference/link/rotation/schedule/active)
      description: Gated by news_portal.ad_placements.configure.
      operationId: newsPortalAdPlacementsUpdate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdPlacementUpdateRequest"
      responses:
        "200":
          description: Ad placement updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    $ref: "#/components/schemas/AdPlacementItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: mediaObjectId does not exist, does not belong to this tenant, is not a verified R2 media object, or is not an allowed mime type for the target placement.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - News Portal Ad Placements
      summary: Soft-delete an ad placement
      description: Gated by news_portal.ad_placements.configure.
      operationId: newsPortalAdPlacementsDelete
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Ad placement deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum: [true]
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
components:
  schemas:
    BlogPostWriteInput:
      type: object
      description: Shared shape for POST /api/v1/blog/posts (all fields required) and PATCH /api/v1/blog/posts/{id} (all fields optional, only present fields change).
      properties:
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: >-
            ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key
            is overwritten with the derived projection; other keys (notably
            awcmsAstro) are preserved.
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        locale:
          type: string
        visibility:
          type: string
          enum: [public, private, unlisted]
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoImageMediaId:
          type: string
          format: uuid
          nullable: true
          description: Explicit "use this image for social/SEO preview" override — takes priority over featuredMediaId.
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        termIds:
          type: array
          items:
            type: string
            format: uuid
        institutionIds:
          description: >-
            Issue #595 — the fourth classification dimension (sql/131).
            Institution is a TABLE rather than a taxonomy type because it
            carries a branch, a region code and its own landing SEO, but on
            this payload it is the same shape as `termIds`. Full-replace
            semantics: absent leaves the assignments alone, `[]` removes them.
          type: array
          items:
            type: string
            format: uuid
        translationGroupId:
          type: string
          format: uuid
          nullable: true
        autoInternalTagLinksDisabled:
          type: boolean
          description: Per-post opt-out of automatic internal tag linking.
    BlogPostSummary:
      type: object
      description: |
        What the LIST endpoint returns by default — deliberately not a `BlogPost`.

        It exists as its own schema because the list used to be documented as returning `BlogPost`, and it never did: no `contentJson`, `excerpt`, `metaDescription`, `canonicalUrl`, or `translationGroupId`. A client that trusted the document read those as `undefined` and published a site with empty articles, with nothing failing. Ask for `?view=full` to get `BlogPost`.
      required:
        [
          id,
          tenantId,
          title,
          slug,
          status,
          visibility,
          locale,
          createdAt,
          updatedAt
        ]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        status:
          type: string
          enum: [draft, review, scheduled, published, archived]
        visibility:
          type: string
          enum: [public, private, unlisted]
        locale:
          type: string
        publishedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
          description: Immutable — the only sort key this list can paginate over soundly.
        updatedAt:
          type: string
          format: date-time
    BlogPost:
      type: object
      required:
        [
          id,
          tenantId,
          title,
          slug,
          status,
          visibility,
          locale,
          createdAt,
          updatedAt,
          version
        ]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        authorTenantUserId:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: >-
            ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key
            is overwritten with the derived projection; other keys (notably
            awcmsAstro) are preserved.
        contentText:
          type: string
          description: >-
            ADR-0100 — DERIVED from bodyPortableText. Still returned (it is what
            the full-text index is built from) but no longer accepted on write.
          readOnly: true
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        status:
          type: string
          enum: [draft, review, scheduled, published, archived]
        visibility:
          type: string
          enum: [public, private, unlisted]
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoImageMediaId:
          type: string
          format: uuid
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        locale:
          type: string
        translationGroupId:
          type: string
          format: uuid
          nullable: true
        autoInternalTagLinksDisabled:
          type: boolean
        publishedAt:
          type: string
          format: date-time
          nullable: true
        scheduledAt:
          type: string
          format: date-time
          nullable: true
        unpublishAt:
          type: string
          format: date-time
          nullable: true
          description: "Issue #591 — when the scheduled-publish job archives this post. Null = stays published until archived by hand."
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
        termIds:
          description: >-
            Category/tag/channel/topic assignments. Returned by
            `GET /api/v1/blog/posts/{id}`, by `PATCH` on the same path, and —
            since Issue #597 — by the `?view=full` traversal that a static site
            is built from. `[]` means the post has none; the field being ABSENT
            means the response shape does not carry it (the lifecycle actions),
            which is a different fact.
          type: array
          items:
            type: string
            format: uuid
        institutionIds:
          description: >-
            Issue #595 — the fourth classification dimension (sql/131).
            Institution is a TABLE rather than a taxonomy type because it
            carries a branch, a region code and its own landing SEO, but on
            this payload it is the same shape as `termIds`, and it is returned
            in the same places for the same reasons.
          type: array
          items:
            type: string
            format: uuid
        authorByline:
          description: >-
            ADR-0109 — the author's OPT-IN public byline, returned by the
            `?view=full` traversal a static site is built from.

            `null` is the normal state and means the article keeps the
            ORGANISATION-level attribution (ADR-0102): a consumer renders no
            individual name for it. It is deliberately NOT the author's internal
            display name — publishing an account name because somebody wrote an
            article is the PII surface Issue #649 refused to open — and it
            carries no id, URL or profile link, only the name.
          type: string
          nullable: true
          maxLength: 120
        redirectCapture:
          description: >-
            Issue #784 — present ONLY on a `PATCH /api/v1/blog/posts/{id}`
            response whose body actually changed `slug` (absent otherwise,
            including on `GET`/`POST` and every lifecycle action). Reports
            what `captureUrlChangeRedirect` (ADR-0039) did with the resulting
            old->new path change, gated by the tenant's own
            `url_change_auto_policy` (`POST
            /api/v1/seo/redirects/capture-url-change` is the same seam,
            driven here automatically instead of by a caller). A `rejected`
            or `invalid` outcome never fails the post update itself — the
            slug change is additive tooling around the edit, not a
            precondition for it.
          type: object
          required: [outcome]
          properties:
            outcome:
              type: string
              enum:
                [created, proposed, skipped, module_disabled, invalid, rejected]
            redirectId:
              description: Present only when outcome is "created" or "proposed".
              type: string
              format: uuid
            code:
              description: Present only when outcome is "rejected".
              type: string
              enum: [SOURCE_CONFLICT, REDIRECT_LOOP, REDIRECT_CHAIN_TOO_LONG]
            message:
              description: Present only when outcome is "rejected" — a human-readable reason no redirect was created.
              type: string
    BlogPageWriteInput:
      type: object
      description: Shared shape for POST /api/v1/blog/pages and PATCH /api/v1/blog/pages/{id} (all fields optional on update).
      properties:
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: >-
            ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key
            is overwritten with the derived projection; other keys (notably
            awcmsAstro) are preserved.
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        locale:
          type: string
        visibility:
          type: string
          enum: [public, private, unlisted]
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        pageType:
          type: string
          enum: [standard, landing, legal, system]
        parentPageId:
          type: string
          format: uuid
          nullable: true
        menuOrder:
          type: integer
    BlogPage:
      type: object
      required:
        [
          id,
          tenantId,
          title,
          slug,
          status,
          visibility,
          pageType,
          locale,
          createdAt,
          updatedAt,
          version
        ]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        authorTenantUserId:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: >-
            ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key
            is overwritten with the derived projection; other keys (notably
            awcmsAstro) are preserved.
        contentText:
          type: string
          description: >-
            ADR-0100 — DERIVED from bodyPortableText. Still returned (it is what
            the full-text index is built from) but no longer accepted on write.
          readOnly: true
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        status:
          type: string
          enum: [draft, review, scheduled, published, archived]
        visibility:
          type: string
          enum: [public, private, unlisted]
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        locale:
          type: string
        pageType:
          type: string
          enum: [standard, landing, legal, system]
        parentPageId:
          type: string
          format: uuid
          nullable: true
        menuOrder:
          type: integer
        publishedAt:
          type: string
          format: date-time
          nullable: true
        scheduledAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
        redirectCapture:
          description: >-
            Issue #787 (sibling of #784) — present ONLY on a `PATCH
            /api/v1/blog/pages/{id}` response whose body actually changed
            `slug` (absent otherwise, including on `GET`/`POST` and every
            lifecycle action). Reports what `captureUrlChangeRedirect`
            (ADR-0039) did with the resulting old->new path change, gated by
            the tenant's own `url_change_auto_policy` (`POST
            /api/v1/seo/redirects/capture-url-change` is the same seam,
            driven here automatically instead of by a caller). A `rejected`
            or `invalid` outcome never fails the page update itself — the
            slug change is additive tooling around the edit, not a
            precondition for it.
          type: object
          required: [outcome]
          properties:
            outcome:
              type: string
              enum:
                [created, proposed, skipped, module_disabled, invalid, rejected]
            redirectId:
              description: Present only when outcome is "created" or "proposed".
              type: string
              format: uuid
            code:
              description: Present only when outcome is "rejected".
              type: string
              enum: [SOURCE_CONFLICT, REDIRECT_LOOP, REDIRECT_CHAIN_TOO_LONG]
            message:
              description: Present only when outcome is "rejected" — a human-readable reason no redirect was created.
              type: string
    BlogTermWriteInput:
      type: object
      properties:
        taxonomyType:
          type: string
          enum: [category, tag, channel, topic]
        parentId:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        slug:
          type: string
        description:
          type: string
          nullable: true
    BlogTerm:
      type: object
      required: [id, tenantId, taxonomyType, name, slug]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        taxonomyType:
          type: string
          enum: [category, tag, channel, topic]
        parentId:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        slug:
          type: string
        description:
          type: string
          nullable: true
    PortableTextSpan:
      type: object
      required: [_type, _key, text]
      properties:
        _type:
          type: string
          enum: [span]
        _key:
          type: string
        text:
          type: string
          description: PLAIN text — never markup.
        marks:
          type: array
          items:
            type: string
          description: >-
            Decorator names (strong, em, code) and/or the _key of an annotation
            declared in this block's own markDefs. A mark naming neither is
            refused at write time as a dangling reference.
    PortableTextLinkAnnotation:
      type: object
      required: [_type, _key, href]
      properties:
        _type:
          type: string
          enum: [link]
        _key:
          type: string
        href:
          type: string
          maxLength: 2048
          description: >-
            Relative, or http/https/mailto/tel. Checked by URL parsing rather
            than a pattern, so java\nscript: and JaVaScRiPt: are refused too.
    PortableTextNode:
      type: object
      required: [_type, _key]
      description: >-
        One node of a CLOSED Portable Text vocabulary (ADR-0100). _type is one
        of block, gallery, videoNews — anything else is refused at write time,
        which is what keeps "there is no field where arbitrary markup could
        live" true.
      properties:
        _type:
          type: string
          enum: [block, gallery, videoNews]
        _key:
          type: string
        style:
          type: string
          enum: [normal, h1, h2, h3, h4, h5, h6, blockquote]
        listItem:
          type: string
          enum: [bullet, number]
        level:
          type: integer
          minimum: 1
          maximum: 6
        children:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextSpan"
        markDefs:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextLinkAnnotation"
        items:
          type: array
          description: Gallery items — present when _type is gallery.
          items:
            type: object
        provider:
          type: string
          description: Present when _type is videoNews.
        videoId:
          type: string
          description: Present when _type is videoNews.
    PortableTextBody:
      type: array
      maxItems: 2000
      description: >-
        ADR-0100 — the canonical article body. contentJson.blocks is a derived,
        LOSSY projection of this, written only so ahliweb/awcms-astro keeps
        rendering until it reads this field directly.
      items:
        $ref: "#/components/schemas/PortableTextNode"
    BlogInstitution:
      type: object
      required: [id, tenantId, branch, name, slug]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        branch:
          type: string
          enum: [legislative, executive]
        name:
          type: string
        slug:
          type: string
        regionCode:
          type: string
          nullable: true
          description: >-
            Dotted idn_admin_regions code (62, 62.71, 62.71.01, 62.71.01.2001).
            Not a foreign key: that master is dataset-versioned and its ids change
            on every import, so the code is the only stable identifier across
            generations. A code no active dataset resolves degrades to "no region
            label" on render rather than failing the read.
        description:
          type: string
          nullable: true
        seoTitle:
          type: string
          nullable: true
        seoDescription:
          type: string
          nullable: true
    BlogInstitutionCreateInput:
      type: object
      required: [branch, name, slug]
      properties:
        branch:
          type: string
          enum: [legislative, executive]
        name:
          type: string
          maxLength: 150
        slug:
          type: string
          description: Lowercase alphanumeric segments separated by single hyphens.
        regionCode:
          type: string
          nullable: true
          pattern: '^[0-9]{2}(\.[0-9]{2}){0,2}(\.[0-9]{4})?$'
        description:
          type: string
          nullable: true
          maxLength: 500
        seoTitle:
          type: string
          nullable: true
          maxLength: 200
        seoDescription:
          type: string
          nullable: true
          maxLength: 320
    BlogInstitutionUpdateInput:
      type: object
      minProperties: 1
      properties:
        branch:
          type: string
          enum: [legislative, executive]
        name:
          type: string
          maxLength: 150
        slug:
          type: string
        regionCode:
          type: string
          nullable: true
          pattern: '^[0-9]{2}(\.[0-9]{2}){0,2}(\.[0-9]{4})?$'
        description:
          type: string
          nullable: true
          maxLength: 500
        seoTitle:
          type: string
          nullable: true
          maxLength: 200
        seoDescription:
          type: string
          nullable: true
          maxLength: 320
    BlogRevision:
      type: object
      required:
        [
          id,
          tenantId,
          resourceType,
          resourceId,
          revisionNumber,
          title,
          status,
          createdAt
        ]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        resourceType:
          type: string
          enum: [post, page]
        resourceId:
          type: string
          format: uuid
        revisionNumber:
          type: integer
        title:
          type: string
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: >-
            ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key
            is overwritten with the derived projection; other keys (notably
            awcmsAstro) are preserved.
        contentText:
          type: string
          description: >-
            ADR-0100 — DERIVED from bodyPortableText. Still returned (it is what
            the full-text index is built from) but no longer accepted on write.
          readOnly: true
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        excerpt:
          type: string
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        status:
          type: string
        changeNote:
          type: string
          nullable: true
        createdByTenantUserId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
    BlogSettings:
      type: object
      properties:
        blogTitle:
          type: string
          nullable: true
        blogDescription:
          type: string
          nullable: true
        defaultLocale:
          type: string
        defaultVisibility:
          type: string
          enum: [public, private, unlisted]
        postsPerPage:
          type: integer
        seoDefaultTitle:
          type: string
          nullable: true
        seoDefaultDescription:
          type: string
          nullable: true
        rssEnabled:
          type: boolean
        sitemapEnabled:
          type: boolean
        contentQualityChecklistPolicy:
          type: object
        socialPreviewFallbackImageMediaId:
          type: string
          format: uuid
          nullable: true
        socialPreviewContentImageFallbackEnabled:
          type: boolean
    BlogMenu:
      type: object
      description: |
        One navigation menu, as `GET /api/v1/blog/menus` returns it.

        It exists as a schema because that endpoint's response was documented as
        an array of bare `object` — which promises a consumer nothing at all.
        Any field could have been renamed or dropped and the frozen consumer
        contract would still have passed, because everything is a subset of
        "object". That is the same defect the list endpoint shipped once over
        `BlogPostSummary`, one step further along: not a wrong shape, but no
        shape.

        `key` is the stable identifier a template selects a menu by (`main`,
        `footer`); `name` is the editorial label and may be renamed at any time.
        A consumer that keys off `name` breaks the first time somebody fixes a
        typo.
      required: [id, tenantId, key, name, items, itemsTruncated]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        key:
          type: string
          description: Stable slug. What a consumer selects a menu by.
        name:
          type: string
          description: Editorial label. NOT an identifier.
        items:
          type: array
          maxItems: 200
          description: Already sorted by `sortOrder`. One level of nesting at most — an item whose parent itself has a parent is refused at write time.
          items:
            $ref: "#/components/schemas/BlogMenuItem"
        itemsTruncated:
          type: boolean
          description: |
            `true` when the menu holds more than the 200 items `items` is
            bounded at, and `items` is only the first 200 of them.

            **Do not save a truncated list back.** `PATCH /api/v1/blog/menus/{id}`
            REPLACES the whole item set, so writing back what you were shown
            would delete every item past the bound. Only a menu written before
            the 200-item cap existed can report `true`; on a write response it
            is always `false`, because the request that produced it was itself
            capped.

            Ties are broken by `id`, so items sharing a `sortOrder` keep the
            same order between identical reads — without that the 200 kept
            would be an arbitrary 200.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
          description: Always null on this list — it filters soft-deleted menus out. Present because the row carries it, and named here rather than left as a surprise field.
    BlogMenuItem:
      type: object
      required: [id, label, linkType, sortOrder]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
          readOnly: true
          description: Returned on a read, never accepted on a write — the tenant comes from the credential.
        menuId:
          type: string
          format: uuid
          readOnly: true
          description: Returned on a read, never accepted on a write — the item's menu is the one it was submitted with.
        parentItemId:
          type: string
          format: uuid
          nullable: true
        label:
          type: string
        linkType:
          type: string
          enum: [post, page, url]
        targetId:
          type: string
          format: uuid
          nullable: true
          description: Required for `post`/`page`. Its existence is deliberately NOT checked against the posts/pages tables, because a menu may point at something not created yet — so a consumer must treat a target that resolves to nothing as a normal state, not an error.
        url:
          type: string
          nullable: true
          description: Required for `url`. An absolute http(s) URL, validated at write time by the same check `canonicalUrl` uses.
        sortOrder:
          type: integer
    BlogWidget:
      type: object
      description: |
        One widget, as `GET /api/v1/blog/widgets` returns it. Its response was
        documented as an array of bare `object` too — see `BlogMenu` for why
        that is worse than a wrong shape.

        `bodyText` is PLAIN TEXT, not markup: the write path refuses unsafe HTML
        rather than sanitizing it, so a consumer must escape this before
        rendering and must not treat it as trusted HTML.
      required: [id, tenantId, position, title, bodyText, isActive, sortOrder]
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        position:
          type: string
          enum: [header, sidebar, footer, content_before, content_after]
        title:
          type: string
        bodyText:
          type: string
          description: Plain text. Escape before rendering.
        isActive:
          type: boolean
          description: Inactive widgets ARE returned by the list — filter on this rather than expecting the endpoint to hide them.
        sortOrder:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    BlogContentBlock:
      description: |
        One block of `content_json`. The vocabulary is closed: a value outside
        the six variants below is not stored and not rendered.

        This schema exists because the vocabulary previously lived only in a
        TypeScript type and one prose sentence, so every consumer re-derived it
        by reading. One did, and disagreed with it in three ways at once —
        silently, because a wrong block type raises no error anywhere. It
        renders wrongly, or the section disappears.

        Held to `blog-content/domain/content-block-rendering.ts`'s
        `CONTENT_BLOCK_TYPES` by `tests/content-block-contract.test.ts`, which
        also holds that constant to the renderer's own switch.

        Note two shapes that are easy to guess wrong:
          - Ordering is a FIELD on `list` (`ordered: true`), not a separate
            `ordered_list` type.
          - `gallery` and `video_news` carry NO `text` field. A renderer that
            falls back to "render `text` as a paragraph" drops them entirely.
      oneOf:
        - type: object
          required: [type, text]
          properties:
            type:
              type: string
              enum: [paragraph]
            text:
              type: string
        - type: object
          required: [type, level, text]
          properties:
            type:
              type: string
              enum: [heading]
            level:
              type: integer
              minimum: 1
              maximum: 6
            text:
              type: string
        - type: object
          required: [type, items]
          properties:
            type:
              type: string
              enum: [list]
            ordered:
              description: Absent or false means an unordered list. There is no separate `ordered_list` type.
              type: boolean
            items:
              type: array
              items:
                type: string
        - type: object
          required: [type, text]
          properties:
            type:
              type: string
              enum: [quote]
            text:
              type: string
        - type: object
          required: [type, items]
          properties:
            type:
              type: string
              enum: [gallery]
            items:
              type: array
              items:
                type: object
                required: [mediaType]
                properties:
                  mediaType:
                    type: string
                    enum: [image, video]
                  url:
                    description: Direct absolute URL. Legacy shape — absent when the item uses `mediaObjectId`.
                    type: string
                  mediaObjectId:
                    description: Verified media registry object id. Resolving it to a URL requires a media lookup; a consumer that cannot perform one must show a placeholder rather than dropping the item.
                    type: string
                    format: uuid
                  caption:
                    type: string
        - type: object
          required: [type, provider, videoId]
          properties:
            type:
              type: string
              enum: [video_news]
            provider:
              description: Allowlisted provider. Only `youtube` is recognised today; an unknown provider must not be turned into a URL by guesswork.
              type: string
            videoId:
              description: Provider video id, validated server-side against the provider's own id shape before storage.
              type: string
            title:
              type: string
            caption:
              type: string
            thumbnailMediaObjectId:
              type: string
              format: uuid
            durationSeconds:
              type: integer
            sourceLabel:
              type: string
    BlogContentJson:
      description: "Structured post/page body. `{ blocks: BlogContentBlock[] }` — never raw HTML, by construction: there is no block variant that carries markup."
      type: object
      properties:
        blocks:
          type: array
          items:
            $ref: "#/components/schemas/BlogContentBlock"
    BlogAdPlacement:
      type: object
      required: [placementType]
      properties:
        placementType:
          type: string
          enum: [global, widget, post, page]
        targetId:
          type: string
          format: uuid
          nullable: true
    BlogInternalTagLinkSettings:
      type: object
      required: [enabled, caseInsensitive, disabledTagIds]
      properties:
        enabled:
          type: boolean
        caseInsensitive:
          type: boolean
        disabledTagIds:
          type: array
          items:
            type: string
            format: uuid
    ContentQualityChecklistResult:
      type: object
      required: [applicable, passed, blockers]
      description: "notApplicableChecklistResult() when full-online R2-only mode is not active for the tenant (applicable=false, passed=true, blockers=[]) — otherwise a full evaluation."
      properties:
        applicable:
          type: boolean
        passed:
          type: boolean
        blockers:
          type: array
          items:
            type: object
            required: [ruleId, message]
            properties:
              ruleId:
                type: string
              message:
                type: string
        warnings:
          type: array
          items:
            type: object
    HomepageSectionConfig:
      description: "Shape depends on sectionType — headline: {postId}; latest_posts: {limit?, categorySlug?}; featured_posts/editor_picks: {postIds: []}; category_grid: {categorySlugs: [], postsPerCategory?}; gallery_block: {mediaObjectIds: [], caption?}. Validated server-side per sectionType; every id/slug must already exist for the same tenant, and gallery_block's mediaObjectIds must each be a verified R2 media object."
      type: object
      additionalProperties: true
    HomepageSectionCreateRequest:
      type: object
      required:
        - sectionKey
        - sectionType
        - config
      properties:
        sectionKey:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
        sectionType:
          type: string
          enum:
            - headline
            - latest_posts
            - featured_posts
            - editor_picks
            - category_grid
            - gallery_block
        title:
          type: string
          nullable: true
        config:
          $ref: "#/components/schemas/HomepageSectionConfig"
        sortOrder:
          type: integer
        isEnabled:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
    ComposedHomepageImage:
      type: object
      properties:
        url:
          type: string
        altText:
          type: string
          nullable: true
        width:
          type: integer
          nullable: true
        height:
          type: integer
          nullable: true
    ComposedHomepagePostCard:
      type: object
      properties:
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        publishedAt:
          type: string
          format: date-time
        image:
          allOf:
            - $ref: "#/components/schemas/ComposedHomepageImage"
          nullable: true
    ComposedHomepageGroup:
      type: object
      properties:
        heading:
          type: string
          nullable: true
          description: Category name for a grid column; null for one ungrouped list.
        slug:
          type: string
          nullable: true
          description: Category slug, so the consumer can link the column to its archive.
        posts:
          type: array
          items:
            $ref: "#/components/schemas/ComposedHomepagePostCard"
    ComposedHomepageSection:
      type: object
      properties:
        sectionKey:
          type: string
        sectionType:
          type: string
          enum:
            - headline
            - latest_posts
            - featured_posts
            - editor_picks
            - category_grid
            - gallery_block
        title:
          type: string
          nullable: true
        groups:
          type: array
          items:
            $ref: "#/components/schemas/ComposedHomepageGroup"
        images:
          type: array
          items:
            $ref: "#/components/schemas/ComposedHomepageImage"
        caption:
          type: string
          nullable: true
        fellBackToLatest:
          type: boolean
          description: True when curation resolved to nothing and the latest eligible articles were substituted.
    PublicAdPlacement:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Internal label. A consumer renders it as alt text only when the media object carries none.
        linkUrl:
          type: string
          nullable: true
        mediaPublicUrl:
          type: string
          description: The media registry's own server-generated URL. There is no client-supplied URL column on this table.
        mediaAltText:
          type: string
          nullable: true
        contentClass:
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
          description: "Editorial-disclosure classification (Issue #783). `standard` is a plain ad or organic content needing no disclosure; `advertorial` is editorial-styled content that is actually paid for; `sponsored` is content explicitly sponsored/underwritten by a third party. A consumer renders a disclosure label next to any placement that is not `standard`."
    PublicBlogPageSummary:
      type: object
      properties:
        title:
          type: string
        slug:
          type: string
        locale:
          type: string
        updatedAt:
          type: string
          format: date-time
    PublicBlogPageDetail:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: The derived block projection this repo's own renderer reads (ADR-0100).
        bodyPortableText:
          description: The canonical body. Empty until `bun run blog:portable-text:backfill` has run for this deployment.
        contentText:
          type: string
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        locale:
          type: string
        publishedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        visibility:
          type: string
          enum: [public, unlisted]
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        pageType:
          type: string
        resolvedMedia:
          type: object
          additionalProperties:
            type: object
            properties:
              publicUrl:
                type: string
              altText:
                type: string
                nullable: true
              mimeType:
                type: string
              width:
                type: integer
                nullable: true
              height:
                type: integer
                nullable: true
          description: Every media object the body references, resolved in one call. An id that no longer resolves is simply absent.
    HomepageSectionItem:
      type: object
      required:
        - id
        - tenantId
        - sectionKey
        - sectionType
        - config
        - sortOrder
        - isEnabled
        - createdAt
        - updatedAt
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        sectionKey:
          type: string
        sectionType:
          type: string
          enum:
            - headline
            - latest_posts
            - featured_posts
            - editor_picks
            - category_grid
            - gallery_block
        title:
          type: string
          nullable: true
        config:
          $ref: "#/components/schemas/HomepageSectionConfig"
        sortOrder:
          type: integer
        isEnabled:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    HomepageSectionUpdateRequest:
      description: sectionType cannot be changed after creation — omit it, do not send the old or a new value.
      type: object
      properties:
        title:
          type: string
          nullable: true
        config:
          $ref: "#/components/schemas/HomepageSectionConfig"
        sortOrder:
          type: integer
        isEnabled:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
    AdPlacementItem:
      type: object
      required:
        - id
        - tenantId
        - placementKey
        - name
        - mediaObjectId
        - mediaPublicUrl
        - mediaAltText
        - mediaPubliclyReferenceable
        - rotationMode
        - priority
        - isActive
        - contentClass
        - createdAt
        - updatedAt
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        placementKey:
          type: string
          enum:
            - header_banner
            - below_headline
            - homepage_middle
            - homepage_bottom
            - article_top
            - article_middle
            - article_bottom
            - sidebar_top
            - sidebar_middle
            - sidebar_bottom
            - category_archive_top
            - search_result_top
        name:
          type: string
        mediaObjectId:
          description: Must reference a verified (`verified`/`attached`) R2 media object belonging to this tenant (`awcms_news_media_objects`) — never a local path or arbitrary external URL.
          type: string
          format: uuid
        mediaPublicUrl:
          type: string
          nullable: true
          description: "Resolved from the media registry (ADR-0036) so a renderer never has to fetch the object separately. `null` when the media object has been soft-deleted."
        mediaAltText:
          type: string
          nullable: true
        mediaPubliclyReferenceable:
          type: boolean
          description: "Whether this ad's media object may appear on a public page — the server's own verdict, not a status to interpret. `false` also covers a soft-deleted object, so a consumer checking only this field cannot render one."
        linkUrl:
          description: Optional, possibly external, link — validated server-side as an absolute http(s) URL only.
          type: string
          nullable: true
        rotationMode:
          type: string
          enum:
            - latest
            - priority
            - random_safe
            - weighted
        priority:
          type: integer
          minimum: 0
        isActive:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        targetType:
          description: Targeting scope (ADR-0044). Orthogonal to `placementKey`, which says WHERE on the page the ad sits — this says WHICH pages the slot is filled on. Rendering a page returns its own targeted ads UNIONED with every `global` ad for that slot.
          type: string
          enum:
            - global
            - widget
            - post
            - page
        targetId:
          description: Required for `widget`/`post`/`page`, must be absent for `global`. Validated at write time as an existing, non-deleted resource of that type in this tenant; not a foreign key, so a target deleted later simply stops matching rather than erroring.
          type: string
          format: uuid
          nullable: true
        contentClass:
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
          description: "Editorial-disclosure classification (Issue #783). A property of this BOOKING, not of the referenced media object — the same creative can run as `standard` in one slot and `sponsored` in another. Defaults to `standard`."
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AdPlacementCreateRequest:
      type: object
      required:
        - placementKey
        - name
        - mediaObjectId
      properties:
        placementKey:
          type: string
          enum:
            - header_banner
            - below_headline
            - homepage_middle
            - homepage_bottom
            - article_top
            - article_middle
            - article_bottom
            - sidebar_top
            - sidebar_middle
            - sidebar_bottom
            - category_archive_top
            - search_result_top
        name:
          type: string
        mediaObjectId:
          type: string
          format: uuid
        linkUrl:
          type: string
          nullable: true
        rotationMode:
          type: string
          enum:
            - latest
            - priority
            - random_safe
            - weighted
          default: latest
        priority:
          type: integer
          minimum: 0
          default: 0
        isActive:
          type: boolean
          default: true
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        targetType:
          description: Targeting scope (ADR-0044). Omit for a site-wide ad.
          type: string
          enum:
            - global
            - widget
            - post
            - page
          default: global
        targetId:
          description: Required for `widget`/`post`/`page`, must be omitted for `global`.
          type: string
          format: uuid
          nullable: true
        contentClass:
          description: "Editorial-disclosure classification (Issue #783). Omit for a plain ad or organic content."
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
          default: standard
    AdPlacementUpdateRequest:
      type: object
      properties:
        placementKey:
          type: string
          enum:
            - header_banner
            - below_headline
            - homepage_middle
            - homepage_bottom
            - article_top
            - article_middle
            - article_bottom
            - sidebar_top
            - sidebar_middle
            - sidebar_bottom
            - category_archive_top
            - search_result_top
        name:
          type: string
        mediaObjectId:
          type: string
          format: uuid
        linkUrl:
          type: string
          nullable: true
        rotationMode:
          type: string
          enum:
            - latest
            - priority
            - random_safe
            - weighted
        priority:
          type: integer
          minimum: 0
        isActive:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        targetType:
          description: Targeting scope (ADR-0044). Must be sent whenever `targetId` is sent — the pair moves together or not at all, so that switching to `global` also clears the stored target.
          type: string
          enum:
            - global
            - widget
            - post
            - page
        targetId:
          description: Required for `widget`/`post`/`page`, must be omitted for `global`. Sending it without `targetType` is a validation error.
          type: string
          format: uuid
          nullable: true
        contentClass:
          description: "Editorial-disclosure classification (Issue #783)."
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
