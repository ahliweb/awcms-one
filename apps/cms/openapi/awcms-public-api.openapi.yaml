# GENERATED FILE -- do not edit by hand.
#
# This is the bundled, published OpenAPI contract, produced by
# `bun run openapi:bundle` (scripts/openapi-bundle.ts, Issue #182) from the
# source fragments in openapi/awcms-public-api.src.yaml and openapi/modules/*.openapi.yaml. Edit
# those files, then regenerate this one -- direct edits here are silently
# overwritten and never reviewed as the source of truth.
openapi: 3.1.0
info:
  title: AWCMS Public API
  version: 0.1.0
  description: |-
    REST contract for the AWCMS foundation modules (tenant-admin, profile-identity,
    identity-access, logging). Every endpoint that accepts a body enforces an
    application-level size cap (default 128 KiB) independent of any reverse-proxy
    limit; a body over the limit is rejected with `413 Payload Too Large` /
    `PAYLOAD_TOO_LARGE`, using the same envelope as every other error response.
servers:
  - url: http://localhost:4321
    description: Local development
tags:
  - name: Foundation
    description: Foundation and platform endpoints.
  - name: Tenant Admin
    description: Tenant, office, tenant settings, and the one-time setup wizard.
  - name: Tenant Domains
    description: Tenant domain/subdomain mapping for host-based public routing (tenant_domain module, ported from awcms-micro) — tenant-scoped, RLS-protected CRUD over hostname mappings plus verification (dns_txt/dns_cname/file/manual) and primary-host selection. A hostname only serves a tenant once it is verified and active; the optional DNS provider adapter is env-gated and never stores a provider credential in the database. verify and set-primary change which host answers for a tenant, so both are ABAC-gated, idempotency-keyed, and audited.
  - name: Identity & Access
    description: Login identity, session authentication, and tenant user membership.
  - name: Profile Identity
    description: Canonical person/organization profile lifecycle, identifiers, and entity links.
  - name: Logging & Audit
    description: Cross-module audit trail (awcms_audit_events) and its read API.
  - name: Module Management
    description: Database-backed module registry, tenant module lifecycle, settings, permission sync, jobs, and health.
  - name: Sync Storage
    description: "Offline-first sync node registration, HMAC-signed push/pull, conflict tracking, and the object sync upload queue. Server-to-node replication is DECLARED EMPTY, not missing (ADR-0077): POST /api/v1/sync/pull reads awcms_domain_events — this repo's one transactional outbox — filtered by an explicit allow-list of replicable event types that currently contains none, so it answers 200 with an empty list. A node polling it is up to date with everything the server has chosen to replicate, which today is nothing. Two things block the first entry, both stated in src/modules/sync-storage/domain/sync-replication.ts: a per-event-type payload projection (a node is HMAC-authenticated, not a session), and commit-visibility safety (event_sequence is assigned at INSERT and visible at COMMIT, so a plain cursor can skip a late-committing event). The push direction (node to server) is fully implemented and unaffected. The operation's own description is unchanged because the pre-migration contract snapshot is frozen, so this notice lives here."
  - name: Workflow Approval
    description: Managed, versioned graph-based approval workflows — definition lifecycle, approval inbox/decisions, delegation, escalation, and administrative recovery.
  - name: Email
    description: Provider-neutral transactional email — template management, bulk announcement/notification enqueue, delivery-queue diagnostics/cancel, and suppression list.
  - name: Management Reporting
    description: Generic management reporting views (tenant activity, access/audit summary, sync health, module usage, email queue health) built as live read-aggregations over the foundation modules' tables.
  - name: Reporting Projections
    description: Module-contributed read-model projection extension to Management Reporting — list registered projection descriptors with live snapshot/freshness status, trigger/resume/cancel an idempotent full rebuild, trigger an on-demand reconciliation against a source control total, and manage/trigger/download scheduled exports (manifest/checksum/expiry, secure tenant-scoped download). A projection is a DERIVED read model, never an authorization source of truth — every operation independently re-checks RBAC/ABAC.
  - name: Domain Event Runtime
    description: Transactional, versioned domain-event outbox and dispatcher admin API — read-only inspection of outbox events and per-consumer deliveries (redacted payload projections only), permission-gated/reason-required/idempotent/audited replay of a dead-lettered delivery, and per-tenant pause/resume of a registered consumer.
  - name: Theming
    description: Tenant-selectable presentation (ADR-0034 Fase 3 — the first website module in the base). Select a trusted, reviewed, build-time theme and configure it by DATA only (design tokens, slot variants, media asset ids, section order, nav placement) — no uploaded code, no arbitrary templates. Every token value is validated by rejection against strict CSS grammars; published versions are immutable; publish/rollback/retire are ABAC-gated, idempotency-keyed, and audited.
  - name: News Media
    description: Direct-to-R2 presigned upload flow for news images (media_library module, ADR-0036 ownership inversion — the registry moved out of the retired news_portal module and the tag/path names were deliberately kept) — create an upload session (server-generated object key + short-lived presigned PUT URL), finalize (real R2 GET + magic-byte MIME sniffing + server-side SHA-256 checksum, never a bare HEAD), and cancel a still-pending_upload session. R2 credentials are never exposed to the browser; only a scoped, expiring presigned URL is returned.
  - name: Blog Content
    description: Tenant-scoped blog/content administration (blog_content module, ported from awcms-mini) — posts and pages with their full lifecycle (draft → review → scheduled/published → archived, soft delete/restore/purge), hierarchical categories/tags, append-only revision history (restore APPENDS a revision, never overwrites), PostgreSQL full-text search, presentation/monetization extensions (templates, hierarchical menus, position-based widgets, advertisements), per-tenant blog settings, internal tag-link policy, and the editorial content-quality checklist. The public, anonymous reader surface (`/blog/{tenantCode}/...` index/detail/archive/search/feed/sitemap, ADR-0009) is served by Astro text/html/xml routes and is deliberately NOT part of this REST contract. Publish/unpublish/purge and settings writes are ABAC-gated, idempotency-keyed, and audited.
  - name: News Portal Homepage Sections
    description: Editorial homepage section composer (blog_content module — absorbed from the retired news_portal module by ADR-0044, which moved ownership without renaming the paths or this tag) — tenant-scoped, RLS-protected CRUD for configurable homepage sections (headline, latest_posts, featured_posts, editor_picks, category_grid, gallery_block). config shape is validated per sectionType server-side; every referenced post/category/media object must already exist for the same tenant (and gallery_block media must be a verified R2 media object). sectionType is immutable after creation; reordering is just a patchable sortOrder field.
  - name: News Portal Ad Placements
    description: R2-only advertisement placement presets for the news portal (blog_content module — absorbed from the retired news_portal module by ADR-0044, which moved ownership without renaming the paths or this tag) — tenant-scoped, RLS-protected CRUD for ads assigned to a fixed set of placement keys. mediaObjectId must reference a verified R2 media object belonging to the same tenant — never a local path or arbitrary external image URL. linkUrl is optional and may be external, but is validated server-side as an absolute http(s) URL only.
  - name: Visitor Analytics
    description: "Privacy-first human visitor statistics for admin and public routes (visitor_analytics module, ported from awcms-micro) — the anonymous public visit-ingest beacon plus the authenticated, ABAC-guarded read API (summary/realtime/sessions/events/pages/devices/locations/security), tenant analytics settings, and the high-risk retention purge. Collection is OFF until an operator opts in, and raw IP/user-agent/geolocation are each independently disabled unless explicitly enabled: visitor identifiers are stored as salted HMAC-SHA256 hashes, never raw. The retention purge is idempotency-keyed, critically audited, and refused while a data_lifecycle legal hold is active."
  - name: Indonesia Regions
    description: "Versioned master data for Indonesia's administrative hierarchy — province / regency-city / district / village (idn_admin_regions module, ADR-0046) — for address forms, coverage mapping, and regional reporting. Read-only lookup with tier/parent/name filters and keyset pagination, plus the imported dataset versions and their upstream provenance. The rows are GLOBAL reference data (identical for every tenant, no tenant_id, no RLS), but every endpoint still requires a session, a tenant context, and a permission grant: what is global is the row, not the authorization. Importing is deliberately NOT in this contract — it is a worker job reading a repo-vendored dump; what IS here are the two audited, idempotency-keyed lifecycle actions (activate, rollback) that decide which imported version the platform serves. The data is a third-party community packaging of the Kepmendagri decree, NOT an official Kementerian Dalam Negeri API or export, and that caveat is returned in the dataset response body rather than left to documentation."
  - name: Data Lifecycle
    description: "Module-contributed high-volume table registry and safe lifecycle engine (data_lifecycle module, ADR-0037, ported from awcms-micro) — read the code-declared retention/partition/archive/purge descriptors and past run history, plan a dry run, and manage legal holds. Real archive/purge execution is deliberately NOT exposed over HTTP; it runs only as a bounded worker job. A legal hold overrides ordinary retention and is non-bypassable: it is enforced through a port that logging and visitor_analytics consume at their own purge composition roots, and placing/releasing one is a maker-checker SoD-guarded, audited action. The registry endpoint returns code-declared metadata only, never row contents."
  - name: SEO & Distribution
    description: Tenant SEO defaults for the central metadata renderer + public discovery/syndication surfaces (seo_distribution module, ADR-0038 discovery scope) — read/update site identity, default social/Organization images, Twitter handle, the tenant-wide noindex switch, and feed/sitemap config. The public robots.txt/sitemap/feed routes themselves are unauthenticated Astro XML/text routes (not part of this OpenAPI contract), host-resolved and cache-validated. config.update is high-risk (rewrites the public metadata/indexability surface), idempotency-keyed, and audited.
  - name: Form Drafts
    description: Generic, domain-agnostic server-side draft store for multi-step forms (form_drafts module) — tenant-scoped, RLS-protected create/read/update/submit/delete of an opaque JSONB payload plus the wizard coordinates needed to resume it. The payload is size-bounded and rejected outright (never silently redacted) when any key at any nesting depth resembles a secret — password/token/secret/credential/apiKey/privateKey — so a caller can never mistake a stripped field for a saved one. What a payload MEANS is owned by the module that created it (moduleKey/wizardKey), never by this one. submit is high-risk (it hands the payload to a domain action), idempotency-keyed, and audited; create deliberately is not, since a retry costs one deletable scratch row.
  - name: Site Search
    description: "Tenant-scoped, cross-content PostgreSQL full-text search over PUBLISHED public website content (site_search module, ADR-0040) — the public, anonymous, host-resolved query/suggest endpoints plus the admin index status/reconcile/rebuild, tenant search configuration, and failed-item diagnostics API. The index is a projection of public content only and is NEVER an authorization source: publication state is enforced at the source-to-index boundary, so a draft/private/deleted/scheduled resource is never even read into it. Query text is always a bound parameter into websearch_to_tsquery (no SQL injection) and snippets are HTML-escaped before the <mark> highlights are inserted (no XSS). index.rebuild is high-risk (delete + re-extract every document); index.reconcile is an idempotent, fully regenerable sync — both are idempotency-keyed and audited, as is settings.update, which changes what the public surface returns."
  - name: Newsletter
    description: "Per-tenant newsletter subscriber list (newsletter module, Issue #598, ADR-0103, PRD §22/§30). The email module could already SEND mail; there was no list a reader could JOIN. All three operations are anonymous — a reader has no account, and PRD §30 forbids making them get one to stop receiving mail — with the tenant resolved from the request HOST rather than a header, so a caller cannot choose whose list they are writing to. Every one answers the SAME neutral body for every outcome, because a distinguishing response turns a public endpoint into a way to ask whether a named person subscribes to a particular newsroom. Double opt-in: consent is recorded when the confirmation link is FOLLOWED, never at submission. Both tokens are stored hashed because both are bearer credentials, and the unsubscribe token is stable for the row's lifetime because it appears in the footer of every message the subscriber receives."
  - name: Site Profile
    description: "Per-tenant SITE CHROME (site_profile module, Issue #596, ADR-0102) — the masthead tagline, footer copyright line, logo and favicon, editorial address, contact email/phone/WhatsApp, and social profile links that every public page renders. Before it, a footer, masthead, contact page and Organization JSON-LD node all had to hard-code the publisher's identity in frontend source, which made a second tenant impossible without a fork. The boundary against seo_distribution is deliberate: awcms_seo_tenant_settings keeps what CRAWLERS see (og:site_name, the JSON-LD Organization node, the default og:image) because each is an SEO output consumed by a meta-tag renderer, while this module owns what PEOPLE read. Nothing is duplicated across the two, so no value can drift, and consumers are never asked to know the split — GET /api/v1/site-profile/composed merges both halves for build clients. Social link URLs are REFUSED rather than sanitized unless absolute http(s), because they are rendered as <a href> on every public page. read and update are separately grantable: changing what every page's contact block says is a different power from reading it. Nothing here is anonymous — 'public read' means the public site's BUILDER can read it, not that anyone can."
  - name: Comments
    description: "Tenant-scoped, moderation-first commenting over PUBLISHED, PUBLIC commentable resources (comments module, ADR-0041) — the public, anonymous, host-resolved submit/list/reply/edit/report/delete-request surface plus the ABAC-guarded admin moderation queue, per-decision transitions, bulk moderation, and tenant comment configuration. A comment is only ever accepted against, or shown on, a resource that satisfies its owning module's declarative publicationFilter, so a draft/private/deleted/scheduled resource never receives or exposes comments, and the comment surface is NEVER an authorization source for the underlying resource. Bodies are stored as raw plain text and HTML-escaped on render (no stored HTML, therefore no stored XSS), permitting only http(s) autolinks with rel=nofollow ugc noopener noreferrer. The public list returns approved rows only and never moderation metadata. Public submit responses are deliberately uniform: an anti-abuse block, an unresolved resource, and an accepted-but-pending comment all return the same neutral body, so the endpoint cannot be used as an oracle for blocked terms or for unpublished content. moderation.approve/restore/delete and settings.update are high-risk, idempotency-keyed, and audited with a reason code."
  - name: Push Delivery
    description: "Transactional outbox for device push notifications (push_delivery module, ADR-0074) — the caller's own device registration/revocation plus the operator's queue diagnostics, message cancel, and end-to-end delivery probe. It is a SECOND outbox on purpose: domain-event-runtime calls its consumers INSIDE the claim transaction by design, and ADR-0006 forbids the external HTTP call a push provider needs from inside a transaction. Two transports, neither of which costs a client byte or a CSP origin: Web Push (RFC 8030/8291/8292, VAPID) to browsers — chosen over the FCM Web SDK, which is 2.1x the per-file asset budget and demands three third-party origins the CSP does not have — and FCM HTTP v1 server-to-Google for native clients. Managing one's own device is self-service (the subject is the caller and no recipient is accepted); everything that touches another person's rows or makes the deployment emit traffic goes through the ABAC chokepoint. Endpoints are credential-grade and are stored hashed/masked, projected raw by exactly one function immediately before a provider call."
paths:
  /api/v1/abac/policies:
    get:
      operationId: listAbacPolicies
      tags:
        - Identity & Access
      summary: List the current tenant's ABAC policies (seeded-empty by default; built-in rules apply).
      responses:
        "200":
          description: The tenant's ABAC policies (limit 100).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/AbacPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createAbacPolicy
      tags:
        - Identity & Access
      summary: Author a new ABAC policy for the current tenant (high-risk access-control change; audit-logged).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - policyCode
                - effect
              properties:
                policyCode:
                  type: string
                  description: Unique per tenant. Starts alphanumeric; letters, digits, '.', '_', '-' only (max 100 chars).
                effect:
                  type: string
                  enum:
                    - allow
                    - deny
                description:
                  type: string
                  nullable: true
      responses:
        "201":
          description: Policy created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AbacPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: policyCode is already taken in this tenant (POLICY_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/abac/policies/{id}:
    patch:
      operationId: updateAbacPolicy
      tags:
        - Identity & Access
      summary: Update an ABAC policy's effect/description and/or enable-disable it (high-risk; audit-logged).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        description: Provide at least one of effect, description, isActive. A null description clears it.
        content:
          application/json:
            schema:
              type: object
              properties:
                effect:
                  type: string
                  enum:
                    - allow
                    - deny
                description:
                  type: string
                  nullable: true
                isActive:
                  type: boolean
      responses:
        "200":
          description: Policy updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AbacPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/assignments:
    post:
      operationId: createRoleAssignment
      tags:
        - Identity & Access
      summary: Assign a role to a tenant user (high-risk, audited, idempotent).
      description: Grants a role to a tenant user. Idempotent at the DB unique index — a repeat assign returns 409. Gated on `identity_access.access_control.assign`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantUserId
                - roleId
              properties:
                tenantUserId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: The created assignment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      tenantUserId:
                        type: string
                        format: uuid
                      roleId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The role is already assigned to this tenant user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deleteRoleAssignment
      tags:
        - Identity & Access
      summary: Revoke a role from a tenant user (high-risk, audited).
      description: Removes a role assignment. 404 when no such assignment exists. Gated on `identity_access.access_control.assign`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantUserId
                - roleId
              properties:
                tenantUserId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: The assignment was removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      removed:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/delegated-grants:
    get:
      operationId: listDelegatedGrants
      tags:
        - Identity & Access
      summary: List delegated-access grants, live and historical.
      description: 'ADR-0090. Revoked and expired grants are listed too: "who could see our data last March, and until when" is the question an audit asks, and a list of only live grants answers a different one. The redemption code is never returned — its column is not even selected.'
      responses:
        "200":
          description: Every grant this tenant has approved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      grants:
                        type: array
                        items:
                          $ref: "#/components/schemas/DelegatedAccessGrant"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: approveDelegatedAccess
      tags:
        - Identity & Access
      summary: Approve delegated access for a partner, at a role you choose.
      description: ADR-0090. Mints a single-use redemption code, returned EXACTLY ONCE in this response and never readable again. The role must already exist in this tenant and may not be a system role, so `owner` cannot be delegated; the grant may last at most 30 days. Guarded by `partner_access.assign`, because what this does is hand a role to somebody outside the organisation.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - partnerTenantId
                - roleId
                - purpose
                - expiresAt
              properties:
                partnerTenantId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
                  description: An existing, non-system role in THIS tenant.
                purpose:
                  type: string
                  maxLength: 500
                  description: Required and never defaulted. "Why does this vendor have access" is the first question an audit asks, and a blank answer supplied by the system reads as an answer.
                expiresAt:
                  type: string
                  format: date-time
      responses:
        "201":
          description: Approved. `accessCode` appears here and nowhere else, ever.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      grantId:
                        type: string
                        format: uuid
                      accessCode:
                        type: string
                        description: Returned once. Hand it to the partner out of band.
                      codeExpiresAt:
                        type: string
                        format: date-time
                      expiresAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No such partner engagement, or no such role in this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/delegated-grants/{id}:
    delete:
      operationId: revokeDelegatedAccess
      tags:
        - Identity & Access
      summary: Revoke delegated access immediately.
      description: ADR-0090. The grant dies, the membership it printed goes inactive, and every session on it is revoked — one transaction, no ordering that can leave one of the three behind. A grant that was already revoked answers 404, which is all the caller is owed.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: reason
          in: query
          required: false
          schema:
            type: string
            maxLength: 500
      responses:
        "200":
          description: Revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      revoked:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/evaluate:
    post:
      operationId: accessEvaluate
      tags:
        - Identity & Access
      summary: "Reflect the ABAC decision for the caller's own access on a hypothetical request (Issue #179)."
      description: Returns what `evaluateAccess` would decide for the CALLER'S OWN access against the tenant's current active ABAC policies. Requires a valid session but no specific permission. The decision is recorded to the ABAC decision log.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AccessEvaluateRequest"
      responses:
        "200":
          description: The evaluated decision.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AccessEvaluateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/access/machine-credentials:
    get:
      operationId: accessListMachineCredentials
      tags:
        - Identity & Access
      summary: List this tenant's machine credentials (never their secrets).
      description: Gated on `identity_access.machine_credentials.read`. Returns derived `active`/`expired`/`revoked` status plus `lastUsedAt`, so a leak can be traced without any secret material leaving the server.
      responses:
        "200":
          description: The tenant's machine credentials, newest first (limit 200).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/MachineCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: accessIssueMachineCredential
      tags:
        - Identity & Access
      summary: Issue a machine credential (high-risk; audit-logged).
      description: |
        Mints a bearer for a non-human caller, bound to an existing tenant user (a service account), and returns the plaintext token EXACTLY ONCE — no endpoint can return it again.

        The credential AUTHENTICATES only: every request it makes still passes module-enabled, RBAC, ABAC (default-deny) and SoD. `allowedPermissionKeys` NARROWS — effective permissions are the intersection with what the service account holds, so granting that account another role never widens an already-issued credential.

        TWO CLASSES, TWO PERMISSIONS. Omit `allowedWriteActions` and this is the read-only credential of ADR-0049 §3, gated on `identity_access.machine_credentials.create`; requests it makes are refused unless the action is `read`. Name write actions and it is the ADR-0092 write class, gated on `identity_access.machine_credentials_write.create` INSTEAD — a separate key so that opening this class does not hand write-minting authority to every role that already holds `create`.

        The write class is narrower in every other direction: only actions in the code ceiling (`create`, `update` — never a high-risk one), at least one `allowedIpCidrs` entry, at most 30 days, and a request whose caller IP is unknown is refused rather than exempted.

        Not idempotency-keyed, deliberately: replaying the response would mean persisting the plaintext token.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IssueMachineCredentialRequest"
      responses:
        "201":
          description: The credential, plus its plaintext token (shown once).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      credential:
                        $ref: "#/components/schemas/MachineCredential"
                      token:
                        type: string
                        description: The plaintext bearer. Shown once; only its hash is stored.
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Request body exceeded the endpoint's size ceiling.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The issuance request failed validation (VALIDATION_FAILED); `error.details` lists every offending field.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/machine-credentials/{id}/revoke:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: accessRevokeMachineCredential
      tags:
        - Identity & Access
      summary: Revoke a machine credential (high-risk; audit-logged).
      description: Effective on the credential's very next request, because authentication reads the same row. Gated on `identity_access.machine_credentials.revoke` — separate from `create` so a leak can be stopped by someone who cannot mint one. Re-revoking returns 409 rather than a silent success.
      responses:
        "200":
          description: The revoked credential.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      credential:
                        $ref: "#/components/schemas/MachineCredential"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The credential is already revoked (ALREADY_REVOKED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/modules:
    get:
      operationId: listAccessModules
      tags:
        - Module Management
      summary: The permission catalog grouped by module (read-only).
      responses:
        "200":
          description: Permission catalog.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      modules:
                        type: array
                        items:
                          type: object
                          properties:
                            moduleKey:
                              type: string
                            activityCode:
                              type: string
                            action:
                              type: string
                            description:
                              type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/access/partner-engagements:
    get:
      operationId: listPartnerEngagements
      tags:
        - Identity & Access
      summary: List the partners that reach this tenant.
      description: ADR-0089. The customer's authoritative view of every partnership into their own tenant. The partner's mirror of this (`GET /api/v1/partner/tenants`) is a convenience served by a narrow SECURITY DEFINER function; this one is the record.
      responses:
        "200":
          description: The partnerships reaching this tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      engagements:
                        type: array
                        items:
                          $ref: "#/components/schemas/PartnerEngagement"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: engagePartner
      tags:
        - Identity & Access
      summary: Engage a partner for this tenant.
      description: 'ADR-0089 — the customer initiates, always. There is no partner-facing counterpart: a partner that could insert its own engagement would be handing itself reach. "Not a registered partner" and "no such tenant" answer identically, so the endpoint cannot be swept as a directory of the platform''s partners.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - partnerTenantId
              properties:
                partnerTenantId:
                  type: string
                  format: uuid
      responses:
        "201":
          description: The partner now reaches this tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      engagement:
                        $ref: "#/components/schemas/PartnerEngagement"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No such partner. Deliberately indistinguishable from "no such tenant" and from "that tenant is not a registered partner".
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: That partner is already engaged for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/partner-engagements/{id}:
    delete:
      operationId: severPartnerEngagement
      tags:
        - Identity & Access
      summary: Sever a partnership, killing every grant under it.
      description: "ADR-0089/ADR-0090. Every live delegated-access grant under the partnership is revoked in the SAME transaction — memberships deactivated, sessions killed. The ordering is enforced by a foreign key rather than by remembering: deleting the engagement while a grant still names it fails."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Severed, with the number of grants revoked with it.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      severed:
                        type: boolean
                      partnerTenantId:
                        type: string
                        format: uuid
                      revokedGrants:
                        type: integer
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/policies:
    get:
      operationId: accessListAbacPolicies
      tags:
        - Identity & Access
      summary: "List the tenant's dynamic ABAC (DSL) policies (Issue #179)."
      description: Reads every stored DSL policy for the tenant (active and inactive). Gated on `identity_access.abac_policies.read`.
      responses:
        "200":
          description: The tenant's DSL ABAC policies.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policies:
                        type: array
                        items:
                          $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: accessCreateAbacPolicy
      tags:
        - Identity & Access
      summary: Author a new dynamic ABAC (DSL) policy (high-risk; audit-logged; only valid DSL is stored).
      description: Validates the condition DSL fail-closed before any write, so an invalid policy can never be stored or enabled. Created disabled by default unless `isActive:true`. Gated on `identity_access.abac_policies.configure`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacDslPolicyWriteRequest"
      responses:
        "200":
          description: The created policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A policy with that policyCode already exists (RESOURCE_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/policies/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      operationId: accessGetAbacPolicy
      tags:
        - Identity & Access
      summary: "Read one dynamic ABAC (DSL) policy (Issue #179)."
      description: Gated on `identity_access.abac_policies.read`. 404 when the policy does not exist in this tenant.
      responses:
        "200":
          description: The policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      operationId: accessUpdateAbacPolicy
      tags:
        - Identity & Access
      summary: Replace a dynamic ABAC (DSL) policy (high-risk; audit-logged; only valid DSL is stored).
      description: Validates the condition DSL fail-closed before any write. Gated on `identity_access.abac_policies.configure`. 404 when the policy does not exist; 409 on a policyCode collision.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacDslPolicyWriteRequest"
      responses:
        "200":
          description: The updated policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A policy with that policyCode already exists (RESOURCE_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/access/policies/{id}/disable:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: accessDisableAbacPolicy
      tags:
        - Identity & Access
      summary: Disable a dynamic ABAC (DSL) policy (high-risk; audit-logged).
      description: Marks the policy inactive so the evaluator stops applying it (deactivate-not-delete). Gated on `identity_access.abac_policies.configure`. 404 when the policy does not exist.
      responses:
        "200":
          description: The disabled policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/policies/{id}/enable:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      operationId: accessEnableAbacPolicy
      tags:
        - Identity & Access
      summary: Enable a dynamic ABAC (DSL) policy (high-risk; audit-logged).
      description: Marks the policy active so the evaluator applies it. Gated on `identity_access.abac_policies.configure`. 404 when the policy does not exist.
      responses:
        "200":
          description: The enabled policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      policy:
                        $ref: "#/components/schemas/AbacDslPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/access/policies/simulate:
    post:
      operationId: accessSimulateAbacPolicy
      tags:
        - Identity & Access
      summary: "Read-only ABAC decision simulation/preview (Issue #179; audited, never mutates)."
      description: Returns what `evaluateAccess` would decide for a hypothetical subject/request/environment against the tenant's active policies, plus a per-policy structural trace (no attribute VALUES, no PII). Writes no decision log. Gated on `identity_access.abac_policies.analyze`; simulating a DIFFERENT existing tenant user additionally requires `identity_access.access_control.read`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AbacSimulationRequest"
      responses:
        "200":
          description: The simulated decision and per-policy trace.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AbacSimulationResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/collect:
    post:
      tags:
        - Visitor Analytics
      summary: Public visitor page-view beacon (anonymous)
      description: "PUBLIC, unauthenticated visit-ingest beacon. Resolves the tenant from the request body's `tenantCode` (the RLS-free tenant root), records a privacy-preserving page-view for `public`-area paths only (IP/user-agent stored as salted hashes; anonymous — no identity). Fire-and-forget: always 202 for a well-formed request whether or not anything was recorded (module disabled, unknown tenant, non-public/non-trackable path all still return 202 without leaking tenant existence). Requires no auth."
      operationId: analyticsCollect
      security: []
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CollectVisitBeaconRequest"
      responses:
        "202":
          description: Beacon accepted (recorded or intentionally ignored — never distinguished).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      accepted:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "413":
          description: Request body exceeds the size limit (BODY_TOO_LARGE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/analytics/devices:
    get:
      tags:
        - Visitor Analytics
      summary: Browser + device-type breakdown
      description: Gated by visitor_analytics.dashboard.read.
      operationId: analyticsDevices
      parameters:
        - name: range
          in: query
          required: false
          description: Time window for the aggregate. Defaults to 7d.
          schema:
            type: string
            enum:
              - 24h
              - 7d
              - 30d
              - 12m
            default: 7d
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Browser and device breakdown.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      range:
                        type: string
                      browsers:
                        type: array
                        items:
                          $ref: "#/components/schemas/NamedCount"
                      devices:
                        type: array
                        items:
                          $ref: "#/components/schemas/NamedCount"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/events:
    get:
      tags:
        - Visitor Analytics
      summary: List visit events (keyset-paginated)
      description: Gated by visitor_analytics.events.read. Newest first. Raw detail (ipHash/userAgentHash) is returned only when the caller ALSO holds visitor_analytics.raw_detail.read — otherwise those fields are null.
      operationId: analyticsEventsList
      parameters:
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a previous page's `nextCursor`.
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: A page of visit events.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      events:
                        type: array
                        items:
                          $ref: "#/components/schemas/VisitEvent"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/locations:
    get:
      tags:
        - Visitor Analytics
      summary: Country breakdown
      description: Gated by visitor_analytics.dashboard.read. Empty until geolocation enrichment is enabled (VISITOR_ANALYTICS_GEO_ENABLED + VISITOR_ANALYTICS_TRUST_CLOUDFLARE) — not an error, just no data yet.
      operationId: analyticsLocations
      parameters:
        - name: range
          in: query
          required: false
          description: Time window for the aggregate. Defaults to 7d.
          schema:
            type: string
            enum:
              - 24h
              - 7d
              - 30d
              - 12m
            default: 7d
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Country breakdown.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      range:
                        type: string
                      countries:
                        type: array
                        items:
                          $ref: "#/components/schemas/NamedCount"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/pages:
    get:
      tags:
        - Visitor Analytics
      summary: Top pages by human pageviews
      description: Gated by visitor_analytics.dashboard.read.
      operationId: analyticsPages
      parameters:
        - name: range
          in: query
          required: false
          description: Time window for the aggregate. Defaults to 7d.
          schema:
            type: string
            enum:
              - 24h
              - 7d
              - 30d
              - 12m
            default: 7d
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Top pages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      range:
                        type: string
                      pages:
                        type: array
                        items:
                          $ref: "#/components/schemas/NamedCount"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/realtime:
    get:
      tags:
        - Visitor Analytics
      summary: Online-now presence counts
      description: Gated by visitor_analytics.realtime.read. Sessions active within the online window (VISITOR_ANALYTICS_ONLINE_WINDOW_SECONDS), split by area.
      operationId: analyticsRealtime
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Realtime presence.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/RealtimeStats"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/retention/purge:
    post:
      tags:
        - Visitor Analytics
      summary: Purge visitor analytics data past retention
      description: "Gated by visitor_analytics.retention.purge. Destructive, high-risk: requires Idempotency-Key, audited `critical`. Deletes events past eventRetentionDays, clears session raw detail past rawDetailRetentionDays, deletes orphaned sessions, and deletes rollups past rollupRetentionDays."
      operationId: analyticsRetentionPurge
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Purge complete (or replay of a prior identical request).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/RetentionPurgeResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/analytics/security:
    get:
      tags:
        - Visitor Analytics
      summary: Bot/crawler traffic breakdown
      description: Gated by visitor_analytics.dashboard.read.
      operationId: analyticsSecurity
      parameters:
        - name: range
          in: query
          required: false
          description: Time window for the aggregate. Defaults to 7d.
          schema:
            type: string
            enum:
              - 24h
              - 7d
              - 30d
              - 12m
            default: 7d
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Bot traffic breakdown.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SecurityView"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/sessions:
    get:
      tags:
        - Visitor Analytics
      summary: List visitor sessions (keyset-paginated)
      description: Gated by visitor_analytics.sessions.read. Newest-active-first. Raw detail (ipAddress/ipHash/userAgentHash/loginIdentifierSnapshot) is returned only when the caller ALSO holds visitor_analytics.raw_detail.read — otherwise those fields are null.
      operationId: analyticsSessionsList
      parameters:
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a previous page's `nextCursor`.
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: A page of visitor sessions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      sessions:
                        type: array
                        items:
                          $ref: "#/components/schemas/VisitorSession"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/analytics/settings:
    get:
      tags:
        - Visitor Analytics
      summary: Read visitor analytics module settings
      description: Gated by visitor_analytics.settings.read. Thin wrapper around Module Management's generic per-tenant settings storage under this module's own permission.
      operationId: analyticsSettingsGet
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Effective module settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleSettingsView"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Visitor Analytics
      summary: Update visitor analytics module settings
      description: Gated by visitor_analytics.settings.update. Shallow JSON-merge patch. Secret-shaped keys/values are rejected before storage. Audited (changed key names only, never values).
      operationId: analyticsSettingsUpdate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Settings updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleSettingsView"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Request body exceeds the size limit (BODY_TOO_LARGE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/analytics/summary:
    get:
      tags:
        - Visitor Analytics
      summary: Aggregate visitor summary for a range
      description: Gated by visitor_analytics.dashboard.read. Human unique visitors, pageviews, bot pageviews, and top paths/browsers/devices/countries over the selected range.
      operationId: analyticsSummary
      parameters:
        - name: range
          in: query
          required: false
          description: Time window for the aggregate. Defaults to 7d.
          schema:
            type: string
            enum:
              - 24h
              - 7d
              - 30d
              - 12m
            default: 7d
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Aggregate summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AnalyticsSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/auth/delegated-access/redeem:
    post:
      operationId: redeemDelegatedAccess
      tags:
        - Identity & Access
      summary: Exchange a delegated-access code for a membership in the customer's tenant.
      description: "ADR-0090. Requires a LIVE SESSION in the caller's own tenant: the code authenticates nothing, and what proves who is redeeming is the global principal behind that session. It returns a MEMBERSHIP, not a session — ordinary sign-in or `POST /api/v1/auth/session/switch` works afterwards, because afterwards they are a member. Minting a session here would mean a second copy of the target tenant's entry policy, and a second copy is where the MFA gate goes quietly missing. Every refusal answers 404."
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
          description: The caller's OWN tenant — the one their session belongs to.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - targetTenantId
                - code
              properties:
                targetTenantId:
                  type: string
                  format: uuid
                  description: The customer tenant the grant was approved in.
                code:
                  type: string
                  description: The single-use code the customer handed over.
      responses:
        "200":
          description: The caller is now a delegated member of the target tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      tenantUserId:
                        type: string
                        format: uuid
                      nextStep:
                        type: string
                        enum:
                          - sign_in_to_tenant
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: "One shape for every refusal: unknown code, expired grant, revoked grant, wrong tenant, membership refused. A holder must not learn whether the code is real."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many attempts from this source. Carries `retry-after`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/invitations/{token}:
    get:
      operationId: getAuthInvitationPreview
      tags:
        - Identity & Access
      summary: Preview an invitation (public, token-bearing).
      security: []
      description: |-
        Returns the tenant's name, the inviter's name, and the expiry — and **never the invited address**. Whoever legitimately holds this link read that address in their own mailbox; whoever holds a stolen one did not.
        Unknown, revoked, already-accepted, expired and belonging-to-another-tenant all answer one identical `404`. **404, not 410**: `410 Gone` would tell a token holder that the token was once valid, which is an oracle worth having if you are working through addresses you scraped. The real reason is written to the tenant's audit trail, never to the response.
        Rate-limited through the shared limiter's source ceiling before any database work.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
        - name: token
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The invitation is live and may be accepted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/InvitationPreview"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          description: Rate limited (RATE_LIMITED). Carries `retry-after`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/invitations/{token}/accept:
    post:
      operationId: postAuthInvitationAccept
      tags:
        - Identity & Access
      summary: Accept an invitation, creating the membership (public, token-bearing).
      security: []
      description: |-
        Creates the profile, identity and tenant user, and materializes the roles the invitation carried. Those roles are read from `awcms_invitation_policies` — **never** from this request body, which declares no privilege field at all. A role that has since become `is_system`, been soft-deleted, or left the catalogue refuses the whole acceptance rather than granting a subset.
        **No session is issued.** The invitee signs in at `/login` afterwards, so the tenant's MFA policy, its SSO-only policy, and the login rate limit all still stand between the new account and a signed-in browser.
        Every refusal — unknown, revoked, already accepted, expired, wrong tenant, or an address that acquired an account in the meantime — answers the same `404`.
        Turnstile-gated with its own action, so a token solved on any other form cannot be spent here.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
        - name: token
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - password
              properties:
                password:
                  type: string
                  minLength: 8
                displayName:
                  type: string
                  maxLength: 120
                  description: Lets the invitee correct the name the inviter typed for them. Omitted, the inviter's version stands.
                turnstileToken:
                  type: string
      responses:
        "200":
          description: The membership exists. Sign in at /login.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      accepted:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: The request body exceeded the size limit (PAYLOAD_TOO_LARGE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Rate limited (RATE_LIMITED). Carries `retry-after`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/login:
    post:
      operationId: postAuthLogin
      tags:
        - Identity & Access
      summary: Authenticate with a login identifier and password; issues a session token.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - loginIdentifier
                - password
              properties:
                loginIdentifier:
                  type: string
                password:
                  type: string
                turnstileToken:
                  type: string
                  description: "Cloudflare Turnstile response token (Issue #186). Required only on a full-online deployment where Turnstile is enabled; absent and ignored on every LAN/offline deployment. Verified server-side before password verification."
      responses:
        "200":
          description: Session issued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many login attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/logout:
    post:
      operationId: postAuthLogout
      tags:
        - Identity & Access
      summary: Revoke the current session.
      responses:
        "200":
          description: Logged out.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      loggedOut:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/me:
    get:
      operationId: getAuthMe
      tags:
        - Identity & Access
      summary: Current authenticated identity.
      responses:
        "200":
          description: Current identity.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      identityId:
                        type: string
                        format: uuid
                      loginIdentifier:
                        type: string
                      profileId:
                        type: string
                        format: uuid
                      status:
                        type: string
                      lastLoginAt:
                        type: string
                        format: date-time
                        nullable: true
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/mfa/admin/reset:
    post:
      operationId: postAuthMfaAdminReset
      tags:
        - Identity & Access
      summary: Administratively reset another user's MFA factor (requires reason).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - identityId
                - reason
              properties:
                identityId:
                  type: string
                  format: uuid
                reason:
                  type: string
      responses:
        "200":
          description: Target MFA reset.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      reset:
                        type: boolean
                      hadFactor:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/auth/mfa/policy:
    get:
      operationId: getAuthMfaPolicy
      tags:
        - Identity & Access
      summary: Read the tenant MFA enforcement policy.
      responses:
        "200":
          description: Tenant MFA policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantMfaPolicy"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      operationId: putAuthMfaPolicy
      tags:
        - Identity & Access
      summary: Set the tenant MFA enforcement level.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - enforcementLevel
              properties:
                enforcementLevel:
                  type: string
                  enum:
                    - optional
                    - required_for_privileged
                    - required_for_all
      responses:
        "200":
          description: Policy updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantMfaPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/auth/mfa/recovery-codes/regenerate:
    post:
      operationId: postAuthMfaRecoveryRegenerate
      tags:
        - Identity & Access
      summary: Invalidate existing recovery codes and issue a fresh single-use set.
      responses:
        "200":
          description: New recovery codes shown once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      recoveryCodes:
                        type: array
                        items:
                          type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: No active MFA.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/status:
    get:
      operationId: getAuthMfaStatus
      tags:
        - Identity & Access
      summary: Current identity's MFA enrollment state.
      responses:
        "200":
          description: MFA status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      enabled:
                        type: boolean
                      factorType:
                        type: string
                        enum:
                          - totp
                      activatedAt:
                        type: string
                        format: date-time
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/mfa/step-up:
    post:
      operationId: postAuthMfaStepUp
      tags:
        - Identity & Access
      summary: Raise the current session to aal2 for high-risk actions.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                code:
                  type: string
                recoveryCode:
                  type: string
      responses:
        "200":
          description: Session stepped up to aal2 (rotated when rising from aal1).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assuranceLevel:
                        type: string
                        enum:
                          - aal2
                      rotated:
                        type: boolean
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many verification attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: MFA misconfigured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/totp/disable:
    post:
      operationId: postAuthMfaDisable
      tags:
        - Identity & Access
      summary: Self-service disable of the current identity's MFA factor.
      responses:
        "200":
          description: MFA disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      disabled:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: No active MFA to disable.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/totp/enroll/start:
    post:
      operationId: postAuthMfaEnrollStart
      tags:
        - Identity & Access
      summary: Begin TOTP enrollment; returns a one-time secret and otpauth URI.
      description: Authorized by EITHER a live session OR an enrollment grant (X-AWCMS-MFA-Enrollment-Token) issued by POST /auth/login when a tenant policy requires MFA for an identity without a factor.
      parameters:
        - name: X-AWCMS-MFA-Enrollment-Token
          in: header
          required: false
          description: Enrollment grant token from a login that returned MFA_ENROLLMENT_REQUIRED; used in place of a session.
          schema:
            type: string
      responses:
        "200":
          description: Pending factor created; secret shown once.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      secret:
                        type: string
                      otpauthUri:
                        type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: MFA already active for this account.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: MFA encryption key is missing/invalid (misconfigured).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/totp/enroll/verify:
    post:
      operationId: postAuthMfaEnrollVerify
      tags:
        - Identity & Access
      summary: Confirm a pending TOTP enrollment; activates the factor and returns recovery codes.
      description: Authorized by EITHER a live session OR an enrollment grant. When authorized via an enrollment grant (a login that returned MFA_ENROLLMENT_REQUIRED), the grant is consumed and a fresh aal2 session token is returned.
      parameters:
        - name: X-AWCMS-MFA-Enrollment-Token
          in: header
          required: false
          description: Enrollment grant token; used in place of a session to complete the "must enroll" login.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - code
              properties:
                code:
                  type: string
      responses:
        "200":
          description: Factor activated; recovery codes shown once. token/expiresAt/assuranceLevel are present only when enrolled via an enrollment grant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      activated:
                        type: boolean
                      recoveryCodes:
                        type: array
                        items:
                          type: string
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      assuranceLevel:
                        type: string
                        enum:
                          - aal2
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          description: MFA misconfigured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/mfa/totp/verify:
    post:
      operationId: postAuthMfaVerify
      tags:
        - Identity & Access
      summary: Complete a login paused with MFA_REQUIRED by submitting a TOTP or recovery code.
      description: Authenticated by possession of the mfaChallengeToken from POST /auth/login, not by a session. On success issues an aal2 session.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mfaChallengeToken
              properties:
                mfaChallengeToken:
                  type: string
                code:
                  type: string
                recoveryCode:
                  type: string
      responses:
        "200":
          description: MFA verified; aal2 session issued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      assuranceLevel:
                        type: string
                        enum:
                          - aal2
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many verification attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: MFA misconfigured.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/password/change:
    post:
      operationId: changeOwnAuthPassword
      tags:
        - Identity & Access
      summary: Change your own password while signed in (self-service, no permission).
      description: |-
        The counterpart to /api/v1/auth/password/reset: that endpoint serves someone who CANNOT sign in and proves control of a mailbox, this one serves someone who is signed in and proves control of the credential. Self-service by construction — the subject is the session bearer and the route accepts no parameter naming anybody else.
        A fresh second factor is required ONLY from callers who have one enrolled. Requiring aal2 unconditionally would permanently prevent every user without MFA from changing their password, and the users most likely to need to are the ones who just learned it leaked. The current password is the re-authentication for everyone; the second factor is additional for those who can supply it.
        On success the password is replaced, the lockout counters are cleared (whoever supplied the current password proved control of the credential), and every OTHER session of that identity is revoked — the calling one survives, because a password change that signs you out of the tab you changed it in reads as a failure and the security property is unaffected.
        Rate limited on the SOURCE despite being authenticated: `currentPassword` is a guessable secret, so this is a credential-guessing surface whenever a session is used by someone who does not know the password behind it. An identifier-keyed bucket would instead let anyone reaching the endpoint hold one person's own password change hostage.
        Audited on both the success and the failure, with the device shape and IP pseudonym, and with no password-shaped attribute — not even a length.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - currentPassword
                - newPassword
              properties:
                currentPassword:
                  type: string
                newPassword:
                  type: string
                  minLength: 8
                  description: Must differ from currentPassword — an unchanged password is refused as a validation error, never answered as a no-op success.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Password changed; the other sessions were revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      changed:
                        type: boolean
                        enum:
                          - true
                      revokedSessionCount:
                        type: integer
                        minimum: 0
        "400":
          description: Validation failed, or currentPassword did not match (INVALID_CREDENTIALS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: MFA is enrolled and this session is not currently stepped up (STEP_UP_REQUIRED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The tenant policy signs this identity in through SSO; there is no password to change (PASSWORD_LOGIN_DISABLED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many password change attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/password/forgot:
    post:
      operationId: postAuthPasswordForgot
      tags:
        - Identity & Access
      summary: Request a password reset link (account-enumeration-safe).
      description: Always returns the same 200 body regardless of whether the identifier matched an eligible account. An unknown identifier, an inactive identity or tenant-user, an identity the tenant has taken off password login, and a successfully queued email are indistinguishable to the caller by response body, status code, or error code. Rate limited per client IP and tenant.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - loginIdentifier
              properties:
                loginIdentifier:
                  type: string
                turnstileToken:
                  type: string
                  description: "Cloudflare Turnstile response token (Issue #186), solved for the `password_reset` action. Required only on a full-online deployment where Turnstile is enabled; absent and ignored on every LAN/offline deployment."
      responses:
        "200":
          description: Request accepted. Says nothing about whether an account exists.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      requested:
                        type: boolean
                      message:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many password reset requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/password/reset:
    post:
      operationId: postAuthPasswordReset
      tags:
        - Identity & Access
      summary: Complete a password reset with a single-use token.
      description: On success the password is replaced, the lockout counters are cleared, the token is burned, and every session belonging to that identity is revoked. Every rejection — unknown token, expired, already used, the identity deactivated since issue, or password login disabled for it — returns the same `PASSWORD_RESET_INVALID` error, so the endpoint cannot be used to fingerprint the state of a token.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - token
                - newPassword
              properties:
                token:
                  type: string
                  description: The raw single-use token from the emailed link.
                newPassword:
                  type: string
                  minLength: 8
                turnstileToken:
                  type: string
                  description: "Cloudflare Turnstile response token (Issue #186), solved for the `password_reset` action. Required only on a full-online deployment where Turnstile is enabled."
      responses:
        "200":
          description: Password changed and all sessions revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      reset:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many password reset attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/preferences:
    get:
      operationId: getOwnDisplayPreferences
      tags:
        - Identity & Access
      summary: The caller's own UI locale, colour theme and time zone (self-service, no permission).
      description: |-
        ADR-0095. Self-service by construction — the subject is the session bearer and the route accepts no tenantUserId, so there is nobody else it could be pointed at. Deliberately UNPERMISSIONED, like GET /api/v1/auth/sessions: inventing a permission for "read your own language" would wall off the feature and install the latent-authz trap (an action nothing seeds denies everyone, tenant owner included).
        All three fields are nullable, and null means "not chosen" rather than a default: a null locale falls through to awcms_tenants.default_locale and then to Accept-Language, so a reader who never opened the switcher still gets the language their browser asked for, and a null timeZone renders UTC. `storable` is false when the identity has no linked principal (sql/112 leaves that legal), which is the one case where a choice cannot be made durable.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The caller's stored display preferences.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      locale:
                        type: string
                        nullable: true
                        enum:
                          - en
                          - id
                          - null
                        description: Stored UI locale, or null when not chosen.
                      theme:
                        type: string
                        nullable: true
                        enum:
                          - system
                          - light
                          - dark
                          - null
                        description: Stored colour theme, or null when not chosen.
                      timeZone:
                        type: string
                        nullable: true
                        description: "Stored IANA time zone, or null when not chosen (which renders UTC). Not enumerated: the zone list is tzdata's, ships with the runtime and changes several times a year, so an enum here would start refusing valid zones within months. sql/130 constrains only the shape for the same reason; Intl is the authority on whether a zone can be rendered."
                      storable:
                        type: boolean
                        description: False when this identity has no linked principal, so a choice can only be cookied for this browser.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: updateOwnDisplayPreferences
      tags:
        - Identity & Access
      summary: Store the caller's UI locale / colour theme (self-service, no permission).
      description: |-
        ADR-0095. Writes the durable copy against the caller's PRINCIPAL — the global person behind every tenant membership — and additionally sets the awcms_locale cookie, which outranks the stored value so the change is visible on the next render rather than the next login.
        A field that is ABSENT is left alone; a field explicitly null is RESET to "not chosen". The two cannot collapse: sql/128 withholds DELETE so a reset must be expressible as null, while a request mentioning only `locale` must not thereby wipe `theme`. Read and write happen in one transaction, so the unmentioned axis is carried forward rather than lost to an interleaving.
        Accepts JSON or application/x-www-form-urlencoded; the form variant answers 303 back to `return_to`, which is validated as a path-absolute same-origin reference and otherwise replaced with /admin.
        Unpermissioned for the same reason as GET above. Changing somebody ELSE's language is not a feature, so there is no permissioned sibling.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                locale:
                  type: string
                  nullable: true
                  enum:
                    - en
                    - id
                    - null
                theme:
                  type: string
                  nullable: true
                  enum:
                    - system
                    - light
                    - dark
                    - null
                timeZone:
                  type: string
                  nullable: true
                  description: IANA zone this deployment can render, or null to reset. Rejected with UNSUPPORTED_TIME_ZONE when Intl cannot resolve it.
          application/x-www-form-urlencoded:
            schema:
              type: object
              properties:
                locale:
                  type: string
                  enum:
                    - en
                    - id
                return_to:
                  type: string
                  description: Path-absolute same-origin path to return to.
      responses:
        "200":
          description: The preferences now in effect.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      locale:
                        type: string
                        nullable: true
                        enum:
                          - en
                          - id
                          - null
                      theme:
                        type: string
                        nullable: true
                        enum:
                          - system
                          - light
                          - dark
                          - null
                      persisted:
                        type: boolean
                        description: False when the identity has no linked principal — the cookie was still set.
        "303":
          description: Form submission accepted; redirect back to the validated return path.
          headers:
            Location:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many preference updates from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/preferences/locale:
    post:
      operationId: setDisplayLocaleCookie
      tags:
        - Identity & Access
      summary: Switch UI language for this browser only — works UNAUTHENTICATED.
      description: |-
        ADR-0095. Sets the awcms_locale cookie and touches no database at all.
        It exists separately from POST /api/v1/auth/preferences because the language switch has to work on /login and on the tenant-selection screen (ADR-0088) — the two places a reader most often discovers they are being shown the wrong language, and the two places where no session exists to hang a preference on. The self-service route requires a bearer and answers 401 without one, so it cannot serve that caller; this one can, and pays for it by never persisting (`persisted` is always false).
        Accepts JSON or application/x-www-form-urlencoded; the form variant answers 303 back to `return_to`, validated as a path-absolute same-origin reference and otherwise replaced with /admin.
        No CSRF token, deliberately: the whole effect is which of two message catalogs a reader is shown — cosmetic, immediately visible, reversible in one click — and the cookie is not HttpOnly, so same-origin script could set it anyway. There is no server state to corrupt.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - locale
              properties:
                locale:
                  type: string
                  enum:
                    - en
                    - id
          application/x-www-form-urlencoded:
            schema:
              type: object
              required:
                - locale
              properties:
                locale:
                  type: string
                  enum:
                    - en
                    - id
                return_to:
                  type: string
      responses:
        "200":
          description: The cookie was set for this browser.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      locale:
                        type: string
                        enum:
                          - en
                          - id
                      persisted:
                        type: boolean
                        enum:
                          - false
                        description: Always false — this route has no database access. Stated rather than omitted so a client comparing the two routes sees the difference in the payload.
        "303":
          description: Form submission accepted; redirect back to the validated return path.
          headers:
            Location:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/auth/profile:
    get:
      operationId: getOwnProfile
      tags:
        - Identity & Access
      summary: The caller's own display name and sign-in address (self-service).
      description: |-
        ADR-0096. Self-service by construction — it accepts no id, so the profile it reads is the one behind the calling session and there is nobody else it could be pointed at. Unpermissioned for the reason its siblings state: inventing a permission for "read your own name" would install the latent-authz trap (an action nothing seeds denies everyone, tenant owner included).
        `loginIdentifier` is returned unmasked. It is the caller's OWN address — the one they type to sign in — so masking it would disclose nothing and would make "am I signed in as the right account" unanswerable.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The caller's own profile.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      profileId:
                        type: string
                        format: uuid
                      displayName:
                        type: string
                      loginIdentifier:
                        type: string
                      publicBylineName:
                        description: ADR-0109 — the name this person's articles are published under, or `null` for "no byline" (the article keeps the organisation-level attribution).
                        type: string
                        nullable: true
                        maxLength: 120
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      operationId: updateOwnProfile
      tags:
        - Identity & Access
      summary: Change the caller's own display name (self-service, no permission).
      description: |-
        ADR-0096. Writes ONLY `display_name`, and only on the profile behind the calling session — the profile id is never accepted from the caller, so there is no id to tamper with and no ownership check that could be forgotten.
        Deliberately cannot change `legal_name` (a verification-relevant field: `verification_status` exists because a legal name is asserted and then checked, so letting the subject rewrite it would make the verification meaningless), `status`, `verification_status`, `risk_level`, or the identifiers. Those are administrative fields that happen to live on the same row, and a self-service endpoint that wrote them would be a privilege escalation wearing a profile editor's clothes.
        Changing the sign-in address is NOT here: that is account recovery and needs proof the new address is the caller's.
        This is a SEPARATE route from the permissioned `PATCH /api/v1/profiles/{id}` rather than a relaxed guard on it — an endpoint with two authorization modes forces every reader to prove which branch applies before they can say anything about its security.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - displayName
              properties:
                displayName:
                  type: string
                  minLength: 1
                  maxLength: 200
                publicBylineName:
                  description: |-
                    ADR-0109 — OPTIONAL, and the three states are distinct: ABSENT leaves the byline unchanged, `null` (or an empty string) clears it, a string sets it. Absence cannot mean "clear" — a PATCH that saved only a display name would delete the byline every time.
                    There is no administrative sibling for this field on purpose: an editor who could set somebody else's byline could publish an article under a colleague's name.
                  type: string
                  nullable: true
                  maxLength: 120
      responses:
        "200":
          description: The stored display name.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      profileId:
                        type: string
                        format: uuid
                      displayName:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many profile updates from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/register:
    post:
      operationId: postAuthRegister
      tags:
        - Identity & Access
      summary: Submit a self-registration request for admin review.
      description: Records a request; it NEVER creates an account. Accepts no password and no privilege field — approval creates the account with an unusable credential and emails a password-reset link. Returns `404` when `AUTH_SELF_REGISTRATION_ENABLED` is not `true`, indistinguishable from a route that does not exist. When enabled, an address that already has an account, an address with a request already pending, and a freshly recorded request all return the identical 200.
      security: []
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - displayName
                - loginIdentifier
              properties:
                displayName:
                  type: string
                  maxLength: 120
                loginIdentifier:
                  type: string
                  maxLength: 320
                  description: Must be an email address — the approval link is sent to it.
                turnstileToken:
                  type: string
                  description: "Cloudflare Turnstile response token (Issue #186), solved for the `register` action. Required only on a full-online deployment where Turnstile is enabled."
      responses:
        "200":
          description: Request accepted. Says nothing about whether the address is already registered or already pending.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      requested:
                        type: boolean
                      message:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          description: Self-registration is not enabled for this deployment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many registration attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/session:
    get:
      operationId: introspectAuthSession
      tags:
        - Identity & Access
      summary: Introspect the calling session — safe claims only (for a cross-origin BFF).
      description: |
        Cross-origin session introspection (ADR-0045 §3, ADR-0049 §7). Intended for `awcms-astro`'s BFF, which holds the session token server-side; a browser never calls it directly.

        Self-service: authorized by holding the session, not by a permission. Returns ONLY claims a portal header needs — never a token, token hash, password state, MFA secret/recovery code, or a raw email/phone identifier.

        Anti-oracle: a missing bearer, an unknown/expired/revoked session, a deactivated identity, and a machine credential presented here all produce the SAME 401. Rate-limited per source; `Cache-Control: private, no-store` on every path.
      responses:
        "200":
          description: Safe claims for the live session.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SessionIntrospection"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many introspection requests from this source (RATE_LIMITED); `Retry-After` carries the window.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/session-handoff/issue:
    post:
      operationId: issueSessionHandoffCode
      tags:
        - Identity & Access
      summary: Mint a one-time code letting a registered BFF obtain a session as the caller.
      description: |
        ADR-0050. The authenticated human asks for a short-lived (≤60 second), single-use code; a registered BFF then spends it server-to-server at `/api/v1/auth/session-handoff/redeem` and receives a session token for that person.

        **Self-service, not permission-gated.** The identity and assurance level come from the presented session, never from the body, so the caller can only ever mint a code for themselves. There is no permission that answers "may I hand my own session to a client I am already logged into", and inventing one would deny everyone including the tenant owner. Same reasoning as `GET /api/v1/auth/session`.

        What constrains it is the CLIENT registry: a code is only issued for a registered, enabled client, bound to a `redirectUri` on that client's **exact-match** allow-list. Prefix and origin matches are refused — `https://app.example.com` prefix-matches `https://app.example.com.evil.test`, and an attacker who can choose the path on a permitted origin can forward the code onward.

        Every rejection — unknown client, non-allow-listed URI, a URI carrying a query or fragment, a non-https URI — answers the same `400 HANDOFF_NOT_ALLOWED`. Distinguishing them turns this into a probe for which clients are registered and which URIs they accept.

        **No `Idempotency-Key`**, deliberately: each call mints a fresh single-use credential, and replaying one would mean two live codes for one request.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IssueSessionHandoffRequest"
      responses:
        "200":
          description: A one-time code, and the redirect_uri it is bound to.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SessionHandoffCodeIssued"
        "400":
          description: The client and redirect_uri combination cannot be used (`HANDOFF_NOT_ALLOWED`), or the body is missing a field.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/auth/session-handoff/redeem:
    post:
      operationId: redeemSessionHandoffCode
      tags:
        - Identity & Access
      summary: Spend a one-time handoff code and receive a session token (server-to-server).
      description: |
        ADR-0050. The only endpoint here authenticated by a **client secret** rather than a session: this is the request that obtains a session, so there is none to present yet. Not a machine credential either. ADR-0092 lets one write inside a narrow ceiling (`create`/`update`, never a high-risk action), and minting a human session is precisely the authority that ceiling exists to keep away from a non-human bearer.

        Call it from the BFF's SERVER. The token must never reach a browser: the BFF stores it server-side and gives the browser its own portal cookie.

        The code is spent exactly once, claimed with a guarded `UPDATE … WHERE redeemed_at IS NULL` so two concurrent redemptions cannot both succeed. The spent row is kept rather than deleted, so a replay is answered from evidence instead of from the absence of it.

        **Every failure is one answer.** Unknown code, expired code, already-spent code, wrong client, wrong `redirectUri`, bad secret — all `401 HANDOFF_REJECTED`, including a malformed body. The distinctions are recorded in the audit trail; handing them to the caller tells whoever holds a stolen code whether it was ever valid.

        The minted session inherits the assurance level the original login REACHED and never more: an `aal1` login cannot be laundered into an `aal2` session.

        **No `Idempotency-Key`**: the code IS the idempotency key, with the opposite contract — the second attempt must fail rather than replay the first response.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedeemSessionHandoffRequest"
      responses:
        "200":
          description: A session token for the human who authenticated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SessionHandoffRedeemed"
        "401":
          description: The handoff could not be completed (`HANDOFF_REJECTED`) — one answer for every cause, deliberately.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/session/switch:
    post:
      operationId: postAuthSessionSwitch
      tags:
        - Identity & Access
      summary: Move the current session to another tenant the same human belongs to.
      description: "ADR-0088. Issues a session in the target tenant and revokes the source session — switching leaves, it does not accumulate. Sessions whose `origin_auth` is `sso` or `handoff` are refused: only a globally verified credential may cross a tenant boundary, otherwise a tenant IdP administrator could assert an address and switch into the tenant where that person really works. Assurance does not travel: the new session starts at `aal1` and the target tenant's MFA policy is applied afresh."
      parameters:
        - name: X-AWCMS-Tenant-ID
          in: header
          required: true
          schema:
            type: string
            format: uuid
          description: The CURRENT tenant, i.e. the session being switched away from.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantId
              properties:
                tenantId:
                  type: string
                  format: uuid
                  description: The tenant to switch to.
      responses:
        "200":
          description: Session issued in the target tenant; the source session is revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      tenantId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: "`SESSION_NOT_SWITCHABLE` (the session was not issued by a global credential), `TENANT_UNAVAILABLE`, or `PASSWORD_LOGIN_DISABLED`."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: '`MEMBERSHIP_NOT_FOUND` — one shape for "you do not belong there", so this endpoint cannot be used to ask whether somebody belongs to a tenant the caller names.'
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/session/tenant:
    post:
      operationId: postAuthSessionTenant
      tags:
        - Identity & Access
      summary: Exchange a tenant-selection token for a session in a named tenant.
      description: ADR-0088. Spends the single-use selection token returned by a `POST /api/v1/auth/login` that carried NO tenant header, and issues a session in the tenant named in the body. The token lives at most 120 seconds, is spent whether or not the exchange succeeds, and can never authenticate any other endpoint — the authorization chokepoint refuses its hash namespace outright. Every gate login applies once a tenant is known applies here too, including the target tenant's MFA policy.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - principalToken
                - tenantId
              properties:
                principalToken:
                  type: string
                  description: The selection token from the 409 body.
                tenantId:
                  type: string
                  format: uuid
                  description: "The tenant to sign in to. Named by the caller: no membership list is disclosed, because reading one would require a cross-tenant scan that FORCE RLS refuses."
      responses:
        "200":
          description: Session issued in the selected tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      token:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
                      tenantId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          description: One shape for every refusal an attacker could probe — unknown, expired or already-spent token, no membership, inactive identity — plus the MFA continuations `MFA_REQUIRED` and `MFA_ENROLLMENT_REQUIRED`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: The tenant is suspended (`TENANT_UNAVAILABLE`) or disables password sign-in for this identity (`PASSWORD_LOGIN_DISABLED`). Reachable only with a genuine token, so it discloses nothing.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many attempts from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sessions:
    get:
      operationId: listOwnAuthSessions
      tags:
        - Identity & Access
      summary: List the caller's own live sessions (self-service, no permission).
      description: 'Where am I signed in? Self-service by construction — the subject is the session bearer and the route accepts no tenantUserId, so there is nobody else it could be pointed at. Deliberately UNPERMISSIONED, like GET /api/v1/auth/session: inventing a permission for "see your own sessions" would wall off the feature and install a latent-authz trap (an action nothing seeds denies everyone, including the tenant owner). Returns no token, no raw IP and no raw User-Agent. clientIpHash is a keyed pseudonym and is null on a deployment without AUTH_IP_HASH_SECRET, because the fallback key is per-process and a stored hash would stop being comparable after a restart.'
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The caller's live sessions, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      sessions:
                        type: array
                        items:
                          $ref: "#/components/schemas/OwnSessionSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many session requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sessions/{id}:
    delete:
      operationId: revokeOwnAuthSession
      tags:
        - Identity & Access
      summary: End one of the caller's own sessions (self-service, no permission).
      description: "Ownership is enforced in the UPDATE's WHERE clause, never by a preceding read. Unknown id, another person's session, another tenant's session and one already revoked or expired all answer 404 — distinguishing them would make this an existence oracle for session ids, and the caller could do nothing differently with the distinction. Revoking the CURRENT session answers 409: that is POST /api/v1/auth/logout's job, which also clears the cookies."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The session was revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      revoked:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The id names the session making this request (SESSION_IS_CURRENT) — use POST /api/v1/auth/logout.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many revocation requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sessions/revoke-all:
    post:
      operationId: revokeOtherOwnAuthSessions
      tags:
        - Identity & Access
      summary: Sign the caller out of every OTHER session (self-service, no permission).
      description: |-
        Ends every live session of the caller's identity except the one making the request. Self-service by construction — the subject is the session bearer and the route accepts no parameter with which to name anybody else — and unpermissioned for the same reason as the two endpoints beside it: this is what a person reaches for after "I think my password leaked", which is exactly when a permission wall is most expensive.
        There is deliberately no `exceptCurrent` flag. The only other value would also end the requesting session, which is `POST /api/v1/auth/logout` — that endpoint additionally clears the cookies this one cannot see, so the flag would ship a second, worse logout whose distinguishing feature is leaving the caller holding a dead cookie.
        It changes no credential and clears no lockout counter: ending stray sessions proves nothing new about the password, and folding the two together would make session hygiene a lockout-reset oracle. Not audited — `awcms_audit_events` records what administrators do to OTHER people; the paired admin endpoint under /api/v1/users/{id}/sessions/revoke-all writes that entry.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: How many other sessions were ended. Zero means there were none.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      revokedCount:
                        type: integer
                        minimum: 0
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "429":
          description: Too many revocation requests from this source.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso-policy:
    get:
      operationId: getSsoPolicy
      tags:
        - Identity & Access
      summary: Read the tenant authentication policy (password/SSO/JIT/break-glass).
      responses:
        "200":
          description: The tenant authentication policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantAuthPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      operationId: updateSsoPolicy
      tags:
        - Identity & Access
      summary: Update the tenant authentication policy (partial upsert; high-risk; audit-logged; break-glass enforced server-side).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                passwordLoginEnabled:
                  type: boolean
                ssoEnabled:
                  type: boolean
                ssoRequired:
                  type: boolean
                autoLinkVerifiedEmail:
                  type: boolean
                jitProvisioningEnabled:
                  type: boolean
                allowedEmailDomains:
                  type: array
                  items:
                    type: string
                breakGlassIdentityIds:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: The updated policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantAuthPolicy"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: sso_required/password_login_disabled without an eligible break-glass owner (BREAK_GLASS_REQUIRED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso-providers:
    get:
      operationId: listSsoProviders
      tags:
        - Identity & Access
      summary: List the tenant's configured OIDC SSO providers (client secrets are never returned).
      responses:
        "200":
          description: The tenant's SSO providers.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      providers:
                        type: array
                        items:
                          $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createSsoProvider
      tags:
        - Identity & Access
      summary: Add a tenant OIDC SSO provider (high-risk; audit-logged). Exactly one of clientSecret / clientSecretEnvVar.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - providerKey
                - displayName
                - issuerUrl
                - clientId
              properties:
                providerKey:
                  type: string
                  description: Stable slug (^[a-z0-9][a-z0-9_-]*$), used in the /sso/{providerKey}/... path.
                displayName:
                  type: string
                issuerUrl:
                  type: string
                  description: HTTPS issuer/discovery base URL.
                clientId:
                  type: string
                clientSecret:
                  type: string
                  nullable: true
                  description: Plaintext client secret (encrypted at rest; never returned). Provide this OR clientSecretEnvVar.
                clientSecretEnvVar:
                  type: string
                  nullable: true
                  description: Name of an env var holding the secret (resolved at token-exchange time; never persisted).
                scopes:
                  type: string
                allowedEmailDomains:
                  type: array
                  items:
                    type: string
                enabled:
                  type: boolean
      responses:
        "200":
          description: The created provider.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: providerKey conflict or per-tenant provider limit reached (SSO_PROVIDER_KEY_CONFLICT / SSO_PROVIDER_LIMIT_EXCEEDED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "500":
          description: The credential encryption key is not configured (SSO_MISCONFIGURED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso-providers/{id}:
    get:
      operationId: getSsoProvider
      tags:
        - Identity & Access
      summary: Read one tenant OIDC SSO provider (client secret never returned).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The provider.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateSsoProvider
      tags:
        - Identity & Access
      summary: Update a tenant OIDC SSO provider (partial; high-risk; audit-logged).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                displayName:
                  type: string
                issuerUrl:
                  type: string
                clientId:
                  type: string
                clientSecret:
                  type: string
                  nullable: true
                clientSecretEnvVar:
                  type: string
                  nullable: true
                scopes:
                  type: string
                allowedEmailDomains:
                  type: array
                  items:
                    type: string
                enabled:
                  type: boolean
      responses:
        "200":
          description: The updated provider.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SsoProvider"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          description: The credential encryption key is not configured (SSO_MISCONFIGURED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deleteSsoProvider
      tags:
        - Identity & Access
      summary: Soft delete a tenant OIDC SSO provider (reason required; high-risk; audit-logged).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: The provider was soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/auth/sso/{providerKey}/callback:
    get:
      operationId: getAuthSsoCallback
      tags:
        - Identity & Access
      summary: OIDC provider redirect target — validates state/nonce/PKCE/ID-token, then mints an opaque awcms session (or a 401 MFA_REQUIRED).
      security: []
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
        - name: state
          in: query
          required: false
          schema:
            type: string
        - name: code
          in: query
          required: false
          schema:
            type: string
        - name: error
          in: query
          required: false
          schema:
            type: string
      responses:
        "302":
          description: Login (or link) succeeded — redirect to the return path.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The provider account is already linked (SSO_ALREADY_LINKED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: The provider could not be reached (SSO_PROVIDER_UNAVAILABLE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso/{providerKey}/link:
    post:
      operationId: postAuthSsoLink
      tags:
        - Identity & Access
      summary: Explicitly link an OIDC provider account to the caller's identity (authenticated + step-up required; never auto-links by email).
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The provider authorization URL to complete the link.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      authorizationUrl:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "502":
          description: The provider could not be reached (SSO_PROVIDER_UNAVAILABLE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso/{providerKey}/start:
    get:
      operationId: getAuthSsoStart
      tags:
        - Identity & Access
      summary: Begin an OIDC SSO login — 302-redirects to the tenant provider's authorization endpoint (Auth Code + PKCE + state + nonce).
      security: []
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
        - name: tenantId
          in: query
          required: false
          description: Tenant id fallback when no tenant header/cookie is present (a fresh browser navigation).
          schema:
            type: string
            format: uuid
        - name: returnTo
          in: query
          required: false
          description: Same-origin relative path to return to after login (sanitized server-side; open-redirect targets are ignored).
          schema:
            type: string
      responses:
        "302":
          description: Redirect to the provider's authorization endpoint.
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "429":
          description: Too many requests from this source (RATE_LIMITED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: The provider could not be reached (SSO_PROVIDER_UNAVAILABLE).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/auth/sso/{providerKey}/unlink:
    post:
      operationId: postAuthSsoUnlink
      tags:
        - Identity & Access
      summary: Unlink the caller's OIDC provider account (authenticated + step-up required; audit-logged).
      parameters:
        - name: providerKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The provider account was unlinked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      unlinked:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: No provider account is currently linked (SSO_NOT_LINKED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/ads:
    get:
      tags:
        - Blog Content
      operationId: blogListAds
      summary: List this tenant's advertisements
      description: Gated by blog_content.ads.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching ads.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      ads:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateAd
      summary: Create an advertisement (RETIRED — always 410)
      deprecated: true
      description: RETIRED by ADR-0044 §4. Always responds 410 ENDPOINT_RETIRED, without auth or any database access. This endpoint stored a free-text imageUrl — any URL an admin typed, rendered straight into a public img src — which is the managed-media bypass ADR-0036 closed. Upload the image through the media library, then use POST /api/v1/news-portal/ad-placements, which requires a verified media object. GET and DELETE on this resource still work, so the ingest job's residue report stays resolvable.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "410":
          description: Endpoint retired. The successor is named in the error message.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/ads/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateAd
      summary: Update an advertisement (RETIRED — always 410)
      deprecated: true
      description: "RETIRED by ADR-0044 §4. Always responds 410 ENDPOINT_RETIRED, without auth or any database access. Closing POST alone would not have sufficed: this endpoint could rewrite imageUrl on an existing ad, the same free-URL bypass by a quieter route that creates no new row. Use PATCH /api/v1/news-portal/ad-placements/{id}, which requires a verified media object."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "410":
          description: Endpoint retired. The successor is named in the error message.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteAd
      summary: Soft delete an advertisement
      description: Gated by blog_content.ads.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Ad soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/institutions:
    get:
      tags:
        - Blog Content
      operationId: blogListInstitutions
      summary: List this tenant's institutions
      description: Gated by blog_content.institutions.read. Optional ?branch= filter, which is what builds each mega menu in one query. Bounded at 200 rows ordered by name.
      parameters:
        - name: branch
          in: query
          schema:
            type: string
            enum:
              - legislative
              - executive
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching institutions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      institutions:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateInstitution
      summary: Register an institution
      description: "Gated by blog_content.institutions.create. Not idempotent: a retry that duplicates a create is refused 409 by the slug unique index, so no Idempotency-Key is read."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogInstitutionCreateInput"
      responses:
        "200":
          description: Institution created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: An institution already exists for this slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/institutions/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetInstitution
      summary: Read one institution
      description: Gated by blog_content.institutions.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The institution.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateInstitution
      summary: Update an institution
      description: Gated by blog_content.institutions.update. Only supplied fields change; an explicit null clears an optional field. An empty body is rejected 400 rather than recorded as a no-op edit.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogInstitutionUpdateInput"
      responses:
        "200":
          description: Institution updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: An institution already exists for this slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteInstitution
      summary: Soft delete an institution
      description: Gated by blog_content.institutions.delete. The articles filed against it keep their link, so restoring the institution restores its archive intact.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Institution soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/institutions/{id}/purge:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPurgeInstitution
      summary: Permanently remove a soft-deleted institution
      description: "Gated by blog_content.institutions.purge. Requires a prior soft delete: the statement matches only a row whose deleted_at is set, so a live institution answers 404 rather than being destroyed. Deletes every awcms_blog_post_institutions link first, so the articles that named it lose this classification permanently. Idempotency-Key is REQUIRED — unlike restore, the effect cannot be safely replayed once the row is gone."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Institution purged.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - purged
                      articleLinksRemoved:
                        type: integer
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/institutions/{id}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestoreInstitution
      summary: Restore a soft-deleted institution
      description: "Gated by blog_content.institutions.restore. No Idempotency-Key is read: the statement only matches a currently soft-deleted row, so a replay writes no audit event and answers 404. Deleting released the slug, so 409 is returned when a live institution has taken it in the meantime."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Institution restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogInstitution"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A live institution now holds this slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/internal-tag-links/settings:
    get:
      tags:
        - Blog Content
      operationId: blogGetInternalTagLinkSettings
      summary: Read this tenant's automatic internal tag linking policy
      description: Gated by blog_content.internal_links.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogInternalTagLinkSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateInternalTagLinkSettings
      summary: Update this tenant's automatic internal tag linking policy
      description: Gated by blog_content.internal_links.configure. disabledTagIds are validated against this tenant's own tag catalog.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                enabled:
                  type: boolean
                caseInsensitive:
                  type: boolean
                disabledTagIds:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: Policy updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogInternalTagLinkSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/menus:
    get:
      tags:
        - Blog Content
      operationId: blogListMenus
      summary: List this tenant's navigation menus (with items)
      description: |
        Gated by blog_content.menus.read. Bounded at 100 menus, name ascending — a site has a handful, and unlike the tag vocabulary this one does not grow with the archive.

        Each menu carries its own `items`, already sorted by `sortOrder`. That costs one query per menu rather than one for all of them, and the route says why in a comment: the queries run on one transaction, so they cannot be issued concurrently.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching menus.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      menus:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogMenu"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateMenu
      summary: Create a navigation menu
      description: Gated by blog_content.menus.configure. Optionally seeds its initial items tree (one level of nesting; link_type post|page|url gates target_id/url).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - key
                - name
              properties:
                key:
                  type: string
                name:
                  type: string
                items:
                  type: array
                  maxItems: 200
                  description: At most 200 entries; a longer array is refused with `VALIDATION_ERROR` before any entry is validated.
                  items:
                    $ref: "#/components/schemas/BlogMenuItem"
      responses:
        "200":
          description: Menu created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A menu already exists for this key.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/menus/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateMenu
      summary: Update a navigation menu (name and/or its items tree)
      description: |
        Gated by blog_content.menus.configure. Providing items REPLACES the
        whole tree — a partial list deletes the rest. At most 200 items.

        Read `itemsTruncated` on whatever you are editing before sending
        `items`: a menu stored before the cap existed reads back bounded, and
        saving that list back would delete everything past the bound.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                items:
                  type: array
                  maxItems: 200
                  description: At most 200 entries; a longer array is refused with `VALIDATION_ERROR` before any entry is validated. Omit the field entirely to leave the existing tree untouched — an empty array CLEARS it.
                  items:
                    $ref: "#/components/schemas/BlogMenuItem"
      responses:
        "200":
          description: Menu updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteMenu
      summary: Soft delete a navigation menu
      description: Gated by blog_content.menus.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Menu soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/pages:
    get:
      tags:
        - Blog Content
      operationId: blogListPages
      summary: List this tenant's non-deleted blog pages
      description: Gated by blog_content.pages.read. Optional ?status= filter, ?limit= bounded.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - draft
              - review
              - scheduled
              - published
              - archived
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching pages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      pages:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreatePage
      summary: Create a draft blog page
      description: Gated by blog_content.pages.create. Not idempotency-keyed (caught by the (tenant_id, locale, slug) partial unique index).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPageWriteInput"
      responses:
        "200":
          description: Page created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A page already exists for this slug/locale.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: One or more image references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPage
      summary: Read one blog page
      description: Gated by blog_content.pages.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The page.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdatePage
      summary: Update a blog page
      description: "Gated by blog_content.pages.update. Only fields present in the body are changed. A significant change snapshots an append-only revision first. Issue #787 (sibling of #784) — when `slug` is present and differs from the stored slug, the SAME transaction also captures an ADR-0039 redirect (proposed or active, per the tenant's url_change_auto_policy); see the response schema's `redirectCapture` field."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPageWriteInput"
      responses:
        "200":
          description: Page updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: One or more image references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeletePage
      summary: Soft delete a blog page
      description: Gated by blog_content.pages.delete.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/pages/{id}/archive:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogArchivePage
      summary: Archive a blog page
      description: Gated by blog_content.pages.archive (ADR-0057). High-risk, requires Idempotency-Key. Removes the page from the public site without destroying it; published_at is retained so a later re-publish keeps the original go-live date.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page archived (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}/publish:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPublishPage
      summary: Publish a blog page
      description: Gated by blog_content.pages.publish (ADR-0057). High-risk, requires Idempotency-Key. Blocked (422) if the content quality checklist fails when full-online R2-only mode is active for the tenant. Pages have a narrower lifecycle than posts - no review, no scheduled - so the only sources for this transition are draft and archived. No social-publishing hook is invoked - that port's trigger is post_published and a page is not an article.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page published (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Publish is blocked by the content quality checklist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}/purge:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPurgePage
      summary: Permanently purge an archived or soft-deleted blog page
      description: Gated by blog_content.pages.purge (ADR-0057). High-risk, irreversible, requires Idempotency-Key. Only an archived or previously soft-deleted page may be purged. Advertisement placements targeting this page neither block the purge nor are deleted with it - they simply go inert, exactly as they already do for a soft-deleted page, because the render query matches target_id against the page being rendered. Their count is returned as adPlacementsNowInert so the change is visible rather than silent.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page purged (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - purged
                      adPlacementsNowInert:
                        type: integer
                        minimum: 0
                        description: Advertisement placements that targeted this page and are now inert.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The page is neither archived nor soft-deleted, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/{id}/quality-checklist:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPageQualityChecklist
      summary: Preview the content quality checklist for a page
      description: Read-only preview for the admin editor. Gated by blog_content.pages.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The checklist result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      pageId:
                        type: string
                        format: uuid
                      qualityChecklist:
                        $ref: "#/components/schemas/ContentQualityChecklistResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/pages/{id}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestorePage
      summary: Restore a soft-deleted blog page
      description: Gated by blog_content.pages.restore (ADR-0057). High-risk, requires Idempotency-Key. Undoes a soft delete, not an archive - lifecycle status is left untouched, so a page soft-deleted while published comes back published. A page that is not soft-deleted answers 404, the same shape as an unknown id, so restore cannot be used to probe which ids exist.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Page restored (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/pages/public:
    get:
      tags:
        - Blog Content
      summary: The static pages a reader can actually reach
      description: "Gated by blog_content.pages.read. NOT the same answer as `/api/v1/blog/pages?status=published`: the admin list is an editor's view and returns private and unlisted pages too, so a consumer reaching for it with a status filter would publish every private page the newsroom has and nothing would report an error. The predicate here is the one the public route enforces, shared with sitemap-blog.xml so the two cannot disagree. Metadata only — a body is fetched per page from /{slug}, because a tenant's legal pages are few but long."
      operationId: blogPagesPublicList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Reachable static pages, in menu order.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      pages:
                        type: array
                        items:
                          $ref: "#/components/schemas/PublicBlogPageSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/pages/public/{slug}:
    get:
      tags:
        - Blog Content
      summary: One reachable static page, body included
      description: Gated by blog_content.pages.read. The predicate is shared byte-for-byte with the public route /blog/{tenantCode}/pages/{slug}, so a draft cannot be read here for the same reason it cannot be read there. An unreachable slug answers 404 without distinguishing "no such page" from "that page is a draft" — not an information-disclosure boundary (the caller is authenticated) but consistency, so a consumer cannot depend on a distinction the browser-facing surface does not make. The body ships as BOTH `contentJson` (the projection this repo renders today) and `bodyPortableText` (the canonical column, ADR-0100), which is what lets a consumer move to the canonical shape on its own schedule.
      operationId: blogPagesPublicDetail
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: slug
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The page, with every referenced media object resolved in one call.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/PublicBlogPageDetail"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts:
    get:
      tags:
        - Blog Content
      operationId: blogListPosts
      summary: List this tenant's non-deleted blog posts
      description: |
        Gated by blog_content.posts.read. Optional ?status= and ?locale= filters, ?limit= bounded (default 20, max 100 — max 50 with `view=full`, whose rows carry `contentJson`).

        Ordering defaults to `updated_at DESC` — right for an admin table, and unsound as a keyset key because editing a post moves it, so a row can cross a page boundary between requests and be skipped or repeated. A caller that needs EVERY post (a build feed) passes `?order=created_at`, which is immutable, and follows `nextCursor` until it is null. `?cursor=` without `?order=created_at` is refused with 400 rather than quietly honoured.

        **The default response is `BlogPostSummary`, not `BlogPost`.** It carries no `contentJson`, `excerpt`, `metaDescription`, `canonicalUrl`, or `translationGroupId`. This document used to say otherwise, and a client that believed it built an entire static site with every article body empty — nothing failed, because a missing field reads as `undefined`. A caller that needs the body asks for `?view=full` (which requires `order=created_at`) and receives `BlogPost`.
      parameters:
        - name: status
          in: query
          schema:
            type: string
            enum:
              - draft
              - review
              - scheduled
              - published
              - archived
        - name: locale
          in: query
          description: Exact match on the post's stored locale. Absent means every locale, which is the right default for an admin table but wasteful for a single-language build feed — that is what this parameter exists for. Not shape-validated beyond non-empty and a 35-character bound, because the write path accepts any non-empty string and a stricter read filter would make a stored locale unreachable. An empty value is a 400, never treated as absent.
          schema:
            type: string
            minLength: 1
            maxLength: 35
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: order
          in: query
          description: Sort key. `created_at` selects the STABLE, cursor-capable traversal; `updated_at` (default) is the admin ordering and rejects `cursor`.
          schema:
            type: string
            enum:
              - created_at
              - updated_at
        - name: cursor
          in: query
          description: Opaque keyset cursor from a previous response's `nextCursor`. Requires `order=created_at`. A malformed value is a 400, never treated as "no cursor".
          schema:
            type: string
        - name: view
          in: query
          description: |
            Response projection. `summary` (default) returns `BlogPostSummary`; `full` returns `BlogPost` — every column the detail endpoint returns, `termIds` and `institutionIds` included.

            Those two were originally left out because fetching them per post would have been one extra query each. A page fetches them for the whole page in one query instead, so the cost is three round trips per page rather than one per post — and without them no consumer could build a category or tag archive at all, which is the state Issue #597 item 1 describes.

            `full` requires `order=created_at`: a full traversal is only sound over the immutable ordering, and the ordering is demanded rather than silently substituted. An unrecognised value is a 400, never a silent fallback to `summary`.
          schema:
            type: string
            enum:
              - summary
              - full
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching posts.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      posts:
                        type: array
                        description: "`BlogPostSummary` by default; `BlogPost` when `view=full`."
                        items:
                          oneOf:
                            - $ref: "#/components/schemas/BlogPostSummary"
                            - $ref: "#/components/schemas/BlogPost"
                      nextCursor:
                        type: string
                        nullable: true
                        description: Present only with `order=created_at`. Null means the traversal is complete.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreatePost
      summary: Create a draft blog post
      description: Gated by blog_content.posts.create. Not idempotency-keyed (a retry duplicating a create is caught by the (tenant_id, locale, slug) partial unique index).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPostWriteInput"
      responses:
        "200":
          description: Post created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A post already exists for this slug/locale, or the referenced media is not R2-verified in full-online R2-only mode (422 for the latter).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: One or more image/video-thumbnail references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPost
      summary: Read one blog post
      description: Gated by blog_content.posts.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The post.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdatePost
      summary: Update a blog post
      description: "Gated by blog_content.posts.update (with an ownership carve-out — a draft's own author may update it without the broader permission). Only fields present in the body are changed. A significant title/contentJson/contentText change snapshots an append-only revision first. Issue #784 — when `slug` is present and differs from the stored slug, the SAME transaction also captures an ADR-0039 redirect (proposed or active, per the tenant's url_change_auto_policy); see the response schema's `redirectCapture` field."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogPostWriteInput"
      responses:
        "200":
          description: Post updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: One or more image/video-thumbnail references are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Blog Content
      operationId: blogDeletePost
      summary: Soft delete a blog post
      description: Gated by blog_content.posts.delete. Soft delete only (deleted_at/deleted_by/delete_reason) — see POST .../purge for the permanent, high-risk purge.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/archive:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogArchivePost
      summary: Archive a blog post
      description: Gated by blog_content.posts.archive. High-risk, requires Idempotency-Key.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post archived (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/internal-links/preview:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogPreviewPostInternalLinks
      summary: Preview automatic internal tag linking for a post
      description: Read-only preview of which tags would be automatically linked in this post's rendered content before publishing. Gated by blog_content.internal_links.preview.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The preview result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      postId:
                        type: string
                        format: uuid
                      enabled:
                        type: boolean
                      disabledReason:
                        type: string
                        nullable: true
                        enum:
                          - deployment_disabled
                          - tenant_disabled
                          - post_disabled
                          - null
                      matches:
                        type: array
                        items:
                          type: object
                      totalLinked:
                        type: integer
                      maxPerPost:
                        type: integer
                      maxPerTag:
                        type: integer
                      vocabulary:
                        type: object
                        description: |
                          How much of the tenant's tag vocabulary the matching engine could see (Issue #648).

                          Only a bounded number of tags enter the engine at once, because it compiles one alternation regex from all of them. Which ones are chosen is by USAGE — the tags carried by the most posts — since those are the ones most likely to occur in prose. Until Issue #648 the selection was the alphabetically-first hundred, inherited from the admin list, so whether a tag was ever linked was decided by its first letter and nothing said so.

                          `truncated: true` is the one reason a tag went unlinked that the editor cannot fix by editing.
                        required:
                          - total
                          - limit
                          - truncated
                        properties:
                          total:
                            type: integer
                            description: Non-deleted tags this tenant has.
                          limit:
                            type: integer
                            description: The cap applied to the candidate list.
                          truncated:
                            type: boolean
                            description: True when the vocabulary is larger than the cap, so some tags cannot be linked at all.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/publish:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPublishPost
      summary: Publish a blog post
      description: Gated by blog_content.posts.publish (no ownership carve-out). High-risk, requires Idempotency-Key. Blocked (422) if the content quality checklist fails when full-online R2-only mode is active for the tenant. On success, also invokes the social-publishing outbox hook (a documented no-op in this base — social_publishing is not ported).
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post published (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Publish is blocked by the content quality checklist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/purge:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogPurgePost
      summary: Permanently purge a soft-deleted blog post
      description: Gated by blog_content.posts.purge. High-risk, irreversible, requires Idempotency-Key. Only a previously soft-deleted post may be purged.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post purged (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The post is not soft-deleted, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/quality-checklist:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPostQualityChecklist
      summary: Preview the content quality checklist for a post
      description: Read-only preview for the admin editor — runs the exact same evaluator POST .../publish and .../schedule enforce. Gated by blog_content.posts.read (no separate permission).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The checklist result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      postId:
                        type: string
                        format: uuid
                      qualityChecklist:
                        $ref: "#/components/schemas/ContentQualityChecklistResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestorePost
      summary: Restore a soft-deleted blog post
      description: Gated by blog_content.posts.restore. High-risk, requires Idempotency-Key.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post restored (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/revisions:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogListPostRevisions
      summary: List a post's revision history
      description: Gated by blog_content.revisions.read. Newest first.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The revision list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      revisions:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogRevision"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/revisions/{revisionId}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revisionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    get:
      tags:
        - Blog Content
      operationId: blogGetPostRevision
      summary: Read one revision of a post
      description: Gated by blog_content.revisions.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The revision.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogRevision"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/posts/{id}/revisions/{revisionId}/restore:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
      - name: revisionId
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogRestorePostRevision
      summary: Restore a post to an earlier revision
      description: Gated by blog_content.revisions.restore. High-risk, requires Idempotency-Key. Restoring APPENDS a new revision snapshot of the restored content — it never overwrites or removes the revision being restored from.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post restored to the given revision (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: One or more image/video-thumbnail references in the restored content are not valid R2 media objects in full-online R2-only mode.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/schedule:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogSchedulePost
      summary: Schedule a blog post for future publishing
      description: Gated by blog_content.posts.schedule. High-risk, requires Idempotency-Key. Same content quality checklist gate as publish. The blog:publish:scheduled job later performs the actual publish once scheduledAt <= now().
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - scheduledAt
              properties:
                scheduledAt:
                  type: string
                  format: date-time
                unpublishAt:
                  type: string
                  format: date-time
                  nullable: true
                  description: "Issue #591 — closes the publication window. When the moment arrives, blog:publish:scheduled transitions the post to archived. Must be strictly after scheduledAt; omitted or null means the post stays published until an editor archives it by hand. Carried in the idempotency request hash, so replaying a key with a different withdrawal date is a 409, not a no-op."
      responses:
        "200":
          description: Post scheduled (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Invalid status transition, or the Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Scheduling is blocked by the content quality checklist.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/posts/{id}/submit-review:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    post:
      tags:
        - Blog Content
      operationId: blogSubmitPostForReview
      summary: Submit a draft post for review
      description: Transitions draft -> review. Gated by blog_content.posts.update (same ownership carve-out as PATCH .../{id}). Not idempotency-keyed — the status transition is naturally idempotent.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Post submitted for review.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogPost"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The post's current status cannot transition to review.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/search:
    get:
      tags:
        - Blog Content
      operationId: blogSearchContent
      summary: Full-text search across posts and pages
      description: Gated by blog_content.search.read. May return content of any status per the caller's granted permission. Keyset-paginated (?cursor=).
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
        - name: resourceType
          in: query
          schema:
            type: string
            enum:
              - post
              - page
        - name: status
          in: query
          schema:
            type: string
            enum:
              - draft
              - review
              - scheduled
              - published
              - archived
        - name: cursor
          in: query
          schema:
            type: string
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching results.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          type: object
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/settings:
    get:
      tags:
        - Blog Content
      operationId: blogGetSettings
      summary: Read this tenant's blog settings
      description: Gated by blog_content.settings.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant's blog settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateSettings
      summary: Update this tenant's blog settings
      description: Gated by blog_content.settings.configure. Only fields present in the body are changed.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Settings updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/templates:
    get:
      tags:
        - Blog Content
      operationId: blogListTemplates
      summary: List this tenant's presentation templates
      description: Gated by blog_content.templates.read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching templates.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      templates:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateTemplate
      summary: Create a presentation template
      description: "Gated by blog_content.templates.configure. layoutJson is a whitelisted shape: { columns: 1|2|3, sidebarPosition: left|right|none }."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - key
                - name
              properties:
                key:
                  type: string
                name:
                  type: string
                layoutJson:
                  type: object
                isActive:
                  type: boolean
      responses:
        "200":
          description: Template created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A template already exists for this key.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/templates/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateTemplate
      summary: Update a presentation template
      description: Gated by blog_content.templates.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                layoutJson:
                  type: object
                isActive:
                  type: boolean
      responses:
        "200":
          description: Template updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteTemplate
      summary: Soft delete a presentation template
      description: Gated by blog_content.templates.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Template soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/terms:
    get:
      tags:
        - Blog Content
      operationId: blogListTerms
      summary: List this tenant's categories/tags
      description: |
        Gated by blog_content.taxonomies.read. Optional ?taxonomyType= filter, ?limit= bounded (default 100, max 200).

        Ordering defaults to `name ASC` — right for the admin taxonomy screen, and unsound as a keyset key because renaming a term moves it, so a row can cross a page boundary between requests and be skipped or repeated. A caller that needs EVERY term passes `?order=created_at`, which is immutable, and follows `nextCursor` until it is null. `?cursor=` without `?order=created_at` is refused with 400 rather than quietly honoured.

        **The default list is bounded and says nothing about what it left out.** For `category` or `channel` that is harmless — a newsroom has a dozen of each. For `tag` it is not: a vocabulary grown over tens of thousands of articles runs to thousands of entries, and a caller that generated one archive page per tag from the default list would build a hundred pages, `200 OK`, with every article filed under a later tag pointing at a page nobody generated. That is what `order=created_at` exists for.
      parameters:
        - name: taxonomyType
          in: query
          schema:
            type: string
            enum:
              - category
              - tag
              - channel
              - topic
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: order
          in: query
          description: Sort key. `created_at` selects the STABLE, cursor-capable traversal; `name` (default) is the admin ordering and rejects `cursor`.
          schema:
            type: string
            enum:
              - created_at
              - name
        - name: cursor
          in: query
          description: Opaque keyset cursor from a previous response's `nextCursor`. Requires `order=created_at`. A malformed value is a 400, never treated as "no cursor".
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching terms.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      terms:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogTerm"
                      nextCursor:
                        type: string
                        nullable: true
                        description: Present only with `order=created_at`. Null once the traversal is complete.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateTerm
      summary: Create a category or tag
      description: Gated by blog_content.taxonomies.configure. A tag must never carry a parentId.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogTermWriteInput"
      responses:
        "200":
          description: Term created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogTerm"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A term already exists for this taxonomyType/slug.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/blog/terms/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateTerm
      summary: Update a category or tag
      description: Gated by blog_content.taxonomies.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlogTermWriteInput"
      responses:
        "200":
          description: Term updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogTerm"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteTerm
      summary: Soft delete a category or tag
      description: Gated by blog_content.taxonomies.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Term soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BlogTerm"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/blog/theme:
    get:
      tags:
        - Blog Content
      operationId: blogGetTheme
      summary: Read this tenant's blog theme mode setting
      description: Gated by blog_content.theme.read. Absence of a row means "inherit the tenant's default_theme".
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The theme setting.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum:
                          - light
                          - dark
                          - system
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateTheme
      summary: Update this tenant's blog theme mode setting
      description: Gated by blog_content.theme.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - mode
              properties:
                mode:
                  type: string
                  enum:
                    - light
                    - dark
                    - system
      responses:
        "200":
          description: Theme setting updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      mode:
                        type: string
                        enum:
                          - light
                          - dark
                          - system
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/widgets:
    get:
      tags:
        - Blog Content
      operationId: blogListWidgets
      summary: List this tenant's widgets
      description: |
        Gated by blog_content.widgets.read. Optional `?position=` filter. Bounded at 100, ordered by `position` then `sortOrder`.

        **Inactive widgets are returned too.** There is no `?activeOnly=` parameter: `isActive` is on every row, and a consumer that wants only the live ones filters on it. An endpoint that hid them would make "this widget is switched off" and "this widget was deleted" the same answer.
      parameters:
        - name: position
          in: query
          schema:
            type: string
            enum:
              - header
              - sidebar
              - footer
              - content_before
              - content_after
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Matching widgets.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      widgets:
                        type: array
                        items:
                          $ref: "#/components/schemas/BlogWidget"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Blog Content
      operationId: blogCreateWidget
      summary: Create a widget
      description: Gated by blog_content.widgets.configure. bodyText is plain text, escaped at render time (no raw-HTML field).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - position
                - title
              properties:
                position:
                  type: string
                  enum:
                    - header
                    - sidebar
                    - footer
                    - content_before
                    - content_after
                title:
                  type: string
                bodyText:
                  type: string
                isActive:
                  type: boolean
                sortOrder:
                  type: integer
      responses:
        "200":
          description: Widget created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/blog/widgets/{id}:
    parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
          format: uuid
    patch:
      tags:
        - Blog Content
      operationId: blogUpdateWidget
      summary: Update a widget
      description: Gated by blog_content.widgets.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                position:
                  type: string
                  enum:
                    - header
                    - sidebar
                    - footer
                    - content_before
                    - content_after
                title:
                  type: string
                bodyText:
                  type: string
                isActive:
                  type: boolean
                sortOrder:
                  type: integer
      responses:
        "200":
          description: Widget updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Blog Content
      operationId: blogDeleteWidget
      summary: Soft delete a widget
      description: Gated by blog_content.widgets.configure.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Widget soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/comments:
    get:
      operationId: listPublicComments
      tags:
        - Comments
      summary: List approved comments for a published commentable resource.
      description: PUBLIC and anonymous. The tenant is resolved from the request host. Only `approved`, non-deleted comments are returned, and never any moderation metadata (reason codes, actor ids, email/ip hashes). An unresolved host, a disabled module, an unpublished resource, and a resource with no comments all return the SAME empty payload — the endpoint is not an existence oracle for unpublished content.
      security: []
      parameters:
        - name: resourceType
          in: query
          required: true
          schema:
            type: string
          description: The commentable resource type, e.g. `blog_post`.
        - name: resourceId
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: locale
          in: query
          required: false
          schema:
            type: string
          description: Defaults to the resolved tenant's default locale.
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Full-precision `created_at` from a previous page's `nextCursor`. Must be sent together with `cursorId`; a timestamp alone cannot disambiguate two comments written in the same microsecond.
        - name: cursorId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Approved comments, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/PublicCommentPage"
        "429":
          description: Per-IP rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: submitPublicComment
      tags:
        - Comments
      summary: Submit a comment against a published commentable resource.
      description: |-
        PUBLIC and anonymous, host-resolved, `Idempotency-Key` required. Anti-abuse gated (honeypot field, submit-timing floor, blocked terms, duplicate fingerprint, per-IP rate limit).
        RESPONSES ARE DELIBERATELY UNIFORM. An unresolved resource, a disabled module, an anti-abuse block, and an accepted-but-held-for-moderation comment ALL return `{"status":"received"}`. Only a comment that is immediately publicly visible returns its id and status. A caller therefore cannot use this endpoint to enumerate blocked terms, probe the timing floor, or discover unpublished resources.
      security: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitCommentRequest"
      responses:
        "200":
          description: Accepted. Either the neutral `{"status":"received"}` acknowledgement or, when the comment is immediately public, its id and status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SubmitCommentResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: The Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request body exceeded the endpoint's size ceiling.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Per-IP rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/{id}:
    patch:
      operationId: editPublicComment
      tags:
        - Comments
      summary: Edit your own comment within its edit window.
      description: "PUBLIC, author-bound: a registered author is matched by session user id, an anonymous author by the stored IP hash. Editing someone else's comment returns 404, identical to a comment that does not exist."
      security: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - body
              properties:
                body:
                  type: string
      responses:
        "200":
          description: The re-rendered, escaped comment HTML.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      bodyHtml:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The edit window for this comment has passed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The edited body failed normalization or the link/length bounds.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/{id}/delete-request:
    post:
      operationId: requestPublicCommentDeletion
      tags:
        - Comments
      summary: Ask for your own comment to be removed.
      description: PUBLIC, author-bound. Within the edit window this soft-deletes the comment immediately. Past the window it files a report for a moderator instead, so thread structure and moderation history stay coherent.
      security: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The request was accepted. `softDeleted` distinguishes an immediate removal from a queued moderator request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      softDeleted:
                        type: boolean
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/comments/{id}/replies:
    post:
      operationId: submitPublicCommentReply
      tags:
        - Comments
      summary: Reply to an existing comment.
      description: PUBLIC. Same policy and anti-abuse gates as a top-level submission, with the parent supplied by the path and depth derived from it and capped. The parent's resource is re-confirmed as still published before the reply is accepted. Returns the same neutral acknowledgement shape as submit.
      security: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SubmitCommentRequest"
      responses:
        "200":
          description: Neutral acknowledgement, or the reply's id when immediately public.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SubmitCommentResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Per-IP rate limit exceeded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/{id}/report:
    post:
      operationId: reportPublicComment
      tags:
        - Comments
      summary: Report a comment for moderator attention.
      description: PUBLIC. De-duplicated per (comment, reporter ip hash, reason) by a database unique index, so repeated reports from one source do not inflate the queue's report count.
      security: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
                  enum:
                    - spam
                    - abuse
                    - offensive
                    - other
                detail:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The report was accepted (or silently de-duplicated).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
        "400":
          $ref: "#/components/responses/BadRequest"
  /api/v1/comments/admin/{id}/archive:
    post:
      operationId: archiveComment
      tags:
        - Comments
      summary: Withdraw an approved comment from public view.
      description: Requires `comments.moderation.archive`. Only an APPROVED comment can be archived. The row is retained with a reserved `archived` reason code so the queue distinguishes it from a plain rejection, and `published_at` is preserved. Idempotency-keyed and audited.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The comment was archived.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModerationResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key conflict, or the comment is not approved and therefore cannot be archived.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/admin/{id}/delete:
    post:
      operationId: deleteCommentAsModerator
      tags:
        - Comments
      summary: Soft delete a comment as a moderator.
      description: |-
        Requires `comments.moderation.delete` (ADR-0058 §B). Legal from every non-terminal status. Non-destructive — the row, its body text and its append-only moderation history are all retained; what is withdrawn is visibility. Open reports on the comment are resolved, since a deleted comment cannot be acted on again.

        This is the only moderator transition with no way back through the API: `deleted` is terminal, and recovering a deleted comment is an operator/database action. Use `reject` for a reversible decision, or `archive` to withdraw an approved comment while keeping it restorable. Idempotency-keyed and audited at `warning` severity.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The comment was soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModerationResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key conflict, or the comment is already deleted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/admin/{id}/moderate:
    post:
      operationId: moderateComment
      tags:
        - Comments
      summary: Approve, reject, or mark a comment as spam.
      description: "`approve` requires `comments.moderation.approve`; `reject` and `spam` both require `comments.moderation.reject` — marking spam is a rejection subtype with the same blast radius, distinguished by its audited reason code rather than by a separate permission. A reason code is mandatory for `reject` and `spam`. Idempotency-keyed and audited."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - decision
              properties:
                decision:
                  type: string
                  enum:
                    - approve
                    - reject
                    - spam
                reasonCode:
                  type: string
                  nullable: true
                  maxLength: 64
                note:
                  type: string
                  nullable: true
                  maxLength: 2000
      responses:
        "200":
          description: The decision was applied.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModerationResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Either the Idempotency-Key was reused with a different request, or the action is not legal from the comment's current status.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/admin/{id}/restore:
    post:
      operationId: restoreComment
      tags:
        - Comments
      summary: Return a rejected, spam, or archived comment to review.
      description: Requires `comments.moderation.restore`. Moves the comment back to `pending` for a fresh decision. A soft-deleted comment is terminal and cannot be restored in-band. Idempotency-keyed and audited.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The comment was returned to pending.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModerationResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key conflict, or the comment's current status cannot transition back to pending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/admin/bulk-moderate:
    post:
      operationId: bulkModerateComments
      tags:
        - Comments
      summary: Apply one moderation action to many comments.
      description: "Guarded by the same permission the single-comment action requires. Each comment is evaluated independently: the response reports which ids were applied and which were skipped, with the reason. A partial result is a 200, not an error — the applied decisions are real and must not be implied to have rolled back."
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - commentIds
                - decision
              properties:
                commentIds:
                  type: array
                  items:
                    type: string
                    format: uuid
                decision:
                  type: string
                  enum:
                    - approve
                    - reject
                    - spam
                reasonCode:
                  type: string
                  nullable: true
                note:
                  type: string
                  nullable: true
      responses:
        "200":
          description: Per-comment outcomes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/BulkModerationResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/comments/admin/queue:
    get:
      operationId: listCommentModerationQueue
      tags:
        - Comments
      summary: Read the moderation queue.
      description: "Requires `comments.moderation.read`. This is the ONLY surface exposing moderation metadata: reason codes, masked author email, and open report counts. Keyset-paginated on `(created_at, id)`."
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - pending
              - approved
              - rejected
              - spam
              - deleted
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Full-precision `created_at`; send with `cursorId`.
        - name: cursorId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: A page of the moderation queue.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModerationQueuePage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/comments/admin/settings:
    get:
      operationId: getCommentSettings
      tags:
        - Comments
      summary: Read this tenant's comment configuration.
      description: Requires `comments.settings.read`. Returns the stored configuration, or the built-in defaults when the tenant has never saved one.
      responses:
        "200":
          description: The tenant's comment configuration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/CommentSettings"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      operationId: updateCommentSettings
      tags:
        - Comments
      summary: Update this tenant's comment configuration.
      description: Requires `comments.settings.update`. A partial body is merged over the current settings. High-risk (it changes the public comment surface), idempotency-keyed, and audited with before/after values. Every bound mirrors a database CHECK constraint, so an out-of-range value is a 400 rather than a constraint violation.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CommentSettings"
      responses:
        "200":
          description: The saved configuration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/CommentSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/data-lifecycle/dry-run:
    post:
      tags:
        - Data Lifecycle
      summary: Compute an on-demand, read-only dry-run lifecycle plan
      description: "Gated by data_lifecycle.plan.analyze. Zero mutation and zero persistence — no Idempotency-Key required. Legal hold is checked first and unconditionally: a held descriptor reports every eligible row as `heldCount`, nothing purgeable."
      operationId: dataLifecycleDryRunCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DataLifecycleDryRunRequest"
      responses:
        "200":
          description: The dry-run plan for the named descriptor.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      plan:
                        $ref: "#/components/schemas/DataLifecycleDryRunPlan"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/data-lifecycle/legal-holds:
    get:
      tags:
        - Data Lifecycle
      summary: List legal holds
      description: Gated by data_lifecycle.legal_hold.read. Optionally filtered by status/descriptorKey. Newest first (limit 200).
      operationId: dataLifecycleLegalHoldsList
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - active
              - released
        - name: descriptorKey
          in: query
          required: false
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: A page of legal holds.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      legalHolds:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleLegalHold"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Data Lifecycle
      summary: Create a legal hold
      description: "Gated by data_lifecycle.legal_hold.create. High-risk mutation: requires Idempotency-Key, reason-required (min 10 chars), audited critical. Creating a hold does NOT grant the ability to release one (default-deny release)."
      operationId: dataLifecycleLegalHoldsCreate
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DataLifecycleCreateLegalHoldRequest"
      responses:
        "200":
          description: Legal hold created, or replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      legalHold:
                        $ref: "#/components/schemas/DataLifecycleLegalHold"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/data-lifecycle/legal-holds/{id}/release:
    post:
      tags:
        - Data Lifecycle
      summary: Release (end) an active legal hold
      description: "Gated by data_lifecycle.legal_hold.release — a DISTINCT permission from create (default-deny release). High-risk mutation: requires Idempotency-Key, releaseReason-required (min 10 chars), audited critical."
      operationId: dataLifecycleLegalHoldsRelease
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DataLifecycleReleaseLegalHoldRequest"
      responses:
        "200":
          description: Legal hold released, or replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      legalHold:
                        $ref: "#/components/schemas/DataLifecycleLegalHold"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The hold is already released (ALREADY_RELEASED), or the Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/data-lifecycle/registry:
    get:
      tags:
        - Data Lifecycle
      summary: List the high-volume table lifecycle registry
      description: Gated by data_lifecycle.registry.read. Code-declared descriptor metadata only (table/owner/scope/cursor/retention bounds/execution mode) — never row contents, never a live count. The response body is identical for every tenant; auth/ABAC still applies. Two arrays, because a table's owner is either a module or infrastructure and the two cannot be spelled the same way (ADR-0076).
      operationId: dataLifecycleRegistryList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The registered high-volume table descriptors.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      descriptors:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleDescriptor"
                      infrastructureDescriptors:
                        type: array
                        description: Descriptors for tables owned by src/lib/ infrastructure rather than by a module (ADR-0076). Always executionMode "delegated" — the generic engine purges on behalf of an owning module, and these have none.
                        items:
                          $ref: "#/components/schemas/DataLifecycleInfrastructureDescriptor"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/data-lifecycle/runs:
    get:
      tags:
        - Data Lifecycle
      summary: Read lifecycle run history
      description: Gated by data_lifecycle.runs.read. Categorized AGGREGATE counts only — never row contents or PII. Optionally filtered by descriptorKey/runType. Newest first (limit 100).
      operationId: dataLifecycleRunsList
      parameters:
        - name: descriptorKey
          in: query
          required: false
          schema:
            type: string
        - name: runType
          in: query
          required: false
          schema:
            type: string
            enum:
              - dry_run
              - archive
              - purge
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: A page of lifecycle run history.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      runs:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleRun"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/data-lifecycle/subject-requests:
    get:
      tags:
        - Data Lifecycle
      summary: List subject requests (and the pending-erasure inbox)
      description: "Gated by data_lifecycle.subject_request.read — a permission that discloses NO subject data: the rows carry ids, a status and the stated reason, never exported content. Filtering by status=pending_approval is the checker's inbox. Newest first (limit 100)."
      operationId: dataLifecycleSubjectRequestsList
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - disclosed
              - pending_approval
              - rejected
              - completed
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: A page of subject requests.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      requests:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleSubjectRequest"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/data-lifecycle/subject-requests/{id}/decide:
    post:
      tags:
        - Data Lifecycle
      summary: Approve (and execute) or reject a pending erasure (checker half)
      description: ADR-0094 Decision 3. Gated by data_lifecycle.subject_erasure.approve — a DIFFERENT permission from the maker's, and holding both is a critical SoD conflict (data_lifecycle.subject_erasure_maker_checker). Approving executes the erasure inside the SAME transaction as the status claim, so two simultaneous approvals cannot both run. Rejecting is a first-class outcome with its own reason. Both are audited critical. Requires Idempotency-Key.
      operationId: dataLifecycleSubjectEraseDecide
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DataLifecycleSubjectDecisionInput"
      responses:
        "200":
          description: Decision recorded (and, when approved, the erasure executed), or replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      request:
                        $ref: "#/components/schemas/DataLifecycleSubjectRequest"
                      erasedTables:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleSubjectErasureTarget"
                      skippedColumns:
                        type: array
                        description: Columns a descriptor named as redactable that the engine did NOT write, because their type cannot hold the sentinel. Reported rather than swallowed.
                        items:
                          type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The decision was attempted by the requester (SOD_MAKER_IS_CHECKER), the request is no longer pending (REQUEST_NOT_PENDING), or the Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/data-lifecycle/subject-requests/erase:
    post:
      tags:
        - Data Lifecycle
      summary: Request erasure of a data subject (maker half)
      description: "ADR-0094 Decision 3. Gated by data_lifecycle.subject_erasure.create and it ERASES NOTHING — it records a request for somebody else to decide. Execution lives behind POST /{id}/decide under a different permission, and the row's own CHECK constraint refuses a decision by its own requester, so a caller holding both keys still cannot approve their own request. High-risk: requires Idempotency-Key, a stated ground, audited critical. The response previews what approving it would actually WRITE (`wouldWrite`) — far fewer tables than the subject appears in, because most are severed by anonymising the identity row and need no write of their own."
      operationId: dataLifecycleSubjectEraseRequest
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DataLifecycleSubjectRequestInput"
      responses:
        "200":
          description: Erasure request recorded (pending approval), or replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      request:
                        $ref: "#/components/schemas/DataLifecycleSubjectRequest"
                      wouldWrite:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleSubjectErasureTarget"
                      retained:
                        type: array
                        description: Tables an erasure deliberately leaves ALONE, and why.
                        items:
                          $ref: "#/components/schemas/DataLifecycleSubjectUnanswered"
                      unanswered:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleSubjectUnanswered"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/data-lifecycle/subject-requests/export:
    post:
      tags:
        - Data Lifecycle
      summary: Export everything this tenant holds about a data subject
      description: "ADR-0094. Gated by data_lifecycle.subject_request.export — its OWN permission, because export and erasure are not one authority. Audited as a DISCLOSURE (action subject_data.disclosed, severity critical) rather than as a read, and a durable row lands in awcms_subject_requests in the same transaction. Requires Idempotency-Key and a stated reason. The response carries `unanswered`: the tables this report deliberately does not reach (global, or matchable by no column) with the reason each owning module wrote — a subject-access report that is incomplete and does not say so is worse than none, because it is signed. Answered PER TENANT (ADR-0094 Decision 1): there is no cross-tenant export."
      operationId: dataLifecycleSubjectExport
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DataLifecycleSubjectRequestInput"
      responses:
        "200":
          description: The subject's data, or replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      request:
                        $ref: "#/components/schemas/DataLifecycleSubjectRequest"
                      tables:
                        type: array
                        items:
                          $ref: "#/components/schemas/DataLifecycleSubjectTableExport"
                      unanswered:
                        type: array
                        description: What this answer does NOT cover, and why.
                        items:
                          $ref: "#/components/schemas/DataLifecycleSubjectUnanswered"
                      truncatedTables:
                        type: array
                        description: Keys of tables that hit the per-table row cap, so the report is INCOMPLETE for them. Empty means complete — a claim worth being able to make explicitly rather than by the absence of a warning. The cap is a safety valve against a pathological subject (an automation account named as actor on a million audit rows), not a page size; there is deliberately no cursor, because a "complete answer" assembled across pages has a boundary at every request where a partial answer can be mistaken for the whole one.
                        items:
                          type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/database/pool/health:
    get:
      operationId: getDatabasePoolHealth
      tags:
        - Foundation
      summary: Database pool/work-class saturation, circuit-breaker state, and per-process capacity for this instance.
      security: []
      responses:
        "200":
          description: Aggregate pool health (never exposes tenant data or query content).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      status:
                        type: string
                        enum:
                          - healthy
                          - degraded
                          - unhealthy
                      databaseReachable:
                        type: boolean
                      circuitBreakerState:
                        type: string
                        enum:
                          - closed
                          - open
                          - half_open
                      workClasses:
                        type: array
                        items:
                          type: object
                          properties:
                            workClass:
                              type: string
                            active:
                              type: integer
                            max:
                              type: integer
                            queued:
                              type: integer
                      capacity:
                        type: object
                        properties:
                          processClass:
                            type: string
                          poolMax:
                            type: integer
                          approvedConnections:
                            type: integer
                          reservedAdminHeadroom:
                            type: integer
                      generatedAt:
                        type: string
                        format: date-time
  /api/v1/domain-events/consumers:
    get:
      operationId: listDomainEventConsumers
      tags:
        - Domain Event Runtime
      summary: List the static consumer registry with per-tenant pause state and backlog counts.
      responses:
        "200":
          description: Consumer registry with pause/backlog state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      consumers:
                        type: array
                        items:
                          type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/domain-events/consumers/{name}/pause:
    post:
      operationId: pauseDomainEventConsumer
      tags:
        - Domain Event Runtime
      summary: Pause a domain event consumer for this tenant (reason required, audited).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
                  minLength: 1
                  maxLength: 500
      responses:
        "200":
          description: Updated consumer state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      consumer:
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/domain-events/consumers/{name}/resume:
    post:
      operationId: resumeDomainEventConsumer
      tags:
        - Domain Event Runtime
      summary: Resume a paused domain event consumer for this tenant (audited).
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Updated consumer state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      consumer:
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/domain-events/deliveries:
    get:
      operationId: listDomainEventDeliveries
      tags:
        - Domain Event Runtime
      summary: List consumer delivery/attempt status (status=dead_letter is the DLQ view).
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - pending
              - delivered
              - dead_letter
              - skipped
        - name: consumerName
          in: query
          required: false
          schema:
            type: string
        - name: eventType
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Delivery list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      deliveries:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/domain-events/deliveries/{id}:
    get:
      operationId: getDomainEventDelivery
      tags:
        - Domain Event Runtime
      summary: Fetch one delivery with its joined event (redacted payload projection). DLQ inspection view.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Delivery detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      delivery:
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/domain-events/deliveries/{id}/replay:
    post:
      operationId: replayDomainEventDelivery
      tags:
        - Domain Event Runtime
      summary: Replay a dead-lettered delivery (permission-gated, reason-required, idempotent, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
                  minLength: 1
                  maxLength: 500
      responses:
        "200":
          description: The newly created replay delivery.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      delivery:
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Delivery not dead-lettered, or consumer no longer supports the event schema.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          $ref: "#/components/responses/BadRequest"
  /api/v1/domain-events/events:
    get:
      operationId: listDomainEvents
      tags:
        - Domain Event Runtime
      summary: List domain event outbox entries (redacted payload projections only, max 200).
      parameters:
        - name: eventType
          in: query
          required: false
          schema:
            type: string
        - name: aggregateType
          in: query
          required: false
          schema:
            type: string
        - name: aggregateId
          in: query
          required: false
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Domain event list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      events:
                        type: array
                        items:
                          type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/domain-events/events/{id}:
    get:
      operationId: getDomainEvent
      tags:
        - Domain Event Runtime
      summary: Fetch one domain event outbox entry (redacted payload projection only).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Domain event detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      event:
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/email/announcements:
    post:
      tags:
        - Email
      operationId: emailCreateAnnouncement
      summary: Enqueue a notification/announcement (idempotent, two-tier ABAC, audited).
      description: Requires Idempotency-Key. email.notification.create for every request; email.announcement.create additionally for role/tenant targets.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                templateKey:
                  type: string
                variables:
                  type: object
                target:
                  type: object
                locale:
                  type: string
      responses:
        "200":
          description: Recipients enqueued.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      recipientCount:
                        type: integer
                      truncated:
                        type: boolean
                        description: True when more users matched the target than the per-request recipient cap, so the remainder were NOT enqueued. Callers must treat a truncated response as a partial send and re-target the remainder.
                      correlationId:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/email/announcements/preview:
    post:
      tags:
        - Email
      operationId: emailPreviewAnnouncement
      summary: Dry-run announcement targeting — recipient count + synthetic sample only.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                templateKey:
                  type: string
                variables:
                  type: object
                target:
                  type: object
                locale:
                  type: string
      responses:
        "200":
          description: Preview (count + sample render).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      matchedCount:
                        type: integer
                        description: Capped at the per-request recipient cap. Read together with `truncated` — a capped preview under-reports the real audience.
                      truncated:
                        type: boolean
                        description: True when more users matched the target than the cap, so `matchedCount` is the cap and not the real total.
                      sample:
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/email/messages:
    get:
      tags:
        - Email
      operationId: emailListMessages
      summary: Email delivery-queue diagnostics (masked recipient only, keyset-paginated).
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - queued
              - sending
              - sent
              - failed
              - retry_wait
              - cancelled
              - suppressed
        - name: cursor
          in: query
          required: false
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Queue page.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/email/messages/{id}/cancel:
    post:
      tags:
        - Email
      operationId: emailCancelMessage
      summary: Cancel a still-queued (queued/retry_wait) email message (audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Message cancelled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The message is past a cancellable state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/email/suppressions:
    get:
      tags:
        - Email
      operationId: emailListSuppressions
      summary: List the email suppression list (masked recipient only).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Suppression entries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Email
      operationId: emailCreateSuppression
      summary: Manually suppress a recipient address (audited).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                recipient:
                  type: string
                reason:
                  type: string
                  enum:
                    - bounced
                    - complained
                    - manual
                    - unsubscribed
      responses:
        "200":
          description: Recipient suppressed (or already suppressed).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/email/suppressions/{id}:
    delete:
      tags:
        - Email
      operationId: emailDeleteSuppression
      summary: Remove a suppression entry (hard delete, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Suppression entry removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/email/templates:
    get:
      tags:
        - Email
      operationId: emailListTemplates
      summary: List tenant email templates (active by default, newest first).
      parameters:
        - name: includeInactive
          in: query
          required: false
          schema:
            type: boolean
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Template list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Email
      operationId: emailCreateTemplate
      summary: Create an email template (audited).
      description: 409 if an active template already exists for the templateKey.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                templateKey:
                  type: string
                name:
                  type: string
                subjectTemplate:
                  type: object
                textBodyTemplate:
                  type: object
                htmlBodyTemplate:
                  type: object
                isActive:
                  type: boolean
      responses:
        "200":
          description: Template created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: An active template already exists for the templateKey.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/email/templates/{id}:
    get:
      tags:
        - Email
      operationId: emailGetTemplate
      summary: Get one email template (full locale map).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Template detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Email
      operationId: emailUpdateTemplate
      summary: Partially update an email template (audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                subjectTemplate:
                  type: object
                textBodyTemplate:
                  type:
                    - object
                    - "null"
                htmlBodyTemplate:
                  type:
                    - object
                    - "null"
                isActive:
                  type: boolean
      responses:
        "200":
          description: Template updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Email
      operationId: emailDeleteTemplate
      summary: Soft-delete an email template (reason required, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Template soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/email/templates/{id}/preview:
    post:
      tags:
        - Email
      operationId: emailPreviewTemplate
      summary: Render a template with synthetic sample data (never a real recipient).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                locale:
                  type: string
                variables:
                  type: object
      responses:
        "200":
          description: Rendered preview.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/email/templates/{id}/restore:
    post:
      tags:
        - Email
      operationId: emailRestoreTemplate
      summary: Restore a soft-deleted email template (audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Template restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/form-drafts:
    get:
      operationId: listFormDrafts
      tags:
        - Form Drafts
      summary: List this tenant's non-deleted form drafts.
      description: "Bounded to the 100 most recently updated non-deleted drafts, newest first. No pagination cursor: this is scratch state a caller filters by its own moduleKey/wizardKey, not a browsable archive."
      parameters:
        - name: moduleKey
          in: query
          required: false
          schema:
            type: string
        - name: wizardKey
          in: query
          required: false
          schema:
            type: string
        - name: status
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/FormDraftStatus"
      responses:
        "200":
          description: Form draft list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      drafts:
                        type: array
                        items:
                          $ref: "#/components/schemas/FormDraft"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createFormDraft
      tags:
        - Form Drafts
      summary: Create a form draft.
      description: "Deliberately NOT idempotency-keyed: the worst case for a retry is one extra low-value scratch row the caller can delete, not a domain side effect. Submitting is the operation that needs a key."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FormDraftCreateRequest"
      responses:
        "200":
          description: Created form draft.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/FormDraft"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "413":
          description: Request body exceeded the endpoint's size ceiling.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/form-drafts/{id}:
    get:
      operationId: getFormDraft
      tags:
        - Form Drafts
      summary: Read one form draft.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Form draft.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/FormDraft"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateFormDraft
      tags:
        - Form Drafts
      summary: Update a draft's step, payload, or expiry.
      description: Only a draft still in `draft` status can be updated; a submitted, abandoned, or expired draft returns 404 rather than distinguishing "wrong state" from "does not exist".
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FormDraftUpdateRequest"
      responses:
        "200":
          description: Updated form draft.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/FormDraft"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Request body exceeded the endpoint's size ceiling.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: deleteFormDraft
      tags:
        - Form Drafts
      summary: Soft-delete (abandon) a form draft.
      description: Idempotent by construction — the `deleted_at IS NULL` guard makes a second call a safe no-op, so no Idempotency-Key is required.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Draft abandoned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
                        enum:
                          - true
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/form-drafts/{id}/submit:
    post:
      operationId: submitFormDraft
      tags:
        - Form Drafts
      summary: Mark a draft as submitted.
      description: "High-risk: hands the payload to a domain action, so an Idempotency-Key is required and the same key with a different body is a conflict. Guarded on `form_drafts.draft.update` — there is no separate `submit` permission, because an action nobody seeds into a role denies even the tenant owner while looking correct."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Submitted form draft.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/FormDraft"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/health:
    get:
      operationId: getHealth
      tags:
        - Foundation
      summary: Liveness/module-count probe.
      security: []
      responses:
        "200":
          description: Service is up.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      status:
                        type: string
                      service:
                        type: string
                      runtime:
                        type: string
                      moduleCount:
                        type: integer
                      generatedAt:
                        type: string
                        format: date-time
  /api/v1/identity/business-scope/assignments:
    get:
      operationId: listBusinessScopeAssignments
      tags:
        - Identity & Access
      summary: "List this tenant's business-scope assignments (Issue #180)."
      description: Lists business-scope assignments for the caller's tenant, optionally filtered by status/tenantUserId/scopeType. Gated on `identity_access.business_scope_assignments.read`.
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - active
              - expired
              - revoked
        - name: tenantUserId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: scopeType
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: The tenant's business-scope assignments (newest first, bounded).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assignments:
                        type: array
                        items:
                          $ref: "#/components/schemas/BusinessScopeAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createBusinessScopeAssignment
      tags:
        - Identity & Access
      summary: Create a business-scope assignment (high-risk, audited, idempotent).
      description: Grants a subject a role/permission context restricted to one business scope. The `(scopeType, scopeId)` reference is validated server-side through the `BusinessScopeHierarchyPort` capability (never trusted from the request alone); an unresolved scope is denied `SCOPE_UNRESOLVED`. Self-grant is denied. Gated on `identity_access.business_scope_assignments.create`. Requires `Idempotency-Key`.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantUserId
                - scopeType
                - scopeId
              properties:
                tenantUserId:
                  type: string
                  format: uuid
                roleId:
                  type: string
                  format: uuid
                  nullable: true
                scopeType:
                  type: string
                  description: Lowercase snake_case scope type (e.g. "office").
                scopeId:
                  type: string
                  format: uuid
                effectiveFrom:
                  type: string
                  format: date-time
                effectiveTo:
                  type: string
                  format: date-time
                  nullable: true
                isTemporary:
                  type: boolean
                reason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The created business-scope assignment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assignment:
                        $ref: "#/components/schemas/BusinessScopeAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/assignments/{id}/revoke:
    post:
      operationId: revokeBusinessScopeAssignment
      tags:
        - Identity & Access
      summary: Revoke a business-scope assignment (high-risk, audited, idempotent).
      description: Revokes an active business-scope assignment (transitions it to `revoked`; append-only lifecycle history is recorded). Revocation takes effect on the next authorization decision immediately. Gated on `identity_access.business_scope_assignments.revoke`. Requires `Idempotency-Key`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - revokeReason
              properties:
                revokeReason:
                  type: string
      responses:
        "200":
          description: The revoked business-scope assignment.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      assignment:
                        $ref: "#/components/schemas/BusinessScopeAssignment"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The assignment is not active, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/conflicts:
    get:
      operationId: listSoDConflictEvaluations
      tags:
        - Identity & Access
      summary: "List the SoD conflict evaluation log (Issue #181)."
      description: "Keyset-paginated, permission-gated segregation-of-duties conflict evaluation history (the conflict preview / audit view). Recorded for every assignment-create and high-risk-action conflict check, regardless of outcome. Safe projection: rule key, subject id, trigger context, outcome, reason, and timestamp only. Gated on `identity_access.business_scope_conflicts.read`."
      parameters:
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Opaque keyset cursor from a previous page's `nextCursor`.
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
        - name: ruleKey
          in: query
          required: false
          schema:
            type: string
        - name: conflictDetected
          in: query
          required: false
          schema:
            type: string
            enum:
              - "true"
              - "false"
      responses:
        "200":
          description: A page of SoD conflict evaluations (newest first) plus the next cursor.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      conflicts:
                        type: array
                        items:
                          $ref: "#/components/schemas/SoDConflictEvaluation"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/identity/business-scope/exceptions:
    get:
      operationId: listSoDConflictExceptions
      tags:
        - Identity & Access
      summary: "List this tenant's SoD conflict exceptions (Issue #181)."
      description: Lists the tenant's segregation-of-duties conflict exceptions, optionally filtered by `status`/`ruleKey`. Gated on `identity_access.business_scope_exceptions.read`.
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - pending
              - approved
              - rejected
              - expired
              - revoked
        - name: ruleKey
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: The tenant's SoD conflict exceptions (newest first, bounded).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exceptions:
                        type: array
                        items:
                          $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createSoDConflictException
      tags:
        - Identity & Access
      summary: Request a SoD conflict exception (high-risk, audited, idempotent).
      description: 'Requests a bounded-lifetime, scope-bound exception to a registered SoD rule (`status: "pending"`, requires separate approval by a different user holding the rule''s approval permission). The rule must exist in the code registry and permit exceptions; the exception must have an end date (no indefinite override). Gated on `identity_access.business_scope_exceptions.create`. Requires `Idempotency-Key`.'
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - ruleKey
                - subjectTenantUserId
                - justification
                - effectiveTo
              properties:
                ruleKey:
                  type: string
                subjectTenantUserId:
                  type: string
                  format: uuid
                scopeType:
                  type: string
                  nullable: true
                  description: Set together with scopeId for a scope-specific exception; both null for a blanket exception.
                scopeId:
                  type: string
                  format: uuid
                  nullable: true
                justification:
                  type: string
                  minLength: 10
                effectiveFrom:
                  type: string
                  format: date-time
                effectiveTo:
                  type: string
                  format: date-time
      responses:
        "200":
          description: The created (pending) SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/exceptions/{id}/approve:
    post:
      operationId: approveSoDConflictException
      tags:
        - Identity & Access
      summary: Approve a pending SoD conflict exception (high-risk, audited, idempotent).
      description: |-
        Approves a pending exception (the sanctioned administrative override). Self-approval is denied on BOTH independence axes, re-checked from the DB row: the approver may be neither the requester nor the subject.

        TWO permissions are checked. Every approval needs the dedicated `identity_access.business_scope_exceptions.approve`. A rule may also name its own checker in `SoDRuleDescriptor.exceptionPolicy.requiresApprovalPermission`, and when that differs from the dedicated key the caller must hold it too (403 `ACCESS_DENIED`). An exception whose rule no installed module declares any more cannot be approved at all (403 `SOD_RULE_UNKNOWN`); rejecting and revoking it stay available.

        Requires `Idempotency-Key`; audited at `critical` severity.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                decisionReason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The approved SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The exception is not pending, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/exceptions/{id}/reject:
    post:
      operationId: rejectSoDConflictException
      tags:
        - Identity & Access
      summary: Reject a pending SoD conflict exception (audited, idempotent).
      description: Rejects a pending exception — the safe outcome (the conflict stays denied). Gated on `identity_access.business_scope_exceptions.reject`. Requires `Idempotency-Key`; audited at `warning` severity.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                decisionReason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The rejected SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The exception is not pending, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/identity/business-scope/exceptions/{id}/revoke:
    post:
      operationId: revokeSoDConflictException
      tags:
        - Identity & Access
      summary: Revoke an approved SoD conflict exception (high-risk, audited, idempotent).
      description: Revokes a previously approved exception, ending the override early (immediately ineffective at the next decision). Reason required. Gated on `identity_access.business_scope_exceptions.revoke`. Requires `Idempotency-Key`; audited at `critical` severity.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - revokeReason
              properties:
                revokeReason:
                  type: string
      responses:
        "200":
          description: The revoked SoD conflict exception.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      exception:
                        $ref: "#/components/schemas/SoDConflictException"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The exception is not approved, or the Idempotency-Key was reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/idn-regions/datasets:
    get:
      tags:
        - Indonesia Regions
      summary: List imported region dataset versions with their provenance
      description: "Gated by idn_admin_regions.dataset.read. Newest import first, with upstream repository, commit SHA, file checksum, decree reference, row count, and lifecycle status. The official-reference caveat travels in the same response body: anyone reading this is deciding whether to trust these rows for something official, and the answer — a community packaging of the decree, not a Kemendagri feed — has to arrive with the data."
      operationId: idnRegionsDatasetList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Imported dataset versions plus static source provenance.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/IdnRegionDataset"
                      activeDatasetCode:
                        type: string
                        nullable: true
                      provenance:
                        $ref: "#/components/schemas/IdnRegionSourceProvenance"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/idn-regions/datasets/{id}/activate:
    post:
      tags:
        - Indonesia Regions
      summary: Activate one imported dataset version
      description: "Gated by idn_admin_regions.dataset.configure. High-risk, `Idempotency-Key` required, audited with the dataset codes on both sides of the switch: this changes what every address form and regional report in the product returns, for every tenant at once. Activating the already-active dataset is a no-op success (`changed: false`), because a retried request that lands on the state it asked for succeeded. Only one dataset can be active — enforced by a partial unique index, so two concurrent activations end with one committed and one rejected."
      operationId: idnRegionsDatasetActivate
      parameters:
        - name: id
          in: path
          required: true
          description: Dataset UUID.
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The activated dataset.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      dataset:
                        $ref: "#/components/schemas/IdnRegionDataset"
                      changed:
                        type: boolean
                        description: false when the dataset was already active.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key was already used with a different request, or the dataset cannot be activated/rolled back in its current state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/idn-regions/datasets/rollback:
    post:
      tags:
        - Indonesia Regions
      summary: Roll back to the previously active dataset version
      description: Gated by idn_admin_regions.dataset.restore. High-risk, `Idempotency-Key` required, audited. The target is resolved from `activated_at` history, never supplied by the caller — letting the client name the destination would make this an activation wearing a safer-sounding name. Returns 409 when there is no previously activated version.
      operationId: idnRegionsDatasetRollback
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The dataset that is now active.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      dataset:
                        $ref: "#/components/schemas/IdnRegionDataset"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key was already used with a different request, or the dataset cannot be activated/rolled back in its current state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/idn-regions/regions:
    get:
      tags:
        - Indonesia Regions
      summary: Look up Indonesia administrative regions
      description: "Gated by idn_admin_regions.region.read. Filter by tier (`level` 1-4), by `parentCode`, and/or by `search` (case-folded substring over the normalized name); keyset-paginated on `code`. Reads the ACTIVE dataset unless `dataset` names another version. When no dataset has been activated the response is an empty page with `reason: no_active_dataset` — a fresh install is a real state, not an error. The rows are global reference data, but the endpoint still requires a session, a tenant context, and the permission grant."
      operationId: idnRegionsList
      parameters:
        - name: dataset
          in: query
          required: false
          description: Dataset code to query. Defaults to the active dataset.
          schema:
            type: string
        - name: level
          in: query
          required: false
          description: 1 province, 2 regency/city, 3 district, 4 village. An out-of-range value is rejected rather than ignored.
          schema:
            type: integer
            minimum: 1
            maximum: 4
        - name: parentCode
          in: query
          required: false
          description: Dotted code of the parent region, e.g. `11.01` to list its districts.
          schema:
            type: string
        - name: search
          in: query
          required: false
          description: Case-folded substring of the region name. LIKE wildcards in the input are matched literally.
          schema:
            type: string
        - name: limit
          in: query
          required: false
          description: Page size, clamped to 200.
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
        - name: after
          in: query
          required: false
          description: Keyset cursor — the `code` of the last item on the previous page.
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: One page of regions from the resolved dataset.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      datasetCode:
                        type: string
                        nullable: true
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/IdnRegionItem"
                      nextCursor:
                        type: string
                        nullable: true
                      reason:
                        type: string
                        nullable: true
                        enum:
                          - no_active_dataset
                          - dataset_not_found
                          - null
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/idn-regions/regions/{code}:
    get:
      tags:
        - Indonesia Regions
      summary: Read one region by its Kemendagri code
      description: Gated by idn_admin_regions.region.read. Returns the region plus its resolved ancestor path, so a caller rendering a full address label needs one request instead of four.
      operationId: idnRegionsGet
      parameters:
        - name: code
          in: path
          required: true
          description: Dotted Kemendagri code, e.g. `11`, `11.01`, `11.01.01`, `11.01.01.2001`.
          schema:
            type: string
        - name: dataset
          in: query
          required: false
          description: Dataset code to query. Defaults to the active dataset.
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The region.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      datasetCode:
                        type: string
                        nullable: true
                      region:
                        $ref: "#/components/schemas/IdnRegionItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/invitations:
    get:
      operationId: listInvitations
      tags:
        - Identity & Access
      summary: List this tenant's invitations (identity_access.invitations.read).
      description: |-
        Keyset-paginated, newest first. The invitee's address is MASKED: these rows are addresses of people who are not users here — often people who never will be — so the unmasked value has no reason to leave the database.
        `roleCodes` is what the invitation carries, and it becomes a real grant only on acceptance. Nothing in this list confers access.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - pending
              - accepted
              - revoked
              - expired
        - name: cursor
          in: query
          required: false
          description: An opaque cursor from a previous response's `nextCursor`. A cursor that does not decode is a 400, never a silent restart.
          schema:
            type: string
      responses:
        "200":
          description: A page of invitations, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      invitations:
                        type: array
                        items:
                          $ref: "#/components/schemas/InvitationSummary"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createInvitation
      tags:
        - Identity & Access
      summary: Invite a person to this tenant (identity_access.invitations.create, audited).
      description: |-
        Issues an offer of membership and mails its link. No account exists until the link is used.
        Inviting and GRANTING A ROLE are two authorities: a body naming `roleIds` additionally requires `identity_access.access_control.assign`, so an administrator holding only `invitations.create` can admit a person and nothing more. A role marked `is_system` is refused (409 ROLE_SYSTEM_PROTECTED), and unknown role ids refuse the whole invitation rather than granting a subset.
        `skipEmailConfirmation` requires the PLATFORM-scoped `identity_access.invitations.configure`, unless the address already holds an active identity in this tenant — which is the same proof, already given. It removes the only evidence that the recipient controls that mailbox.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateInvitationInput"
      responses:
        "201":
          description: The invitation was issued. `delivery` is `unavailable` when the tenant has no active `auth.invitation` template or the address is suppressed — the invitation still exists and can be resent.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/InvitationCreated"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The address already has an account (IDENTIFIER_TAKEN) or a pending invitation (INVITATION_ALREADY_PENDING); a named role is a system role (ROLE_SYSTEM_PROTECTED); or the Idempotency-Key was already used with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/invitations/{id}/resend:
    post:
      operationId: resendInvitation
      tags:
        - Identity & Access
      summary: Resend an invitation, rotating its token (identity_access.invitations.create, audited).
      description: |-
        Guarded by `create`, not by an action of its own: resend MINTS A NEW TOKEN, which is exactly the authority `create` already names.
        The previous link stops working immediately. Without rotation, "resend" would grow N live links from one invitation, and revoking it would mean revoking N secrets nobody counted.
        Capped at five resends per invitation by a database CHECK; past that, revoke and issue a new one. No Idempotency-Key — replaying would have to return a token that has already been rotated away, or persist a plaintext token in the idempotency store.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: A fresh link was issued and the previous one is dead.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      resendCount:
                        type: integer
                      delivery:
                        type: string
                        enum:
                          - queued
                          - unavailable
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The resend ceiling has been reached (INVITATION_RESEND_LIMIT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/invitations/{id}/revoke:
    post:
      operationId: revokeInvitation
      tags:
        - Identity & Access
      summary: Revoke a pending invitation (identity_access.invitations.revoke, audited).
      description: |-
        Kills the link; keeps the row. The surviving row is what answers "who offered what, to whom, and what happened" — which is why this activity has no `delete`.
        An invitation that is not pending and one that does not exist answer identically, so the response cannot be used to enumerate ids or to learn that a given invitation was already accepted.
        No Idempotency-Key: the UPDATE carries `AND status = 'pending'`, so a double submit revokes once and the second call answers 404.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  nullable: true
                  maxLength: 500
      responses:
        "200":
          description: The invitation is revoked and its link is dead.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - revoked
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/logs/audit:
    get:
      operationId: listAuditEvents
      tags:
        - Logging & Audit
      summary: List audit trail events for the current tenant.
      parameters:
        - name: resourceType
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Audit event list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/AuditEvent"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/media/enforcement:
    get:
      tags:
        - News Media
      summary: Read whether managed-media enforcement is active for this tenant
      description: Gated by media_library.enforcement.read. Reports whether this tenant's content media references must resolve to verified registry objects, and — when enforcement cannot be enabled — the deployment-config reasons why (ADR-0036 step 5a). `reasons` name environment variables, never their values, so nothing secret is exposed; the endpoint is still permission-gated rather than public.
      operationId: mediaEnforcementRead
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Current enforcement state for this tenant plus this deployment's media readiness.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/MediaEnforcementState"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - News Media
      summary: Turn managed-media enforcement ON for this tenant (one-way)
      description: "Gated by media_library.enforcement.enable. High-risk, requires Idempotency-Key. Enables managed-media enforcement: from then on, content media references must resolve to verified, same-tenant registry objects rather than raw URLs (ADR-0036 step 5a). This is the switch a brochure-site tenant (`blog_content` + `tenant_domain`, no news portal) previously did not have. This operation is one-way and there is deliberately no counterpart that disables enforcement — a tenant able to switch its own media validation off is a confirmed exploit this design exists to prevent (see migration `sql/043`'s header). A deployment that must roll back does so by changing its `NEWS_MEDIA_R2_*` configuration. Idempotent: re-enabling an already-enforcing tenant succeeds, refreshes the timestamp, and returns `alreadyEnforced: true`; only a successful enable is recorded under the Idempotency-Key, so a rejected (not-ready) attempt may retry the same key after the R2 config is fixed."
      operationId: mediaEnforcementEnable
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Enforcement is active for this tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/MediaEnforcementEnabled"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Either the deployment's media storage is not ready, so enforcement cannot be enabled (`MANAGED_MEDIA_NOT_READY`) — `error.details.reasons` says which check failed; a 409 rather than a 400 on purpose (the request is well-formed and the caller is authorized; it is the deployment, not the request body, that must change) — or the Idempotency-Key was reused with a different request (`IDEMPOTENCY_CONFLICT`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/media/news-images/upload-sessions:
    post:
      tags:
        - News Media
      summary: Create a direct-to-R2 presigned upload session for a news image
      description: Gated by media_library.media.create. Returns a `pending_upload` metadata row plus a short-lived presigned PUT URL scoped to exactly one server-generated object key. Raw R2 credentials are never exposed to the browser.
      operationId: newsMediaUploadSessionsCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNewsMediaUploadSessionRequest"
      responses:
        "200":
          description: Upload session created — a `pending_upload` metadata row plus a short-lived presigned PUT URL.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/NewsMediaUploadSessionCreated"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "502":
          description: News media R2 storage is not configured/enabled for this deployment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/media/news-images/upload-sessions/{id}/cancel:
    post:
      tags:
        - News Media
      summary: Cancel a still-pending-upload session
      description: Gated by media_library.media.cancel. Transitions a `pending_upload` session to `failed`.
      operationId: newsMediaUploadSessionsCancel
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Upload session cancelled (status `failed`).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/NewsMediaObjectItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Upload session is not `pending_upload` (already uploaded/verified/attached/failed).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/media/news-images/upload-sessions/{id}/finalize:
    post:
      tags:
        - News Media
      summary: Finalize an upload session — real R2 GET + magic-byte MIME sniffing + server-side SHA-256 checksum (never a bare HEAD)
      description: Gated by media_library.media.verify. High-risk, requires Idempotency-Key. Verifies the object actually uploaded to R2 (HEAD for existence/real size, then a full GET), sniffs the MIME type from the object's real magic bytes, and computes a SHA-256 checksum server-side. A client-claimed `checksumSha256` is only a transport-corruption cross-check, never a substitute for the MIME sniff — HEAD alone can never promote a media object to `verified`.
      operationId: newsMediaUploadSessionsFinalize
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinalizeNewsMediaUploadSessionRequest"
      responses:
        "200":
          description: Object verified — media object status is now `verified` (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/NewsMediaObjectItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Upload session is not `pending_upload`, has expired (`UPLOAD_SESSION_EXPIRED`), or the Idempotency-Key was reused with a different request (`IDEMPOTENCY_CONFLICT`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: Uploaded object failed content verification (`UPLOAD_VERIFICATION_FAILED`). `error.details.reason` is one of `object_not_found`, `size_exceeded`, `mime_not_recognized`, `mime_not_allowed`, `mime_mismatch`, `checksum_mismatch`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: Unable to verify the uploaded object right now (R2 provider error/circuit breaker open) — retry shortly.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/media/objects:
    get:
      operationId: mediaResolveObjects
      tags:
        - News Media
      summary: Batch-resolve media object ids to their public reference.
      description: |
        Resolves up to 100 media object ids at once to `{publicUrl, altText, mimeType, width, height, sizeBytes, creditLine, sourceName, copyrightStatus}`. Gated on `media_library.media.read`; read-only, so a machine credential (ADR-0049) may hold it.

        Issue #782 — `creditLine`/`sourceName`/`copyrightStatus` are populated ONLY when the object's rights-verification decision is `verified`; otherwise all three are `null`. This is a distinct verification concept from the upload-integrity `verified`/`attached` status below.

        An object resolves ONLY when it is `verified` or `attached`, belongs to the calling tenant, and is not soft-deleted. Everything else — unknown, cross-tenant, unverified, deleted — is returned in `unresolved` rather than dropped, so a caller can tell "this resource has no image" from "this resource's image reference is broken". A malformed (non-uuid) id is a 400 instead: "you sent junk" and "that object is not referenceable" are different facts.

        Batch rather than one-per-id because a build feed resolves every image on a page at once, and the underlying query is already a single `id = ANY(...)`.
      parameters:
        - name: ids
          in: query
          required: true
          description: Comma-separated media object uuids, at most 100.
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Resolved references, plus the ids that did not resolve.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/ResolvedMediaReference"
                      unresolved:
                        type: array
                        description: Ids that are unknown, cross-tenant, unverified, or soft-deleted — one bucket on purpose, so the response is not an oracle over which.
                        items:
                          type: string
                          format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/media/objects/{id}:
    delete:
      operationId: mediaObjectSoftDelete
      tags:
        - News Media
      summary: Soft delete one media object.
      description: |
        Gated on `media_library.media.delete`. High-risk, requires `Idempotency-Key`. Body: `{ "reason": string }` — required, trimmed, at most 500 characters, and recorded on the audit row.

        Soft delete only: `deleted_at`/`deleted_by`/`delete_reason` are set and `status` is left alone. The R2 object is untouched.

        This BREAKS live references on purpose — `GET /api/v1/media/objects` resolves only non-deleted objects, so a post whose `featured_media_id` points here begins resolving to nothing immediately. That is the intended outcome for the case this endpoint serves (a policy-violating image must stop being served), and it is recoverable through `POST /api/v1/media/objects/{id}/restore`. The endpoint deliberately does not scan for referencing rows first: that would require this module to know its own consumers.

        An already-deleted object and an unknown id both answer 404 — a distinct "already deleted" would let a caller without `media.read` probe which ids exist.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SoftDeleteMediaObjectRequest"
      responses:
        "200":
          description: The media object is soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/MediaObjectLifecycleResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was reused with a different request (`IDEMPOTENCY_CONFLICT`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: mediaObjectRightsUpdate
      tags:
        - News Media
      summary: Edit a media object's usage-rights metadata.
      description: |
        Gated on `media_library.media.update` (Issue #615, `sql/137`) — the eighth media permission, added together with this endpoint rather than ahead of it. Requires `Idempotency-Key`: `update` is not a high-risk action, but a rights adjudication replayed by a retrying client would write a second audit entry claiming a second decision.

        **Issue #794 — the permission split by request content.** Since Issue #782/PR #791, `rightsVerificationStatus === 'verified'` makes `creditLine`/`sourceName`/`copyrightStatus` cross into the PUBLIC `GET /api/v1/media/objects` response, so `media.update` alone let whoever could type a credit line also self-attest it cleared for publication. The body's SHAPE now decides the required permission(s): a request that omits `rightsVerificationStatus` needs only `media_library.media.update`, unchanged. A request that includes `rightsVerificationStatus` (any transition, including into or out of `verified`/`rejected`) additionally needs `media_library.media.adjudicate_rights` — UNLESS `rightsVerificationStatus` is the ONLY field present, in which case `media_library.media.adjudicate_rights` is sufficient on its own (a designated rights reviewer need not also hold `media.update` to adjudicate). Either permission missing from the combination the request actually needs denies the WHOLE request with `403 ACCESS_DENIED` — never a partial write.

        **Rights, not accessibility, and not the byte check.** This endpoint deliberately cannot edit `altText` (an accessibility obligation) or `caption` (editorial copy). It is also not `media.verify`: that permission and a `verified` object status mean the BYTES passed a MIME-sniff and checksum. Whether a licence permits publication is a person reading a contract, and one word for both would make the legal half read as done whenever a file sniffed clean.

        Every field is optional but the body may not be empty. `null` clears a field; omitting it leaves the stored value alone. A request that changes nothing is refused, because an audit row for a no-op is indistinguishable from one for a decision.

        Changing `rightsVerificationStatus` stamps `rightsVerifiedBy` from the AUTHENTICATED actor and `rightsVerifiedAt` from the transaction clock — never from the body. Returning to `unverified` clears both, so the record never names a verifier for a verification that no longer stands.

        A soft-deleted object and an unknown id both answer 404.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MediaRightsUpdateRequest"
      responses:
        "200":
          description: The updated media object.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/MediaObjectRights"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was reused with a different request (`IDEMPOTENCY_CONFLICT`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/media/objects/{id}/purge:
    post:
      operationId: mediaObjectPurge
      tags:
        - News Media
      summary: Hard-delete the registry row of an already soft-deleted media object.
      description: |
        Gated on `media_library.media.purge`. High-risk, requires `Idempotency-Key`, and cannot be undone. No body.

        **Clears the registry row, NOT the R2 bytes.** The `news-media:reconcile` job owns the bucket and has the ordering discipline for deleting from it; a second writer here would mean two processes with different ideas of what is safe to remove. Accepted, stated cost: a window where the R2 object outlives its registry row, closed by the next reconciliation tick, which sees a key with no row and treats it as an orphan-in-R2.

        The object must ALREADY be soft-deleted — purging a live object answers 404 rather than destroying it.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The registry row is gone. The R2 object is removed later by the reconciliation job.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/MediaObjectLifecycleResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Another resource still holds a foreign key to this object (`MEDIA_OBJECT_REFERENCED` — remove the reference first), or the Idempotency-Key was reused with a different request (`IDEMPOTENCY_CONFLICT`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/media/objects/{id}/restore:
    post:
      operationId: mediaObjectRestore
      tags:
        - News Media
      summary: Undo a soft delete.
      description: |
        Gated on `media_library.media.restore`. High-risk, requires `Idempotency-Key`. No body.

        Clears `deleted_at`/`deleted_by`/`delete_reason` and stamps `restored_at`/`restored_by`. `status` is untouched — soft delete is orthogonal to it — so the object returns to whatever lifecycle state it was in.

        Restoring an object that is NOT soft-deleted answers 404 rather than succeeding as a no-op: "there was nothing to undo" and "it worked" must not share a response.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The media object is restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/MediaObjectRestored"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was reused with a different request (`IDEMPOTENCY_CONFLICT`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/media/objects/list:
    get:
      operationId: mediaObjectList
      tags:
        - News Media
      summary: Browse this tenant's media registry.
      description: |
        Gated on `media_library.media.read`. Keyset-paginated, newest first, 50 per page. Read-only, so a machine credential (ADR-0049) may hold it.

        **A separate path from `GET /api/v1/media/objects`, deliberately.** That endpoint demands `?ids=` — it is a batch RESOLVER built for the `awcms-astro` build to swap ids for public URLs. Teaching it a "no `ids` means list everything" branch would turn a request that is a 400 today into a dump of the whole registry: a contract change wearing the clothes of an addition, and one no existing caller could opt out of.

        `list` can never be mistaken for an object id — the object routes require a uuid and answer 400 otherwise — so this static path and `/{id}` do not contend.

        **Unlike the resolver, this returns rows in ANY status** (`pending_upload`, `failed`, `orphaned`) and, with `deletion`, soft-deleted ones. That inverts the resolver's public-safety rule on purpose: an administrator opens this list precisely because of the objects that are not healthy, and the lifecycle endpoints need a way to find their targets. Nothing returned here may be used as a public reference — that is what the resolver is for.

        The projection omits `bucket_name`/`storage_driver` (deployment facts) and `owner_resource_type`/`owner_resource_id` (vestigial since ADR-0036 moved attachment to the consumer's FK).

        `cursor` is opaque: pass back `nextCursor` verbatim. A malformed one is a 400, never silently treated as "no cursor" — that would serve page 1 to a caller who asked for page 4.
      parameters:
        - name: status
          in: query
          required: false
          description: Exact media object status. An unrecognised value is a 400, never ignored.
          schema:
            type: string
            enum:
              - pending_upload
              - uploaded
              - verified
              - attached
              - orphaned
              - deleted
              - failed
        - name: mimeType
          in: query
          required: false
          description: Exact mime type. Lowercased before matching, since that is how the registry stores it.
          schema:
            type: string
        - name: deletion
          in: query
          required: false
          description: Which soft-delete state to list. Defaults to `live`; `deleted` is what a restore/purge workflow needs, and a boolean could not ask for it.
          schema:
            type: string
            enum:
              - live
              - deleted
              - all
            default: live
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a previous response's `nextCursor`.
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: One page of media objects, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/MediaObjectListItem"
                      nextCursor:
                        type: string
                        nullable: true
                        description: Null on the last page.
                      pageSize:
                        type: integer
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/media/public-origin:
    get:
      operationId: mediaPublicOrigin
      tags:
        - News Media
      summary: The origin media public URLs are served from.
      description: |-
        Requires `media_library.media.read`. Deployment config, not tenant data: every tenant on a deployment is served from the same bucket.

        Exists for build clients such as `awcms-astro`, whose Content-Security-Policy must name the media host in `img-src` BEFORE it fetches any object — an image resolved correctly still renders as nothing when the policy blocks the host it lives on. The alternative is copying `NEWS_MEDIA_R2_PUBLIC_BASE_URL` into the consumer by hand, which is two copies of one value that agree until one is edited.

        `origin` is scheme + host + port, for the host-wide CSP form; `baseUrl` includes the path, for the tighter prefix form. Both are reported because neither choice is this API's to make.

        A deployment that serves no public media (LAN/offline profiles) answers `200` with `configured: false` rather than an error, so a build can omit the `img-src` entry instead of failing. A value that is set but unparseable, or on a scheme that cannot serve media, is reported the same way and never echoed back.
      responses:
        "200":
          description: The configured media origin, or an explicit "not configured".
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    required:
                      - configured
                      - origin
                      - baseUrl
                    properties:
                      configured:
                        type: boolean
                      origin:
                        type: string
                        nullable: true
                        example: https://media.example.com
                      baseUrl:
                        type: string
                        nullable: true
                        example: https://media.example.com/news
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/modules:
    get:
      operationId: listModuleCatalog
      tags:
        - Module Management
      summary: List the module catalog (code registry merged with DB lifecycle state).
      responses:
        "200":
          description: Module catalog.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      modules:
                        type: array
                        items:
                          $ref: "#/components/schemas/ModuleCatalogEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/modules/{moduleKey}:
    get:
      operationId: getModuleCatalogEntry
      tags:
        - Module Management
      summary: Fetch one module's catalog entry.
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Module detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleCatalogEntry"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/modules/{moduleKey}/audit:
    get:
      operationId: getModuleAuditSummary
      tags:
        - Module Management
      summary: Recent module-management activity for one module.
      description: 'Tenant enable/disable, settings updates, health checks and preset applies recorded against this module key. Guarded by `logging.audit_trail.read`, not a module-management permission: these are audit-log rows, and whoever may not read the audit log must not get a filtered view of it through another door. An unregistered `moduleKey` answers 404, because an empty list would read as "nothing happened".'
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Recent activity, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      moduleKey:
                        type: string
                      limit:
                        type: integer
                      entries:
                        type: array
                        items:
                          type: object
                          properties:
                            action:
                              type: string
                            resourceType:
                              type: string
                            severity:
                              type: string
                            message:
                              type: string
                            createdAt:
                              type: string
                              format: date-time
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/modules/{moduleKey}/health:
    get:
      operationId: getModuleHealth
      tags:
        - Module Management
      summary: Passive, bounded module readiness signals (no live provider call).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Module health report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleHealthReport"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/modules/{moduleKey}/health/check:
    post:
      operationId: checkModuleHealth
      tags:
        - Module Management
      summary: Explicit on-demand health check (records history; may run provider checks).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Module health report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleHealthReport"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/modules/{moduleKey}/jobs:
    get:
      operationId: listModuleJobs
      tags:
        - Module Management
      summary: The module's declared operational commands (documentation only).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Module job list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      moduleKey:
                        type: string
                      jobs:
                        type: array
                        items:
                          $ref: "#/components/schemas/ModuleJob"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/modules/{moduleKey}/permissions:
    get:
      operationId: getModulePermissionSync
      tags:
        - Module Management
      summary: Permission sync/status report for one module (read-only).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Permission sync report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModulePermissionSyncReport"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/modules/sync:
    post:
      operationId: syncModuleRegistry
      tags:
        - Module Management
      summary: Sync trusted code descriptors into the database registry.
      responses:
        "200":
          description: Sync result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleSyncResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/BadRequest"
  /api/v1/news-portal/ad-placements:
    get:
      tags:
        - News Portal Ad Placements
      summary: List this tenant's ad placements (admin view)
      description: Gated by news_portal.ad_placements.read.
      operationId: newsPortalAdPlacementsList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Ad placements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      placements:
                        type: array
                        items:
                          $ref: "#/components/schemas/AdPlacementItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - News Portal Ad Placements
      summary: Create an ad placement (R2-only image, verified media object required)
      description: Gated by news_portal.ad_placements.configure. `mediaObjectId` must reference a verified (`verified`/`attached`) R2 media object for this tenant.
      operationId: newsPortalAdPlacementsCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdPlacementCreateRequest"
      responses:
        "200":
          description: Ad placement created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AdPlacementItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: mediaObjectId does not exist, does not belong to this tenant, is not a verified R2 media object, or is not an allowed mime type for the target placement.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/news-portal/ad-placements/{id}:
    patch:
      tags:
        - News Portal Ad Placements
      summary: Update an ad placement (media reference/link/rotation/schedule/active)
      description: Gated by news_portal.ad_placements.configure.
      operationId: newsPortalAdPlacementsUpdate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AdPlacementUpdateRequest"
      responses:
        "200":
          description: Ad placement updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/AdPlacementItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: mediaObjectId does not exist, does not belong to this tenant, is not a verified R2 media object, or is not an allowed mime type for the target placement.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - News Portal Ad Placements
      summary: Soft-delete an ad placement
      description: Gated by news_portal.ad_placements.configure.
      operationId: newsPortalAdPlacementsDelete
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Ad placement deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/news-portal/ad-placements/active:
    get:
      tags:
        - News Portal Ad Placements
      summary: Currently-runnable creatives per slot, already rotated and capped
      description: Gated by blog_content.ad_placements.read. Returns all twelve slots, including the three this repo's own templates do not render — the sidebar exists in the consuming front end, and an endpoint shaped around what this repo draws would withhold inventory the consumer exists to show. Rotation and the per-slot item cap are applied HERE so four rotation modes are not re-implemented elsewhere; random_safe and weighted make the response deliberately not byte-stable, so it is not cacheable and does not try to be. An invalid targetType is refused rather than falling back to global, because silently widening an ad query is how a placement booked against one article appears on all of them.
      operationId: newsPortalAdPlacementsActive
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: targetType
          in: query
          required: false
          schema:
            type: string
            enum:
              - global
              - widget
              - post
              - page
          description: The kind of page being rendered. Omit for the global inventory.
        - name: targetId
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Required when targetType is not global, and refused when it is.
      responses:
        "200":
          description: Selected creatives per placement key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      slots:
                        type: object
                        additionalProperties:
                          type: array
                          items:
                            $ref: "#/components/schemas/PublicAdPlacement"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/news-portal/homepage-sections:
    get:
      tags:
        - News Portal Homepage Sections
      summary: List this tenant's homepage sections (admin view)
      description: Gated by news_portal.homepage_sections.read.
      operationId: newsPortalHomepageSectionsList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Homepage sections.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      sections:
                        type: array
                        items:
                          $ref: "#/components/schemas/HomepageSectionItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - News Portal Homepage Sections
      summary: Create a homepage section
      description: Gated by news_portal.homepage_sections.configure. `config` is validated per `sectionType`; every referenced post/category/media object must already exist for this tenant (and gallery_block media must be a verified R2 media object).
      operationId: newsPortalHomepageSectionsCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HomepageSectionCreateRequest"
      responses:
        "200":
          description: Homepage section created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/HomepageSectionItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: sectionKey is already in use for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: config references content that does not exist, does not belong to this tenant, or (for gallery_block) is not a verified R2 media object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/news-portal/homepage-sections/{id}:
    patch:
      tags:
        - News Portal Homepage Sections
      summary: Update a homepage section (title/config/sortOrder/isEnabled/schedule) — sectionType is immutable
      description: Gated by news_portal.homepage_sections.configure.
      operationId: newsPortalHomepageSectionsUpdate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HomepageSectionUpdateRequest"
      responses:
        "200":
          description: Homepage section updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/HomepageSectionItem"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: config references content that does not exist, does not belong to this tenant, or (for gallery_block) is not a verified R2 media object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - News Portal Homepage Sections
      summary: Soft-delete a homepage section
      description: Gated by news_portal.homepage_sections.configure.
      operationId: newsPortalHomepageSectionsDelete
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Homepage section deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/news-portal/homepage-sections/composed:
    get:
      tags:
        - News Portal Homepage Sections
      summary: The RESOLVED homepage, for a build client that renders its own templates
      description: "Gated by blog_content.homepage_sections.read. Returns what the front page actually shows, not its configuration: every reference resolved against live public content, the deterministic fallback applied to a curated slot whose articles have all gone, and the render caps already enforced. A consumer must not resolve the configuration itself — that would re-implement the publication predicate in a second repository on a second deploy cadence, and the first disagreement is a draft article on somebody's front page. Media arrive as resolved public URLs here (unlike /api/v1/site-profile/composed, which returns ids) because a homepage is many short-lived images rather than one long-lived logo."
      operationId: newsPortalHomepageComposed
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The composed homepage.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      isEmpty:
                        type: boolean
                        description: True when the tenant has composed nothing; the caller renders its own chronological listing.
                      sections:
                        type: array
                        items:
                          $ref: "#/components/schemas/ComposedHomepageSection"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/newsletter/confirm:
    post:
      operationId: newsletterConfirm
      tags:
        - Newsletter
      summary: Confirm a pending subscription
      security: []
      description: |-
        Anonymous and per-IP rate-limited. The moment consent is recorded: `consent_at` and `consent_ip_hash` are written here, because what happened is that somebody followed a link from an inbox.

        The token is SPENT on use — keeping a used bearer credential is keeping a credential. A token that matches nothing, one already spent, and a row in the wrong state all answer the same body as success; distinguishing them would let somebody holding a guessed token learn whether it was ever valid.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewsletterTokenRequest"
      responses:
        "200":
          description: Accepted. Says nothing about the token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewsletterNeutralResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many requests from this source (`RATE_LIMITED`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/newsletter/subscribe:
    post:
      operationId: newsletterSubscribe
      tags:
        - Newsletter
      summary: Subscribe an address to this tenant's newsletter
      security: []
      description: |-
        Anonymous and per-IP rate-limited. Idempotent (FR-NWL-005): a second POST for the same address does not create a second row — the unique index over `(tenant_id, email_normalized)` and one `ON CONFLICT` statement do that, not a read-then-write two concurrent submissions could interleave with.

        Answers `200` with the SAME message for every outcome: a new address, an address already active, one that is suppressed, and a host that resolves to no tenant. It never says which. A malformed body still answers `400` — that is a statement about the REQUEST, not about any address, so it leaks nothing.

        Writes a `pending` row and sends a confirmation mail. Consent is NOT recorded here: `consent_at` is written when the link is followed, so the record says what happened rather than what was asked for. A tenant with no active `derived.newsletter_confirmation` template sends no mail and the row stays `pending` — the correct failure, since silently activating without confirmation would be the wrong one.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewsletterSubscribeRequest"
      responses:
        "200":
          description: Accepted. Says nothing about the address.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewsletterNeutralResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many subscription requests from this source (`RATE_LIMITED`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/newsletter/subscribers/{id}/suppress:
    post:
      operationId: newsletterSubscriberSuppress
      tags:
        - Newsletter
      summary: Suppress a subscriber
      description: |-
        Gated by `newsletter.subscribers.configure`. The only authenticated write in this module, and the only one that needs to be — everything else a subscriber's state can do, the subscriber does.

        `unsubscribed` is the SUBSCRIBER's decision and they may reverse it by signing up again. `suppressed` is the OPERATOR's or the provider's — a hard bounce, an abuse report, a legal instruction — and re-subscribing must not clear it. That is why they are two states rather than one `inactive`, and why this endpoint exists rather than an admin-side call to the public unsubscribe.

        `reason` is REQUIRED: this is the one state a subscriber cannot leave, so it is the one somebody will later ask about. It goes on the audit row, which records the MASKED address so the trail does not become a second copy of the list.

        No `Idempotency-Key`: suppressing an already-suppressed row writes the same state, and requiring a key for a naturally idempotent transition would be ceremony.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
                  minLength: 1
                  maxLength: 500
      responses:
        "200":
          description: The subscriber is suppressed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - suppressed
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/newsletter/unsubscribe:
    post:
      operationId: newsletterUnsubscribe
      tags:
        - Newsletter
      summary: End a subscription
      security: []
      description: |-
        Anonymous, and that is a REQUIREMENT rather than a convenience (PRD §30): unsubscribing must be easy and must not require a login. Takes the token and nothing else — no session, no tenant header, no email address. Requiring any of those would mean a person who wants out has to prove who they are first, and the token already proves they hold the link.

        The row is KEPT. "This person asked to stop, on this date" is what answers a later complaint, and deleting it leaves nothing to answer with. A SUPPRESSED row is not touched: suppression is the operator's decision, and an old link must not move a row out of the one state it cannot leave.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NewsletterTokenRequest"
      responses:
        "200":
          description: Accepted. Says nothing about the token.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NewsletterNeutralResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "429":
          description: Too many requests from this source (`RATE_LIMITED`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/offices:
    get:
      operationId: listOffices
      tags:
        - Tenant Admin
      summary: List offices for the current tenant — keyset-paginated, newest first.
      parameters:
        - name: cursor
          in: query
          required: false
          description: Opaque cursor from a previous response's nextCursor. A malformed value is rejected with 400.
          schema:
            type: string
      responses:
        "200":
          description: Live offices for the tenant (limit 100), newest first, with an opaque nextCursor for the next page (null on the last page).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/Office"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createOffice
      tags:
        - Tenant Admin
      summary: Create an office.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - officeCode
                - officeName
              properties:
                officeCode:
                  type: string
                officeName:
                  type: string
                officeType:
                  type: string
                  enum:
                    - head_office
                    - branch
                    - store
                    - warehouse
                    - other
                parentOfficeId:
                  type: string
                  format: uuid
                  nullable: true
                  description: Must reference a live office in the SAME tenant. An id that is unknown, soft-deleted, or belongs to another tenant is rejected identically with 400 — the responses are deliberately indistinguishable so the field cannot be used to probe for office ids elsewhere on the platform.
      responses:
        "201":
          description: Office created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Office"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: officeCode is already taken by a live office in this tenant (OFFICE_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/offices/{id}:
    get:
      operationId: getOffice
      tags:
        - Tenant Admin
      summary: Fetch one office.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Office detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Office"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateOffice
      tags:
        - Tenant Admin
      summary: Update an office.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                officeName:
                  type: string
                officeType:
                  type: string
                  enum:
                    - head_office
                    - branch
                    - store
                    - warehouse
                    - other
                status:
                  type: string
                  enum:
                    - active
                    - inactive
      responses:
        "200":
          description: Office updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Office"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteOffice
      tags:
        - Tenant Admin
      summary: Soft-delete an office (audited, restorable).
      description: Sets deleted_at/deleted_by/delete_reason; the office code is freed for reuse and the row remains restorable via POST /api/v1/offices/{id}/restore. Not a hard delete.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        description: Optional. A bodyless delete is accepted (delete_reason stays null); a present reason is stored and audited. A blank reason is rejected.
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Office soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - deleted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/offices/{id}/restore:
    post:
      operationId: restoreOffice
      tags:
        - Tenant Admin
      summary: Restore a soft-deleted office (audited).
      description: Clears the delete stamps and records restored_at/restored_by. 404 when the id is not currently soft-deleted (idempotent-safe). 409 when a live office has since taken the same code.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Office restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Office"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: A live office already uses this office code (OFFICE_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/partner/tenants:
    get:
      operationId: listManagedTenants
      tags:
        - Identity & Access
      summary: The partner's own view of which tenants have engaged it.
      description: ADR-0089. Served by a narrow SECURITY DEFINER function (`sql/119`), because the engagement rows belong to the TARGET tenant and are unreadable from here under FORCE RLS. The partner tenant is taken from the CALLER'S CONTEXT, never from the request, so nobody can ask for somebody else's book. Returns no `engagedBy` — that is a third party's identifier the partner does not need.
      responses:
        "200":
          description: The tenants this partner manages.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      managedTenants:
                        type: array
                        items:
                          $ref: "#/components/schemas/ManagedTenant"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/partners:
    get:
      operationId: listRegisteredPartners
      tags:
        - Identity & Access
      summary: "PLATFORM: the partner registry."
      description: "ADR-0089. Lists every partner registered on the deployment, and is PLATFORM-scoped for exactly that reason: a tenant-scoped version of this read is the cross-tenant directory the same ADR refused as a table. Reachable only when the acting tenant IS the platform tenant, and every row carries the platform tenant's `tenant_id` so FORCE RLS hides them from any other session even if a grant row existed. Both mechanisms are load-bearing; neither is a backstop for the other."
      responses:
        "200":
          description: The registry, newest first (limit 200).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/RegisteredPartner"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: registerPartner
      tags:
        - Identity & Access
      summary: "PLATFORM: register an existing tenant as a partner (audit-logged)."
      description: |-
        ADR-0089. Declares who may BE a partner — the platform's half of a split this ADR keeps apart from the customer's `partner_access.configure` ("which partners reach MY tenant"). No single actor holds both.
        It creates nothing but a row. A partner is an ordinary tenant: the one named here must already exist, this does not provision it, and the row grants nothing. It is the PRECONDITION a customer's engagement checks through a foreign key, and `activeRoleGrants` never reads it.
        `status` is not accepted — `sql/116` pins it to `active` until something reads suspension. There is no DELETE: the row is the target of foreign keys from engagements and from delegated grants that deliberately outlive them, so retirement will be a status change, not a removal.
        Not idempotency-keyed: both natural keys are globally unique, so a duplicate submit is a 409 naming which one was taken rather than a second row.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterPartnerRequest"
      responses:
        "201":
          description: The registered partner.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      partner:
                        $ref: "#/components/schemas/RegisteredPartner"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: No such tenant on this deployment (TENANT_NOT_FOUND).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The tenant is already a partner, the partnerCode is taken, or the platform tenant named itself (CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "422":
          description: The registration failed validation (VALIDATION_FAILED); `error.details` lists every offending field.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/partners/{partnerTenantId}/status:
    patch:
      operationId: setPartnerRegistryStatus
      tags:
        - Identity & Access
      summary: Suspend or reinstate a registered partner (PLATFORM-scoped, audited).
      description: |-
        Writes one column. Suspending stops every delegated actor the partner placed from being served, in every customer tenant, at their next request — enforced at the authorization chokepoint, not by a job, so there is no window.

        It revokes NOTHING. No grant row is touched and no engagement is severed: a grant is the record of who could see a customer's data and until when, and that has to stay answerable after a partner is suspended. Effectiveness is computed per request, so reinstating restores every surviving grant's reach without rewriting a row.

        Two permissions, both PLATFORM-scoped, because they are two authorities: `identity_access.partner_registry.disable` to suspend and `identity_access.partner_registry.restore` to reinstate. Setting the status it already has succeeds with `changed: false` and writes no audit row.
      parameters:
        - name: partnerTenantId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - active
                    - suspended
      responses:
        "200":
          description: The partner, and whether this request changed anything.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      partner:
                        $ref: "#/components/schemas/RegisteredPartner"
                      changed:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: "`status` was absent or not one of the two values the CHECK constraint allows (VALIDATION_FAILED)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/profiles:
    get:
      operationId: listProfiles
      tags:
        - Profile Identity
      summary: List/search profiles for the current tenant.
      parameters:
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum:
              - person
              - organization
        - name: status
          in: query
          required: false
          schema:
            type: string
        - name: q
          in: query
          required: false
          schema:
            type: string
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Profile list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/ProfileMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createProfile
      tags:
        - Profile Identity
      summary: Create a person/organization profile.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - profileType
                - displayName
              properties:
                profileType:
                  type: string
                  enum:
                    - person
                    - organization
                displayName:
                  type: string
                legalName:
                  type: string
                  nullable: true
                riskLevel:
                  type: string
                  enum:
                    - low
                    - normal
                    - high
      responses:
        "200":
          description: Profile created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ProfileMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/profiles/{id}:
    get:
      operationId: getProfile
      tags:
        - Profile Identity
      summary: Fetch one profile.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Profile detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ProfileMasked"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateProfile
      tags:
        - Profile Identity
      summary: Update a profile.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                displayName:
                  type: string
                legalName:
                  type: string
                  nullable: true
                riskLevel:
                  type: string
                  enum:
                    - low
                    - normal
                    - high
                verificationStatus:
                  type: string
                  enum:
                    - unverified
                    - pending
                    - verified
                status:
                  type: string
                  enum:
                    - active
                    - inactive
      responses:
        "200":
          description: Profile updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ProfileMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteProfile
      tags:
        - Profile Identity
      summary: Soft delete a profile.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Profile soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/profiles/{id}/identifiers:
    post:
      operationId: addProfileIdentifier
      tags:
        - Profile Identity
      summary: Attach a typed identifier (email/phone/national_id/tax_id/...) to a profile.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - identifierType
                - value
              properties:
                identifierType:
                  type: string
                  enum:
                    - email
                    - phone
                    - whatsapp
                    - national_id
                    - tax_id
                    - external_code
                    - other
                value:
                  type: string
                isPrimary:
                  type: boolean
      responses:
        "200":
          description: Identifier attached (masked value returned).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ProfileIdentifierMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: An identifier of this type with the same value already exists for this tenant.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/profiles/{id}/links:
    get:
      operationId: listProfileLinks
      tags:
        - Profile Identity
      summary: List cross-module entity links for a profile (e.g. employee, vendor, customer).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Entity link list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/ProfileEntityLink"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/profiles/{id}/restore:
    post:
      operationId: restoreProfile
      tags:
        - Profile Identity
      summary: Restore a soft-deleted profile.
      description: Counterpart of `DELETE /api/v1/profiles/{id}` (ADR-0058 §A). Clears `deleted_at`/`deleted_by` and stamps `restored_at`/`restored_by`; `delete_reason` is kept, since why the profile was deleted stays true after it is restored. A profile that does not exist and a profile that is not soft-deleted both answer 404 — a distinguishable answer would let a caller probe which profile ids exist.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Profile restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ProfileMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/profiles/resolve:
    get:
      operationId: resolveProfile
      tags:
        - Profile Identity
      summary: Resolve a profile by a typed identifier (email/phone/national_id/tax_id/...).
      parameters:
        - name: type
          in: query
          required: true
          schema:
            type: string
            enum:
              - email
              - phone
              - whatsapp
              - national_id
              - tax_id
              - external_code
              - other
        - name: value
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Matching profile, if any.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    nullable: true
                    properties:
                      profile:
                        $ref: "#/components/schemas/ProfileMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/push/diagnostics:
    get:
      operationId: getPushDiagnostics
      tags:
        - Push Delivery
      summary: The tenant's push outbox — queue counts, recent messages, attempts, devices.
      description: "One endpoint rather than four because the four parts are only readable together: they come from ONE transaction, so a queue count and a message list can never describe two different instants. Bounded rather than paginated — the historical tail is removed by retention (`bun run push:queue:purge`), not browsed. Endpoints are masked everywhere and notification bodies are never projected. `configured` reports the DEPLOYMENT's push flag and adapter name, which is what distinguishes a stuck queue from a deployment where push is simply off. Requires `push_delivery.diagnostics.read`."
      parameters:
        - name: status
          in: query
          required: false
          description: Narrow the message list to one queue status.
          schema:
            $ref: "#/components/schemas/PushMessageStatus"
        - name: limit
          in: query
          required: false
          description: '1-200, default 50. Out-of-range values are REJECTED rather than clamped: a caller asking for 1000 and silently receiving 200 would read the short list as "that is all there is".'
          schema:
            type: integer
            minimum: 1
            maximum: 200
      responses:
        "200":
          description: Push outbox diagnostics.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      summary:
                        $ref: "#/components/schemas/PushQueueSummary"
                      messages:
                        type: array
                        items:
                          $ref: "#/components/schemas/PushMessage"
                      attempts:
                        type: array
                        items:
                          $ref: "#/components/schemas/PushDeliveryAttempt"
                      subscriptions:
                        type: array
                        items:
                          $ref: "#/components/schemas/PushSubscription"
                      configured:
                        $ref: "#/components/schemas/PushDeploymentConfig"
                  meta:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Unknown status filter, or a limit outside 1-200.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/push/messages/{id}/cancel:
    post:
      operationId: cancelPushMessage
      tags:
        - Push Delivery
      summary: Cancel a push notification that has not been sent yet.
      description: "Only `queued` and `retry_wait` are cancellable. A row in `sending` has been claimed by a dispatcher pass that may already be inside the HTTP call, so recording it as cancelled would be a claim this system cannot substantiate. Deliberately NOT idempotency-keyed: a second call performs no new work, so there is no replay contract to honour. Requires `push_delivery.messages.cancel`."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The message was cancelled before delivery.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      cancelled:
                        type: boolean
                        enum:
                          - true
                  meta:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Not cancellable. One answer for "no such message", "already sent", "already failed", "already cancelled" and "being sent right now" — distinguishing them would hand a narrow grant an existence oracle over the queue.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/push/stream:
    get:
      operationId: streamPushQueueSummary
      tags:
        - Push Delivery
      summary: Server-sent stream of the push queue summary, re-authorized every tick.
      description: |-
        `text/event-stream`. The only SSE endpoint in this contract, and the one place where an authorization decision would otherwise become a STANDING permission: an ordinary route returns its connection to the pool before any byte reaches the client, so a thirty-minute stream would keep serving a role revoked at minute two. ADR-0075 decides otherwise — every tick opens its own transaction, runs the full guard chain again, and reads only after it allows.

        Three event names. `push-queue-summary` carries a `PushQueueSummary` every 5 s. `authorization-revoked` is terminal and the client must NOT reconnect — whatever it held is gone. `stream-error` is terminal and retryable, and is deliberately a DIFFERENT name: telling a client its access was revoked when the database was merely busy is a lie in the direction that gets investigated as a permissions bug.

        A connection ends after 10 minutes regardless; `EventSource` reconnects by itself, so the ceiling costs a reconnect while its absence would cost a connection slot forever. The message and attempt lists are NOT streamed — they are bounded at 50 rows and change shape rather than value. Requires `push_delivery.diagnostics.read`, re-checked per tick.
      responses:
        "200":
          description: The event stream.
          content:
            text/event-stream:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/push/subscriptions:
    get:
      operationId: listOwnPushSubscriptions
      tags:
        - Push Delivery
      summary: List the caller's own registered push devices.
      description: "Every device the calling identity has registered in this tenant, newest first, with the endpoint MASKED — the raw endpoint is credential-grade and is projected by exactly one server-side function, immediately before a provider call. Also returns whether Web Push is configured on this deployment and, if so, the VAPID application server key the browser needs for PushManager.subscribe(). That key is public by definition; it travels with the list so a client needs one round trip rather than two. A machine credential presented here receives the ordinary 401: it has no device."
      responses:
        "200":
          description: The caller's devices plus the deployment's Web Push capability.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      subscriptions:
                        type: array
                        items:
                          $ref: "#/components/schemas/PushSubscription"
                      webPush:
                        $ref: "#/components/schemas/WebPushCapability"
                  meta:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      operationId: registerOwnPushSubscription
      tags:
        - Push Delivery
      summary: Register (or re-activate) a push device for the caller.
      description: 'Takes the browser''s own PushSubscription.toJSON() shape plus a transport discriminator. The owner is the calling identity and cannot be supplied: there is no recipient parameter on this endpoint. Re-registration is safe and expected — browsers re-issue PushManager.subscribe() on every page load once permission is granted, so a conflict on the endpoint hash updates the existing row (and re-activates it if a push service had previously reported it gone) instead of creating a second one. Returns 201 in both cases: "created" versus "already yours" is a distinction the client cannot act on. A suspended tenant is refused (ADR-0073).'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushSubscriptionRegistration"
      responses:
        "201":
          description: The registered device.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      subscription:
                        $ref: "#/components/schemas/PushSubscription"
                  meta:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Malformed registration — unknown transport, a non-https endpoint, an endpoint pointing at a literal private address, or key material that is not the RFC 8291 size.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/push/subscriptions/{id}:
    delete:
      operationId: revokeOwnPushSubscription
      tags:
        - Push Delivery
      summary: Retire one of the caller's own push devices.
      description: 'Ownership is matched inside the UPDATE rather than checked after a read, so there is no window between the two and no way to learn whether an id belongs to somebody else. "No such subscription", "belongs to another user" and "already revoked" all answer 404. The row survives as evidence that the device was revoked and when, but the stored endpoint does not: a user-initiated revocation destroys it, because unlike a subscription the push service reported gone, this one may still be perfectly usable.'
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: The device was revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      revoked:
                        type: boolean
                        enum:
                          - true
                  meta:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/push/test:
    post:
      operationId: sendPushTestNotification
      tags:
        - Push Delivery
      summary: Queue a test notification to the caller's own devices.
      description: "Proves the parts of the chain nothing else can see — that the VAPID key pair matches the one the browser subscribed with, that the service worker registered at the right scope, that the operating system is not withholding permission. Each of those fails as a queue that drains cleanly and a device that shows nothing. The recipient is the CALLER and the endpoint accepts no recipient parameter: a test that took one would be an arbitrary-notification surface, delivering system-branded text of the sender's choosing to any colleague's lock screen. Title and body are fixed for the same reason. Requires `push_delivery.diagnostics.check`."
      responses:
        "200":
          description: How many devices the test was queued to.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      queuedCount:
                        type: integer
                        minimum: 1
                  meta:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Push is disabled on this deployment (the dispatcher would claim nothing, leaving the probe queued forever), or the caller has no active device to send to.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/registration-requests:
    get:
      operationId: listRegistrationRequests
      tags:
        - Identity & Access
      summary: Pending self-registration requests for this tenant.
      description: Oldest first, bounded. Login identifiers are MASKED — a reviewer decides on a name and a domain, and this is the one response that would otherwise expose every applicant address at once.
      responses:
        "200":
          description: The pending queue.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/RegistrationRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/registration-requests/{id}/approve:
    post:
      operationId: approveRegistrationRequest
      tags:
        - Identity & Access
      summary: Approve a request, creating a real account.
      description: "The only path that materializes profile + identity + tenant_user, hence its own permission (`registration_requests.approve`), separate from `reject`. The account is created with an UNUSABLE password and the applicant is emailed a password-reset link; `delivery` reports whether that link could actually be queued. `roleIds` is optional and defaults to none — an approval never grants a role by default, and a SYSTEM role (`owner`) is refused with 409 `ROLE_SYSTEM_PROTECTED`: admitting a person to a tenant is not the authority to hand out the tenant's whole permission catalogue."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                roleIds:
                  type: array
                  items:
                    type: string
                    format: uuid
      responses:
        "200":
          description: Account created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      approved:
                        type: boolean
                      identityId:
                        type: string
                        format: uuid
                      tenantUserId:
                        type: string
                        format: uuid
                      delivery:
                        type: string
                        enum:
                          - queued
                          - unavailable
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: "`IDENTIFIER_TAKEN` — an account with that login identifier already exists; or `ROLE_SYSTEM_PROTECTED` — one of `roleIds` names a system role, which no approval may grant."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/registration-requests/{id}/reject:
    post:
      operationId: rejectRegistrationRequest
      tags:
        - Identity & Access
      summary: Reject a request. Creates nothing and notifies nobody.
      description: A rejection email would confirm to an anonymous submitter that this tenant exists and reviewed them — the same disclosure the public submit endpoint refuses to make.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Request rejected.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      rejected:
                        type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/reports/access-audit:
    get:
      tags:
        - Management Reporting
      operationId: reportsGetAccessAudit
      summary: Access/audit summary — ABAC allow/deny counts (30-day window + all-time) and audit-event total.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Access/audit summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/reports/email-health:
    get:
      tags:
        - Management Reporting
      operationId: reportsGetEmailHealth
      summary: Email queue health — per-status counts, failed/retry backlog, oldest queued, most recent sent.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Email queue health summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/reports/exports:
    get:
      tags:
        - Reporting Projections
      operationId: reportsListScheduledExports
      summary: List scheduled export configs for the caller's tenant.
      parameters:
        - name: projectionKey
          in: query
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Scheduled export configs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Reporting Projections
      operationId: reportsCreateScheduledExport
      summary: Create a scheduled export config. High-risk — requires Idempotency-Key, audited.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - projectionKey
                - format
                - scheduleIntervalMinutes
              properties:
                projectionKey:
                  type: string
                format:
                  type: string
                  enum:
                    - csv
                    - json
                scheduleIntervalMinutes:
                  type: integer
                filter:
                  type: object
      responses:
        "200":
          description: Scheduled export created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/reports/exports/{id}/disable:
    post:
      tags:
        - Reporting Projections
      operationId: reportsDisableScheduledExport
      summary: Disable (soft-delete) a scheduled export config. High-risk — requires Idempotency-Key, reason-required, audited.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Scheduled export disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/reports/exports/runs:
    get:
      tags:
        - Reporting Projections
      operationId: reportsListExportRuns
      summary: Export run history (manifest/checksum/expiry evidence), optionally filtered by projectionKey.
      parameters:
        - name: projectionKey
          in: query
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Export run history.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/reports/exports/runs/{id}/download:
    get:
      tags:
        - Reporting Projections
      operationId: reportsDownloadExportRun
      summary: Secure, tenant-scoped, checksum-verified download of a completed export artifact. Re-checks RBAC/ABAC at download time; refuses an expired artifact with 410 Gone.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The export artifact (CSV or JSON), with an X-Checksum-Sha256 header.
          content:
            text/csv:
              schema:
                type: string
            application/json:
              schema:
                type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "410":
          description: The export artifact has expired.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/reports/exports/trigger:
    post:
      tags:
        - Reporting Projections
      operationId: reportsTriggerExport
      summary: Manually generate an export of a projection's current snapshot. High-risk — requires Idempotency-Key, audited.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - projectionKey
                - format
              properties:
                projectionKey:
                  type: string
                format:
                  type: string
                  enum:
                    - csv
                    - json
      responses:
        "200":
          description: Export run generated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/reports/module-usage:
    get:
      tags:
        - Management Reporting
      operationId: reportsGetModuleUsage
      summary: Module usage — one generic "has data" row-count signal per registered module.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Module usage summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/reports/projections:
    get:
      tags:
        - Reporting Projections
      operationId: reportsListProjections
      summary: List every registered tenant-scoped projection descriptor's live snapshot/freshness.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Projection summaries (filtered to those the caller may read).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/reports/projections/{key}:
    get:
      tags:
        - Reporting Projections
      operationId: reportsGetProjection
      summary: A single projection's snapshot/freshness plus recent reconciliation history.
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Projection detail.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/reports/projections/{key}/rebuild:
    post:
      tags:
        - Reporting Projections
      operationId: reportsRebuildProjection
      summary: Trigger (or resume) a full idempotent projection rebuild. High-risk — requires Idempotency-Key, reason-required, audited.
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Rebuild run triggered (or the already-running run returned).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/reports/projections/{key}/rebuild/cancel:
    post:
      tags:
        - Reporting Projections
      operationId: reportsCancelProjectionRebuild
      summary: Request cooperative cancellation of the currently-running rebuild. High-risk — requires Idempotency-Key, audited.
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Cancellation requested.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/reports/projections/{key}/reconcile:
    post:
      tags:
        - Reporting Projections
      operationId: reportsReconcileProjection
      summary: On-demand reconciliation of a projection's metrics against a freshly computed source control total. No Idempotency-Key (zero business-state mutation).
      parameters:
        - name: key
          in: path
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Reconciliation snapshot recorded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/reports/sync-health:
    get:
      tags:
        - Management Reporting
      operationId: reportsGetSyncHealth
      summary: Sync health — node counts, last push/pull, open conflicts, pending/failed objects, derived health flags.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Sync health summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/reports/tenant-activity:
    get:
      tags:
        - Management Reporting
      operationId: reportsGetTenantActivity
      summary: Tenant activity summary — name/status/created, active user & office counts, most recent login.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Tenant activity summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/roles:
    get:
      operationId: listRoles
      tags:
        - Identity & Access
      summary: List the current tenant's (non-deleted) roles with a permission count.
      responses:
        "200":
          description: The tenant's roles (limit 100), each with a permission count.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createRole
      tags:
        - Identity & Access
      summary: Create a custom role for the current tenant (audited; requires access_control.configure).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - roleCode
                - roleName
              properties:
                roleCode:
                  type: string
                  description: "Slug: 2–64 chars, lowercase letters, digits, '_' or '-'. Unique per tenant among live roles."
                roleName:
                  type: string
      responses:
        "201":
          description: Role created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: roleCode is already taken by a live role in this tenant (ROLE_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/roles/{id}:
    patch:
      operationId: updateRole
      tags:
        - Identity & Access
      summary: Rename a role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - roleName
              properties:
                roleName:
                  type: string
      responses:
        "200":
          description: Role updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      operationId: deleteRole
      tags:
        - Identity & Access
      summary: Soft-delete a role (audited; requires access_control.configure). System roles are rejected with 409.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  description: Optional free-text reason echoed into the soft-delete audit event.
      responses:
        "200":
          description: Role soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The role is a system role and cannot be deleted (ROLE_SYSTEM_PROTECTED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/roles/{id}/permissions:
    post:
      operationId: grantRolePermission
      tags:
        - Identity & Access
      summary: Grant a catalogued permission to a role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - permissionId
              properties:
                permissionId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Permission granted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      roleId:
                        type: string
                        format: uuid
                      permissionId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The permission is already granted to this role (ROLE_PERMISSION_ALREADY_GRANTED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: revokeRolePermission
      tags:
        - Identity & Access
      summary: Revoke a permission from a role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - permissionId
              properties:
                permissionId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Permission revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      roleId:
                        type: string
                        format: uuid
                      permissionId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/roles/{id}/restore:
    post:
      operationId: restoreRole
      tags:
        - Identity & Access
      summary: Restore a soft-deleted role (audited; requires access_control.configure).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Role restored.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/Role"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The role's code was re-used by a live role while it was deleted (ROLE_CODE_ALREADY_EXISTS).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/seo/config:
    get:
      tags:
        - SEO & Distribution
      summary: Read this tenant's SEO defaults
      description: Gated by seo_distribution.config.read. Returns the tenant's SEO defaults (site identity, default social/Organization images, Twitter handle, tenant-wide noindex switch, and feed/sitemap config); a neutral default object when no config row exists yet. Tenant-scoped (withTenant + RLS FORCE).
      operationId: seoConfigRead
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant's resolved SEO defaults.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoTenantSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      tags:
        - SEO & Distribution
      summary: Replace this tenant's SEO defaults
      description: "Gated by seo_distribution.config.update. High-risk mutation — rewrites the public metadata/indexability surface (including the tenant-wide noindex switch): requires Idempotency-Key and is audited. Full replace of the mutable fields (PUT semantics); unknown keys are ignored. Tenant-scoped (withTenant + RLS FORCE)."
      operationId: seoConfigUpdate
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeoConfigUpdateRequest"
      responses:
        "200":
          description: The saved SEO defaults, or a replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoTenantSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/seo/not-found:
    get:
      tags:
        - SEO & Distribution
      summary: List the privacy-minimized 404 governance dashboard
      description: Top 404 observations by hit count (sanitized path + bare referrer domain only — never full URLs/queries/secrets). unresolvedOnly=true filters to open items. Gated by seo_distribution.not_found.read.
      operationId: seoNotFoundList
      parameters:
        - name: unresolvedOnly
          in: query
          required: false
          schema:
            type: boolean
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: This tenant's 404 observations.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    required:
                      - observations
                    properties:
                      observations:
                        type: array
                        items:
                          $ref: "#/components/schemas/SeoNotFoundObservation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/seo/not-found/{id}:
    post:
      tags:
        - SEO & Distribution
      summary: Resolve a 404 observation
      description: Mark an observation resolved, optionally attaching a same-tenant suggested redirect id (validated to exist). Audited. Gated by seo_distribution.not_found.update.
      operationId: seoNotFoundResolve
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                suggestedRedirectId:
                  type: string
                  format: uuid
                  nullable: true
      responses:
        "200":
          description: The resolved observation.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoNotFoundObservation"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - SEO & Distribution
      summary: Dismiss a 404 observation
      description: Hard-delete an observation the operator does not want to track. Audited. Gated by seo_distribution.not_found.update.
      operationId: seoNotFoundDismiss
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The observation was dismissed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/seo/redirects:
    get:
      tags:
        - SEO & Distribution
      summary: List/search/filter redirect rules
      description: Tenant-scoped exact-path redirect rules, keyset-paginated newest first (limit 100). Filter by state, targetType, or a source-path substring q. Gated by seo_distribution.redirect.read. Tenant-scoped (withTenant + RLS FORCE).
      operationId: seoRedirectsList
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
        - name: cursor
          in: query
          required: false
          schema:
            type: string
          description: Opaque keyset cursor from a previous page.
        - name: state
          in: query
          required: false
          schema:
            type: string
            enum:
              - active
              - inactive
              - archived
        - name: targetType
          in: query
          required: false
          schema:
            type: string
            enum:
              - relative_same_tenant
              - verified_external
        - name: q
          in: query
          required: false
          schema:
            type: string
          description: Substring match on the normalized source path.
      responses:
        "200":
          description: This tenant's redirect rules.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    required:
                      - redirects
                      - nextCursor
                    properties:
                      redirects:
                        type: array
                        items:
                          $ref: "#/components/schemas/SeoRedirect"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - SEO & Distribution
      summary: Create a redirect rule
      description: "High-risk: requires an Idempotency-Key, audited. The target is validated through the frozen open-redirect guard and the rule is rejected if it conflicts with an existing source/scope, self-redirects, or would create a loop / over-long chain. Gated by seo_distribution.redirect.create. Tenant-scoped (withTenant + RLS FORCE)."
      operationId: seoRedirectsCreate
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeoRedirectCreateRequest"
      responses:
        "200":
          description: Redirect rule created (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoRedirect"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Source/scope conflict, redirect loop, over-long chain, or idempotency-key reuse with a different payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/seo/redirects/{id}:
    get:
      tags:
        - SEO & Distribution
      summary: Read one redirect rule
      operationId: seoRedirectsGet
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The redirect rule.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoRedirect"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - SEO & Distribution
      summary: Update a redirect rule
      description: Replace the mutable fields (source path is immutable). Re-validates the target through the frozen open-redirect guard + the conflict/loop/chain safety gate. Audited. Gated by seo_distribution.redirect.update.
      operationId: seoRedirectsUpdate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeoRedirectUpdateRequest"
      responses:
        "200":
          description: Redirect rule updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoRedirect"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Source conflict, redirect loop, or over-long chain.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - SEO & Distribution
      summary: Soft-delete a redirect rule
      description: Soft delete (a non-empty reason is required). Restore/purge via the lifecycle endpoint. Audited. Gated by seo_distribution.redirect.delete.
      operationId: seoRedirectsDelete
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
                  maxLength: 500
      responses:
        "200":
          description: Redirect rule soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoRedirect"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/seo/redirects/{id}/lifecycle:
    post:
      tags:
        - SEO & Distribution
      summary: Transition a redirect rule's state / delete lifecycle
      description: activate | deactivate | archive | restore | purge. Idempotency-keyed, audited. purge needs seo_distribution.redirect.delete; every other action needs seo_distribution.redirect.update (dynamic guard).
      operationId: seoRedirectsLifecycle
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - action
              properties:
                action:
                  type: string
                  enum:
                    - activate
                    - deactivate
                    - archive
                    - restore
                    - purge
      responses:
        "200":
          description: The lifecycle transition result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-key reuse with a different payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/seo/redirects/capture-url-change:
    post:
      tags:
        - SEO & Distribution
      summary: Capture a URL change into a redirect proposal/rule
      description: Turn an old→new public path change into an audited redirect PROPOSAL (inactive) or active rule per the tenant's url_change_auto_policy (overridable). Idempotency-keyed. Gated by seo_distribution.redirect.create.
      operationId: seoRedirectsCaptureUrlChange
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - oldPath
                - newPath
                - changeType
              properties:
                oldPath:
                  type: string
                newPath:
                  type: string
                changeType:
                  type: string
                  enum:
                    - slug_change
                    - domain_change
                    - locale_change
                policy:
                  type: string
                  enum:
                    - skip
                    - propose
                    - create
                localeScope:
                  type: string
                  nullable: true
                domainScopeHost:
                  type: string
                  nullable: true
                reason:
                  type: string
                  nullable: true
      responses:
        "200":
          description: The capture outcome (skipped/proposed/created).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Conflict/loop/chain rejection, or idempotency-key reuse with a different payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/seo/redirects/import:
    post:
      tags:
        - SEO & Distribution
      summary: Bulk-import redirect rules (with dry run)
      description: "Validate + safety-check up to 200 rules. dryRun: true returns a per-item report writing nothing; a real import is all-or-nothing (idempotency-keyed, audited). Gated by seo_distribution.redirect.create."
      operationId: seoRedirectsImport
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - redirects
              properties:
                dryRun:
                  type: boolean
                  default: false
                redirects:
                  type: array
                  maxItems: 200
                  items:
                    $ref: "#/components/schemas/SeoRedirectCreateRequest"
      responses:
        "200":
          description: The import report.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-key reuse with a different payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/seo/redirects/settings:
    get:
      tags:
        - SEO & Distribution
      summary: Read this tenant's redirect governance policy
      description: The legacy-blog auto-redirect toggle (INERT in awcms — no /news route family) and the default URL-change auto-capture policy. Gated by seo_distribution.redirect.read.
      operationId: seoRedirectSettingsRead
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: This tenant's redirect policy.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoRedirectSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      tags:
        - SEO & Distribution
      summary: Update this tenant's redirect governance policy
      description: "High-risk (the legacy-blog toggle changes public routing intent): requires an Idempotency-Key, audited. Gated by seo_distribution.redirect.update."
      operationId: seoRedirectSettingsUpdate
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeoRedirectSettings"
      responses:
        "200":
          description: Redirect policy updated (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SeoRedirectSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-key reuse with a different payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/seo/redirects/validate:
    post:
      tags:
        - SEO & Distribution
      summary: Validate a redirect rule (dry run) + preview its chain
      description: "Read-only: normalize + validate a proposed rule (frozen open-redirect guard), preview the redirect chain it would produce, and explain any conflict/loop/over-long chain — writing nothing. Gated by seo_distribution.redirect.read."
      operationId: seoRedirectsValidate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SeoRedirectCreateRequest"
      responses:
        "200":
          description: The validation + chain-preview result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/settings:
    get:
      operationId: getSettings
      tags:
        - Tenant Admin
      summary: Current tenant's settings.
      responses:
        "200":
          description: Tenant settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      tenantName:
                        type: string
                      legalName:
                        type: string
                        nullable: true
                      defaultLocale:
                        type: string
                      defaultTheme:
                        type: string
                      timezone:
                        type: string
                      featureFlags:
                        type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      operationId: patchSettings
      tags:
        - Tenant Admin
      summary: Update tenant settings.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                tenantName:
                  type: string
                legalName:
                  type: string
                  nullable: true
                defaultLocale:
                  type: string
                  enum:
                    - id
                    - en
                    - ms
                    - ar
                defaultTheme:
                  type: string
                  enum:
                    - light
                    - dark
                    - system
                timezone:
                  type: string
                featureFlags:
                  type: object
      responses:
        "200":
          description: Updated tenant settings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/setup/initialize:
    post:
      operationId: postSetupInitialize
      tags:
        - Tenant Admin
      summary: Bootstrap the first tenant, owner, office, role, and access assignment. Rejected once already locked.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantCode
                - tenantName
                - officeCode
                - officeName
                - ownerLoginIdentifier
                - ownerPassword
                - ownerDisplayName
              properties:
                tenantCode:
                  type: string
                tenantName:
                  type: string
                officeCode:
                  type: string
                officeName:
                  type: string
                ownerLoginIdentifier:
                  type: string
                ownerPassword:
                  type: string
                  minLength: 8
                ownerDisplayName:
                  type: string
                turnstileToken:
                  type: string
                  description: "Cloudflare Turnstile response token (Issue #186). Required only on a full-online deployment where Turnstile is enabled; absent and ignored on every LAN/offline deployment. Verified server-side before the tenant/owner bootstrap."
      responses:
        "200":
          description: Tenant bootstrapped.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      tenantId:
                        type: string
                        format: uuid
                      officeId:
                        type: string
                        format: uuid
                      ownerProfileId:
                        type: string
                        format: uuid
                      ownerIdentityId:
                        type: string
                        format: uuid
                      ownerTenantUserId:
                        type: string
                        format: uuid
                      ownerRoleId:
                        type: string
                        format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          description: Setup already completed.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/setup/status:
    get:
      operationId: getSetupStatus
      tags:
        - Tenant Admin
      summary: Whether the one-time setup wizard has already run.
      security: []
      responses:
        "200":
          description: Setup lock state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      locked:
                        type: boolean
                      tenantId:
                        type: string
                        format: uuid
                      lockedAt:
                        type: string
                        format: date-time
  /api/v1/site-profile:
    get:
      operationId: siteProfileRead
      tags:
        - Site Profile
      summary: Read this tenant's site chrome
      description: Gated by `site_profile.profile.read`. Returns an EMPTY profile (every field null, `socialLinks` empty) when the tenant has never saved one, so a consumer has a single shape to render and never branches on "no row yet". Tenant-scoped (withTenant + RLS FORCE).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: This tenant's site chrome.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteProfile"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      operationId: siteProfileUpdate
      tags:
        - Site Profile
      summary: Replace this tenant's site chrome
      description: 'Gated by `site_profile.profile.update`, separately grantable from `.read` because changing what every public page''s contact block says is a different power from reading it. FULL REPLACE, not a patch: an omitted or empty field is stored as null. That is deliberate — a form that submits its whole state cannot express "leave this one alone", and pretending it could would make clearing a wrong phone number impossible. Requires an Idempotency-Key and is audited; the audit row records WHICH FIELDS are set, never their values, because an address and a phone number are contact data and the audit log is read more widely than the screen.'
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteProfileWriteInput"
      responses:
        "200":
          description: Saved site chrome.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteProfile"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/site-profile/composed:
    get:
      operationId: siteProfileComposedRead
      tags:
        - Site Profile
      summary: Site identity for a build client, both halves in one answer
      description: "Gated by `site_profile.profile.read`. ADR-0102 splits OWNERSHIP — `awcms_seo_tenant_settings` keeps what crawlers see (og:site_name, the JSON-LD Organization node, the default og:image) and `awcms_site_profile` owns what people read. That boundary is right for ownership and wrong for consumers, so the composition happens here, once, rather than in every build template that would otherwise have to call two endpoints and merge them — and drift when one of them forgot. `logoMediaId`/`faviconMediaId` are media object IDS, not URLs: a consumer resolves them through media_library exactly as it resolves an article image, which is what keeps managed-media enforcement meaningful."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Composed site identity.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ComposedSiteIdentity"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/site-search/index/failures:
    get:
      operationId: siteSearchIndexFailures
      tags:
        - Site Search
      summary: Read the search index failed-item diagnostics
      description: Gated by `site_search.diagnostics.read`. Returns up to 100 unresolved failed items (aggregated per source/resource/locale) with a SANITIZED error class and detail — never a raw stack trace. Each reconcile clears its own source's prior failures, so this reflects the latest run.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Failed-item diagnostics.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/SiteSearchIndexFailure"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/site-search/index/rebuild:
    post:
      operationId: siteSearchIndexRebuild
      tags:
        - Site Search
      summary: Force a full search index rebuild
      description: "Gated by `site_search.index.rebuild` — a HIGH-RISK action: it deletes every one of the tenant's index documents for the registered sources and re-extracts them. Idempotent (the end state is identical regardless of prior state), Idempotency-Key'd, and audited."
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Rebuild run summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteSearchIndexRunResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/site-search/index/reconcile:
    post:
      operationId: siteSearchIndexReconcile
      tags:
        - Site Search
      summary: Run an idempotent search index reconciliation
      description: Gated by `site_search.index.reconcile`. Upserts the tenant's currently public documents (skipping any whose checksum already matches) and deletes index documents whose source row is gone or no longer public, so an archive/delete/unpublish never leaves a stale public result. Running it while already in sync is a no-op. Deliberately NOT classified high-risk (a fully regenerable projection sync), but still Idempotency-Key'd and audited.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Reconcile run summary.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteSearchIndexRunResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/site-search/index/status:
    get:
      operationId: siteSearchIndexStatus
      tags:
        - Site Search
      summary: Read this tenant's search index status, freshness, and recent runs
      description: "Gated by `site_search.index.read`. Bounded: document counts by resource type, the most recent index run, the open failed-item count, and the 10 most recent runs."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Index status and recent runs.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      status:
                        $ref: "#/components/schemas/SiteSearchIndexStatus"
                      recentRuns:
                        type: array
                        items:
                          $ref: "#/components/schemas/SiteSearchIndexRun"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/site-search/query:
    get:
      operationId: siteSearchQuery
      tags:
        - Site Search
      security: []
      summary: Public full-text search over this site's published content (anonymous)
      description: "PUBLIC, unauthenticated cross-content search. The tenant is resolved from the request host (never a session or a tenant header); results come from the tenant's search index, which contains PUBLISHED PUBLIC content only and is never an authorization source. The query text is always a bound parameter into `websearch_to_tsquery`, snippets are HTML-escaped before the `<mark>` highlights are inserted, and the endpoint is per-IP rate-limited, query-length-bounded (max 128 characters) and result-capped. An unresolved host, a disabled module, a search-disabled tenant, and a too-short query all return the SAME neutral empty payload — never a distinguishing error. A request carrying a cross-origin `Origin` resolves its tenant from THAT ORIGIN against the tenant-domain table — never from the host, and never through the host chain's default-tenant fallback (ADR-0107). It is answered with `Access-Control-Allow-Origin` only when the origin is an `active` domain of an `active` tenant; any other origin gets the same neutral empty payload with no grant header. There is no `OPTIONS` handler: a `GET` carrying only CORS-safelisted headers is a simple request, so a consumer must not add custom headers."
      parameters:
        - name: q
          in: query
          required: false
          description: Raw search text. Trimmed, whitespace-collapsed, and bounded before use.
          schema:
            type: string
            maxLength: 128
        - name: type
          in: query
          required: false
          description: Restrict results to one resource type. Honored only when the tenant's `enabledResourceTypes` admits it; otherwise silently ignored.
          schema:
            type: string
        - name: channel
          in: query
          required: false
          description: "Term filter (Issue #633). One query parameter per facet name declared by the search-source registry — `channel`, `topic`, `institution`, `region` today. Filters are ANDed with each other and with `type`. The value is a slug or code (never a label), bounded to 200 characters. An unknown facet name is IGNORED rather than rejected, so the tracking parameters that ride along on a shared link do not turn a search into a 400."
          schema:
            type: string
            maxLength: 200
        - name: topic
          in: query
          required: false
          description: See `channel`.
          schema:
            type: string
            maxLength: 200
        - name: institution
          in: query
          required: false
          description: See `channel`.
          schema:
            type: string
            maxLength: 200
        - name: region
          in: query
          required: false
          description: See `channel`. The value is a dotted `idn_admin_regions` code (`62`, `62.71`, `62.71.01`), because that is what an article stores.
          schema:
            type: string
            maxLength: 200
        - name: locale
          in: query
          required: false
          description: BCP-47-ish content locale. Malformed values fall back to the tenant default.
          schema:
            type: string
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a previous response's `nextCursor`.
          schema:
            type: string
      responses:
        "200":
          description: Search results (possibly empty).
          headers:
            Access-Control-Allow-Origin:
              description: Present ONLY for a cross-origin request whose `Origin` is an active domain of an active tenant (ADR-0107). Echoed verbatim, never `*`. Absent for a same-origin request and for a refused origin — the absence IS the refusal.
              required: false
              schema:
                type: string
            Vary:
              description: Always `Origin`. The body is identical for a granted and a refused request; the headers are not, so a cache must key on it.
              required: false
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteSearchQueryResult"
        "429":
          description: Too many search requests from this source.
          headers:
            Vary:
              description: Always `Origin`. The limiter answers before the origin is classified, so this response never carries a grant — but it is still one of the answers this URL gives (ADR-0107).
              required: false
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/site-search/settings:
    get:
      operationId: siteSearchSettingsRead
      tags:
        - Site Search
      summary: Read this tenant's search configuration
      description: Gated by `site_search.settings.read`. Returns the module defaults when the tenant has never written a config row. Tenant-scoped (withTenant + RLS FORCE).
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Tenant search configuration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteSearchSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      operationId: siteSearchSettingsUpdate
      tags:
        - Site Search
      summary: Replace this tenant's search configuration
      description: "Gated by `site_search.settings.update`. Changes what the PUBLIC search surface returns (including the tenant-wide search on/off switch and the opt-in query-analytics switch), so it requires an Idempotency-Key and is audited. Merge semantics: omitted fields keep their current value; unknown keys are ignored."
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SiteSearchSettingsUpdateRequest"
      responses:
        "200":
          description: Saved tenant search configuration.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteSearchSettings"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key was already used with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          description: Request body exceeded the endpoint's size ceiling.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/site-search/suggest:
    get:
      operationId: siteSearchSuggest
      tags:
        - Site Search
      security: []
      summary: Public bounded title typeahead (anonymous)
      description: "PUBLIC, unauthenticated trigram typeahead over indexed titles, tenant and locale scoped. Same host-based tenant resolution, per-IP rate limit, query bounds and neutral empty payload as the search endpoint. Returns at most the tenant's configured `suggestionLimit`; returns an empty list when the tenant has suggestions turned off. A request carrying a cross-origin `Origin` resolves its tenant from THAT ORIGIN against the tenant-domain table — never from the host, and never through the host chain's default-tenant fallback (ADR-0107). It is answered with `Access-Control-Allow-Origin` only when the origin is an `active` domain of an `active` tenant; any other origin gets the same neutral empty payload with no grant header. There is no `OPTIONS` handler: a `GET` carrying only CORS-safelisted headers is a simple request, so a consumer must not add custom headers."
      parameters:
        - name: q
          in: query
          required: false
          schema:
            type: string
            maxLength: 128
        - name: locale
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Title suggestions (possibly empty).
          headers:
            Access-Control-Allow-Origin:
              description: Present ONLY for a cross-origin request whose `Origin` is an active domain of an active tenant (ADR-0107). Echoed verbatim, never `*`. Absent for a same-origin request and for a refused origin — the absence IS the refusal.
              required: false
              schema:
                type: string
            Vary:
              description: Always `Origin`. The body is identical for a granted and a refused request; the headers are not, so a cache must key on it.
              required: false
              schema:
                type: string
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/SiteSearchSuggestResult"
        "429":
          description: Too many suggestion requests from this source.
          headers:
            Vary:
              description: Always `Origin`. The limiter answers before the origin is classified, so this response never carries a grant — but it is still one of the answers this URL gives (ADR-0107).
              required: false
              schema:
                type: string
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/sync/conflicts:
    get:
      operationId: syncListConflicts
      tags:
        - Sync Storage
      summary: List sync conflicts for the tenant (bearer session, not HMAC).
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - open
              - resolved
      responses:
        "200":
          description: Recent sync conflicts, newest first (limit 50).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      conflicts:
                        type: array
                        items:
                          type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/sync/conflicts/{id}/resolve:
    post:
      operationId: syncResolveConflict
      tags:
        - Sync Storage
      summary: Resolve a sync conflict (bearer session, not HMAC, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - resolution
              properties:
                resolution:
                  type: string
                  enum:
                    - accept_incoming
                    - keep_existing
                    - manual
                note:
                  type: string
      responses:
        "200":
          description: Conflict resolved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Conflict is already resolved.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "413":
          $ref: "#/components/responses/BadRequest"
  /api/v1/sync/nodes:
    get:
      operationId: syncListNodes
      tags:
        - Sync Storage
      summary: List sync nodes for the tenant (bearer session or SSR cookie, not HMAC).
      description: Admin-facing view of node registrations — distinct from the machine-to-machine HMAC endpoints (/sync/push, /sync/pull, /sync/status, /sync/objects*).
      responses:
        "200":
          description: All sync nodes registered for the tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      nodes:
                        type: array
                        items:
                          type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/sync/nodes/{id}:
    patch:
      operationId: syncUpdateNode
      tags:
        - Sync Storage
      summary: Activate/deactivate or rename a sync node (audited).
      description: "Deactivating a node takes effect immediately: every HMAC sync endpoint already rejects a non-active node with 403."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                status:
                  type: string
                  enum:
                    - active
                    - inactive
                nodeName:
                  type: string
                  minLength: 1
      responses:
        "200":
          description: Sync node updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          $ref: "#/components/responses/BadRequest"
  /api/v1/sync/object-queue:
    get:
      operationId: syncListObjectQueue
      tags:
        - Sync Storage
      summary: List object sync queue entries tenant-wide (bearer session, not HMAC).
      description: Admin-facing, all-nodes view — distinct from the node-scoped HMAC GET /sync/objects/status.
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum:
              - pending
              - sent
              - failed
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Object sync queue entries tenant-wide (limit 200), newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      objects:
                        type: array
                        items:
                          type: object
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/sync/object-queue/{id}/retry:
    post:
      operationId: syncRetryObjectQueueEntry
      tags:
        - Sync Storage
      summary: Manually retry a failed object sync queue entry (audited).
      description: Human override of the automatic exponential-backoff schedule. Only `failed` entries are eligible; `pending`/`sent` are rejected with 409.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Entry reset to pending.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Only failed entries can be retried.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/sync/objects:
    post:
      operationId: syncEnqueueObjects
      tags:
        - Sync Storage
      summary: Enqueue local objects for object-storage sync (upsert by objectKey, HMAC-authenticated).
      security:
        - syncHmac: []
          tenantHeader: []
      parameters:
        - $ref: "#/components/parameters/SyncNodeId"
        - $ref: "#/components/parameters/SyncTimestamp"
        - $ref: "#/components/parameters/SyncSignature"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - objects
              properties:
                objects:
                  type: array
                  items:
                    type: object
                    required:
                      - objectKey
                      - localPath
                      - checksumSha256
                      - byteSize
                    properties:
                      objectKey:
                        type: string
                      localPath:
                        type: string
                      checksumSha256:
                        type: string
                        pattern: ^[a-f0-9]{64}$
                      byteSize:
                        type: integer
                        minimum: 0
      responses:
        "200":
          description: Objects queued (re-enqueuing an existing objectKey upserts it back to pending).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      queued:
                        type: integer
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "413":
          $ref: "#/components/responses/BadRequest"
  /api/v1/sync/objects/status:
    get:
      operationId: syncGetObjectsStatus
      tags:
        - Sync Storage
      summary: List this node's pending/failed object sync queue entries (HMAC-authenticated).
      security:
        - syncHmac: []
          tenantHeader: []
      parameters:
        - $ref: "#/components/parameters/SyncNodeId"
        - $ref: "#/components/parameters/SyncTimestamp"
        - $ref: "#/components/parameters/SyncSignature"
      responses:
        "200":
          description: Non-sent object sync queue entries for this node (limit 100), oldest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      objects:
                        type: array
                        items:
                          type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/sync/pull:
    post:
      operationId: syncPull
      tags:
        - Sync Storage
      summary: Pull events newer than the node's stored checkpoint (HMAC-authenticated).
      security:
        - syncHmac: []
          tenantHeader: []
      parameters:
        - $ref: "#/components/parameters/SyncNodeId"
        - $ref: "#/components/parameters/SyncTimestamp"
        - $ref: "#/components/parameters/SyncSignature"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                limit:
                  type: integer
                  minimum: 1
                  maximum: 500
      responses:
        "200":
          description: Events since the node's last checkpoint, and the new checkpoint.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      events:
                        type: array
                        items:
                          type: object
                      checkpoint:
                        type: integer
                      hasMore:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "413":
          $ref: "#/components/responses/BadRequest"
  /api/v1/sync/push:
    post:
      operationId: syncPush
      tags:
        - Sync Storage
      summary: Push a batch of local events to the server (idempotent per batchId, HMAC-authenticated).
      security:
        - syncHmac: []
          tenantHeader: []
      parameters:
        - $ref: "#/components/parameters/SyncNodeId"
        - $ref: "#/components/parameters/SyncTimestamp"
        - $ref: "#/components/parameters/SyncSignature"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - batchId
                - events
              properties:
                batchId:
                  type: string
                events:
                  type: array
                  minItems: 1
                  maxItems: 500
                  items:
                    type: object
                    required:
                      - eventType
                      - aggregateType
                      - payload
                    properties:
                      eventType:
                        type: string
                      aggregateType:
                        type: string
                      aggregateId:
                        type: string
                        format: uuid
                      baseVersion:
                        type: integer
                        minimum: 0
                      payload: {}
      responses:
        "200":
          description: Batch accepted (or already applied, if the batchId was seen before).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      batchId:
                        type: string
                      accepted:
                        type: integer
                      conflicted:
                        type: integer
                      duplicate:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "413":
          $ref: "#/components/responses/BadRequest"
  /api/v1/sync/status:
    get:
      operationId: syncGetStatus
      tags:
        - Sync Storage
      summary: Get the calling node's sync status (HMAC-authenticated).
      security:
        - syncHmac: []
          tenantHeader: []
      parameters:
        - $ref: "#/components/parameters/SyncNodeId"
        - $ref: "#/components/parameters/SyncTimestamp"
        - $ref: "#/components/parameters/SyncSignature"
      responses:
        "200":
          description: Sync node status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/tenant/domains:
    get:
      tags:
        - Tenant Domains
      summary: List this tenant's domain/subdomain mappings
      description: Gated by tenant_domain.domains.read. Non-deleted mappings only, keyset-paginated newest first (limit 100).
      operationId: tenantDomainsList
      parameters:
        - name: cursor
          in: query
          required: false
          description: Opaque keyset cursor from a previous page's `nextCursor`.
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: A page of tenant domain mappings.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      domains:
                        type: array
                        items:
                          $ref: "#/components/schemas/TenantDomain"
                      nextCursor:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Tenant Domains
      summary: Add a tenant domain/subdomain mapping
      description: Gated by tenant_domain.domains.create. A duplicate normalized hostname always returns a generic 409 (never reveals whether the hostname belongs to another tenant). Audited.
      operationId: tenantDomainsCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTenantDomainRequest"
      responses:
        "200":
          description: Mapping created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantDomain"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The hostname is already mapped to a tenant (HOSTNAME_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/tenant/domains/{id}:
    get:
      tags:
        - Tenant Domains
      summary: Read one tenant domain mapping
      description: Gated by tenant_domain.domains.read. Unknown/cross-tenant/soft-deleted id all return a generic 404.
      operationId: tenantDomainsGet
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant domain mapping.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantDomain"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags:
        - Tenant Domains
      summary: Partially update a tenant domain mapping
      description: Gated by tenant_domain.domains.update. Idempotent by construction. `hostname`/`is_primary` are immutable here and `status` can never be set to `active` (use POST .../verify). Audited.
      operationId: tenantDomainsUpdate
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTenantDomainRequest"
      responses:
        "200":
          description: Mapping updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantDomain"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags:
        - Tenant Domains
      summary: Soft-delete a tenant domain mapping
      description: Gated by tenant_domain.domains.delete. Reason-required soft delete; never hard-deletes, and frees the normalized hostname for reuse. Audited.
      operationId: tenantDomainsDelete
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeleteTenantDomainRequest"
      responses:
        "200":
          description: Mapping soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      deleted:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/tenant/domains/{id}/set-primary:
    post:
      tags:
        - Tenant Domains
      summary: Set a tenant domain as the active primary
      description: Gated by tenant_domain.domains.set_primary. Requires Idempotency-Key. Atomically makes the domain this tenant's single primary, unsetting any previous primary. Only an `active` (verified) domain is eligible. Audited.
      operationId: tenantDomainsSetPrimary
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Domain set as primary, or replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantDomain"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Domain is not `active` (INVALID_STATUS_TRANSITION), a concurrent request already changed the primary (CONCURRENT_UPDATE), or the Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/tenant/domains/{id}/verify:
    post:
      tags:
        - Tenant Domains
      summary: Verify a tenant domain by DNS TXT record
      description: Gated by tenant_domain.domains.verify. Requires Idempotency-Key. Resolves the TXT records at the domain's server-minted `verificationRecordName` and activates the domain only if one of them carries its `verificationRecordValue` exactly (ADR-0106). The lookup runs outside the database transaction. A miss records `status = failed` and answers 409 DOMAIN_NOT_VERIFIED; a resolver failure changes nothing and answers 503. Rate limited per tenant. Audited on both outcomes.
      operationId: tenantDomainsVerify
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Domain verified (status `active`), or replay of a prior identical request.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantDomain"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The DNS TXT challenge was not found and the domain is now `failed` (DOMAIN_NOT_VERIFIED); a challenge was just issued and has not been published yet (DOMAIN_NOT_VERIFIED); the hostname is too long to carry one (DOMAIN_NOT_VERIFIABLE); the status is not verifiable (INVALID_STATUS_TRANSITION); the row changed mid-verification (CONFLICT); or the Idempotency-Key was reused with a different request (IDEMPOTENCY_CONFLICT).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "429":
          description: Too many verification attempts for this tenant (RATE_LIMITED). Carries `Retry-After`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          description: The DNS lookup could not be completed (SERVICE_UNAVAILABLE). Nothing was written — the domain's status is unchanged.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/tenant/modules:
    get:
      operationId: listTenantModules
      tags:
        - Module Management
      summary: Every registered module's enablement state for the current tenant.
      responses:
        "200":
          description: Tenant module list.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      modules:
                        type: array
                        items:
                          $ref: "#/components/schemas/TenantModule"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/tenant/modules/{moduleKey}/disable:
    post:
      operationId: disableTenantModule
      tags:
        - Module Management
      summary: Disable a module for the current tenant (reason required).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Module disabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      moduleKey:
                        type: string
                      tenantEnabled:
                        type: boolean
                        enum:
                          - false
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Rejected — core module or active reverse dependency.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/tenant/modules/{moduleKey}/enable:
    post:
      operationId: enableTenantModule
      tags:
        - Module Management
      summary: Enable a module for the current tenant (dependency-validated).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Module enabled.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      moduleKey:
                        type: string
                      tenantEnabled:
                        type: boolean
                        enum:
                          - true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Rejected — dependency/state conflict.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/tenant/modules/{moduleKey}/settings:
    get:
      operationId: getTenantModuleSettings
      tags:
        - Module Management
      summary: Effective tenant module settings (defaults + tenant override).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Module settings view.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleSettingsView"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      operationId: updateTenantModuleSettings
      tags:
        - Module Management
      summary: Shallow-merge non-secret settings for a module (rejects secret-shaped keys/values).
      parameters:
        - name: moduleKey
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        "200":
          description: Updated module settings view.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ModuleSettingsView"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/tenant/modules/matrix:
    get:
      operationId: getTenantModuleMatrix
      tags:
        - Module Management
      summary: Every module by what matters for this tenant, in two queries.
      description: 'Single-tenant scope, never cross-tenant. Adds two lifecycle warnings by re-running the real enable/disable validation for each module''s actual current state: `dependencyWarning` only for a DISABLED module ("would enabling succeed now?") and `reverseDependencyWarning` only for an ENABLED one ("would disabling be blocked?"). No health column — this base has no batched health reader, and a per-row one would be an N+1.'
      responses:
        "200":
          description: Module matrix for the caller's tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      modules:
                        type: array
                        items:
                          type: object
                          properties:
                            moduleKey:
                              type: string
                            name:
                              type: string
                            version:
                              type: string
                            status:
                              type: string
                            isCore:
                              type: boolean
                            isProtected:
                              type: boolean
                            tenantEnabled:
                              type: boolean
                            disableReason:
                              type: string
                              nullable: true
                            dependencies:
                              type: array
                              items:
                                type: string
                            dependencyWarning:
                              nullable: true
                              type: object
                              properties:
                                code:
                                  type: string
                                message:
                                  type: string
                            reverseDependencyWarning:
                              nullable: true
                              type: object
                              properties:
                                code:
                                  type: string
                                message:
                                  type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/tenant/modules/presets:
    get:
      operationId: listTenantModulePresets
      tags:
        - Module Management
      summary: Named module profiles, and optionally a dry-run plan for one.
      description: "Applying a preset DISABLES every enabled, unlisted, unprotected module, so `?preset=<name>` returns the plan without writing anything — an operator switching a live tenant's profile sees the disable list before it happens. An unknown name returns the catalog with `plan: null`."
      parameters:
        - name: preset
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Preset catalog, plus a plan when `preset` was supplied.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      presets:
                        type: array
                        items:
                          type: object
                          properties:
                            name:
                              type: string
                            label:
                              type: string
                            description:
                              type: string
                            enabledModuleKeys:
                              type: array
                              items:
                                type: string
                      plan:
                        nullable: true
                        type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/tenant/modules/presets/{presetName}/apply:
    post:
      operationId: applyTenantModulePreset
      tags:
        - Module Management
      summary: Bring the tenant's module state to a named profile.
      description: "Enables every module the preset lists and disables every enabled, unlisted, unprotected module. Each change runs the real lifecycle validation, so a change can be rejected: `complete: false` with per-module reasons is a real outcome, not an error."
      parameters:
        - name: presetName
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Preset applied, completely or partially.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      presetName:
                        type: string
                      complete:
                        type: boolean
                      plan:
                        type: object
                      changes:
                        type: array
                        items:
                          type: object
                          properties:
                            moduleKey:
                              type: string
                            action:
                              type: string
                              enum:
                                - enable
                                - disable
                            outcome:
                              type: string
                              enum:
                                - applied
                                - rejected
                            code:
                              type: string
                            message:
                              type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/tenant/navigation/sidebar:
    get:
      operationId: getTenantSidebarArrangement
      tags:
        - Module Management
      summary: This tenant's admin sidebar arrangement, plus the arrangeable entry set.
      description: 'The entry set is code-derived (`listModules()` navigation plus the synthetic core items) and is returned for the editor to render; it is never writable. `arrangement` is the tenant''s stored delta — a tenant with no rows gets empty arrays, meaning "use the code default". Deliberately unfiltered by permission and by tenant-disabled module: this arranges the menu rather than previewing one operator''s view of it.'
      responses:
        "200":
          description: Entry set plus the tenant's current overrides.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      entries:
                        type: array
                        items:
                          type: object
                          properties:
                            entryKey:
                              type: string
                            path:
                              type: string
                            moduleKey:
                              type: string
                            moduleName:
                              type: string
                            defaultTypeKey:
                              type: string
                            defaultLabel:
                              type: string
                      arrangement:
                        type: object
                        properties:
                          types:
                            type: array
                            items:
                              type: object
                              properties:
                                typeKey:
                                  type: string
                                labelOverride:
                                  type: string
                                  nullable: true
                                position:
                                  type: integer
                                hidden:
                                  type: boolean
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                entryKey:
                                  type: string
                                typeKey:
                                  type: string
                                  nullable: true
                                position:
                                  type: integer
                                labelOverride:
                                  type: string
                                  nullable: true
                                hidden:
                                  type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      operationId: saveTenantSidebarArrangement
      tags:
        - Module Management
      summary: Replace this tenant's sidebar arrangement.
      description: "A full replace, not a merge: the payload IS the arrangement, so an omitted override disappears. Only overrides are submitted — each is resolved by `entryKey` against the code-derived default and one that matches nothing is ignored, so a request can never introduce a menu link. Requires `module_management.navigation.configure`."
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - types
                - items
              properties:
                types:
                  type: array
                  items:
                    type: object
                    properties:
                      typeKey:
                        type: string
                      labelOverride:
                        type: string
                        nullable: true
                      position:
                        type: integer
                      hidden:
                        type: boolean
                items:
                  type: array
                  items:
                    type: object
                    properties:
                      entryKey:
                        type: string
                      typeKey:
                        type: string
                        nullable: true
                      position:
                        type: integer
                      labelOverride:
                        type: string
                        nullable: true
                      hidden:
                        type: boolean
      responses:
        "200":
          description: Arrangement saved.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      entries:
                        type: array
                        items:
                          type: object
                          properties:
                            entryKey:
                              type: string
                            path:
                              type: string
                            moduleKey:
                              type: string
                            moduleName:
                              type: string
                            defaultTypeKey:
                              type: string
                            defaultLabel:
                              type: string
                      arrangement:
                        type: object
                        properties:
                          types:
                            type: array
                            items:
                              type: object
                              properties:
                                typeKey:
                                  type: string
                                labelOverride:
                                  type: string
                                  nullable: true
                                position:
                                  type: integer
                                hidden:
                                  type: boolean
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                entryKey:
                                  type: string
                                typeKey:
                                  type: string
                                  nullable: true
                                position:
                                  type: integer
                                labelOverride:
                                  type: string
                                  nullable: true
                                hidden:
                                  type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "413":
          $ref: "#/components/responses/BadRequest"
    delete:
      operationId: resetTenantSidebarArrangement
      tags:
        - Module Management
      summary: Drop every override, returning this tenant to the code default.
      responses:
        "200":
          description: Arrangement reset.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      entries:
                        type: array
                        items:
                          type: object
                          properties:
                            entryKey:
                              type: string
                            path:
                              type: string
                            moduleKey:
                              type: string
                            moduleName:
                              type: string
                            defaultTypeKey:
                              type: string
                            defaultLabel:
                              type: string
                      arrangement:
                        type: object
                        properties:
                          types:
                            type: array
                            items:
                              type: object
                              properties:
                                typeKey:
                                  type: string
                                labelOverride:
                                  type: string
                                  nullable: true
                                position:
                                  type: integer
                                hidden:
                                  type: boolean
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                entryKey:
                                  type: string
                                typeKey:
                                  type: string
                                  nullable: true
                                position:
                                  type: integer
                                labelOverride:
                                  type: string
                                  nullable: true
                                hidden:
                                  type: boolean
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/tenants:
    get:
      operationId: listTenants
      tags:
        - Tenant Admin
      summary: List every tenant on the deployment (PLATFORM-scoped).
      description: "Gated by tenant_admin.tenant_provisioning.read, which is scope: platform (ADR-0053) — the chokepoint refuses it unless the acting tenant IS the platform tenant. Deliberately platform-scoped rather than tenant-scoped: this lists EVERY tenant, so a tenant-scoped read would let any customer enumerate the platform's customer list. The projection is narrow on purpose (code, name, status, created_at) — a directory answers who is on the deployment, not what is inside their tenant."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant directory, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/TenantDirectoryEntry"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: provisionTenant
      tags:
        - Tenant Admin
      summary: Provision a new tenant with its owner account (PLATFORM-scoped).
      description: "Gated by tenant_admin.tenant_provisioning.create, which is scope: platform. Creates the tenant, its head office, the owner profile/identity/tenant-user, the system `owner` role, and that role's grants — the SAME code path the setup wizard uses, so the `WHERE scope = 'tenant'` filter on those grants cannot drift between the two. A provisioned tenant NEVER receives platform-scoped permissions. `Idempotency-Key` required; audited in the platform tenant's log. Returns 409 when the tenant_code is taken."
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - tenantCode
                - tenantName
                - officeCode
                - officeName
                - ownerLoginIdentifier
                - ownerPassword
                - ownerDisplayName
              properties:
                tenantCode:
                  type: string
                tenantName:
                  type: string
                officeCode:
                  type: string
                officeName:
                  type: string
                ownerLoginIdentifier:
                  type: string
                ownerPassword:
                  type: string
                  minLength: 8
                ownerDisplayName:
                  type: string
      responses:
        "200":
          description: The provisioned tenant.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      tenant:
                        type: object
                        properties:
                          id:
                            type: string
                            format: uuid
                          tenantCode:
                            type: string
                          tenantName:
                            type: string
                          officeId:
                            type: string
                            format: uuid
                          ownerTenantUserId:
                            type: string
                            format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key reused with a different request, or tenant_code already taken.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/tenants/{id}/restore:
    post:
      operationId: restoreTenant
      tags:
        - Tenant Admin
      summary: Lift a tenant suspension and resume service (PLATFORM-scoped).
      description: "ADR-0073. Gated by tenant_admin.tenant_lifecycle.restore — a SEPARATE permission from disable, on purpose: during an incident you want someone who can bring a customer back without being able to cut one off, the same split machine_credentials already draws between create and revoke. Restoring an already-active tenant returns 200 with changed=false."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  maxLength: 500
                  nullable: true
      responses:
        "200":
          description: The tenant's resulting status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantLifecycleResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/tenants/{id}/suspend:
    post:
      operationId: suspendTenant
      tags:
        - Tenant Admin
      summary: Suspend a tenant, stopping service (PLATFORM-scoped).
      description: "ADR-0073. Gated by tenant_admin.tenant_lifecycle.disable, which is scope: platform — suspending a tenant changes ANOTHER party's state, so no ordinary tenant may hold it however its roles are arranged. No revocation sweep runs and none is needed: the chokepoint checks the TENANT, not the credential, so every live session and every machine credential (which can live up to a year) is refused from its next request onward. Before this existed, suspending a tenant killed its public site instantly and left its admin sessions and machine credentials fully working. Re-suspending an already-suspended tenant returns 200 with changed=false and writes no transition row. Suspending the PLATFORM tenant returns 409: it would be refused every action including the one that lifts the suspension."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
                  maxLength: 500
                  nullable: true
                  description: Recorded on the tenant, in its status-transition log, and in its own audit trail — a suspended customer is entitled to see why.
      responses:
        "200":
          description: The tenant's resulting status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/TenantLifecycleResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The PLATFORM tenant cannot be suspended (PLATFORM_TENANT_PROTECTED) — it would be refused every action, including the one that lifts the suspension.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/theming:
    get:
      tags:
        - Theming
      summary: Read this tenant's theme selection, available themes, draft, and version history
      description: "Everything the theming admin surface needs: the available (reviewed, build-time) theme descriptors, this tenant's active theme pointer, its current draft config (if any), and its published version history. Gated by `theming.config.read`."
      operationId: themingRead
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: This tenant's theming state.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ThemingState"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/theming/draft:
    put:
      tags:
        - Theming
      summary: Save/replace this tenant's draft theme config
      description: "Save the single draft config for a chosen theme (bounded, validated design tokens, slot variants, media asset ids, section order, nav placement). The body is validated against the theme descriptor (the CSS-injection spine + declared-surface bounding) before any DB work. High-risk (the draft is what publish promotes): requires an `Idempotency-Key`, audited. Gated by `theming.config.update`."
      operationId: themingDraftUpdate
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ThemeConfigRequest"
      responses:
        "200":
          description: Draft saved (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ThemeDraft"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The `Idempotency-Key` was already used with a different request payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/theming/preview:
    post:
      tags:
        - Theming
      summary: Create a short-lived, non-indexable preview session for the draft
      description: Mint an authorized, short-lived, non-indexable preview of the current draft and return its URL (`/theming/preview/{token}`) + expiry. The raw token is returned once; only its hash is stored. Audited. Gated by `theming.preview.create`. Not idempotency-keyed (each preview is a distinct disposable token).
      operationId: themingPreviewCreate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                ttlMinutes:
                  type: integer
                  minimum: 1
                  maximum: 120
                  description: Preview lifetime in minutes (default 30, hard cap 120).
      responses:
        "200":
          description: The preview session URL + expiry.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    required:
                      - previewUrl
                      - expiresAt
                    properties:
                      previewUrl:
                        type: string
                      expiresAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/theming/publish:
    post:
      tags:
        - Theming
      summary: Publish the draft as an immutable version (and make it live)
      description: "Publish the current draft as a new IMMUTABLE version and make it the live look (INSERT-only; published versions are immutable). High-risk: requires an `Idempotency-Key`, audited. Gated by `theming.version.publish`."
      operationId: themingPublish
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Published (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ThemeVersion"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The `Idempotency-Key` was already used with a different request payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/theming/retire:
    post:
      tags:
        - Theming
      summary: Retire the active theme (fall back to the default)
      description: "Clear the active theme pointer so the site falls back to the default theme; published versions stay intact (history/rollback). High-risk: requires an `Idempotency-Key`, audited. Gated by `theming.version.archive`."
      operationId: themingRetire
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Retired (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    required:
                      - previousThemeKey
                    properties:
                      previousThemeKey:
                        type: string
                        nullable: true
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The `Idempotency-Key` was already used with a different request payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/theming/rollback:
    post:
      tags:
        - Theming
      summary: Roll the active theme back to an earlier published version
      description: "Move the active pointer to an earlier published version of this tenant (never mutates a version row). High-risk: requires an `Idempotency-Key`, audited. Gated by `theming.version.restore`."
      operationId: themingRollback
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - versionId
              properties:
                versionId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Rolled back (or an idempotent replay).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ThemeVersion"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The `Idempotency-Key` was already used with a different request payload.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/theming/validate:
    post:
      tags:
        - Theming
      summary: Validate a theme config (dry run) + preview its token CSS
      description: "Read-only: validate a proposed theme config against its theme descriptor and, when valid, return the exact `text/css` custom-property block it would produce — writing nothing. Gated by `theming.config.read`."
      operationId: themingValidate
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ThemeConfigRequest"
      responses:
        "200":
          description: The validation result (+ token CSS when valid).
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/ThemeValidationResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/user-groups:
    get:
      operationId: listUserGroups
      tags:
        - Identity & Access
      summary: List the tenant's user groups (identity_access.user_groups.read).
      description: |-
        A group is a SUBJECT that can hold role grants, so this list carries a `roleCount` beside `memberCount`: a group holding grants is an authority no amount of looking at PEOPLE would reveal.
        `source` is `local` or `scim`. A `scim` group is managed by an external directory and refuses local mutation with 409 GROUP_EXTERNALLY_MANAGED — SCIM itself is not implemented, only the refusal, because a local edit a later sync silently reverts is worse than one that was never accepted.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant's live user groups, ordered by code.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      groups:
                        type: array
                        items:
                          $ref: "#/components/schemas/UserGroupSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      operationId: createUserGroup
      tags:
        - Identity & Access
      summary: Create a local user group (identity_access.user_groups.create, audited).
      description: "Creates a group with no members and no grants. `source` is never accepted from the request: a caller who could declare a group `scim` would be declaring it un-editable through the only surface that exists, with no directory behind it to edit it instead."
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - groupCode
                - groupName
              properties:
                groupCode:
                  type: string
                  pattern: ^[a-z][a-z0-9_-]{1,63}$
                groupName:
                  type: string
                  maxLength: 200
                description:
                  type: string
                  nullable: true
                  maxLength: 1000
      responses:
        "201":
          description: The created group.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/UserGroupRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: A live group already holds this code (GROUP_CODE_TAKEN).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/user-groups/{id}:
    get:
      operationId: listUserGroupMembers
      tags:
        - Identity & Access
      summary: List one group's members (identity_access.user_groups.read).
      description: |-
        Tenant-user ids only. The login identifier is PII and the caller who may read this already has the user list to resolve them against.
        An unknown group and an empty group answer identically — the alternative needs a pre-read that says whether an id exists in this tenant, which is the existence oracle every other read here refuses to be.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The group's members.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      members:
                        type: array
                        items:
                          type: object
                          properties:
                            tenantUserId:
                              type: string
                              format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    patch:
      operationId: updateUserGroup
      tags:
        - Identity & Access
      summary: Rename a local user group (identity_access.user_groups.update, audited).
      description: "Changes the DISPLAY name and description. `groupCode` is not editable: it is the tenant-facing identity of the group and the thing a tenant's own runbooks name, so renaming it silently would make those wrong."
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - groupName
              properties:
                groupName:
                  type: string
                  maxLength: 200
                description:
                  type: string
                  nullable: true
                  maxLength: 1000
      responses:
        "200":
          description: The updated group.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    $ref: "#/components/schemas/UserGroupRecord"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The group is directory-managed (GROUP_EXTERNALLY_MANAGED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/user-groups/members:
    post:
      operationId: addUserGroupMember
      tags:
        - Identity & Access
      summary: Add a tenant user to a group (identity_access.user_groups.assign, audited).
      description: |-
        A grant in everything but name: membership confers every role the group holds, at once and with no further step. `assign` is a HIGH-RISK action, so this additionally passes the segregation-of-duties chokepoint.
        Idempotent by intent: re-adding somebody already in the group answers 200 with `added: false`. Adding a person to a group they are already in changes nothing about their access, and a 409 there would be an error message about the state the caller asked for.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserGroupMembershipInput"
      responses:
        "200":
          description: Membership after the call.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      added:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The group is directory-managed (GROUP_EXTERNALLY_MANAGED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: removeUserGroupMember
      tags:
        - Identity & Access
      summary: Remove a tenant user from a group (identity_access.user_groups.assign, audited).
      description: Takes away every role the group confers on that person, effective on their next authorization decision — there is no cache between membership and the grant read.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserGroupMembershipInput"
      responses:
        "200":
          description: The membership was removed.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      removed:
                        type: boolean
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The group is directory-managed (GROUP_EXTERNALLY_MANAGED).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/users:
    get:
      operationId: listTenantUsers
      tags:
        - Identity & Access
      summary: List the current tenant's users with their assigned role codes (login identifiers masked).
      responses:
        "200":
          description: The tenant's users (limit 100), with masked login identifiers and assigned role codes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      items:
                        type: array
                        items:
                          $ref: "#/components/schemas/TenantUserMasked"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/users/{id}:
    patch:
      operationId: setTenantUserStatus
      tags:
        - Identity & Access
      summary: Activate or deactivate a tenant user (high-risk, audited).
      description: Sets the tenant user's status. `awcms_tenant_users` has no `deleted_at`, so deactivate = status `inactive`, reactivate = status `active`. Gated on `identity_access.access_control.configure`.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - status
              properties:
                status:
                  type: string
                  enum:
                    - active
                    - inactive
      responses:
        "200":
          description: The tenant user's new status.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      id:
                        type: string
                        format: uuid
                      status:
                        type: string
                        enum:
                          - active
                          - inactive
                      updatedAt:
                        type: string
                        format: date-time
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/users/{id}/sessions:
    get:
      operationId: listTenantUserSessions
      tags:
        - Identity & Access
      summary: List one tenant user's live sessions (identity_access.user_sessions.read).
      description: |-
        Where is this person signed in? Gated on `identity_access.user_sessions.read` — the SENSITIVE half of the pair, because it is a standing window into a colleague's movements. The revoke endpoint beside it destroys access instead of disclosing anything, which is why the two are separate permissions and why an incident responder can hold the second without the first.
        Returns no token, no token hash, no raw IP and no raw User-Agent. `isCallerSession` is true only when an administrator points this at their own tenant user. A malformed id, an id from another tenant and an unknown id all answer 404 — distinguishing them would make this a probe for which tenant user ids exist.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: The tenant user's live sessions, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      sessions:
                        type: array
                        items:
                          $ref: "#/components/schemas/TenantUserSessionSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/users/{id}/sessions/revoke-all:
    post:
      operationId: revokeAllTenantUserSessions
      tags:
        - Identity & Access
      summary: End every live session of one tenant user (identity_access.user_sessions.revoke, audited).
      description: |-
        The incident control. Effective on the next request of every revoked session, because authentication reads the same row. Grantable WITHOUT `user_sessions.read`: stopping a suspected-compromised account should not cost a standing window into where everyone is signed in.
        The CALLER's own session is never revoked — the exclusion matches nothing for any target other than the caller's own tenant user, and stops an administrator from signing themselves out of the console mid-incident by clicking their own row. `keptCallerSession` reports when that happened; signing yourself out everywhere is the unpermissioned self-service surface under `/api/v1/auth/sessions`.
        No `Idempotency-Key`: the second call finds nothing live and reports `revokedCount: 0`, so there is no duplicate to suppress. Audited even when it revokes nothing.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: How many sessions were ended.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
                    properties:
                      revokedCount:
                        type: integer
                        minimum: 0
                      keptCallerSession:
                        type: boolean
                        description: True when the target was the caller's own tenant user, so their current session was left alive.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/workflows/definitions:
    get:
      tags:
        - Workflow Approval
      operationId: workflowsListDefinitions
      summary: List workflow definitions (latest version per workflow key).
      description: One row per distinct workflowKey (latest version, or latest matching the lifecycleStatus filter). See GET /definitions/{id} for full version history.
      parameters:
        - name: lifecycleStatus
          in: query
          schema:
            type: string
            enum:
              - draft
              - active
              - retired
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Latest version per distinct workflow key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Workflow Approval
      operationId: workflowsCreateDefinition
      summary: Create a new draft workflow definition.
      description: Creates a draft version 1 (or the next draft version if the workflowKey already has history). Guarded by workflow.definition.create.
      parameters:
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - workflowKey
                - name
                - graph
              properties:
                workflowKey:
                  type: string
                name:
                  type: string
                description:
                  type: string
                graph:
                  type: object
                factsSchema:
                  type: array
                  items:
                    type: object
      responses:
        "200":
          description: Draft definition created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/workflows/definitions/{id}:
    get:
      tags:
        - Workflow Approval
      operationId: workflowsGetDefinition
      summary: Get a workflow definition and its full version history.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Definition detail plus every version of the same workflow key.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    put:
      tags:
        - Workflow Approval
      operationId: workflowsUpdateDefinition
      summary: Update a draft workflow definition in place.
      description: 409 if the definition is not a draft (fork a new version via POST /new-version instead).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                description:
                  type: string
                graph:
                  type: object
                factsSchema:
                  type: array
                  items:
                    type: object
      responses:
        "200":
          description: Draft definition updated.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The definition is not a draft.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      tags:
        - Workflow Approval
      operationId: workflowsDeleteDefinition
      summary: Soft-delete a draft workflow definition (idempotent, audited).
      description: High-risk — requires Idempotency-Key. 409 for any non-draft (published/retired version history is permanent).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Draft definition soft-deleted.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request, or the definition is not a draft.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/definitions/{id}/new-version:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsCreateDefinitionNewVersion
      summary: Fork a new draft version from an existing definition.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: New draft version created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/workflows/definitions/{id}/publish:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsPublishDefinition
      summary: Publish/activate a draft workflow definition version (idempotent, audited).
      description: Transitions draft to active, retiring any previously-active version of the same workflow key in the same transaction. High-risk — requires Idempotency-Key.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Definition published.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request, or the definition is not a draft.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/definitions/{id}/retire:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsRetireDefinition
      summary: Voluntarily retire an active workflow definition version (idempotent, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Definition retired.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request, or the definition is not active.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/definitions/{id}/validate:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsValidateDefinition
      summary: Dry-run validate a workflow definition graph (stored, or a candidate in the body).
      description: Read-only, non-persisting. Never fails the HTTP call for an invalid graph — returns the validation result in the body.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                graph: {}
                factsSchema: {}
      responses:
        "200":
          description: Validation result.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/workflows/delegations:
    get:
      tags:
        - Workflow Approval
      operationId: workflowsListDelegations
      summary: List workflow delegations.
      parameters:
        - name: delegatorTenantUserId
          in: query
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Delegations (limit 100), newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags:
        - Workflow Approval
      operationId: workflowsCreateDelegation
      summary: Create a workflow delegation from the calling tenant user (idempotent, audited).
      description: A tenant user can only delegate their OWN standing. High-risk — requires Idempotency-Key.
      parameters:
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - delegateTenantUserId
                - reason
              properties:
                delegateTenantUserId:
                  type: string
                  format: uuid
                workflowKey:
                  type: string
                resourceType:
                  type: string
                effectiveFrom:
                  type: string
                  format: date-time
                effectiveTo:
                  type: string
                  format: date-time
                reason:
                  type: string
      responses:
        "200":
          description: Delegation created.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/delegations/{id}/revoke:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsRevokeDelegation
      summary: Revoke a workflow delegation (delegator only, idempotent, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Delegation revoked.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/instances/{id}:
    get:
      tags:
        - Workflow Approval
      operationId: workflowsGetInstance
      summary: Get a workflow instance's detail and immutable action history.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Instance detail plus decision/audit history, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/workflows/instances/{id}/cancel:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsCancelInstance
      summary: Administrative recovery — cancel a running (pending) workflow instance (idempotent, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - reason
              additionalProperties: false
              properties:
                reason:
                  type: string
      responses:
        "200":
          description: Instance cancelled, along with every one of its pending tasks.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Idempotency-Key reused with a different request, or the instance is not pending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/tasks:
    get:
      tags:
        - Workflow Approval
      operationId: workflowsGetPendingTasks
      summary: Consolidated approval inbox — keyset-paginated, filterable task list.
      parameters:
        - name: workflowKey
          in: query
          schema:
            type: string
        - name: resourceType
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
            enum:
              - pending
              - completed
              - skipped
              - cancelled
        - name: overdue
          in: query
          schema:
            type: boolean
        - name: search
          in: query
          schema:
            type: string
        - name: cursor
          in: query
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      responses:
        "200":
          description: Tasks matching the filters (limit 100) with an opaque nextCursor for the next page.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/workflows/tasks/{id}/decisions:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsRecordTaskDecision
      summary: Record a decision (approve/reject) for a pending workflow task (idempotent, audited).
      description: The task completes only once its quorum rule is satisfied; the instance advances through the graph only once the task completes. High-risk — requires Idempotency-Key.
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - decision
              properties:
                decision:
                  type: string
                  enum:
                    - approve
                    - reject
                reason:
                  type: string
      responses:
        "200":
          description: Decision recorded.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: Access denied by RBAC/ABAC/self-approval, or the caller is not an eligible decider for this task.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request, or the task's decision has already been recorded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/tasks/{id}/force-decision:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsForceTaskDecision
      summary: Administrative recovery — force-approve/force-reject a pending task, bypassing quorum (idempotent, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - decision
                - reason
              properties:
                decision:
                  type: string
                  enum:
                    - approve
                    - reject
                reason:
                  type: string
      responses:
        "200":
          description: Task force-decided and the instance advanced accordingly.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request, or the task is not pending.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
  /api/v1/workflows/tasks/{id}/reassign:
    post:
      tags:
        - Workflow Approval
      operationId: workflowsReassignTask
      summary: Administrative recovery — reassign a pending task to another tenant user (idempotent, audited).
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: true
          schema:
            type: string
        - $ref: "#/components/parameters/CorrelationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - toTenantUserId
                - reason
              properties:
                toTenantUserId:
                  type: string
                  format: uuid
                reason:
                  type: string
      responses:
        "200":
          description: Task reassigned.
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    enum:
                      - true
                  data:
                    type: object
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Idempotency-Key reused with a different request, the task is not pending, or the target has already decided this task (reassigning to them would let one person's authority count twice toward quorum).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Opaque session token issued by POST /auth/login.
    tenantHeader:
      type: apiKey
      in: header
      name: X-AWCMS-Tenant-ID
      description: Active tenant context for tenant-scoped API.
    syncHmac:
      type: apiKey
      in: header
      name: X-AWCMS-Signature
      description: HMAC-SHA256 signature over "<timestamp>.<body>" for machine-to-machine sync endpoints (with X-AWCMS-Node-ID and X-AWCMS-Timestamp).
  parameters:
    CorrelationId:
      name: X-Correlation-ID
      in: header
      required: false
      schema:
        type: string
    SyncNodeId:
      name: X-AWCMS-Node-ID
      in: header
      required: true
      description: Node code identifying the calling sync node (auto-registers on first contact).
      schema:
        type: string
    SyncTimestamp:
      name: X-AWCMS-Timestamp
      in: header
      required: true
      description: ISO-8601 timestamp of the request, validated against the allowed skew (anti-replay).
      schema:
        type: string
    SyncSignature:
      name: X-AWCMS-Signature
      in: header
      required: true
      description: HMAC-SHA256 signature over "<timestamp>.<body>".
      schema:
        type: string
  responses:
    BadRequest:
      description: Validation error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    Unauthorized:
      description: Missing or invalid session.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    Forbidden:
      description: Access denied by RBAC/ABAC.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
    NotFound:
      description: Resource not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ApiError"
  schemas:
    AbacDslPolicy:
      type: object
      description: "One stored dynamic ABAC (DSL) policy (Issue #179). Applicability fields are wildcards when null."
      properties:
        id:
          type: string
          format: uuid
        policyCode:
          type: string
        effect:
          type: string
          enum:
            - allow
            - deny
        description:
          type: string
          nullable: true
        moduleKey:
          type: string
          nullable: true
        activityCode:
          type: string
          nullable: true
        action:
          type: string
          nullable: true
        resourceType:
          type: string
          nullable: true
        dslVersion:
          type: integer
        priority:
          type: integer
        conditions:
          $ref: "#/components/schemas/AbacDslPolicyConditions"
        isActive:
          type: boolean
        isDslManaged:
          type: boolean
          description: Whether the evaluator CONSUMES this policy (ADR-0033). The cache loads `is_active AND is_dsl_managed`, so `false` — every row authored through the flat /api/v1/abac/policies CRUD — is stored but never compiled and never affects a decision. This list returns every policy row for the tenant, flat and DSL alike, so without this field a client cannot tell an inert policy from one in force.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AbacDslPolicyConditions:
      type: object
      description: "A bounded, deterministic condition AST (Issue #179). A node is either a composition — exactly one of `allOf` (array; empty = true), `anyOf` (array; empty = false), or `not` (a single node) — or a leaf `{ attr, op, value | valueAttr }` over the server-side attribute allow-list (`subject.*`, `resource.*`, `action`, `env.*`) with the bounded operator set (eq, ne, in, nin, lt, lte, gt, gte, exists). No regex, functions, or arbitrary expressions. Depth ≤ 32, ≤ 512 nodes."
      additionalProperties: true
    AbacDslPolicyWriteRequest:
      type: object
      required:
        - policyCode
        - effect
        - conditions
      properties:
        policyCode:
          type: string
          description: 3-100 chars, alphanumerics plus . _ - (not at the edges).
        effect:
          type: string
          enum:
            - allow
            - deny
        description:
          type: string
          nullable: true
        moduleKey:
          type: string
          nullable: true
          description: Applicability filter — null is a wildcard.
        activityCode:
          type: string
          nullable: true
        action:
          type: string
          nullable: true
        resourceType:
          type: string
          nullable: true
        dslVersion:
          type: integer
          description: Defaults to the current DSL version (1). A value newer than supported is rejected.
        priority:
          type: integer
          description: Lower evaluates first (deterministic). Defaults to 100.
        conditions:
          $ref: "#/components/schemas/AbacDslPolicyConditions"
        isActive:
          type: boolean
          description: On create only — author enabled immediately. Defaults to false (author, then enable).
    AbacPolicy:
      type: object
      properties:
        id:
          type: string
          format: uuid
        policyCode:
          type: string
        effect:
          type: string
          enum:
            - allow
            - deny
        description:
          type: string
          nullable: true
        isActive:
          type: boolean
    AbacSimulationRequest:
      type: object
      required:
        - request
      properties:
        subject:
          type: object
          properties:
            tenantUserId:
              type: string
              format: uuid
              nullable: true
              description: Simulate an EXISTING tenant user (resolves their real roles). Different from the caller requires identity_access.access_control.read.
            roles:
              type: array
              items:
                type: string
              description: Simulate a hypothetical role-code set instead of an existing user.
        request:
          type: object
          required:
            - moduleKey
            - activityCode
            - action
          properties:
            moduleKey:
              type: string
            activityCode:
              type: string
            action:
              type: string
            resourceType:
              type: string
              nullable: true
            resourceAttributes:
              type: object
              additionalProperties: true
              description: Hypothetical resource attributes (treated as server-verified).
        environment:
          type: object
          properties:
            ipTrusted:
              type: boolean
            now:
              type: string
              format: date-time
              nullable: true
    AbacSimulationResponse:
      type: object
      properties:
        decision:
          type: object
          properties:
            allowed:
              type: boolean
            reason:
              type: string
            matchedPolicy:
              type: string
              nullable: true
            matchedPolicyVersion:
              type: integer
              nullable: true
        evaluatedPolicies:
          type: array
          description: Per-policy structural trace — booleans only, never attribute VALUES (no PII).
          items:
            type: object
            properties:
              policyCode:
                type: string
              effect:
                type: string
                enum:
                  - allow
                  - deny
              dslVersion:
                type: integer
              priority:
                type: integer
              applicable:
                type: boolean
              conditionSatisfied:
                type: boolean
                nullable: true
              invalid:
                type: boolean
        subject:
          type: object
          properties:
            tenantUserId:
              type: string
            roles:
              type: array
              items:
                type: string
    AccessEvaluateRequest:
      type: object
      required:
        - moduleKey
        - activityCode
        - action
      properties:
        moduleKey:
          type: string
        activityCode:
          type: string
        action:
          type: string
        resourceType:
          type: string
        resourceId:
          type: string
          format: uuid
        resourceAttributes:
          type: object
          additionalProperties: true
    AccessEvaluateResponse:
      type: object
      properties:
        allowed:
          type: boolean
        reason:
          type: string
        matchedPolicy:
          type: string
          nullable: true
        matchedPolicyVersion:
          type: integer
          nullable: true
    AdPlacementCreateRequest:
      type: object
      required:
        - placementKey
        - name
        - mediaObjectId
      properties:
        placementKey:
          type: string
          enum:
            - header_banner
            - below_headline
            - homepage_middle
            - homepage_bottom
            - article_top
            - article_middle
            - article_bottom
            - sidebar_top
            - sidebar_middle
            - sidebar_bottom
            - category_archive_top
            - search_result_top
        name:
          type: string
        mediaObjectId:
          type: string
          format: uuid
        linkUrl:
          type: string
          nullable: true
        rotationMode:
          type: string
          enum:
            - latest
            - priority
            - random_safe
            - weighted
          default: latest
        priority:
          type: integer
          minimum: 0
          default: 0
        isActive:
          type: boolean
          default: true
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        targetType:
          description: Targeting scope (ADR-0044). Omit for a site-wide ad.
          type: string
          enum:
            - global
            - widget
            - post
            - page
          default: global
        targetId:
          description: Required for `widget`/`post`/`page`, must be omitted for `global`.
          type: string
          format: uuid
          nullable: true
        contentClass:
          description: "Editorial-disclosure classification (Issue #783). Omit for a plain ad or organic content."
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
          default: standard
    AdPlacementItem:
      type: object
      required:
        - id
        - tenantId
        - placementKey
        - name
        - mediaObjectId
        - mediaPublicUrl
        - mediaAltText
        - mediaPubliclyReferenceable
        - rotationMode
        - priority
        - isActive
        - contentClass
        - createdAt
        - updatedAt
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        placementKey:
          type: string
          enum:
            - header_banner
            - below_headline
            - homepage_middle
            - homepage_bottom
            - article_top
            - article_middle
            - article_bottom
            - sidebar_top
            - sidebar_middle
            - sidebar_bottom
            - category_archive_top
            - search_result_top
        name:
          type: string
        mediaObjectId:
          description: Must reference a verified (`verified`/`attached`) R2 media object belonging to this tenant (`awcms_news_media_objects`) — never a local path or arbitrary external URL.
          type: string
          format: uuid
        mediaPublicUrl:
          type: string
          nullable: true
          description: Resolved from the media registry (ADR-0036) so a renderer never has to fetch the object separately. `null` when the media object has been soft-deleted.
        mediaAltText:
          type: string
          nullable: true
        mediaPubliclyReferenceable:
          type: boolean
          description: Whether this ad's media object may appear on a public page — the server's own verdict, not a status to interpret. `false` also covers a soft-deleted object, so a consumer checking only this field cannot render one.
        linkUrl:
          description: Optional, possibly external, link — validated server-side as an absolute http(s) URL only.
          type: string
          nullable: true
        rotationMode:
          type: string
          enum:
            - latest
            - priority
            - random_safe
            - weighted
        priority:
          type: integer
          minimum: 0
        isActive:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        targetType:
          description: Targeting scope (ADR-0044). Orthogonal to `placementKey`, which says WHERE on the page the ad sits — this says WHICH pages the slot is filled on. Rendering a page returns its own targeted ads UNIONED with every `global` ad for that slot.
          type: string
          enum:
            - global
            - widget
            - post
            - page
        targetId:
          description: Required for `widget`/`post`/`page`, must be absent for `global`. Validated at write time as an existing, non-deleted resource of that type in this tenant; not a foreign key, so a target deleted later simply stops matching rather than erroring.
          type: string
          format: uuid
          nullable: true
        contentClass:
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
          description: "Editorial-disclosure classification (Issue #783). A property of this BOOKING, not of the referenced media object — the same creative can run as `standard` in one slot and `sponsored` in another. Defaults to `standard`."
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    AdPlacementUpdateRequest:
      type: object
      properties:
        placementKey:
          type: string
          enum:
            - header_banner
            - below_headline
            - homepage_middle
            - homepage_bottom
            - article_top
            - article_middle
            - article_bottom
            - sidebar_top
            - sidebar_middle
            - sidebar_bottom
            - category_archive_top
            - search_result_top
        name:
          type: string
        mediaObjectId:
          type: string
          format: uuid
        linkUrl:
          type: string
          nullable: true
        rotationMode:
          type: string
          enum:
            - latest
            - priority
            - random_safe
            - weighted
        priority:
          type: integer
          minimum: 0
        isActive:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        targetType:
          description: Targeting scope (ADR-0044). Must be sent whenever `targetId` is sent — the pair moves together or not at all, so that switching to `global` also clears the stored target.
          type: string
          enum:
            - global
            - widget
            - post
            - page
        targetId:
          description: Required for `widget`/`post`/`page`, must be omitted for `global`. Sending it without `targetType` is a validation error.
          type: string
          format: uuid
          nullable: true
        contentClass:
          description: "Editorial-disclosure classification (Issue #783)."
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
    AnalyticsSummary:
      type: object
      properties:
        range:
          type: string
        humanUniqueVisitors:
          type: integer
        humanPageviews:
          type: integer
        botPageviews:
          type: integer
        adminUniqueUsers:
          type: integer
        publicUniqueVisitors:
          type: integer
        topPaths:
          type: array
          items:
            $ref: "#/components/schemas/NamedCount"
        topBrowsers:
          type: array
          items:
            $ref: "#/components/schemas/NamedCount"
        topDevices:
          type: array
          items:
            $ref: "#/components/schemas/NamedCount"
        topCountries:
          type: array
          items:
            $ref: "#/components/schemas/NamedCount"
    ApiError:
      type: object
      required:
        - success
        - error
      properties:
        success:
          type: boolean
          enum:
            - false
        error:
          type: object
          required:
            - code
            - message
          properties:
            code:
              type: string
            message:
              type: string
            details: {}
        meta:
          $ref: "#/components/schemas/ApiMeta"
    ApiMeta:
      type: object
      properties:
        correlationId:
          type: string
        requestId:
          type: string
    AuditEvent:
      type: object
      properties:
        id:
          type: string
          format: uuid
        actorTenantUserId:
          type: string
          format: uuid
          nullable: true
        moduleKey:
          type: string
        action:
          type: string
        resourceType:
          type: string
        resourceId:
          type: string
          nullable: true
        severity:
          type: string
        message:
          type: string
        attributes:
          type: object
          nullable: true
        correlationId:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
    BlogAdPlacement:
      type: object
      required:
        - placementType
      properties:
        placementType:
          type: string
          enum:
            - global
            - widget
            - post
            - page
        targetId:
          type: string
          format: uuid
          nullable: true
    BlogContentBlock:
      description: |
        One block of `content_json`. The vocabulary is closed: a value outside
        the six variants below is not stored and not rendered.

        This schema exists because the vocabulary previously lived only in a
        TypeScript type and one prose sentence, so every consumer re-derived it
        by reading. One did, and disagreed with it in three ways at once —
        silently, because a wrong block type raises no error anywhere. It
        renders wrongly, or the section disappears.

        Held to `blog-content/domain/content-block-rendering.ts`'s
        `CONTENT_BLOCK_TYPES` by `tests/content-block-contract.test.ts`, which
        also holds that constant to the renderer's own switch.

        Note two shapes that are easy to guess wrong:
          - Ordering is a FIELD on `list` (`ordered: true`), not a separate
            `ordered_list` type.
          - `gallery` and `video_news` carry NO `text` field. A renderer that
            falls back to "render `text` as a paragraph" drops them entirely.
      oneOf:
        - type: object
          required:
            - type
            - text
          properties:
            type:
              type: string
              enum:
                - paragraph
            text:
              type: string
        - type: object
          required:
            - type
            - level
            - text
          properties:
            type:
              type: string
              enum:
                - heading
            level:
              type: integer
              minimum: 1
              maximum: 6
            text:
              type: string
        - type: object
          required:
            - type
            - items
          properties:
            type:
              type: string
              enum:
                - list
            ordered:
              description: Absent or false means an unordered list. There is no separate `ordered_list` type.
              type: boolean
            items:
              type: array
              items:
                type: string
        - type: object
          required:
            - type
            - text
          properties:
            type:
              type: string
              enum:
                - quote
            text:
              type: string
        - type: object
          required:
            - type
            - items
          properties:
            type:
              type: string
              enum:
                - gallery
            items:
              type: array
              items:
                type: object
                required:
                  - mediaType
                properties:
                  mediaType:
                    type: string
                    enum:
                      - image
                      - video
                  url:
                    description: Direct absolute URL. Legacy shape — absent when the item uses `mediaObjectId`.
                    type: string
                  mediaObjectId:
                    description: Verified media registry object id. Resolving it to a URL requires a media lookup; a consumer that cannot perform one must show a placeholder rather than dropping the item.
                    type: string
                    format: uuid
                  caption:
                    type: string
        - type: object
          required:
            - type
            - provider
            - videoId
          properties:
            type:
              type: string
              enum:
                - video_news
            provider:
              description: Allowlisted provider. Only `youtube` is recognised today; an unknown provider must not be turned into a URL by guesswork.
              type: string
            videoId:
              description: Provider video id, validated server-side against the provider's own id shape before storage.
              type: string
            title:
              type: string
            caption:
              type: string
            thumbnailMediaObjectId:
              type: string
              format: uuid
            durationSeconds:
              type: integer
            sourceLabel:
              type: string
    BlogContentJson:
      description: "Structured post/page body. `{ blocks: BlogContentBlock[] }` — never raw HTML, by construction: there is no block variant that carries markup."
      type: object
      properties:
        blocks:
          type: array
          items:
            $ref: "#/components/schemas/BlogContentBlock"
    BlogInstitution:
      type: object
      required:
        - id
        - tenantId
        - branch
        - name
        - slug
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        branch:
          type: string
          enum:
            - legislative
            - executive
        name:
          type: string
        slug:
          type: string
        regionCode:
          type: string
          nullable: true
          description: 'Dotted idn_admin_regions code (62, 62.71, 62.71.01, 62.71.01.2001). Not a foreign key: that master is dataset-versioned and its ids change on every import, so the code is the only stable identifier across generations. A code no active dataset resolves degrades to "no region label" on render rather than failing the read.'
        description:
          type: string
          nullable: true
        seoTitle:
          type: string
          nullable: true
        seoDescription:
          type: string
          nullable: true
    BlogInstitutionCreateInput:
      type: object
      required:
        - branch
        - name
        - slug
      properties:
        branch:
          type: string
          enum:
            - legislative
            - executive
        name:
          type: string
          maxLength: 150
        slug:
          type: string
          description: Lowercase alphanumeric segments separated by single hyphens.
        regionCode:
          type: string
          nullable: true
          pattern: ^[0-9]{2}(\.[0-9]{2}){0,2}(\.[0-9]{4})?$
        description:
          type: string
          nullable: true
          maxLength: 500
        seoTitle:
          type: string
          nullable: true
          maxLength: 200
        seoDescription:
          type: string
          nullable: true
          maxLength: 320
    BlogInstitutionUpdateInput:
      type: object
      minProperties: 1
      properties:
        branch:
          type: string
          enum:
            - legislative
            - executive
        name:
          type: string
          maxLength: 150
        slug:
          type: string
        regionCode:
          type: string
          nullable: true
          pattern: ^[0-9]{2}(\.[0-9]{2}){0,2}(\.[0-9]{4})?$
        description:
          type: string
          nullable: true
          maxLength: 500
        seoTitle:
          type: string
          nullable: true
          maxLength: 200
        seoDescription:
          type: string
          nullable: true
          maxLength: 320
    BlogInternalTagLinkSettings:
      type: object
      required:
        - enabled
        - caseInsensitive
        - disabledTagIds
      properties:
        enabled:
          type: boolean
        caseInsensitive:
          type: boolean
        disabledTagIds:
          type: array
          items:
            type: string
            format: uuid
    BlogMenu:
      type: object
      description: |
        One navigation menu, as `GET /api/v1/blog/menus` returns it.

        It exists as a schema because that endpoint's response was documented as
        an array of bare `object` — which promises a consumer nothing at all.
        Any field could have been renamed or dropped and the frozen consumer
        contract would still have passed, because everything is a subset of
        "object". That is the same defect the list endpoint shipped once over
        `BlogPostSummary`, one step further along: not a wrong shape, but no
        shape.

        `key` is the stable identifier a template selects a menu by (`main`,
        `footer`); `name` is the editorial label and may be renamed at any time.
        A consumer that keys off `name` breaks the first time somebody fixes a
        typo.
      required:
        - id
        - tenantId
        - key
        - name
        - items
        - itemsTruncated
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        key:
          type: string
          description: Stable slug. What a consumer selects a menu by.
        name:
          type: string
          description: Editorial label. NOT an identifier.
        items:
          type: array
          maxItems: 200
          description: Already sorted by `sortOrder`. One level of nesting at most — an item whose parent itself has a parent is refused at write time.
          items:
            $ref: "#/components/schemas/BlogMenuItem"
        itemsTruncated:
          type: boolean
          description: |
            `true` when the menu holds more than the 200 items `items` is
            bounded at, and `items` is only the first 200 of them.

            **Do not save a truncated list back.** `PATCH /api/v1/blog/menus/{id}`
            REPLACES the whole item set, so writing back what you were shown
            would delete every item past the bound. Only a menu written before
            the 200-item cap existed can report `true`; on a write response it
            is always `false`, because the request that produced it was itself
            capped.

            Ties are broken by `id`, so items sharing a `sortOrder` keep the
            same order between identical reads — without that the 200 kept
            would be an arbitrary 200.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
          description: Always null on this list — it filters soft-deleted menus out. Present because the row carries it, and named here rather than left as a surprise field.
    BlogMenuItem:
      type: object
      required:
        - id
        - label
        - linkType
        - sortOrder
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
          readOnly: true
          description: Returned on a read, never accepted on a write — the tenant comes from the credential.
        menuId:
          type: string
          format: uuid
          readOnly: true
          description: Returned on a read, never accepted on a write — the item's menu is the one it was submitted with.
        parentItemId:
          type: string
          format: uuid
          nullable: true
        label:
          type: string
        linkType:
          type: string
          enum:
            - post
            - page
            - url
        targetId:
          type: string
          format: uuid
          nullable: true
          description: Required for `post`/`page`. Its existence is deliberately NOT checked against the posts/pages tables, because a menu may point at something not created yet — so a consumer must treat a target that resolves to nothing as a normal state, not an error.
        url:
          type: string
          nullable: true
          description: Required for `url`. An absolute http(s) URL, validated at write time by the same check `canonicalUrl` uses.
        sortOrder:
          type: integer
    BlogPage:
      type: object
      required:
        - id
        - tenantId
        - title
        - slug
        - status
        - visibility
        - pageType
        - locale
        - createdAt
        - updatedAt
        - version
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        authorTenantUserId:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key is overwritten with the derived projection; other keys (notably awcmsAstro) are preserved.
        contentText:
          type: string
          description: ADR-0100 — DERIVED from bodyPortableText. Still returned (it is what the full-text index is built from) but no longer accepted on write.
          readOnly: true
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        status:
          type: string
          enum:
            - draft
            - review
            - scheduled
            - published
            - archived
        visibility:
          type: string
          enum:
            - public
            - private
            - unlisted
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        locale:
          type: string
        pageType:
          type: string
          enum:
            - standard
            - landing
            - legal
            - system
        parentPageId:
          type: string
          format: uuid
          nullable: true
        menuOrder:
          type: integer
        publishedAt:
          type: string
          format: date-time
          nullable: true
        scheduledAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
        redirectCapture:
          description: "Issue #787 (sibling of #784) — present ONLY on a `PATCH /api/v1/blog/pages/{id}` response whose body actually changed `slug` (absent otherwise, including on `GET`/`POST` and every lifecycle action). Reports what `captureUrlChangeRedirect` (ADR-0039) did with the resulting old->new path change, gated by the tenant's own `url_change_auto_policy` (`POST /api/v1/seo/redirects/capture-url-change` is the same seam, driven here automatically instead of by a caller). A `rejected` or `invalid` outcome never fails the page update itself — the slug change is additive tooling around the edit, not a precondition for it."
          type: object
          required:
            - outcome
          properties:
            outcome:
              type: string
              enum:
                - created
                - proposed
                - skipped
                - module_disabled
                - invalid
                - rejected
            redirectId:
              description: Present only when outcome is "created" or "proposed".
              type: string
              format: uuid
            code:
              description: Present only when outcome is "rejected".
              type: string
              enum:
                - SOURCE_CONFLICT
                - REDIRECT_LOOP
                - REDIRECT_CHAIN_TOO_LONG
            message:
              description: Present only when outcome is "rejected" — a human-readable reason no redirect was created.
              type: string
    BlogPageWriteInput:
      type: object
      description: Shared shape for POST /api/v1/blog/pages and PATCH /api/v1/blog/pages/{id} (all fields optional on update).
      properties:
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key is overwritten with the derived projection; other keys (notably awcmsAstro) are preserved.
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        locale:
          type: string
        visibility:
          type: string
          enum:
            - public
            - private
            - unlisted
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        pageType:
          type: string
          enum:
            - standard
            - landing
            - legal
            - system
        parentPageId:
          type: string
          format: uuid
          nullable: true
        menuOrder:
          type: integer
    BlogPost:
      type: object
      required:
        - id
        - tenantId
        - title
        - slug
        - status
        - visibility
        - locale
        - createdAt
        - updatedAt
        - version
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        authorTenantUserId:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key is overwritten with the derived projection; other keys (notably awcmsAstro) are preserved.
        contentText:
          type: string
          description: ADR-0100 — DERIVED from bodyPortableText. Still returned (it is what the full-text index is built from) but no longer accepted on write.
          readOnly: true
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        status:
          type: string
          enum:
            - draft
            - review
            - scheduled
            - published
            - archived
        visibility:
          type: string
          enum:
            - public
            - private
            - unlisted
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoImageMediaId:
          type: string
          format: uuid
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        locale:
          type: string
        translationGroupId:
          type: string
          format: uuid
          nullable: true
        autoInternalTagLinksDisabled:
          type: boolean
        publishedAt:
          type: string
          format: date-time
          nullable: true
        scheduledAt:
          type: string
          format: date-time
          nullable: true
        unpublishAt:
          type: string
          format: date-time
          nullable: true
          description: "Issue #591 — when the scheduled-publish job archives this post. Null = stays published until archived by hand."
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        version:
          type: integer
        termIds:
          description: "Category/tag/channel/topic assignments. Returned by `GET /api/v1/blog/posts/{id}`, by `PATCH` on the same path, and — since Issue #597 — by the `?view=full` traversal that a static site is built from. `[]` means the post has none; the field being ABSENT means the response shape does not carry it (the lifecycle actions), which is a different fact."
          type: array
          items:
            type: string
            format: uuid
        institutionIds:
          description: "Issue #595 — the fourth classification dimension (sql/131). Institution is a TABLE rather than a taxonomy type because it carries a branch, a region code and its own landing SEO, but on this payload it is the same shape as `termIds`, and it is returned in the same places for the same reasons."
          type: array
          items:
            type: string
            format: uuid
        authorByline:
          description: |-
            ADR-0109 — the author's OPT-IN public byline, returned by the `?view=full` traversal a static site is built from.
            `null` is the normal state and means the article keeps the ORGANISATION-level attribution (ADR-0102): a consumer renders no individual name for it. It is deliberately NOT the author's internal display name — publishing an account name because somebody wrote an article is the PII surface Issue #649 refused to open — and it carries no id, URL or profile link, only the name.
          type: string
          nullable: true
          maxLength: 120
        redirectCapture:
          description: "Issue #784 — present ONLY on a `PATCH /api/v1/blog/posts/{id}` response whose body actually changed `slug` (absent otherwise, including on `GET`/`POST` and every lifecycle action). Reports what `captureUrlChangeRedirect` (ADR-0039) did with the resulting old->new path change, gated by the tenant's own `url_change_auto_policy` (`POST /api/v1/seo/redirects/capture-url-change` is the same seam, driven here automatically instead of by a caller). A `rejected` or `invalid` outcome never fails the post update itself — the slug change is additive tooling around the edit, not a precondition for it."
          type: object
          required:
            - outcome
          properties:
            outcome:
              type: string
              enum:
                - created
                - proposed
                - skipped
                - module_disabled
                - invalid
                - rejected
            redirectId:
              description: Present only when outcome is "created" or "proposed".
              type: string
              format: uuid
            code:
              description: Present only when outcome is "rejected".
              type: string
              enum:
                - SOURCE_CONFLICT
                - REDIRECT_LOOP
                - REDIRECT_CHAIN_TOO_LONG
            message:
              description: Present only when outcome is "rejected" — a human-readable reason no redirect was created.
              type: string
    BlogPostSummary:
      type: object
      description: |
        What the LIST endpoint returns by default — deliberately not a `BlogPost`.

        It exists as its own schema because the list used to be documented as returning `BlogPost`, and it never did: no `contentJson`, `excerpt`, `metaDescription`, `canonicalUrl`, or `translationGroupId`. A client that trusted the document read those as `undefined` and published a site with empty articles, with nothing failing. Ask for `?view=full` to get `BlogPost`.
      required:
        - id
        - tenantId
        - title
        - slug
        - status
        - visibility
        - locale
        - createdAt
        - updatedAt
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        status:
          type: string
          enum:
            - draft
            - review
            - scheduled
            - published
            - archived
        visibility:
          type: string
          enum:
            - public
            - private
            - unlisted
        locale:
          type: string
        publishedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
          description: Immutable — the only sort key this list can paginate over soundly.
        updatedAt:
          type: string
          format: date-time
    BlogPostWriteInput:
      type: object
      description: Shared shape for POST /api/v1/blog/posts (all fields required) and PATCH /api/v1/blog/posts/{id} (all fields optional, only present fields change).
      properties:
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key is overwritten with the derived projection; other keys (notably awcmsAstro) are preserved.
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        locale:
          type: string
        visibility:
          type: string
          enum:
            - public
            - private
            - unlisted
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        seoImageMediaId:
          type: string
          format: uuid
          nullable: true
          description: Explicit "use this image for social/SEO preview" override — takes priority over featuredMediaId.
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        termIds:
          type: array
          items:
            type: string
            format: uuid
        institutionIds:
          description: "Issue #595 — the fourth classification dimension (sql/131). Institution is a TABLE rather than a taxonomy type because it carries a branch, a region code and its own landing SEO, but on this payload it is the same shape as `termIds`. Full-replace semantics: absent leaves the assignments alone, `[]` removes them."
          type: array
          items:
            type: string
            format: uuid
        translationGroupId:
          type: string
          format: uuid
          nullable: true
        autoInternalTagLinksDisabled:
          type: boolean
          description: Per-post opt-out of automatic internal tag linking.
    BlogRevision:
      type: object
      required:
        - id
        - tenantId
        - resourceType
        - resourceId
        - revisionNumber
        - title
        - status
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        resourceType:
          type: string
          enum:
            - post
            - page
        resourceId:
          type: string
          format: uuid
        revisionNumber:
          type: integer
        title:
          type: string
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: ADR-0100 — the non-body ENVELOPE, optional on write. Its blocks key is overwritten with the derived projection; other keys (notably awcmsAstro) are preserved.
        contentText:
          type: string
          description: ADR-0100 — DERIVED from bodyPortableText. Still returned (it is what the full-text index is built from) but no longer accepted on write.
          readOnly: true
        bodyPortableText:
          $ref: "#/components/schemas/PortableTextBody"
        excerpt:
          type: string
          nullable: true
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        status:
          type: string
        changeNote:
          type: string
          nullable: true
        createdByTenantUserId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
    BlogSettings:
      type: object
      properties:
        blogTitle:
          type: string
          nullable: true
        blogDescription:
          type: string
          nullable: true
        defaultLocale:
          type: string
        defaultVisibility:
          type: string
          enum:
            - public
            - private
            - unlisted
        postsPerPage:
          type: integer
        seoDefaultTitle:
          type: string
          nullable: true
        seoDefaultDescription:
          type: string
          nullable: true
        rssEnabled:
          type: boolean
        sitemapEnabled:
          type: boolean
        contentQualityChecklistPolicy:
          type: object
        socialPreviewFallbackImageMediaId:
          type: string
          format: uuid
          nullable: true
        socialPreviewContentImageFallbackEnabled:
          type: boolean
    BlogTerm:
      type: object
      required:
        - id
        - tenantId
        - taxonomyType
        - name
        - slug
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        taxonomyType:
          type: string
          enum:
            - category
            - tag
            - channel
            - topic
        parentId:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        slug:
          type: string
        description:
          type: string
          nullable: true
    BlogTermWriteInput:
      type: object
      properties:
        taxonomyType:
          type: string
          enum:
            - category
            - tag
            - channel
            - topic
        parentId:
          type: string
          format: uuid
          nullable: true
        name:
          type: string
        slug:
          type: string
        description:
          type: string
          nullable: true
    BlogWidget:
      type: object
      description: |
        One widget, as `GET /api/v1/blog/widgets` returns it. Its response was
        documented as an array of bare `object` too — see `BlogMenu` for why
        that is worse than a wrong shape.

        `bodyText` is PLAIN TEXT, not markup: the write path refuses unsafe HTML
        rather than sanitizing it, so a consumer must escape this before
        rendering and must not treat it as trusted HTML.
      required:
        - id
        - tenantId
        - position
        - title
        - bodyText
        - isActive
        - sortOrder
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        position:
          type: string
          enum:
            - header
            - sidebar
            - footer
            - content_before
            - content_after
        title:
          type: string
        bodyText:
          type: string
          description: Plain text. Escape before rendering.
        isActive:
          type: boolean
          description: Inactive widgets ARE returned by the list — filter on this rather than expecting the endpoint to hide them.
        sortOrder:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    BulkModerationResult:
      type: object
      properties:
        applied:
          type: array
          items:
            type: string
            format: uuid
        appliedCount:
          type: integer
        skipped:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              reason:
                type: string
                enum:
                  - not_found
                  - illegal_transition
    BusinessScopeAssignment:
      type: object
      description: "One business-scope assignment (Issue #180) — a subject granted a role/permission context restricted to one generic business scope, with effective dating and a lifecycle status."
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        tenantUserId:
          type: string
          format: uuid
        roleId:
          type: string
          format: uuid
          nullable: true
        scopeType:
          type: string
        scopeId:
          type: string
          format: uuid
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: string
          format: date-time
          nullable: true
        isTemporary:
          type: boolean
        reason:
          type: string
          nullable: true
        grantedByTenantUserId:
          type: string
          format: uuid
        approvedByTenantUserId:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
          enum:
            - active
            - expired
            - revoked
        revokedAt:
          type: string
          format: date-time
          nullable: true
        revokedByTenantUserId:
          type: string
          format: uuid
          nullable: true
        revokeReason:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CollectVisitBeaconRequest:
      type: object
      required:
        - tenantCode
        - path
      properties:
        tenantCode:
          type: string
          description: Public tenant code the beacon is reporting for.
        path:
          type: string
          description: The page path being reported (must start with '/'). Sanitized server-side; sensitive query params are stripped.
        referrer:
          type: string
          nullable: true
          description: Optional referrer URL; only its bare hostname is ever stored.
    CommentKeysetCursor:
      type: object
      nullable: true
      description: Full-precision keyset cursor. `createdAt` is microsecond-precision TEXT, not a JavaScript-rounded timestamp; paging on a rounded value silently skips every row sharing the same millisecond.
      properties:
        createdAt:
          type: string
        id:
          type: string
          format: uuid
    CommentSettings:
      type: object
      description: Per-tenant comment configuration. Every numeric bound mirrors a CHECK constraint in sql/066.
      properties:
        defaultPolicyMode:
          type: string
          enum:
            - disabled
            - authenticated-only
            - moderated-anonymous
            - moderated-registered
        requireModeration:
          type: boolean
        allowAnonymous:
          type: boolean
        editWindowSeconds:
          type: integer
          minimum: 0
          maximum: 86400
        maxDepth:
          type: integer
          minimum: 0
          maximum: 8
          description: Only ever TIGHTENS the structural hard cap of 4; a larger value does not deepen the rendered tree.
        maxLength:
          type: integer
          minimum: 100
          maximum: 4000
        maxLinksPerComment:
          type: integer
          minimum: 0
          maximum: 20
        minSubmitSeconds:
          type: integer
          minimum: 0
          maximum: 600
        rateLimitPerHour:
          type: integer
          minimum: 1
          maximum: 1000
        blockedTerms:
          type: array
          maxItems: 200
          items:
            type: string
            maxLength: 100
        turnstileEnabled:
          type: boolean
        notifyOnReply:
          type: boolean
    ComposedHomepageGroup:
      type: object
      properties:
        heading:
          type: string
          nullable: true
          description: Category name for a grid column; null for one ungrouped list.
        slug:
          type: string
          nullable: true
          description: Category slug, so the consumer can link the column to its archive.
        posts:
          type: array
          items:
            $ref: "#/components/schemas/ComposedHomepagePostCard"
    ComposedHomepageImage:
      type: object
      properties:
        url:
          type: string
        altText:
          type: string
          nullable: true
        width:
          type: integer
          nullable: true
        height:
          type: integer
          nullable: true
    ComposedHomepagePostCard:
      type: object
      properties:
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        publishedAt:
          type: string
          format: date-time
        image:
          allOf:
            - $ref: "#/components/schemas/ComposedHomepageImage"
          nullable: true
    ComposedHomepageSection:
      type: object
      properties:
        sectionKey:
          type: string
        sectionType:
          type: string
          enum:
            - headline
            - latest_posts
            - featured_posts
            - editor_picks
            - category_grid
            - gallery_block
        title:
          type: string
          nullable: true
        groups:
          type: array
          items:
            $ref: "#/components/schemas/ComposedHomepageGroup"
        images:
          type: array
          items:
            $ref: "#/components/schemas/ComposedHomepageImage"
        caption:
          type: string
          nullable: true
        fellBackToLatest:
          type: boolean
          description: True when curation resolved to nothing and the latest eligible articles were substituted.
    ComposedSiteIdentity:
      description: Both halves of site identity in one answer. The four SEO-owned fields are named exactly as seo_distribution names them, so this is visibly a passthrough rather than a second definition that could drift.
      allOf:
        - $ref: "#/components/schemas/SiteProfile"
        - type: object
          properties:
            siteName:
              type: string
              nullable: true
            organizationName:
              type: string
              nullable: true
            organizationLogoMediaId:
              type: string
              format: uuid
              nullable: true
            defaultSocialMediaId:
              type: string
              format: uuid
              nullable: true
    ContentQualityChecklistResult:
      type: object
      required:
        - applicable
        - passed
        - blockers
      description: notApplicableChecklistResult() when full-online R2-only mode is not active for the tenant (applicable=false, passed=true, blockers=[]) — otherwise a full evaluation.
      properties:
        applicable:
          type: boolean
        passed:
          type: boolean
        blockers:
          type: array
          items:
            type: object
            required:
              - ruleId
              - message
            properties:
              ruleId:
                type: string
              message:
                type: string
        warnings:
          type: array
          items:
            type: object
    CreateInvitationInput:
      type: object
      required:
        - loginIdentifier
        - displayName
      properties:
        loginIdentifier:
          type: string
          maxLength: 320
          description: An email address. Stored trimmed and NEVER lowercased, matching every identifier lookup on the auth path.
        displayName:
          type: string
          maxLength: 120
        roleIds:
          type: array
          maxItems: 20
          items:
            type: string
            format: uuid
          description: Absent or empty admits the person without granting anything. A non-empty list additionally requires identity_access.access_control.assign.
        skipEmailConfirmation:
          type: boolean
          default: false
          description: PLATFORM-scoped unless the address already holds an active identity in this tenant. Only the literal `true` opts in.
    CreateNewsMediaUploadSessionRequest:
      type: object
      required:
        - mimeType
        - byteSize
      additionalProperties: false
      properties:
        mimeType:
          type: string
          description: "Must be one of the deployment's configured NEWS_MEDIA_R2_ALLOWED_MIME_TYPES (default: image/jpeg, image/png, image/webp, image/gif — image/svg+xml is never allowed by default)."
          example: image/jpeg
        byteSize:
          type: integer
          minimum: 1
          description: Claimed size in bytes — shape-only check against NEWS_MEDIA_R2_MAX_UPLOAD_BYTES; the real size is re-checked from R2 itself at finalize time.
        originalFilename:
          type: string
          nullable: true
          description: Stored as display-only metadata — never part of the server-generated object key.
        altText:
          type: string
          nullable: true
        caption:
          type: string
          nullable: true
    CreateTenantDomainRequest:
      type: object
      required:
        - hostname
      properties:
        hostname:
          type: string
          description: DNS hostname (no port). Normalized to lowercase for uniqueness/lookup.
        domainType:
          type: string
          enum:
            - subdomain
            - custom_domain
          default: custom_domain
        routeMode:
          type: string
          enum:
            - canonical
            - legacy_blog
          default: canonical
        redirectToPrimary:
          type: boolean
          default: false
    DataLifecycleCreateLegalHoldRequest:
      type: object
      required:
        - scopeDescription
        - reason
        - authorityReference
      additionalProperties: false
      properties:
        descriptorKey:
          type: string
          nullable: true
          description: Omit or null for a tenant-wide hold applying to every registered descriptor.
        scopeDescription:
          type: string
          maxLength: 2000
        reason:
          type: string
          minLength: 10
          maxLength: 2000
        authorityReference:
          type: string
          maxLength: 2000
          description: e.g. a court order or regulator reference number.
        endsAt:
          type: string
          format: date-time
          nullable: true
          description: Reporting metadata only (an operator's expected review date) — NEVER an automatic-expiry mechanism. A hold stops applying only via an explicit, audited release.
    DataLifecycleDescriptor:
      type: object
      description: Trusted code-declared metadata (never tenant/request-controlled) — see src/modules/_shared/module-contract.ts's HighVolumeTableDescriptor. additionalProperties is true because the full descriptor also carries nested partition/archive/deletion/legalHold/requiredIndexes/existingAdopter policy objects; only the flat scalar facts are enumerated here.
      required:
        - key
        - tableName
        - ownerModuleKey
        - scope
        - cursorColumn
        - retentionClass
        - retentionMinDays
        - retentionMaxDays
        - defaultRetentionDays
        - batchLimit
        - executionMode
      additionalProperties: true
      properties:
        key:
          type: string
        tableName:
          type: string
        ownerModuleKey:
          type: string
        scope:
          type: string
          enum:
            - tenant
            - global
        cursorColumn:
          type: string
        retentionClass:
          type: string
        retentionMinDays:
          type: integer
        retentionMaxDays:
          type: integer
        defaultRetentionDays:
          type: integer
        batchLimit:
          type: integer
        executionMode:
          type: string
          enum:
            - delegated
            - generic
    DataLifecycleDryRunPlan:
      type: object
      required:
        - descriptorKey
        - cutoffAt
        - eligibleCount
        - heldCount
        - archivedCount
        - purgeableCount
        - blockedCount
        - matchedHoldIds
      additionalProperties: false
      properties:
        descriptorKey:
          type: string
        cutoffAt:
          type: string
          format: date-time
        eligibleCount:
          type: integer
        heldCount:
          type: integer
        archivedCount:
          type: integer
        purgeableCount:
          type: integer
        blockedCount:
          type: integer
        matchedHoldIds:
          type: array
          items:
            type: string
            format: uuid
    DataLifecycleDryRunRequest:
      type: object
      required:
        - descriptorKey
      additionalProperties: false
      properties:
        descriptorKey:
          type: string
          description: A key from GET /api/v1/data-lifecycle/registry.
        retentionDaysOverride:
          type: integer
          description: Optional — clamped to the descriptor's [retentionMinDays, retentionMaxDays] bounds. Cannot widen eligibility around a legal hold (the hold check runs first, unconditionally).
    DataLifecycleInfrastructureDescriptor:
      type: object
      description: "Trusted code-declared metadata for a table with NO owning module — see src/modules/data-lifecycle/domain/infrastructure-lifecycle-registry.ts. Identical to DataLifecycleDescriptor except that ownerPath (a src/lib/ directory) replaces ownerModuleKey: naming a module here would be a false ownership claim, which is the defect this registry exists to make unstateable."
      required:
        - key
        - tableName
        - ownerPath
        - scope
        - cursorColumn
        - retentionClass
        - retentionMinDays
        - retentionMaxDays
        - defaultRetentionDays
        - batchLimit
        - executionMode
      additionalProperties: true
      properties:
        key:
          type: string
        tableName:
          type: string
        ownerPath:
          type: string
          description: The src/lib/<subsystem>/ directory that owns the schema.
        scope:
          type: string
          enum:
            - tenant
            - global
        cursorColumn:
          type: string
        retentionClass:
          type: string
        retentionMinDays:
          type: integer
        retentionMaxDays:
          type: integer
        defaultRetentionDays:
          type: integer
        batchLimit:
          type: integer
        executionMode:
          type: string
          enum:
            - delegated
    DataLifecycleLegalHold:
      type: object
      required:
        - id
        - tenantId
        - descriptorKey
        - scopeDescription
        - reason
        - authorityReference
        - status
        - startsAt
        - endsAt
        - requestedBy
        - releasedBy
        - releasedAt
        - releaseReason
        - createdAt
        - updatedAt
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        descriptorKey:
          type: string
          nullable: true
          description: null means a tenant-wide hold applying to every registered descriptor.
        scopeDescription:
          type: string
        reason:
          type: string
        authorityReference:
          type: string
        status:
          type: string
          enum:
            - active
            - released
        startsAt:
          type: string
          format: date-time
        endsAt:
          type: string
          format: date-time
          nullable: true
        requestedBy:
          type: string
          format: uuid
        releasedBy:
          type: string
          format: uuid
          nullable: true
        releasedAt:
          type: string
          format: date-time
          nullable: true
        releaseReason:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    DataLifecycleReleaseLegalHoldRequest:
      type: object
      required:
        - releaseReason
      additionalProperties: false
      properties:
        releaseReason:
          type: string
          minLength: 10
          maxLength: 2000
    DataLifecycleRun:
      type: object
      required:
        - id
        - descriptorKey
        - runType
        - status
        - eligibleCount
        - heldCount
        - archivedCount
        - purgeableCount
        - purgedCount
        - blockedCount
        - errorCount
        - cutoffAt
        - startedAt
        - finishedAt
        - triggeredBy
        - createdAt
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        descriptorKey:
          type: string
        runType:
          type: string
          enum:
            - dry_run
            - archive
            - purge
        status:
          type: string
          enum:
            - completed
            - partial
            - failed
        eligibleCount:
          type: integer
        heldCount:
          type: integer
        archivedCount:
          type: integer
        purgeableCount:
          type: integer
        purgedCount:
          type: integer
        blockedCount:
          type: integer
        errorCount:
          type: integer
        cutoffAt:
          type: string
          format: date-time
          nullable: true
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
        triggeredBy:
          type: string
          format: uuid
          nullable: true
          description: null for a scheduled/system job run; a tenant_user_id for an on-demand API-triggered run.
        createdAt:
          type: string
          format: date-time
    DataLifecycleSubjectDecisionInput:
      type: object
      required:
        - decision
        - reason
      properties:
        decision:
          type: string
          enum:
            - approve
            - reject
        reason:
          type: string
          minLength: 8
    DataLifecycleSubjectErasureTarget:
      type: object
      properties:
        key:
          type: string
        tableName:
          type: string
        erasure:
          type: string
          enum:
            - anonymize
            - hard_delete
            - status_transition_then_purge
        rowsAffected:
          type: integer
    DataLifecycleSubjectRequest:
      type: object
      properties:
        id:
          type: string
          format: uuid
        subjectTenantUserId:
          type: string
          format: uuid
        requestType:
          type: string
          enum:
            - export
            - erasure
        status:
          type: string
          enum:
            - disclosed
            - pending_approval
            - rejected
            - completed
          description: "`disclosed` is an export that happened; an export is never pending, because it waits for nobody."
        reason:
          type: string
        requestedBy:
          type: string
          format: uuid
        requestedAt:
          type: string
          format: date-time
        decidedBy:
          type: string
          format: uuid
          nullable: true
          description: Always a DIFFERENT tenant user from requestedBy — enforced by a CHECK constraint, not only by the SoD rule.
        decidedAt:
          type: string
          format: date-time
          nullable: true
        decisionReason:
          type: string
          nullable: true
        completedAt:
          type: string
          format: date-time
          nullable: true
        tablesAnswered:
          type: integer
          nullable: true
        tablesUnanswered:
          type: integer
          nullable: true
          description: Tables outside this answer (global, or matchable by no column). Carried so the record states its own coverage.
        rowsAffected:
          type: integer
          nullable: true
    DataLifecycleSubjectRequestInput:
      type: object
      required:
        - tenantUserId
        - reason
      properties:
        tenantUserId:
          type: string
          format: uuid
          description: The SUBJECT — a tenant user in the caller's own tenant (ADR-0094 Decision 1, never the global principal).
        reason:
          type: string
          minLength: 8
          description: Why the disclosure or erasure is being made. Recorded on the request row and in the audit trail; the checker's only input for an irreversible decision.
    DataLifecycleSubjectTableExport:
      type: object
      properties:
        key:
          type: string
        tableName:
          type: string
        ownerModuleKey:
          type: string
        redactedColumns:
          type: array
          description: Columns withheld from the export — hashes, tokens, secrets — even though the row is the subject's own.
          items:
            type: string
        rows:
          type: array
          items:
            type: object
            additionalProperties: true
        truncated:
          type: boolean
          description: "True when this table filled the per-table row cap, so the export is incomplete for it. Reported rather than swallowed: a subject-access export that quietly returned the first N rows would answer a legal obligation with a number dressed as an answer."
    DataLifecycleSubjectUnanswered:
      type: object
      properties:
        key:
          type: string
        tableName:
          type: string
        ownerModuleKey:
          type: string
        reason:
          type: string
          enum:
            - global
            - no_subject_column
        rationale:
          type: string
    DelegatedAccessGrant:
      type: object
      description: "A delegated-access grant as its OWNER tenant sees it (ADR-0090). The redemption code is absent by construction: the column is never selected."
      properties:
        id:
          type: string
          format: uuid
        partnerTenantId:
          type: string
          format: uuid
        roleId:
          type: string
          format: uuid
        purpose:
          type: string
        expiresAt:
          type: string
          format: date-time
        redeemedAt:
          type: string
          format: date-time
          nullable: true
        revokedAt:
          type: string
          format: date-time
          nullable: true
        grantedTenantUserId:
          type: string
          format: uuid
          nullable: true
    DeleteTenantDomainRequest:
      type: object
      required:
        - reason
      properties:
        reason:
          type: string
          description: Non-empty soft-delete reason (audited).
    FinalizeNewsMediaUploadSessionRequest:
      type: object
      additionalProperties: false
      properties:
        checksumSha256:
          type: string
          nullable: true
          description: Optional. When supplied, compared against the checksum computed server-side from the bytes actually read from R2 — a transport-corruption check only, never a substitute for the server-side MIME sniff.
          pattern: ^[0-9a-fA-F]{64}$
          example: e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
    FormDraft:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        moduleKey:
          type: string
        wizardKey:
          type: string
        resourceType:
          type: string
        resourceId:
          type: string
          nullable: true
        currentStep:
          type: string
        payload:
          type: object
          description: Opaque JSON object owned by the creating module. Never contains a key resembling a secret — such payloads are rejected on write.
        status:
          $ref: "#/components/schemas/FormDraftStatus"
        createdBy:
          type: string
          format: uuid
        updatedBy:
          type: string
          format: uuid
        submittedBy:
          type: string
          format: uuid
          nullable: true
        expiresAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        submittedAt:
          type: string
          format: date-time
          nullable: true
    FormDraftCreateRequest:
      type: object
      required:
        - moduleKey
        - wizardKey
        - resourceType
        - currentStep
        - payload
      properties:
        moduleKey:
          type: string
          description: Lowercase snake_case, 2-64 characters, starting with a letter.
        wizardKey:
          type: string
          description: Lowercase snake_case, 2-64 characters, starting with a letter.
        resourceType:
          type: string
          description: Lowercase snake_case, 2-64 characters, starting with a letter.
        resourceId:
          type: string
          maxLength: 128
          description: Free-form text, not a uuid — a draft may reference a not-yet-created resource or a non-UUID external identifier.
        currentStep:
          type: string
          maxLength: 64
        payload:
          type: object
          description: JSON object, at most 32768 bytes serialized. Rejected outright if any key at any depth matches password/token/secret/credential/ apiKey/privateKey.
        expiresAt:
          type: string
          format: date-time
    FormDraftStatus:
      type: string
      enum:
        - draft
        - submitted
        - abandoned
        - expired
    FormDraftUpdateRequest:
      type: object
      description: At least one of currentStep, payload, or expiresAt is required.
      properties:
        currentStep:
          type: string
          maxLength: 64
        payload:
          type: object
        expiresAt:
          type: string
          format: date-time
          nullable: true
    HomepageSectionConfig:
      description: "Shape depends on sectionType — headline: {postId}; latest_posts: {limit?, categorySlug?}; featured_posts/editor_picks: {postIds: []}; category_grid: {categorySlugs: [], postsPerCategory?}; gallery_block: {mediaObjectIds: [], caption?}. Validated server-side per sectionType; every id/slug must already exist for the same tenant, and gallery_block's mediaObjectIds must each be a verified R2 media object."
      type: object
      additionalProperties: true
    HomepageSectionCreateRequest:
      type: object
      required:
        - sectionKey
        - sectionType
        - config
      properties:
        sectionKey:
          type: string
          pattern: ^[a-z0-9][a-z0-9_-]{0,63}$
        sectionType:
          type: string
          enum:
            - headline
            - latest_posts
            - featured_posts
            - editor_picks
            - category_grid
            - gallery_block
        title:
          type: string
          nullable: true
        config:
          $ref: "#/components/schemas/HomepageSectionConfig"
        sortOrder:
          type: integer
        isEnabled:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
    HomepageSectionItem:
      type: object
      required:
        - id
        - tenantId
        - sectionKey
        - sectionType
        - config
        - sortOrder
        - isEnabled
        - createdAt
        - updatedAt
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        sectionKey:
          type: string
        sectionType:
          type: string
          enum:
            - headline
            - latest_posts
            - featured_posts
            - editor_picks
            - category_grid
            - gallery_block
        title:
          type: string
          nullable: true
        config:
          $ref: "#/components/schemas/HomepageSectionConfig"
        sortOrder:
          type: integer
        isEnabled:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    HomepageSectionUpdateRequest:
      description: sectionType cannot be changed after creation — omit it, do not send the old or a new value.
      type: object
      properties:
        title:
          type: string
          nullable: true
        config:
          $ref: "#/components/schemas/HomepageSectionConfig"
        sortOrder:
          type: integer
        isEnabled:
          type: boolean
        startsAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
    IdnRegionDataset:
      type: object
      required:
        - id
        - datasetCode
        - status
        - rowCount
      properties:
        id:
          type: string
          format: uuid
        datasetCode:
          type: string
          description: Deterministic code derived from the upstream commit and file checksum, so re-importing identical bytes collides instead of creating a duplicate version.
        status:
          type: string
          enum:
            - validated
            - active
            - superseded
            - rejected
        rowCount:
          type: integer
        sourceRepository:
          type: string
        sourceCommitSha:
          type: string
        sourceLicense:
          type: string
        sourceReference:
          type: string
          nullable: true
          description: The Kepmendagri decree the imported file's own header cites.
        sourceFileSha256:
          type: string
        createdAt:
          type: string
          format: date-time
        activatedAt:
          type: string
          format: date-time
          nullable: true
        rejectionReason:
          type: string
          nullable: true
          description: Why a `rejected` dataset failed validation — the joined problems planDatasetImport reported (unparsed lines, duplicate codes, orphaned parents, a missing tier). Null for every other status. Render as escaped text, never HTML.
    IdnRegionItem:
      type: object
      required:
        - code
        - codeCompact
        - level
        - regionType
        - name
      properties:
        code:
          type: string
          description: Dotted Kemendagri code, e.g. `11.01.01.2001`.
        codeCompact:
          type: string
          description: The same code without separators.
        parentCode:
          type: string
          nullable: true
        level:
          type: integer
          minimum: 1
          maximum: 4
        regionType:
          type: string
          enum:
            - province
            - regency
            - district
            - village
        localTerm:
          type: string
          nullable: true
          description: Indonesian term this row carries (Provinsi/Kabupaten/Kota/Desa/Kelurahan/Desa Adat). Null where upstream states none — districts ship bare names, and filling in "Kecamatan" would be wrong for the provinces that call the tier "Distrik".
        name:
          type: string
          description: Official name exactly as upstream spells it, prefix included.
        shortName:
          type: string
          nullable: true
          description: Name with the tier term stripped ("Aceh Selatan" for "Kabupaten Aceh Selatan").
        fullPathName:
          type: string
          nullable: true
          description: Ancestor path in breadcrumb order, e.g. "Aceh, Kabupaten Aceh Selatan, Bakongan, Keude Bakongan".
    IdnRegionSourceProvenance:
      type: object
      description: Static, code-level facts about WHICH upstream project this data is vendored from — true of every import, unlike the per-import fields on IdnRegionDataset.
      properties:
        repository:
          type: string
        sourcePath:
          type: string
        license:
          type: string
        dumpPath:
          type: string
        manifestPath:
          type: string
        upstreamStatement:
          type: string
        decreeReference:
          type: string
        officialReferenceCaveat:
          type: string
          description: Must be surfaced wherever this dataset is shown to an operator — this is a community packaging of the decree, not an official Kemendagri API or export.
    InvitationCreated:
      type: object
      required:
        - id
        - delivery
        - roleCodes
      properties:
        id:
          type: string
          format: uuid
        delivery:
          type: string
          enum:
            - queued
            - unavailable
        roleCodes:
          type: array
          items:
            type: string
    InvitationPreview:
      type: object
      description: Deliberately carries NO email address — see the operation description.
      required:
        - tenantName
        - inviterName
        - expiresAt
      properties:
        tenantName:
          type: string
        inviterName:
          type: string
          description: Empty when the inviter's account has since been deactivated, so the shape stays stable.
        expiresAt:
          type: string
          format: date-time
    InvitationSummary:
      type: object
      required:
        - id
        - loginIdentifierMasked
        - displayName
        - status
        - resendCount
        - expiresAt
        - createdAt
        - roleCodes
      properties:
        id:
          type: string
          format: uuid
        loginIdentifierMasked:
          type: string
          description: The invitee's address, masked. The unmasked value is never returned by any surface.
        displayName:
          type: string
        status:
          type: string
          enum:
            - pending
            - accepted
            - revoked
            - expired
        skipEmailConfirmation:
          type: boolean
        resendCount:
          type: integer
        expiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        roleCodes:
          type: array
          items:
            type: string
    IssueMachineCredentialRequest:
      type: object
      required:
        - name
        - tenantUserId
        - allowedPermissionKeys
        - expiresAt
      properties:
        name:
          type: string
          maxLength: 120
          description: Operator-facing label, e.g. "awcms-astro build feed".
        tenantUserId:
          type: string
          format: uuid
          description: An existing tenant user in THIS tenant (the service account).
        allowedPermissionKeys:
          type: array
          minItems: 1
          maxItems: 50
          description: Permission keys (`module.activity.action`) this credential may use. Required and non-empty — an empty list means "can do nothing", never "unrestricted".
          items:
            type: string
        allowedWriteActions:
          type: array
          maxItems: 2
          description: |
            ADR-0092. OPTIONAL — omitting it (or sending an empty array) issues the read-only credential of ADR-0049, which is what every caller written before this class got and still gets.

            Naming any action switches the guard to `identity_access.machine_credentials_write.create` and makes `allowedIpCidrs` mandatory. `read` is REJECTED here rather than accepted as a no-op: every credential may already read whatever its `allowedPermissionKeys` name, and accepting it would suggest otherwise.
          items:
            type: string
            enum:
              - create
              - update
        allowedIpCidrs:
          type: array
          maxItems: 20
          description: |
            ADR-0092. IPv4/IPv6 literals or CIDR blocks. Required and non-empty when `allowedWriteActions` is non-empty, and REJECTED when it is empty — a read-only credential is not restricted by this list, so accepting one would describe a binding that is not enforced.

            Unparseable entries are refused at issuance. At request time an unreadable entry matches nothing, so the failure would otherwise be a credential that reads as bound and can never authenticate.
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
          description: Required, in the future, at most 365 days away — or at most 30 days when `allowedWriteActions` is non-empty. There is no perpetual credential.
    IssueSessionHandoffRequest:
      type: object
      required:
        - clientKey
        - redirectUri
      properties:
        clientKey:
          type: string
          description: The registered BFF client's public key. Not a secret, and never treated as one — the secret authenticates at redeem time.
        redirectUri:
          type: string
          format: uri
          description: Must appear EXACTLY in the client's registered allow-list. https only, no query, no fragment.
    MachineCredential:
      type: object
      description: A machine credential (ADR-0049; write class ADR-0092). Never carries the token or its hash — an operator tracing a leak needs to know which credential is alive and whether anything still uses it, and none of that requires secret material.
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        tenantUserId:
          type: string
          format: uuid
          description: The service account this credential authenticates AS. Its roles, narrowed by allowedPermissionKeys, are the whole of what the credential may do.
        allowedPermissionKeys:
          type: array
          description: NARROWING filter — effective permissions are the intersection with what the service account holds. It can never widen them.
          items:
            type: string
        allowedWriteActions:
          type: array
          description: ADR-0092. Empty on every read-only credential, which is every credential issued before the write class existed. Effective write actions are the intersection with the CODE ceiling, so this list can only narrow it.
          items:
            type: string
        allowedIpCidrs:
          type: array
          description: ADR-0092. Non-empty if and only if allowedWriteActions is. Consulted ONLY for write actions — a read request is never restricted by it.
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
          description: Refreshed at most hourly, so an idle or leaked credential is visible without a write on every request.
        revokedAt:
          type: string
          format: date-time
          nullable: true
        createdByTenantUserId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        status:
          type: string
          description: Derived from the clock and revokedAt, not stored.
          enum:
            - active
            - expired
            - revoked
    ManagedTenant:
      type: object
      description: "A tenant that has engaged this partner, as the PARTNER sees it (ADR-0089). Deliberately narrower than `PartnerEngagement`: no `engagedBy`, because that is a third party's identifier."
      properties:
        tenantId:
          type: string
          format: uuid
        tenantCode:
          type: string
        tenantName:
          type: string
        tenantStatus:
          type: string
        engagedAt:
          type: string
          format: date-time
    MediaEnforcementEnabled:
      type: object
      required:
        - enforced
        - enforcedAt
        - alreadyEnforced
      additionalProperties: false
      properties:
        enforced:
          type: boolean
          enum:
            - true
          description: Always true — a non-enforcing outcome is reported as a 409, never as a 200 whose `enforced` is false.
        enforcedAt:
          type: string
          format: date-time
          description: When this activation was confirmed. Refreshed on re-activation so it always reflects the most recent confirmed-ready enable.
        alreadyEnforced:
          type: boolean
          description: True when the tenant was already enforcing before this call — an idempotent no-change, still audited.
    MediaEnforcementState:
      type: object
      required:
        - enforced
        - ready
        - reasons
        - detail
      additionalProperties: false
      properties:
        enforced:
          type: boolean
          description: Whether this tenant's content media references must resolve to verified registry objects. Once true it stays true — enforcement is one-way by design.
        ready:
          type: boolean
          description: Whether this DEPLOYMENT's media storage is configured well enough for enforcement to be enabled. Independent of `enforced` — both must hold for validation to actually run.
        reasons:
          type: array
          description: Machine-readable readiness failures; empty when `ready` is true.
          items:
            type: string
            enum:
              - news_media_r2_disabled
              - news_media_r2_config_incomplete
              - news_media_r2_shares_sync_storage_bucket_or_credentials
        detail:
          type: array
          description: Human-readable evidence, one entry per failing check. Names environment variables, never their values.
          items:
            type: string
    MediaObjectLifecycleResult:
      type: object
      description: Result of a terminal lifecycle action. `status` here is the ACTION's outcome, not the object's `status` column.
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum:
            - deleted
            - purged
    MediaObjectListItem:
      type: object
      description: One registry row as the browse listing presents it. NOT a public reference — unlike `ResolvedMediaReference`, an item here may be in any status, including one that is unsafe to publish.
      properties:
        id:
          type: string
          format: uuid
        objectKey:
          type: string
        publicUrl:
          type: string
        originalFilename:
          type: string
          nullable: true
        mimeType:
          type: string
        sizeBytes:
          type: integer
          nullable: true
        checksumSha256:
          type: string
          nullable: true
        width:
          type: integer
          nullable: true
        height:
          type: integer
          nullable: true
        altText:
          type: string
          nullable: true
        caption:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - pending_upload
            - uploaded
            - verified
            - attached
            - orphaned
            - deleted
            - failed
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
        deleteReason:
          type: string
          nullable: true
        restoredAt:
          type: string
          format: date-time
          nullable: true
    MediaObjectRestored:
      type: object
      description: Result of a restore. `status` here IS the object's own lifecycle status column, untouched by soft delete and therefore whatever it was before.
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum:
            - pending_upload
            - uploaded
            - verified
            - attached
            - orphaned
            - deleted
            - failed
        restoredAt:
          type: string
          format: date-time
          nullable: true
    MediaObjectRights:
      type: object
      description: A media object as returned by the rights endpoint. Includes the full registry row; the rights fields are listed here.
      properties:
        id:
          type: string
          format: uuid
        creditLine:
          type: string
          nullable: true
        sourceName:
          type: string
          nullable: true
        rightsNotes:
          type: string
          nullable: true
        copyrightStatus:
          type: string
          enum:
            - unknown
            - owned
            - licensed
            - public_domain
            - permission_granted
            - fair_use
        rightsVerificationStatus:
          type: string
          enum:
            - unverified
            - verified
            - rejected
        rightsVerifiedBy:
          type: string
          format: uuid
          nullable: true
        rightsVerifiedAt:
          type: string
          format: date-time
          nullable: true
    MediaRightsUpdateRequest:
      type: object
      minProperties: 1
      description: At least one field. `null` clears; an omitted field is left alone.
      properties:
        creditLine:
          type: string
          nullable: true
          maxLength: 300
          description: What gets printed beside the image. Not the alt text and not the caption.
        sourceName:
          type: string
          nullable: true
          maxLength: 200
          description: Where the file came from, which is often not who is credited.
        rightsNotes:
          type: string
          nullable: true
          maxLength: 2000
        copyrightStatus:
          type: string
          enum:
            - unknown
            - owned
            - licensed
            - public_domain
            - permission_granted
            - fair_use
          description: "`unknown` is a real answer, not a missing one — most of a legacy archive is exactly that."
        rightsVerificationStatus:
          type: string
          enum:
            - unverified
            - verified
            - rejected
          description: A HUMAN judgement about a licence. Changing it is audited at `warning` severity.
    ModerationQueueItem:
      type: object
      properties:
        id:
          type: string
          format: uuid
        threadId:
          type: string
          format: uuid
        resourceType:
          type: string
        resourceUrl:
          type: string
        parentId:
          type: string
          format: uuid
          nullable: true
        depth:
          type: integer
        bodyText:
          type: string
          description: Raw stored plain text — never pre-rendered HTML.
        status:
          type: string
          enum:
            - pending
            - approved
            - rejected
            - spam
            - deleted
        authorKind:
          type: string
        authorDisplayName:
          type: string
          nullable: true
        authorEmailMasked:
          type: string
          nullable: true
          description: Masked form only; the raw address is never stored.
        moderationReasonCode:
          type: string
          nullable: true
        reportCount:
          type: integer
        createdAt:
          type: string
          format: date-time
    ModerationQueuePage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ModerationQueueItem"
        nextCursor:
          $ref: "#/components/schemas/CommentKeysetCursor"
    ModerationResult:
      type: object
      properties:
        commentId:
          type: string
          format: uuid
        status:
          type: string
          enum:
            - pending
            - approved
            - rejected
            - spam
            - deleted
    ModuleCatalogEntry:
      type: object
      properties:
        moduleKey:
          type: string
        name:
          type: string
        version:
          type: string
        description:
          type: string
        status:
          type: string
        type:
          type: string
          nullable: true
        isCore:
          type: boolean
        dependencies:
          type: array
          items:
            type: string
        api:
          type: object
          nullable: true
          properties:
            openApiPath:
              type: string
            basePath:
              type: string
        events:
          type: object
          nullable: true
        lastSyncedAt:
          type: string
          format: date-time
          nullable: true
    ModuleHealthReport:
      type: object
      properties:
        moduleKey:
          type: string
        status:
          type: string
          enum:
            - healthy
            - degraded
            - failed
            - unknown
        signals:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              status:
                type: string
                enum:
                  - pass
                  - fail
                  - not_applicable
              detail:
                type: string
        generatedAt:
          type: string
          format: date-time
    ModuleJob:
      type: object
      properties:
        moduleKey:
          type: string
        command:
          type: string
        purpose:
          type: string
        recommendedSchedule:
          type: string
        environmentNotes:
          type: string
        safeInOfflineLan:
          type: boolean
    ModulePermissionSyncReport:
      type: object
      properties:
        moduleKey:
          type: string
        entries:
          type: array
          items:
            type: object
            properties:
              moduleKey:
                type: string
              activityCode:
                type: string
              action:
                type: string
              status:
                type: string
                enum:
                  - synced
                  - missing
                  - orphaned
                  - mismatched_description
              descriptorDescription:
                type: string
                nullable: true
              catalogDescription:
                type: string
                nullable: true
    ModuleSettingsView:
      type: object
      properties:
        moduleKey:
          type: string
        schemaVersion:
          type: integer
        defaults:
          type: object
          additionalProperties: true
        tenantOverride:
          type: object
          additionalProperties: true
        effective:
          type: object
          additionalProperties: true
        updatedAt:
          type: string
          format: date-time
          nullable: true
    ModuleSyncResult:
      type: object
      properties:
        created:
          type: array
          items:
            type: string
        updated:
          type: array
          items:
            type: string
        unchanged:
          type: array
          items:
            type: string
        orphaned:
          type: array
          items:
            type: string
    NamedCount:
      type: object
      properties:
        name:
          type: string
        count:
          type: integer
    NewsletterNeutralResult:
      type: object
      properties:
        success:
          type: boolean
          enum:
            - true
        data:
          type: object
          properties:
            message:
              type: string
              description: One sentence, true whatever happened. Identical across every outcome by design — see this fragment's header.
    NewsletterSubscribeRequest:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          maxLength: 254
          description: Shape-checked only — something, an `@`, something with a dot, no spaces. A stricter regex would reject valid addresses and buy nothing, because the actual proof that an address exists and belongs to the person who typed it is the confirmation link nobody else can follow.
        locale:
          type: string
          nullable: true
          pattern: ^[a-z]{2}(-[A-Z]{2})?$
          description: Language for the confirmation mail. Falls back to `en`.
      description: There is deliberately no consent field. PRD §30 forbids a pre-ticked consent, and having no field at all is stronger than defaulting one to false — consent is recorded when the confirmation link is followed, so it cannot be defaulted, pre-ticked, or inferred from a submission.
    NewsletterTokenRequest:
      type: object
      required:
        - token
      properties:
        token:
          type: string
          maxLength: 200
          pattern: ^[A-Za-z0-9_-]+$
          description: "The base64url token from the emailed link. Bounded and shape-checked BEFORE it is hashed: an anonymous endpoint that will hash whatever it is handed is an anonymous endpoint that will hash a megabyte."
    NewsMediaObjectItem:
      type: object
      required:
        - id
        - tenantId
        - objectKey
        - publicUrl
        - mimeType
        - status
        - createdAt
        - updatedAt
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        moduleKey:
          type: string
        ownerResourceType:
          type: string
          nullable: true
          enum:
            - blog_post
            - blog_page
            - homepage_section
            - gallery_item
            - ad
            - video_thumbnail
            - seo_image
            - null
        ownerResourceId:
          type: string
          format: uuid
          nullable: true
        storageDriver:
          type: string
          enum:
            - cloudflare_r2
        objectKey:
          type: string
        originalFilename:
          type: string
          nullable: true
        publicUrl:
          type: string
          format: uri
        mimeType:
          type: string
        sizeBytes:
          type: integer
          nullable: true
        checksumSha256:
          type: string
          nullable: true
        width:
          type: integer
          nullable: true
        height:
          type: integer
          nullable: true
        altText:
          type: string
          nullable: true
        caption:
          type: string
          nullable: true
        status:
          type: string
          enum:
            - pending_upload
            - uploaded
            - verified
            - attached
            - orphaned
            - deleted
            - failed
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    NewsMediaUploadSessionCreated:
      type: object
      required:
        - objectId
        - objectKey
        - presignedUrl
        - expiresAt
      additionalProperties: false
      properties:
        objectId:
          type: string
          format: uuid
        objectKey:
          type: string
          description: Server-generated — news-media/{tenantId}/{yyyy}/{mm}/{uuid}.{ext}. Never derived from client input.
          example: news-media/3fa85f64-5717-4562-b3fc-2c963f66afa6/2026/07/1b9d6bcd-bbfd-4b2d-9b5d-ab8dfbbd4bed.jpg
        presignedUrl:
          type: string
          format: uri
          description: Short-lived presigned PUT URL scoped to exactly one object key. Never includes raw R2 credentials.
        expiresAt:
          type: string
          format: date-time
    Office:
      type: object
      properties:
        id:
          type: string
          format: uuid
        officeCode:
          type: string
        officeName:
          type: string
        officeType:
          type: string
        parentOfficeId:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    OwnSessionSummary:
      type: object
      description: One live session belonging to the caller. Carries no token, no raw IP and no raw User-Agent.
      required:
        - id
        - issuedAt
        - expiresAt
        - assuranceLevel
        - originAuth
        - current
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        assuranceLevel:
          type: string
          enum:
            - aal1
            - aal2
        originAuth:
          type: string
          enum:
            - password
            - sso
            - handoff
          description: How the session was issued. A step-up rotation carries the original value forward.
        clientIpHash:
          type:
            - string
            - "null"
          description: Keyed, non-reversible pseudonym of the client IP. null when the deployment has no stable AUTH_IP_HASH_SECRET.
        userAgentSummary:
          type:
            - string
            - "null"
        current:
          type: boolean
          description: True for the session this request is authenticated by.
    PartnerEngagement:
      type: object
      description: A partnership reaching THIS tenant (ADR-0089). The partner's name comes from the RLS-free `awcms_tenants` row rather than being denormalised into the mapping — a copy could go stale with nobody noticing.
      properties:
        id:
          type: string
          format: uuid
        partnerTenantId:
          type: string
          format: uuid
        partnerTenantCode:
          type: string
        partnerTenantName:
          type: string
        engagedAt:
          type: string
          format: date-time
    PortableTextBody:
      type: array
      maxItems: 2000
      description: ADR-0100 — the canonical article body. contentJson.blocks is a derived, LOSSY projection of this, written only so ahliweb/awcms-astro keeps rendering until it reads this field directly.
      items:
        $ref: "#/components/schemas/PortableTextNode"
    PortableTextLinkAnnotation:
      type: object
      required:
        - _type
        - _key
        - href
      properties:
        _type:
          type: string
          enum:
            - link
        _key:
          type: string
        href:
          type: string
          maxLength: 2048
          description: "Relative, or http/https/mailto/tel. Checked by URL parsing rather than a pattern, so java\\nscript: and JaVaScRiPt: are refused too."
    PortableTextNode:
      type: object
      required:
        - _type
        - _key
      description: One node of a CLOSED Portable Text vocabulary (ADR-0100). _type is one of block, gallery, videoNews — anything else is refused at write time, which is what keeps "there is no field where arbitrary markup could live" true.
      properties:
        _type:
          type: string
          enum:
            - block
            - gallery
            - videoNews
        _key:
          type: string
        style:
          type: string
          enum:
            - normal
            - h1
            - h2
            - h3
            - h4
            - h5
            - h6
            - blockquote
        listItem:
          type: string
          enum:
            - bullet
            - number
        level:
          type: integer
          minimum: 1
          maximum: 6
        children:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextSpan"
        markDefs:
          type: array
          items:
            $ref: "#/components/schemas/PortableTextLinkAnnotation"
        items:
          type: array
          description: Gallery items — present when _type is gallery.
          items:
            type: object
        provider:
          type: string
          description: Present when _type is videoNews.
        videoId:
          type: string
          description: Present when _type is videoNews.
    PortableTextSpan:
      type: object
      required:
        - _type
        - _key
        - text
      properties:
        _type:
          type: string
          enum:
            - span
        _key:
          type: string
        text:
          type: string
          description: PLAIN text — never markup.
        marks:
          type: array
          items:
            type: string
          description: Decorator names (strong, em, code) and/or the _key of an annotation declared in this block's own markDefs. A mark naming neither is refused at write time as a dangling reference.
    ProfileEntityLink:
      type: object
      properties:
        id:
          type: string
          format: uuid
        moduleKey:
          type: string
        entityType:
          type: string
        entityId:
          type: string
          format: uuid
        linkRole:
          type: string
        createdAt:
          type: string
          format: date-time
    ProfileIdentifierMasked:
      type: object
      properties:
        id:
          type: string
          format: uuid
        profileId:
          type: string
          format: uuid
        identifierType:
          type: string
        maskedValue:
          type: string
        isPrimary:
          type: boolean
        verificationStatus:
          type: string
    ProfileMasked:
      type: object
      properties:
        id:
          type: string
          format: uuid
        profileType:
          type: string
        displayName:
          type: string
        legalName:
          type: string
          nullable: true
        status:
          type: string
        verificationStatus:
          type: string
        riskLevel:
          type: string
        mergedIntoProfileId:
          type: string
          format: uuid
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
        deleteReason:
          type: string
          nullable: true
        restoredAt:
          type: string
          format: date-time
          nullable: true
    PublicAdPlacement:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Internal label. A consumer renders it as alt text only when the media object carries none.
        linkUrl:
          type: string
          nullable: true
        mediaPublicUrl:
          type: string
          description: The media registry's own server-generated URL. There is no client-supplied URL column on this table.
        mediaAltText:
          type: string
          nullable: true
        contentClass:
          type: string
          enum:
            - standard
            - advertorial
            - sponsored
          description: "Editorial-disclosure classification (Issue #783). `standard` is a plain ad or organic content needing no disclosure; `advertorial` is editorial-styled content that is actually paid for; `sponsored` is content explicitly sponsored/underwritten by a third party. A consumer renders a disclosure label next to any placement that is not `standard`."
    PublicBlogPageDetail:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
        slug:
          type: string
        excerpt:
          type: string
          nullable: true
        contentJson:
          $ref: "#/components/schemas/BlogContentJson"
          description: The derived block projection this repo's own renderer reads (ADR-0100).
        bodyPortableText:
          description: The canonical body. Empty until `bun run blog:portable-text:backfill` has run for this deployment.
        contentText:
          type: string
        seoTitle:
          type: string
          nullable: true
        metaDescription:
          type: string
          nullable: true
        canonicalUrl:
          type: string
          nullable: true
        locale:
          type: string
        publishedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        visibility:
          type: string
          enum:
            - public
            - unlisted
        featuredMediaId:
          type: string
          format: uuid
          nullable: true
        pageType:
          type: string
        resolvedMedia:
          type: object
          additionalProperties:
            type: object
            properties:
              publicUrl:
                type: string
              altText:
                type: string
                nullable: true
              mimeType:
                type: string
              width:
                type: integer
                nullable: true
              height:
                type: integer
                nullable: true
          description: Every media object the body references, resolved in one call. An id that no longer resolves is simply absent.
    PublicBlogPageSummary:
      type: object
      properties:
        title:
          type: string
        slug:
          type: string
        locale:
          type: string
        updatedAt:
          type: string
          format: date-time
    PublicComment:
      type: object
      description: A single publicly visible comment. Deliberately carries NO moderation metadata and no author contact data — `bodyHtml` is the escaped render of stored plain text, containing only `<br>` and safe http(s) `<a>` elements this server emitted.
      properties:
        id:
          type: string
          format: uuid
        parentId:
          type: string
          format: uuid
          nullable: true
        depth:
          type: integer
          minimum: 0
          maximum: 4
        bodyHtml:
          type: string
        authorDisplayName:
          type: string
          nullable: true
        authorKind:
          type: string
          enum:
            - anonymous
            - registered
        createdAt:
          type: string
          format: date-time
        editedAt:
          type: string
          format: date-time
          nullable: true
    PublicCommentPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/PublicComment"
        nextCursor:
          $ref: "#/components/schemas/CommentKeysetCursor"
    PushDeliveryAttempt:
      type: object
      description: 'One row per ATTEMPT. Successes are included as well as failures: a provider that started answering slowly, or one that changed which endpoints it accepts, is a pattern across both. A failures-only view answers "what broke" and never "since when".'
      properties:
        id:
          type: string
          format: uuid
        messageId:
          type: string
          format: uuid
        attemptNo:
          type: integer
        outcome:
          type: string
          enum:
            - success
            - failure
        providerName:
          type: string
        providerResponseSnippet:
          type: string
          nullable: true
          description: Truncated provider reply. Never the payload, never the endpoint.
        errorMessage:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
    PushDeploymentConfig:
      type: object
      description: "Deployment-level facts, not tenant configuration: both credentials authenticate the SERVER (an FCM service account belongs to one Firebase project; a VAPID key pair identifies one application server), so there is no per-tenant override and there is not meant to be one."
      properties:
        enabled:
          type: boolean
        provider:
          type: string
          nullable: true
          enum:
            - log
            - fcm
            - web_push
    PushMessage:
      type: object
      description: "One row per (notification, device). `body` is deliberately not projected: an operator diagnosing delivery needs the category, the status and the error, and the notification text is the one field here that carries user-facing content."
      properties:
        id:
          type: string
          format: uuid
        category:
          type: string
        status:
          $ref: "#/components/schemas/PushMessageStatus"
        priority:
          type: string
          enum:
            - low
            - normal
            - high
        title:
          type: string
        targetPath:
          type: string
          nullable: true
          description: Same-origin path only — never an absolute URL.
        retryCount:
          type: integer
        lastError:
          type: string
          nullable: true
        providerName:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        sentAt:
          type: string
          format: date-time
          nullable: true
        nextAttemptAt:
          type: string
          format: date-time
          nullable: true
        tenantUserId:
          type: string
          format: uuid
        endpointMasked:
          type: string
        transport:
          $ref: "#/components/schemas/PushTransport"
    PushMessageStatus:
      type: string
      enum:
        - queued
        - retry_wait
        - sending
        - sent
        - failed
        - cancelled
    PushQueueSummary:
      type: object
      properties:
        countsByStatus:
          type: object
          description: Every status, including the ones with zero rows. A summary that omitted empty buckets would make "no failures" and "failures nobody asked about" render identically.
          additionalProperties:
            type: integer
        oldestPendingAt:
          type: string
          format: date-time
          nullable: true
        nextAttemptAt:
          type: string
          format: date-time
          nullable: true
        activeSubscriptions:
          type: integer
        disabledSubscriptions:
          type: integer
    PushSubscription:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenantUserId:
          type: string
          format: uuid
        transport:
          $ref: "#/components/schemas/PushTransport"
        endpointMasked:
          type: string
          description: Origin plus a short tail for a Web Push URL, or a short head/tail pair for an opaque FCM token. Never the authenticating middle.
        status:
          type: string
          enum:
            - active
            - disabled
        disabledReason:
          type: string
          nullable: true
          description: Why the device stopped receiving — `revoked_by_user`, or what the push service reported (404/410, FCM `UNREGISTERED`). Disabled rather than deleted, because a deleted row and one that never existed are indistinguishable, and the difference is exactly what "why did this user stop getting notifications" needs.
        lastSuccessAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
    PushSubscriptionRegistration:
      type: object
      required:
        - transport
        - endpoint
      properties:
        transport:
          $ref: "#/components/schemas/PushTransport"
        endpoint:
          type: string
          maxLength: 2048
          description: "The push service URL (web_push, https: only, never a literal private address) or the FCM registration token. Bounded because it is an opaque credential-shaped string written by an authenticated caller; real values are ~200 characters."
        keys:
          type: object
          description: REQUIRED for web_push, and rejected for fcm — a client sending both has confused its two registration paths, and silently dropping half of what it sent would store a row that cannot deliver while reporting success.
          properties:
            p256dh:
              type: string
              description: base64url, decoding to 65 bytes (uncompressed P-256 point, RFC 8291 §3.1).
            auth:
              type: string
              description: base64url, decoding to 16 bytes (RFC 8291 §3.2).
    PushTransport:
      type: string
      enum:
        - web_push
        - fcm
      description: "`web_push` is RFC 8030/8291/8292 to a browser; `fcm` is FCM HTTP v1 to a native Android/iOS client. The choice is the CLIENT's — a deployment can serve both, and the dispatcher's configured provider must match the transport of the row it claims."
    RealtimeStats:
      type: object
      properties:
        onlineHumanCount:
          type: integer
        onlineAdminCount:
          type: integer
        onlinePublicCount:
          type: integer
        onlineApiCount:
          type: integer
        onlineWindowSeconds:
          type: integer
        lastUpdatedAt:
          type: string
          format: date-time
    RedeemSessionHandoffRequest:
      type: object
      required:
        - clientKey
        - clientSecret
        - code
        - redirectUri
      properties:
        clientKey:
          type: string
        clientSecret:
          type: string
          description: Server-to-server only. A browser must never hold this.
        code:
          type: string
        redirectUri:
          type: string
          format: uri
          description: Must equal the URI the code was issued against.
    RegisteredPartner:
      type: object
      description: A row of the platform's partner registry (ADR-0089). `tenantCode` and `tenantName` come from `awcms_tenants` through a join rather than being denormalised here, so the registry can never hold a stale copy of a tenant's name.
      properties:
        id:
          type: string
          format: uuid
        partnerTenantId:
          type: string
          format: uuid
          description: The tenant that IS the partner. It already existed; this row did not create it.
        partnerCode:
          type: string
        displayName:
          type: string
        status:
          type: string
          description: ADR-0093. `suspended` stops every delegated actor this partner placed from being served, in every customer tenant, at their next request — and touches no grant row.
          enum:
            - active
            - suspended
        registeredAt:
          type: string
          format: date-time
        tenantCode:
          type: string
        tenantName:
          type: string
    RegisterPartnerRequest:
      type: object
      required:
        - partnerTenantId
        - partnerCode
        - displayName
      properties:
        partnerTenantId:
          type: string
          format: uuid
          description: An EXISTING tenant on this deployment, and not the platform tenant itself. The platform learns this id out of band — there is no picker and there is not meant to be.
        partnerCode:
          type: string
          maxLength: 64
          description: "Lower-case letters, digits and hyphens; starts and ends with a letter or digit. Globally unique. The format is enforced in the application rather than by a CHECK because the uniqueness index is global: a stray capital produces a second row that reads like the first one to a human."
        displayName:
          type: string
          maxLength: 160
    RegistrationRequest:
      type: object
      description: A pending self-registration request. The login identifier is masked — the raw address is never returned in a list.
      properties:
        id:
          type: string
          format: uuid
        loginIdentifierMasked:
          type: string
        displayName:
          type: string
        requestedAt:
          type: string
          format: date-time
    ResolvedMediaReference:
      type: object
      description: A media object safe to reference publicly (verified/attached, same tenant, not deleted).
      properties:
        id:
          type: string
          format: uuid
        publicUrl:
          type: string
        altText:
          type: string
          nullable: true
        mimeType:
          type: string
        width:
          type: integer
          nullable: true
        height:
          type: integer
          nullable: true
        sizeBytes:
          type: integer
          nullable: true
        creditLine:
          type: string
          nullable: true
          description: "Issue #782. The photo credit — what gets printed beside the image. `null` unless the source object's rights claim has been HUMAN-verified (internal `rightsVerificationStatus = 'verified'`); an unverified or rejected claim never reaches this field, even if the object has a credit line on file."
        sourceName:
          type: string
          nullable: true
          description: "Issue #782. Where the file came from (often distinct from who is credited). Same verification gate as `creditLine`: `null` unless the rights claim has been verified."
        copyrightStatus:
          type: string
          nullable: true
          enum:
            - unknown
            - owned
            - licensed
            - public_domain
            - permission_granted
            - fair_use
            - null
          description: 'Issue #782. Same verification gate as `creditLine`/`sourceName`: `null` unless the rights claim has been verified — including when the underlying object''s copyright status is the real, non-null `''unknown''` value (verification and "unknown" are different facts, and only one of them is nullability here).'
    RetentionPurgeResult:
      type: object
      properties:
        eventsDeleted:
          type: integer
        sessionsRawDetailCleared:
          type: integer
        sessionsDeleted:
          type: integer
        rollupsDeleted:
          type: integer
        hasMore:
          type: boolean
          description: True when at least one step filled its batch, so data past retention remains. This endpoint performs ONE bounded pass on purpose — the size of the work is unknown when the caller presses the button, and an interactive request must not hold a purge of unbounded size open. Call it again, or let the scheduled `analytics:purge` job (which loops) finish the tenant.
    Role:
      type: object
      properties:
        id:
          type: string
          format: uuid
        roleCode:
          type: string
        roleName:
          type: string
        isSystem:
          type: boolean
        permissionCount:
          type: integer
    SecurityView:
      type: object
      properties:
        range:
          type: string
        botPageviews:
          type: integer
        topBotReasons:
          type: array
          items:
            $ref: "#/components/schemas/NamedCount"
        botPageviewsByArea:
          type: array
          items:
            $ref: "#/components/schemas/NamedCount"
    SeoConfigUpdateRequest:
      type: object
      description: PUT body for seoConfigUpdate — a full replace of the mutable SEO-defaults fields. Every field is optional in the request; an omitted string field is treated as null, and omitted booleans/limit fall back to their defaults (noindex=false, sitemap/feeds enabled=true, feedItemLimit=50). Unknown keys are ignored.
      additionalProperties: true
      properties:
        siteName:
          type: string
          nullable: true
          maxLength: 200
        defaultMetaDescription:
          type: string
          nullable: true
          maxLength: 500
        defaultSocialMediaId:
          type: string
          format: uuid
          nullable: true
        twitterSiteHandle:
          type: string
          nullable: true
          maxLength: 80
        organizationName:
          type: string
          nullable: true
          maxLength: 200
        organizationLogoMediaId:
          type: string
          format: uuid
          nullable: true
        defaultRobotsNoindex:
          type: boolean
        feedTitle:
          type: string
          nullable: true
          maxLength: 200
        feedDescription:
          type: string
          nullable: true
          maxLength: 500
        feedLogoMediaId:
          type: string
          format: uuid
          nullable: true
        feedItemLimit:
          type: integer
          minimum: 1
          maximum: 200
        includedResourceTypes:
          type: array
          nullable: true
          maxItems: 50
          items:
            type: string
            maxLength: 64
        sitemapEnabled:
          type: boolean
        feedsEnabled:
          type: boolean
    SeoNotFoundObservation:
      type: object
      description: A privacy-minimized aggregate 404 observation (sanitized path + bare referrer domain only).
      required:
        - id
        - normalizedPath
        - referrerDomain
        - locale
        - domainHost
        - hitCount
        - firstSeenAt
        - lastSeenAt
        - suggestedRedirectId
        - resolvedAt
      properties:
        id:
          type: string
          format: uuid
        normalizedPath:
          type: string
          description: Sanitized, normalized request path (query dropped).
        referrerDomain:
          type: string
          nullable: true
          description: Bare referrer hostname only (never the full referrer URL).
        locale:
          type: string
          nullable: true
        domainHost:
          type: string
          nullable: true
        hitCount:
          type: integer
        firstSeenAt:
          type: string
          format: date-time
        lastSeenAt:
          type: string
          format: date-time
        suggestedRedirectId:
          type: string
          format: uuid
          nullable: true
        resolvedAt:
          type: string
          format: date-time
          nullable: true
    SeoRedirect:
      type: object
      description: A tenant-scoped exact-path redirect rule (awcms_seo_redirects).
      required:
        - id
        - sourcePath
        - normalizedSourcePath
        - localeScope
        - domainScopeHost
        - targetType
        - target
        - statusCode
        - state
        - effectiveFrom
        - effectiveUntil
        - preserveQuery
        - reason
        - origin
        - hitCount
        - lastHitAt
        - createdAt
        - updatedAt
        - deletedAt
      properties:
        id:
          type: string
          format: uuid
        sourcePath:
          type: string
          description: The source path as entered (display form).
        normalizedSourcePath:
          type: string
          description: The canonical, match-safe source path (dot-segments resolved, duplicate slashes collapsed, CRLF/traversal/Unicode-confusion rejected).
        localeScope:
          type: string
          nullable: true
          description: Optional locale narrowing; null = all locales.
        domainScopeHost:
          type: string
          nullable: true
          description: Optional server-derived host narrowing (a verified normalized_hostname); null = all of this tenant's hosts.
        targetType:
          type: string
          enum:
            - relative_same_tenant
            - verified_external
        target:
          type: string
          description: The (normalized) redirect target — a relative same-origin path or an absolute URL to one of this tenant's verified hosts. Validated through the frozen open-redirect guard.
        statusCode:
          type: integer
          enum:
            - 301
            - 302
            - 307
            - 308
        state:
          type: string
          enum:
            - active
            - inactive
            - archived
        effectiveFrom:
          type: string
          format: date-time
          nullable: true
        effectiveUntil:
          type: string
          format: date-time
          nullable: true
        preserveQuery:
          type: boolean
        reason:
          type: string
          nullable: true
        origin:
          type: string
          enum:
            - manual
            - slug_change
            - domain_change
            - locale_change
            - import
            - legacy_blog
        hitCount:
          type: integer
        lastHitAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        deletedAt:
          type: string
          format: date-time
          nullable: true
    SeoRedirectCreateRequest:
      type: object
      required:
        - sourcePath
        - target
      properties:
        sourcePath:
          type: string
          maxLength: 2048
          description: The source path (path-absolute; normalized before matching/uniqueness).
        target:
          type: string
          maxLength: 2048
          description: A relative same-origin path or an absolute URL to one of this tenant's verified hosts. The target TYPE is derived; any unsafe/cross-host/protocol-relative/CRLF target is rejected by the frozen guard.
        localeScope:
          type: string
          nullable: true
        domainScopeHost:
          type: string
          nullable: true
          description: Must be one of this tenant's verified domains, or null.
        statusCode:
          type: integer
          enum:
            - 301
            - 302
            - 307
            - 308
          default: 301
        state:
          type: string
          enum:
            - active
            - inactive
            - archived
          default: active
        effectiveFrom:
          type: string
          format: date-time
          nullable: true
        effectiveUntil:
          type: string
          format: date-time
          nullable: true
        preserveQuery:
          type: boolean
          default: false
        reason:
          type: string
          nullable: true
          maxLength: 500
        origin:
          type: string
          enum:
            - manual
            - slug_change
            - domain_change
            - locale_change
            - import
            - legacy_blog
          default: manual
    SeoRedirectSettings:
      type: object
      description: Per-tenant redirect governance policy (awcms_seo_redirect_settings).
      required:
        - legacyBlogRedirectEnabled
        - urlChangeAutoPolicy
      properties:
        legacyBlogRedirectEnabled:
          type: boolean
          default: false
          description: INERT in awcms (no /news route family) — retained for parity. When true (and the tenant has a verified primary host) /blog/{tenantCode}... would 301-redirect to the canonical /news... equivalent. Default false = behavior unchanged.
        urlChangeAutoPolicy:
          type: string
          enum:
            - skip
            - propose
            - create
          default: propose
          description: Default action when a URL change is captured. 'propose' (default) creates an INACTIVE rule for operator review; 'create' activates immediately; 'skip' does nothing.
    SeoRedirectUpdateRequest:
      type: object
      required:
        - target
      description: Update the mutable fields (source path is immutable — supplied only at create).
      properties:
        target:
          type: string
          maxLength: 2048
        localeScope:
          type: string
          nullable: true
        domainScopeHost:
          type: string
          nullable: true
        statusCode:
          type: integer
          enum:
            - 301
            - 302
            - 307
            - 308
          default: 301
        state:
          type: string
          enum:
            - active
            - inactive
            - archived
          default: active
        effectiveFrom:
          type: string
          format: date-time
          nullable: true
        effectiveUntil:
          type: string
          format: date-time
          nullable: true
        preserveQuery:
          type: boolean
          default: false
        reason:
          type: string
          nullable: true
          maxLength: 500
    SeoTenantSettings:
      type: object
      description: Resolved, render-ready tenant SEO defaults. Media id fields are bare object ids resolved through media_library at render time (never raw URLs). All string fields are trimmed and empty-collapsed to null.
      required:
        - siteName
        - defaultMetaDescription
        - defaultSocialMediaId
        - twitterSiteHandle
        - organizationName
        - organizationLogoMediaId
        - defaultRobotsNoindex
        - feedTitle
        - feedDescription
        - feedLogoMediaId
        - feedItemLimit
        - includedResourceTypes
        - sitemapEnabled
        - feedsEnabled
      additionalProperties: false
      properties:
        siteName:
          type: string
          nullable: true
          maxLength: 200
        defaultMetaDescription:
          type: string
          nullable: true
          maxLength: 500
        defaultSocialMediaId:
          type: string
          format: uuid
          nullable: true
        twitterSiteHandle:
          type: string
          nullable: true
          maxLength: 80
        organizationName:
          type: string
          nullable: true
          maxLength: 200
        organizationLogoMediaId:
          type: string
          format: uuid
          nullable: true
        defaultRobotsNoindex:
          type: boolean
          description: Tenant-wide "this whole site is noindex" switch — when true every surface degrades to noindex and robots.txt disallows all crawling.
        feedTitle:
          type: string
          nullable: true
          maxLength: 200
        feedDescription:
          type: string
          nullable: true
          maxLength: 500
        feedLogoMediaId:
          type: string
          format: uuid
          nullable: true
        feedItemLimit:
          type: integer
          minimum: 1
          maximum: 200
          description: Max items per feed (RSS/Atom/JSON); default 50. Only ever makes a feed smaller than the code ceiling.
        includedResourceTypes:
          type: array
          nullable: true
          maxItems: 50
          items:
            type: string
            maxLength: 64
          description: Optional allow-list of resourceType slugs to include in sitemap/feeds; null means all eligible types.
        sitemapEnabled:
          type: boolean
        feedsEnabled:
          type: boolean
    SessionHandoffCodeIssued:
      type: object
      properties:
        code:
          type: string
          description: Single-use, ≤60 second lifetime. Returned once and never stored in clear text.
        redirectUri:
          type: string
          format: uri
          description: The URI the code is bound to. Redemption must present the same value.
        expiresAt:
          type: string
          format: date-time
    SessionHandoffRedeemed:
      type: object
      properties:
        token:
          type: string
          description: A session token for the human who authenticated. Store it SERVER-SIDE; it must never reach a browser.
        expiresAt:
          type: string
          format: date-time
        assuranceLevel:
          type: string
          enum:
            - aal1
            - aal2
          description: Inherited from the original login, never above it.
    SessionIntrospection:
      type: object
      description: Safe claims for a live session (ADR-0049 §7). Deliberately excludes the raw login identifier that GET /api/v1/auth/me returns.
      properties:
        identityId:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        displayName:
          type: string
        roles:
          type: array
          items:
            type: string
        assuranceLevel:
          type: string
          enum:
            - aal1
            - aal2
        expiresAt:
          type: string
          format: date-time
        scopes:
          type: array
          description: Active, effective-dated business-scope references — the same evaluation the authorization layer uses.
          items:
            type: object
            properties:
              scopeType:
                type: string
              scopeId:
                type: string
                format: uuid
    SiteProfile:
      type: object
      properties:
        tagline:
          type: string
          nullable: true
          maxLength: 200
        copyrightNotice:
          type: string
          nullable: true
          maxLength: 300
        logoMediaId:
          type: string
          format: uuid
          nullable: true
        faviconMediaId:
          description: "Separate from the logo on purpose: a favicon is square, tiny and usually a different crop, and one field for both would force every tenant to accept whichever compromise the renderer picked."
          type: string
          format: uuid
          nullable: true
        editorialAddress:
          description: Free text and multi-line on purpose — Indonesian addresses do not decompose into street/city/postcode reliably, and forcing them to would produce a form nobody can fill in correctly.
          type: string
          nullable: true
          maxLength: 1000
        contactEmail:
          type: string
          nullable: true
          maxLength: 320
        contactPhone:
          type: string
          nullable: true
          maxLength: 50
        whatsappNumber:
          description: "Kept separate from contactPhone: a newsroom's WhatsApp tip line is frequently not its switchboard number."
          type: string
          nullable: true
          maxLength: 50
        socialLinks:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/SocialProfileLink"
        updatedAt:
          type: string
          format: date-time
          nullable: true
    SiteProfileWriteInput:
      type: object
      properties:
        tagline:
          type: string
          nullable: true
        copyrightNotice:
          type: string
          nullable: true
        logoMediaId:
          type: string
          format: uuid
          nullable: true
        faviconMediaId:
          type: string
          format: uuid
          nullable: true
        editorialAddress:
          type: string
          nullable: true
        contactEmail:
          type: string
          nullable: true
        contactPhone:
          type: string
          nullable: true
        whatsappNumber:
          type: string
          nullable: true
        socialLinks:
          type: array
          maxItems: 20
          items:
            $ref: "#/components/schemas/SocialProfileLink"
    SiteSearchFacets:
      type: object
      description: |
        Counts per facet value for the whole result set (Issue #607 for content type, #633 for terms), computed on every page — they describe the result set rather than the page, so omitting them after the first page would make them look like they had changed.

        A facet's OWN filter is never applied to its own count: a facet answers "what else is there", and applying it would zero every other value the moment a reader picks one, leaving no way back to results that still exist. Every OTHER filter is applied — so the `type` counts reflect the active term filters, and each term facet reflects `type` and the other term filters.

        Every remaining predicate is shared with the result query character for character, so a count can never advertise a document the reader cannot reach. Bounded to 20 values PER facet.
      properties:
        resourceTypes:
          type: array
          items:
            $ref: "#/components/schemas/SiteSearchFacetValue"
        terms:
          type: object
          description: |
            Term facets keyed by facet name — `channel`, `topic`, `institution`, `region`, and whatever a future module's search-source descriptor contributes. A facet with no matching documents is ABSENT rather than present-and-empty: the reader is being told what else is there, and an empty list is not something else.

            Filter by one with a query parameter of the same name (`?channel=politik`). Filters are ANDed. Match is on `value` (a slug or code), never `label` — a label is display text an editor may rewrite, and a filter that broke when somebody fixed a capital letter would be a filter nobody trusts.
          additionalProperties:
            type: array
            items:
              $ref: "#/components/schemas/SiteSearchTermFacetValue"
      required:
        - resourceTypes
        - terms
    SiteSearchFacetValue:
      type: object
      properties:
        value:
          type: string
          description: A `resourceType` as stored — `blog_post`, `blog_page`, and whatever a future module contributes.
        count:
          type: integer
      required:
        - value
        - count
    SiteSearchIndexFailure:
      type: object
      properties:
        sourceKey:
          type: string
        resourceId:
          type: string
        locale:
          type: string
        errorClass:
          type: string
        errorDetail:
          type: string
          nullable: true
          description: Sanitized detail only.
        occurrenceCount:
          type: integer
        firstSeenAt:
          type: string
          format: date-time
        lastSeenAt:
          type: string
          format: date-time
      required:
        - sourceKey
        - resourceId
        - locale
        - errorClass
        - errorDetail
        - occurrenceCount
        - firstSeenAt
        - lastSeenAt
    SiteSearchIndexRun:
      type: object
      properties:
        id:
          type: string
          format: uuid
        runType:
          type: string
          enum:
            - rebuild
            - reconcile
            - reindex
        status:
          type: string
          enum:
            - running
            - succeeded
            - failed
        trigger:
          type: string
          enum:
            - manual
            - scheduled
        documentsIndexed:
          type: integer
        documentsUpdated:
          type: integer
        documentsRemoved:
          type: integer
        documentsUnchanged:
          type: integer
        sourcesProcessed:
          type: integer
        failureCount:
          type: integer
        lastError:
          type: string
          nullable: true
          description: Sanitized error summary only — never a raw stack trace.
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
          nullable: true
      required:
        - id
        - runType
        - status
        - trigger
        - documentsIndexed
        - documentsUpdated
        - documentsRemoved
        - documentsUnchanged
        - sourcesProcessed
        - failureCount
        - lastError
        - startedAt
        - finishedAt
    SiteSearchIndexRunResult:
      type: object
      properties:
        runId:
          type: string
          format: uuid
        runType:
          type: string
          enum:
            - rebuild
            - reconcile
            - reindex
        status:
          type: string
          enum:
            - succeeded
            - failed
        totalIndexed:
          type: integer
        totalRemoved:
          type: integer
        failureCount:
          type: integer
        results:
          type: array
          items:
            $ref: "#/components/schemas/SiteSearchSourceResult"
      required:
        - runId
        - runType
        - status
        - totalIndexed
        - totalRemoved
        - failureCount
        - results
    SiteSearchIndexStatus:
      type: object
      properties:
        documentCount:
          type: integer
        byResourceType:
          type: array
          items:
            type: object
            properties:
              resourceType:
                type: string
              count:
                type: integer
            required:
              - resourceType
              - count
        latestIndexedAt:
          type: string
          format: date-time
          nullable: true
        lastRun:
          oneOf:
            - $ref: "#/components/schemas/SiteSearchIndexRun"
            - type: "null"
        openFailureCount:
          type: integer
      required:
        - documentCount
        - byResourceType
        - latestIndexedAt
        - lastRun
        - openFailureCount
    SiteSearchQueryResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SiteSearchResultItem"
        nextCursor:
          type: string
          nullable: true
        facets:
          $ref: "#/components/schemas/SiteSearchFacets"
        query:
          type: string
          description: The NORMALIZED query actually executed (empty when nothing ran).
        locale:
          type: string
        reason:
          type: string
          enum:
            - empty
            - too_short
            - too_long
          description: Present only when the submitted query was rejected by the input bounds.
      required:
        - items
        - nextCursor
        - facets
        - query
        - locale
    SiteSearchResultItem:
      type: object
      description: One public search hit. `url` is a server-built absolute PATH, never a source-supplied URL.
      properties:
        resourceType:
          type: string
        resourceId:
          type: string
        url:
          type: string
        title:
          type: string
        snippet:
          type: string
          description: Pre-escaped safe HTML. The whole `ts_headline` output is HTML-escaped FIRST and only then are the non-HTML sentinels replaced with `<mark>`/`</mark>`, so the only markup this can ever contain is those two tags.
        locale:
          type: string
        rank:
          type: number
      required:
        - resourceType
        - resourceId
        - url
        - title
        - snippet
        - locale
        - rank
    SiteSearchSettings:
      type: object
      properties:
        enabled:
          type: boolean
        enabledResourceTypes:
          type: array
          nullable: true
          description: "`null` means every admitted resource type."
          items:
            type: string
        resultLimit:
          type: integer
          minimum: 1
          maximum: 100
        minQueryLength:
          type: integer
          minimum: 1
          maximum: 20
        suggestionsEnabled:
          type: boolean
        suggestionLimit:
          type: integer
          minimum: 1
          maximum: 20
        analyticsEnabled:
          type: boolean
          description: Opt-in, minimized query logging. Even when on, only a sha256 of the normalized query, its length, the locale and the result count are stored — never the raw query.
      required:
        - enabled
        - enabledResourceTypes
        - resultLimit
        - minQueryLength
        - suggestionsEnabled
        - suggestionLimit
        - analyticsEnabled
    SiteSearchSettingsUpdateRequest:
      type: object
      description: Every field is optional — an omitted field keeps its current value.
      properties:
        enabled:
          type: boolean
        enabledResourceTypes:
          type: array
          nullable: true
          maxItems: 50
          items:
            type: string
            maxLength: 64
        resultLimit:
          type: integer
          minimum: 1
          maximum: 100
        minQueryLength:
          type: integer
          minimum: 1
          maximum: 20
        suggestionsEnabled:
          type: boolean
        suggestionLimit:
          type: integer
          minimum: 1
          maximum: 20
        analyticsEnabled:
          type: boolean
    SiteSearchSourceResult:
      type: object
      description: Per-source outcome of one reconcile/rebuild pass.
      properties:
        sourceKey:
          type: string
        resourceType:
          type: string
        sourceCount:
          type: integer
          description: Currently public source rows — the "reconciliation matches source counts" signal.
        added:
          type: integer
        updated:
          type: integer
        unchanged:
          type: integer
        removed:
          type: integer
        failures:
          type: integer
      required:
        - sourceKey
        - resourceType
        - sourceCount
        - added
        - updated
        - unchanged
        - removed
        - failures
    SiteSearchSuggestionItem:
      type: object
      properties:
        resourceType:
          type: string
        resourceId:
          type: string
        url:
          type: string
        title:
          type: string
        locale:
          type: string
      required:
        - resourceType
        - resourceId
        - url
        - title
        - locale
    SiteSearchSuggestResult:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SiteSearchSuggestionItem"
        query:
          type: string
        locale:
          type: string
      required:
        - items
        - query
        - locale
    SiteSearchTermFacetValue:
      type: object
      properties:
        value:
          type: string
          description: The stable value a filter matches and a URL carries — a slug (`politik`) or a code (`62.71`).
        label:
          type: string
          description: Display text for `value`. Falls back to the value itself where the source stored no separate label (the `region` facet, whose human name lives in a dataset-versioned table this does not join).
        count:
          type: integer
      required:
        - value
        - label
        - count
    SocialProfileLink:
      type: object
      required:
        - platform
        - url
      properties:
        platform:
          description: A label, deliberately NOT an enumeration. Closing the set would mean a migration every time a newsroom joins a new network, and unlike a content block type nothing renders differently per value — an unknown platform degrades to a plain link.
          type: string
          maxLength: 40
        url:
          description: "Absolute http(s) only. REFUSED rather than sanitized otherwise: this is rendered as an <a href> on every public page, so a javascript:/data: value would be stored XSS with a very long reach."
          type: string
          maxLength: 500
    SoDConflictEvaluation:
      type: object
      description: "One append-only segregation-of-duties conflict-check decision log entry (Issue #181). Safe projection — no request/resource payload."
      properties:
        id:
          type: string
          format: uuid
        ruleKey:
          type: string
        subjectTenantUserId:
          type: string
          format: uuid
          nullable: true
        triggerContext:
          type: string
          enum:
            - assignment_create
            - high_risk_decision
        conflictDetected:
          type: boolean
        resolvedVia:
          type: string
          enum:
            - none
            - exception
            - denied
        decisionReason:
          type: string
        occurredAt:
          type: string
          format: date-time
    SoDConflictException:
      type: object
      description: "One segregation-of-duties conflict exception (Issue #181) — a scope-bound, time-bound, revocable, audited override to a detected conflict."
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        ruleKey:
          type: string
        subjectTenantUserId:
          type: string
          format: uuid
        scopeType:
          type: string
          nullable: true
        scopeId:
          type: string
          format: uuid
          nullable: true
        justification:
          type: string
        requestedByTenantUserId:
          type: string
          format: uuid
        approvedByTenantUserId:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
          enum:
            - pending
            - approved
            - rejected
            - expired
            - revoked
        effectiveFrom:
          type: string
          format: date-time
        effectiveTo:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SoftDeleteMediaObjectRequest:
      type: object
      required:
        - reason
      properties:
        reason:
          type: string
          minLength: 1
          maxLength: 500
          description: Why the object is being removed. Trimmed before length-checking, so a whitespace-only value fails as "required". Written to the audit row, which outlives the object it describes — hence the bound.
    SsoProvider:
      type: object
      description: A tenant OIDC SSO provider. The client secret is never included — only whether one is configured and, for the env-referenced case, the variable name.
      properties:
        id:
          type: string
          format: uuid
        providerKey:
          type: string
        providerType:
          type: string
        displayName:
          type: string
        issuerUrl:
          type: string
        clientId:
          type: string
        secretSource:
          type: string
          enum:
            - encrypted
            - env
        clientSecretEnvVar:
          type: string
          nullable: true
        scopes:
          type: string
        allowedEmailDomains:
          type: array
          items:
            type: string
        enabled:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SubmitCommentRequest:
      type: object
      required:
        - body
      properties:
        body:
          type: string
        resourceType:
          type: string
          description: Required when submitting to `/api/v1/comments`; implied by the parent on a reply.
        resourceId:
          type: string
          format: uuid
        locale:
          type: string
        parentId:
          type: string
          format: uuid
          nullable: true
        authorDisplayName:
          type: string
          nullable: true
          maxLength: 120
        authorEmail:
          type: string
          nullable: true
          description: Never stored raw. Normalized, then persisted only as a sha256 hash plus a masked form for the moderation queue.
        website:
          type: string
          nullable: true
          description: The honeypot field. Hidden from human visitors; any non-empty value marks the submission as automated.
        timingToken:
          type: string
          nullable: true
          description: The HMAC-signed token issued when the form was rendered. Supplies the elapsed-time measurement for the anti-abuse timing floor without trusting a client-supplied number.
        subscribeToReplies:
          type: boolean
          description: Opt in to reply notifications. Requires `authorEmail` and the tenant's `notifyOnReply` setting; the address is encrypted at rest and never returned.
    SubmitCommentResult:
      type: object
      description: "Either the neutral acknowledgement (`status: received`) or, only for an immediately public comment, its id and status."
      properties:
        status:
          type: string
          enum:
            - received
            - pending
            - approved
        publiclyVisible:
          type: boolean
        commentId:
          type: string
          format: uuid
    TenantAuthPolicy:
      type: object
      properties:
        tenantId:
          type: string
          format: uuid
        passwordLoginEnabled:
          type: boolean
        ssoEnabled:
          type: boolean
        ssoRequired:
          type: boolean
        autoLinkVerifiedEmail:
          type: boolean
        jitProvisioningEnabled:
          type: boolean
        allowedEmailDomains:
          type: array
          items:
            type: string
        breakGlassIdentityIds:
          type: array
          items:
            type: string
            format: uuid
        updatedAt:
          type: string
          format: date-time
          nullable: true
    TenantDirectoryEntry:
      type: object
      properties:
        id:
          type: string
          format: uuid
        tenantCode:
          type: string
        tenantName:
          type: string
        status:
          type: string
        createdAt:
          type: string
          format: date-time
    TenantDomain:
      type: object
      description: A tenant hostname/subdomain mapping. `verification_token_hash` is never exposed.
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
        hostname:
          type: string
        normalizedHostname:
          type: string
        domainType:
          type: string
          enum:
            - subdomain
            - custom_domain
        routeMode:
          type: string
          enum:
            - canonical
            - legacy_blog
        status:
          type: string
          enum:
            - pending_verification
            - active
            - suspended
            - failed
        isPrimary:
          type: boolean
        redirectToPrimary:
          type: boolean
        verificationMethod:
          type: string
          nullable: true
          description: Server-managed. `dns_txt` for every domain this application creates; `null` only on a row created before ADR-0106 that has not been verified since. Rows migrated from an older build may still carry `dns_cname`, `file` or `manual`, which this application no longer writes or honours.
          enum:
            - dns_txt
            - dns_cname
            - file
            - manual
            - null
        verificationRecordName:
          type: string
          nullable: true
          description: Server-minted DNS name to publish the challenge at (`_awcms-verify.<hostname>`). Read-only.
        verificationRecordValue:
          type: string
          nullable: true
          description: Server-minted TXT value that must appear at `verificationRecordName`. Read-only, and not a secret — it is published in DNS.
        verifiedAt:
          type: string
          format: date-time
          nullable: true
        lastCheckedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        createdBy:
          type: string
          format: uuid
          nullable: true
        updatedBy:
          type: string
          format: uuid
          nullable: true
    TenantLifecycleResult:
      type: object
      description: Result of a suspend/restore transition (ADR-0073).
      properties:
        tenantId:
          type: string
          format: uuid
        status:
          type: string
          description: The tenant's status after the call.
        changed:
          type: boolean
          description: False when the tenant was already in the requested status — the caller got what they asked for, and no transition row was written.
      required:
        - tenantId
        - status
        - changed
    TenantMfaPolicy:
      type: object
      properties:
        tenantId:
          type: string
          format: uuid
        enforcementLevel:
          type: string
          enum:
            - optional
            - required_for_privileged
            - required_for_all
        updatedAt:
          type: string
          format: date-time
          nullable: true
    TenantModule:
      type: object
      properties:
        moduleKey:
          type: string
        name:
          type: string
        version:
          type: string
        isCore:
          type: boolean
        tenantEnabled:
          type: boolean
        enabledAt:
          type: string
          format: date-time
          nullable: true
        disabledAt:
          type: string
          format: date-time
          nullable: true
        disableReason:
          type: string
          nullable: true
    TenantUserMasked:
      type: object
      properties:
        id:
          type: string
          format: uuid
        loginIdentifierMasked:
          type: string
          description: Masked login identifier — PII is never returned raw in a list.
        status:
          type: string
        roles:
          type: array
          items:
            type: string
          description: Assigned role codes (empty when the user holds no role).
    TenantUserSessionSummary:
      type: object
      description: One live session of a named tenant user, as seen by an administrator. Same exclusions as OwnSessionSummary — no token, no raw IP, no raw User-Agent.
      required:
        - id
        - issuedAt
        - expiresAt
        - assuranceLevel
        - originAuth
        - isCallerSession
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        issuedAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        assuranceLevel:
          type: string
          enum:
            - aal1
            - aal2
        originAuth:
          type: string
          enum:
            - password
            - sso
            - handoff
        clientIpHash:
          type:
            - string
            - "null"
          description: Keyed, non-reversible pseudonym of the client IP. null when the deployment has no stable AUTH_IP_HASH_SECRET.
        userAgentSummary:
          type:
            - string
            - "null"
        isCallerSession:
          type: boolean
          description: True only when the endpoint is pointed at the caller's own tenant user AND this row is the session making the request.
    ThemeConfigRequest:
      type: object
      description: A tenant's DATA-only theme configuration. Every key/value is validated against the chosen theme descriptor; unknown tokens/slots/assets/sections are rejected, and token values are validated by rejection against strict CSS grammars (no url()/expression()/@import/javascript:/comment-breakout).
      required:
        - themeKey
      properties:
        themeKey:
          type: string
          maxLength: 64
          description: A registered (build-time) theme key.
        tokenOverrides:
          type: object
          additionalProperties:
            type: string
          description: tokenKey -> validated token value (color/dimension/number, or a font-family allow-list key). Unknown token keys are rejected.
        slotSelections:
          type: object
          additionalProperties:
            type: string
          description: slotKey -> chosen variant key (from the slot's allow-list).
        assetRefs:
          type: object
          additionalProperties:
            type: string
            format: uuid
            nullable: true
          description: assetSlotKey -> media object UUID (never a URL); null clears the slot.
        sectionOrder:
          type: array
          items:
            type: string
          description: An ordering of the theme's declared content-section keys.
        navPlacement:
          type: string
          description: One of the theme's declared nav placements.
    ThemeDraft:
      type: object
      required:
        - versionId
        - themeKey
        - config
        - configHash
      properties:
        versionId:
          type: string
          format: uuid
        themeKey:
          type: string
        config:
          type: object
          description: The validated, normalized config as stored.
        configHash:
          type: string
    ThemeValidationResult:
      type: object
      required:
        - valid
        - errors
        - previewCss
      properties:
        valid:
          type: boolean
        errors:
          type: array
          items:
            type: object
            required:
              - field
              - message
            properties:
              field:
                type: string
              message:
                type: string
        previewCss:
          type: string
          nullable: true
          description: The `:root { … }` custom-property block that would be served, when valid; null when invalid.
    ThemeVersion:
      type: object
      required:
        - versionId
        - themeKey
        - versionNumber
      properties:
        versionId:
          type: string
          format: uuid
        themeKey:
          type: string
        versionNumber:
          type: integer
          nullable: true
        configHash:
          type: string
        publishedAt:
          type: string
          format: date-time
          nullable: true
    ThemingState:
      type: object
      required:
        - themes
        - state
        - draft
        - versions
      properties:
        themes:
          type: array
          description: Available reviewed, build-time theme descriptors.
          items:
            type: object
        state:
          type: object
          required:
            - activeThemeKey
            - activeVersionId
            - draftThemeKey
          properties:
            activeThemeKey:
              type: string
              nullable: true
            activeVersionId:
              type: string
              format: uuid
              nullable: true
            draftThemeKey:
              type: string
              nullable: true
        draft:
          type: object
          nullable: true
        versions:
          type: array
          items:
            $ref: "#/components/schemas/ThemeVersion"
    UpdateTenantDomainRequest:
      type: object
      description: At least one field required. `status` may not be `active` (use verify); `hostname`/`is_primary` are immutable. `verificationMethod` and `verificationRecordName`/`Value` are server-managed (ADR-0106) and are REFUSED with 400 rather than ignored if supplied.
      properties:
        domainType:
          type: string
          enum:
            - subdomain
            - custom_domain
        routeMode:
          type: string
          enum:
            - canonical
            - legacy_blog
        status:
          type: string
          enum:
            - pending_verification
            - suspended
            - failed
        redirectToPrimary:
          type: boolean
    UserGroupMembershipInput:
      type: object
      required:
        - userGroupId
        - tenantUserId
      properties:
        userGroupId:
          type: string
          format: uuid
        tenantUserId:
          type: string
          format: uuid
    UserGroupRecord:
      type: object
      required:
        - id
        - groupCode
        - groupName
        - source
      properties:
        id:
          type: string
          format: uuid
        groupCode:
          type: string
        groupName:
          type: string
        description:
          type: string
          nullable: true
        source:
          type: string
          enum:
            - local
            - scim
    UserGroupSummary:
      type: object
      description: One live user group, with the two counts that make the list decidable.
      required:
        - id
        - groupCode
        - groupName
        - source
        - memberCount
        - roleCount
      properties:
        id:
          type: string
          format: uuid
        groupCode:
          type: string
        groupName:
          type: string
        description:
          type: string
          nullable: true
        source:
          type: string
          enum:
            - local
            - scim
        externalId:
          type: string
          nullable: true
          description: The directory's own identifier. It is the sync key rather than groupCode, because humans rename group codes and a rename must not orphan the group.
        memberCount:
          type: integer
          minimum: 0
        roleCount:
          type: integer
          minimum: 0
          description: Active role grants held BY the group. Everyone in the group holds them too.
    VisitEvent:
      type: object
      description: A single page-view/API event. Raw-detail fields (ipHash/userAgentHash) are null unless the caller holds visitor_analytics.raw_detail.read.
      properties:
        id:
          type: string
          format: uuid
        visitorSessionId:
          type: string
          format: uuid
          nullable: true
        identityId:
          type: string
          format: uuid
          nullable: true
        occurredAt:
          type: string
          format: date-time
        method:
          type: string
        statusCode:
          type: integer
          nullable: true
        area:
          type: string
        routePattern:
          type: string
          nullable: true
        pathSanitized:
          type: string
        referrerDomain:
          type: string
          nullable: true
        durationMs:
          type: integer
          nullable: true
        userAgentParsed:
          type: object
          additionalProperties: true
        geo:
          type: object
          additionalProperties: true
        humanStatus:
          type: string
          enum:
            - human
            - bot
            - unknown
        correlationId:
          type: string
          nullable: true
        ipHash:
          type: string
          nullable: true
        userAgentHash:
          type: string
          nullable: true
    VisitorSession:
      type: object
      description: A visitor session. Raw-detail fields (ipAddress/ipHash/userAgentHash/loginIdentifierSnapshot) are null unless the caller holds visitor_analytics.raw_detail.read.
      properties:
        id:
          type: string
          format: uuid
        visitorKeyHash:
          type: string
        identityId:
          type: string
          format: uuid
          nullable: true
        isAuthenticated:
          type: boolean
        area:
          type: string
        currentPath:
          type: string
          nullable: true
        firstSeenAt:
          type: string
          format: date-time
        lastSeenAt:
          type: string
          format: date-time
        browserName:
          type: string
          nullable: true
        browserVersionMajor:
          type: string
          nullable: true
        osName:
          type: string
          nullable: true
        deviceType:
          type: string
          nullable: true
        isHuman:
          type: boolean
        botReason:
          type: string
          nullable: true
        countryCode:
          type: string
          nullable: true
        region:
          type: string
          nullable: true
        city:
          type: string
          nullable: true
        timezone:
          type: string
          nullable: true
        loginIdentifierSnapshot:
          type: string
          nullable: true
        ipHash:
          type: string
          nullable: true
        ipAddress:
          type: string
          nullable: true
        userAgentHash:
          type: string
          nullable: true
    WebPushCapability:
      type: object
      properties:
        enabled:
          type: boolean
          description: "False is a normal answer, not an error: an offline/LAN deployment has no push service to reach and no VAPID key pair configured."
        applicationServerKey:
          type: string
          nullable: true
          description: base64url VAPID public key, passed straight to PushManager.subscribe(). Public by definition — it is the value the browser hands the push service to identify this application server. Rotating it does NOT re-key existing subscriptions; it makes every one of them permanently undeliverable until the user subscribes again.
security:
  - bearerAuth: []
    tenantHeader: []
